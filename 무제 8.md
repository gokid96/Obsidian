# 헥사고날 아키텍처 (Hexagonal Architecture)

**비즈니스 로직을 외부 요소로부터 완전히 분리하는 아키텍처**

```
전통적인 레이어드 아키텍처:

Controller → Service → Repository → DB

문제:
  Service가 Repository에 직접 의존
  → DB가 바뀌면 Service도 수정
  → 테스트 시 DB 필요
  → 비즈니스 로직이 인프라에 종속
```

```
헥사고날 아키텍처:

[REST API]  [Kafka]  [gRPC]
     ↓         ↓       ↓
     └─────────┴───────┘
              ↓
        [어댑터 (입력)]
              ↓
        [포트 (인터페이스)]
              ↓
        [도메인 (핵심 비즈니스 로직)]
              ↓
        [포트 (인터페이스)]
              ↓
        [어댑터 (출력)]
              ↓
     ┌────────┴───────┐
     ↓                ↓
   [DB]            [외부 API]
```

---

## 핵심 개념

**도메인 (Domain)**

```
핵심 비즈니스 로직
→ 외부 의존 없음
→ 순수 Java/Kotlin 코드
→ DB, API, 프레임워크 몰라도 됨

예시:
  주문 금액 계산
  재고 차감 로직
  할인 정책 적용
```

**포트 (Port)**

```
도메인이 외부와 소통하는 인터페이스

입력 포트 (Inbound Port):
  외부에서 도메인을 호출하는 인터페이스
  → UseCase 인터페이스

출력 포트 (Outbound Port):
  도메인이 외부를 호출하는 인터페이스
  → Repository 인터페이스
  → 외부 API 인터페이스
```

**어댑터 (Adapter)**

```
포트의 실제 구현체
외부 세계와 도메인을 연결

입력 어댑터 (Inbound Adapter):
  REST Controller
  Kafka Consumer
  gRPC Server

출력 어댑터 (Outbound Adapter):
  JPA Repository 구현체
  외부 API 클라이언트
  Redis 클라이언트
```

---

## 포트와 어댑터

```
입력 흐름:
REST 요청
  ↓
OrderController (입력 어댑터)
  ↓
CreateOrderUseCase (입력 포트 - 인터페이스)
  ↓
OrderService (도메인 - 구현체)
  ↓
OrderRepository (출력 포트 - 인터페이스)
  ↓
OrderJpaRepository (출력 어댑터 - 구현체)
  ↓
DB
```

---

## 코드로 보기

**입력 포트 (UseCase 인터페이스)**

```java
// 도메인 레이어
public interface CreateOrderUseCase {
    OrderId createOrder(CreateOrderCommand command);
}
```

**도메인 (핵심 비즈니스 로직)**

```java
// 도메인 레이어
@Service
public class OrderService implements CreateOrderUseCase {

    private final OrderRepository orderRepository;  // 출력 포트
    private final PaymentPort paymentPort;          // 출력 포트

    @Override
    public OrderId createOrder(CreateOrderCommand command) {
        // 순수 비즈니스 로직
        Order order = Order.create(command.getItems());
        order.validateStock();
        order.applyDiscount(command.getCoupon());

        orderRepository.save(order);
        paymentPort.requestPayment(order);

        return order.getId();
    }
}
```

**출력 포트 (Repository 인터페이스)**

```java
// 도메인 레이어
public interface OrderRepository {
    void save(Order order);
    Optional<Order> findById(OrderId id);
}
```

**입력 어댑터 (REST Controller)**

```java
// 어댑터 레이어
@RestController
@RequiredArgsConstructor
public class OrderController {

    private final CreateOrderUseCase createOrderUseCase;  // 입력 포트

    @PostMapping("/orders")
    public ResponseEntity<OrderResponse> createOrder(
            @RequestBody OrderRequest request) {

        CreateOrderCommand command = OrderMapper.toCommand(request);
        OrderId orderId = createOrderUseCase.createOrder(command);

        return ResponseEntity.ok(new OrderResponse(orderId));
    }
}
```

**출력 어댑터 (JPA 구현체)**

```java
// 어댑터 레이어
@Repository
@RequiredArgsConstructor
public class OrderJpaAdapter implements OrderRepository {

    private final OrderJpaRepository jpaRepository;

    @Override
    public void save(Order order) {
        OrderEntity entity = OrderMapper.toEntity(order);
        jpaRepository.save(entity);
    }

    @Override
    public Optional<Order> findById(OrderId id) {
        return jpaRepository.findById(id.getValue())
                .map(OrderMapper::toDomain);
    }
}
```

---

## 레이어드 아키텍처와 비교

```
레이어드 아키텍처:
  Controller → Service → Repository → DB
  
  의존성 방향이 항상 아래로
  Service가 Repository 구현체에 직접 의존
  → DB 바뀌면 Service 수정 필요
  → 테스트 시 DB 필요

헥사고날 아키텍처:
  Controller → UseCase(인터페이스) ← Service → Repository(인터페이스) ← JpaAdapter → DB

  도메인이 인터페이스에만 의존
  → DB 바뀌어도 도메인 수정 없음
  → 테스트 시 Mock으로 대체 가능
```

|항목|레이어드|헥사고날|
|---|---|---|
|의존성|구현체에 의존|인터페이스에 의존|
|테스트|DB 필요|Mock으로 가능|
|유연성|낮음|높음|
|복잡도|낮음|높음|
|적합|단순한 서비스|복잡한 도메인|

---

## 테스트가 쉬워지는 이유

```
레이어드 아키텍처 테스트:
  OrderService 테스트
  → OrderRepository(JPA) 필요
  → DB 연결 필요
  → 테스트 느리고 복잡

헥사고날 아키텍처 테스트:
  OrderService 테스트
  → OrderRepository(인터페이스) Mock으로 대체
  → DB 연결 불필요
  → 순수 비즈니스 로직만 테스트
  → 빠르고 단순
```

```java
// 테스트 예시
@Test
void 주문_생성_테스트() {
    // Mock으로 대체 (DB 불필요)
    OrderRepository mockRepository = mock(OrderRepository.class);
    PaymentPort mockPayment = mock(PaymentPort.class);

    OrderService orderService = new OrderService(
        mockRepository,
        mockPayment
    );

    // 순수 비즈니스 로직만 테스트
    OrderId orderId = orderService.createOrder(command);

    assertThat(orderId).isNotNull();
    verify(mockRepository).save(any(Order.class));
}
```

---

## 언제 도입하나?

```
도입 적합:
  도메인 로직이 복잡한 서비스
  DB, 외부 API 교체 가능성이 있을 때
  테스트 커버리지를 높이고 싶을 때
  장기적으로 유지보수할 서비스

도입 부적합:
  단순한 CRUD 서비스
  빠른 개발이 우선일 때
  팀이 아키텍처에 익숙하지 않을 때
  → 레이어드 아키텍처로 충분
```

---

## 핵심 Q&A

**Q. 헥사고날 아키텍처가 뭔가?**

비즈니스 로직을 외부 요소로부터 완전히 분리하는 아키텍처 포트(인터페이스)와 어댑터(구현체)로 도메인과 외부를 연결 도메인은 DB, 프레임워크, 외부 API를 몰라도 됨

**Q. 포트와 어댑터가 뭔가?**

포트는 도메인이 외부와 소통하는 인터페이스 어댑터는 포트의 실제 구현체로 외부 세계와 연결 입력 포트는 UseCase, 출력 포트는 Repository 인터페이스

**Q. 레이어드 아키텍처와 뭐가 다른가?**

레이어드는 구현체에 직접 의존해서 DB가 바뀌면 서비스도 수정 헥사고날은 인터페이스에만 의존해서 구현체가 바뀌어도 도메인 수정 불필요 테스트 시 Mock으로 대체가 쉬워서 단위 테스트가 용이

---

## 핵심 요약

|항목|내용|
|---|---|
|정의|비즈니스 로직을 외부로부터 완전히 분리|
|도메인|핵심 비즈니스 로직, 외부 의존 없음|
|포트|도메인과 외부를 연결하는 인터페이스|
|어댑터|포트의 실제 구현체|
|입력 어댑터|Controller, Kafka Consumer|
|출력 어댑터|JPA Repository, 외부 API 클라이언트|
|장점|테스트 용이, 유연한 교체, 관심사 분리|
|단점|복잡도 증가, 초기 개발 비용|