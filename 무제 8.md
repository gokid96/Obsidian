# Redis 주요 기능 & 활용

## Redis란?

**메모리 기반 초고속 데이터 저장소**

```
일반 DB:
  데이터 → 디스크 저장
  조회 시 디스크에서 읽음
  → 느림 (ms 단위)

Redis:
  데이터 → 메모리 저장
  조회 시 메모리에서 읽음
  → 빠름 (μs 단위)
```

단순 캐시를 넘어서 다양한 자료구조와 기능으로 실무에서 폭넓게 활용

---

## 자료구조

**String**

```
가장 기본적인 자료구조
키 → 값 (문자열, 숫자, JSON)

SET user:1 "김철수"
GET user:1  → "김철수"

SET count 0
INCR count  → 1  (원자적 증가)
INCR count  → 2

활용:
  캐시, 세션, 카운터, 분산 락
```

**List**

```
순서 있는 문자열 목록
양쪽 끝에서 push/pop 가능

LPUSH queue "작업1"  → 왼쪽에 추가
RPUSH queue "작업2"  → 오른쪽에 추가
LPOP queue           → 왼쪽에서 꺼냄
RPOP queue           → 오른쪽에서 꺼냄

활용:
  작업 큐, 최근 본 목록, 알림 목록
```

**Hash**

```
필드-값 쌍의 집합
객체를 표현하기 좋음

HSET user:1 name "김철수" age 30 email "kim@test.com"
HGET user:1 name   → "김철수"
HGETALL user:1     → {name: 김철수, age: 30, email: ...}

활용:
  사용자 정보, 상품 정보, 세션 데이터
```

**Set**

```
중복 없는 문자열 집합
순서 없음

SADD tags "java" "spring" "redis"
SMEMBERS tags  → {java, spring, redis}
SISMEMBER tags "java"  → 1 (있음)
SISMEMBER tags "python"  → 0 (없음)

집합 연산:
SINTER set1 set2  → 교집합
SUNION set1 set2  → 합집합
SDIFF set1 set2   → 차집합

활용:
  팔로우/팔로워, 좋아요, 태그
```

**Sorted Set (ZSet)**

```
점수(score)로 정렬된 집합
중복 없음, 순서 있음

ZADD ranking 100 "김철수"
ZADD ranking 200 "이영희"
ZADD ranking 150 "박지성"

ZRANGE ranking 0 -1 WITHSCORES
→ 김철수(100), 박지성(150), 이영희(200)

ZREVRANGE ranking 0 2  → 상위 3명 (내림차순)
ZRANK ranking "김철수"  → 0 (0부터 시작)

활용:
  실시간 랭킹, 리더보드, 우선순위 큐
```

**Bitmap**

```
비트 단위 데이터 저장
메모리 매우 효율적

SETBIT login:20240101 userId 1  → 로그인 표시
GETBIT login:20240101 userId    → 1 (로그인 함)
BITCOUNT login:20240101         → 오늘 로그인 수

활용:
  출석 체크, DAU 계산, 기능 플래그
```

---

## 주요 기능

**TTL (만료 시간)**

```
키에 만료 시간 설정
→ 자동으로 삭제

SET session:abc "user_data" EX 3600  → 1시간 후 삭제
EXPIRE key 60                         → 60초 후 삭제
TTL key                               → 남은 시간 확인

활용:
  세션, 캐시, 임시 토큰
```

**Pub/Sub**

```
발행/구독 패턴

Publisher:
  PUBLISH channel "메시지"

Subscriber:
  SUBSCRIBE channel
  → 메시지 수신

활용:
  실시간 알림, 채팅
  Redisson 분산 락 해제 알림
```

**트랜잭션 (MULTI/EXEC)**

```
여러 명령어를 원자적으로 실행

MULTI
SET key1 "value1"
SET key2 "value2"
INCR counter
EXEC  → 한꺼번에 실행

→ 중간에 다른 클라이언트 끼어들기 불가
→ 단, 롤백은 지원 안 함
```

**Lua 스크립트**

```
복잡한 로직을 원자적으로 실행

EVAL "
  local val = redis.call('GET', KEYS[1])
  if val == ARGV[1] then
    redis.call('DEL', KEYS[1])
    return 1
  end
  return 0
" 1 lock:key myServerId

→ GET + 비교 + DEL을 원자적으로
→ 분산 락 해제에 활용
```

---

## 실제 활용 패턴

**1. 캐시**

```
DB 조회 결과를 Redis에 저장
→ 다음 요청은 Redis에서 바로 반환

흐름:
요청 → Redis 확인
  → 있으면 (Cache Hit): Redis에서 반환
  → 없으면 (Cache Miss): DB 조회 → Redis 저장 → 반환

TTL 설정으로 자동 만료
→ 데이터 신선도 유지
```

**2. 세션 저장**

```
서버가 여러 대일 때
→ 세션을 각 서버 메모리에 저장하면
→ 다른 서버로 요청이 가면 세션 없음!

Redis에 세션 저장:
  → 어떤 서버로 요청이 가도
  → Redis에서 세션 조회
  → 일관된 로그인 유지

SET session:{sessionId} {userData} EX 3600
```

**3. 분산 락**

```
여러 서버가 동시에 같은 자원 접근 방지

SET lock:product:1 {serverId} NX EX 30
→ NX: 없을 때만 저장 (락 획득)
→ EX: 30초 후 자동 해제

실무: Redisson 라이브러리 사용
→ Watchdog으로 자동 갱신
→ 안전한 해제
```

**4. 실시간 랭킹**

```
Sorted Set으로 실시간 랭킹 구현

점수 업데이트:
ZINCRBY ranking 10 "김철수"  → 점수 +10

상위 10명 조회:
ZREVRANGE ranking 0 9 WITHSCORES

내 순위 조회:
ZREVRANK ranking "김철수"

→ DB 집계 쿼리 없이 실시간으로
```

**5. 중복 요청 방지**

```
같은 요청이 여러 번 올 때

SET request:{requestId} 1 NX EX 60
→ NX: 처음 요청만 저장 성공
→ 이미 있으면 실패 → 중복 요청 차단

활용:
  멱등성 보장
  중복 결제 방지
```

**6. 대기열 (Queue)**

```
선착순 이벤트 처리

입장:
RPUSH queue userId

순서대로 처리:
LPOP queue → 가장 먼저 온 사용자

현재 대기 순번:
LPOS queue userId

→ 수만 명이 동시 접속해도
→ 순서대로 처리
```

**7. 카운터**

```
조회수, 좋아요 수 실시간 집계

INCR post:1:views  → 원자적 증가
GET post:1:views   → 현재 조회수

→ DB 업데이트 없이 Redis에서 처리
→ 주기적으로 DB에 반영 (배치)
```

---

## 영속성 (Persistence)

메모리 기반이라 서버 재시작 시 데이터 유실 위험

**RDB (Redis Database)**

```
특정 시점의 스냅샷을 디스크에 저장

장점: 파일 크기 작음, 복구 빠름
단점: 스냅샷 사이 데이터 유실 가능
```

**AOF (Append Only File)**

```
모든 쓰기 명령어를 로그로 기록

장점: 데이터 유실 최소화
단점: 파일 크기 큼, 복구 느림
```

```
실무:
  캐시 용도 → 영속성 불필요
  세션, 랭킹 → RDB or AOF 설정
  중요 데이터 → DB에 저장, Redis는 캐시만
```

---

## 주의사항

**1. 메모리 관리**

```
메모리가 꽉 차면?
→ maxmemory-policy 설정으로 제거 정책 결정

allkeys-lru: 가장 오래 안 쓴 것부터 제거
volatile-lru: TTL 있는 것 중 오래 안 쓴 것
allkeys-random: 랜덤 제거
noeviction: 제거 안 함 → 에러 반환
```

**2. 단일 스레드**

```
Redis는 단일 스레드
→ 명령어 하나씩 순차 처리
→ 오래 걸리는 명령어가 전체 블로킹

위험한 명령어:
KEYS *        → 전체 키 스캔 (사용 금지!)
SMEMBERS      → 큰 Set 전체 조회
HGETALL       → 큰 Hash 전체 조회

대안:
SCAN          → KEYS * 대신 커서 기반 순차 탐색
```

**3. 캐시 문제**

```
캐시 스탬피드:
  캐시 만료 시 다수가 동시에 DB 조회
  → DB 부하 폭증
  → 분산 락으로 해결

캐시 무효화:
  DB 데이터 변경 시 캐시 삭제/갱신 필요
  → 일관성 관리 중요
```

---

## 핵심 Q&A

**Q. Redis가 뭔가?**

메모리 기반 초고속 데이터 저장소 String, List, Hash, Set, Sorted Set 등 다양한 자료구조 지원 캐시, 세션, 분산 락, 랭킹, 대기열 등 폭넓게 활용

**Q. Redis를 캐시로만 쓰나?**

아니다 분산 락, 세션 저장, 실시간 랭킹, 대기열, 카운터 등 다양한 용도로 활용 자료구조별로 적합한 사용 패턴이 다름

**Q. Redis 데이터가 날아가면?**

메모리 기반이라 재시작 시 유실 가능 RDB나 AOF로 영속성 설정 가능 중요 데이터는 DB에 저장하고 Redis는 캐시로만 쓰는 게 안전

---

## 핵심 요약

|자료구조|활용|
|---|---|
|String|캐시, 세션, 카운터, 분산 락|
|List|작업 큐, 최근 목록|
|Hash|객체 저장, 사용자 정보|
|Set|팔로우, 좋아요, 태그|
|Sorted Set|실시간 랭킹, 리더보드|
|Bitmap|출석 체크, DAU|

|기능|활용|
|---|---|
|TTL|캐시 만료, 세션|
|Pub/Sub|실시간 알림|
|Lua 스크립트|원자적 복합 연산|
|분산 락|동시 접근 제어|
|대기열|선착순 처리|