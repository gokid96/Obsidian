오후 3:14

# 아키텍처 정리

## 전체 개념

```
서비스 구조
  └── 모놀리식      → 단순, 초기 서비스
  └── MSA          → 독립 배포, 장애 격리

서비스 간 통신
  └── EDA          → 이벤트 기반, 느슨한 결합
  └── API Gateway  → 단일 진입점
  └── BFF          → 클라이언트별 최적화

분산 트랜잭션
  └── Saga 패턴    → 보상 트랜잭션
  └── Outbox 패턴  → 이벤트 유실 방지

인프라
  └── 서비스 디스커버리 → 동적 주소 관리
  └── 사이드카 패턴     → 공통 기능 분리
  └── 분산 트레이싱     → 요청 흐름 추적

코드 설계
  └── 헥사고날 아키텍처 → 포트와 어댑터
  └── 클린 아키텍처     → 의존성 안쪽으로
```

---

## 상황별 선택 가이드

**서비스 규모**

```
초기 / 소규모
  → 모놀리식
  → 단순하고 빠른 개발
  → MSA는 오버엔지니어링

규모 커지면
  → MSA로 전환
  → 트래픽 많은 서비스부터 분리
  → 한 번에 다 바꾸지 말 것
```

**서비스 간 통신**

```
즉시 응답 필요
  → 동기 통신 (REST, gRPC)

응답 기다릴 필요 없음
  → 비동기 통신 (Kafka, EDA)

클라이언트 유형 여러 개
  → BFF 패턴
```

**분산 트랜잭션**

```
MSA에서 여러 서비스에 걸친 트랜잭션
  → Saga 패턴
  → 코레오그래피: 서비스 수 적음
  → 오케스트레이션: 복잡한 흐름

DB 저장 + 이벤트 발행 동시에
  → Outbox 패턴
  → 원자성 보장
```

**코드 구조**

```
단순 CRUD
  → 레이어드 아키텍처로 충분

복잡한 도메인 로직
  → 헥사고날 or 클린 아키텍처
  → 테스트 용이, 유연한 교체

장기 운영 서비스
  → 클린 아키텍처
  → 의존성 명확히 관리
```

**인프라**

```
서비스 주소가 동적으로 바뀜
  → 서비스 디스커버리 (Eureka)

공통 기능 중복
  → 사이드카 패턴
  → 언어 무관하게 적용

MSA 디버깅 어려움
  → 분산 트레이싱 (Zipkin, Jaeger)
```

---

## 전체 개념 한 줄 정리

|개념|한 줄 정리|
|---|---|
|모놀리식|모든 기능이 하나의 서버, 단순하지만 확장 어려움|
|MSA|기능별 독립 서비스, 독립 배포와 장애 격리|
|EDA|이벤트로 서비스 간 통신, 느슨한 결합|
|Saga 패턴|분산 트랜잭션, 실패 시 보상 트랜잭션|
|Outbox 패턴|DB 저장과 이벤트 발행 원자성 보장|
|API Gateway|단일 진입점, 인증/라우팅/Rate Limiting|
|서비스 디스커버리|서비스 주소 동적 관리, Eureka|
|BFF|클라이언트별 전용 백엔드, 최적화된 응답|
|분산 트레이싱|Trace ID로 전체 요청 흐름 추적|
|사이드카 패턴|공통 기능을 보조 컨테이너로 분리|
|헥사고날|포트와 어댑터로 도메인 보호|
|클린 아키텍처|의존성 항상 안쪽으로, 동심원 구조|

## 면접 단골 질문

**Q. 모놀리식 vs MSA 언제 선택하나?**

```
초기/소규모 → 모놀리식
  빠른 개발, 단순한 운영

규모 커지면 → MSA
  독립 배포, 장애 격리, 팀별 자율성

MSA는 복잡도 비용이 크기 때문에
충분한 규모가 될 때 전환
```

**Q. MSA에서 트랜잭션을 어떻게 처리하나?**

```
각 서비스가 독립 DB → 분산 트랜잭션 불가
→ Saga 패턴으로 해결

코레오그래피: 이벤트로 서비스끼리 직접 조율
오케스트레이션: 중앙 오케스트레이터가 조율

DB 저장 + 이벤트 발행 원자성
→ Outbox 패턴
```

**Q. API Gateway vs BFF 차이는?**

```
API Gateway: 모든 클라이언트 공통
  인증, 라우팅, Rate Limiting

BFF: 클라이언트별 전용
  데이터 조합, 최적화된 응답

실무: API Gateway + BFF 함께 사용
```

**Q. 헥사고날 vs 클린 아키텍처 차이는?**

```
목표: 같음 (비즈니스 로직 보호)
표현: 다름
  헥사고날 → 포트와 어댑터
  클린 → 동심원, 레이어 명확

실무에서는 엄격히 구분 안 하고 혼용
핵심은 의존성이 항상 안쪽으로
```

**Q. 사이드카 패턴이 뭔가?**

```
메인 서비스 옆에 보조 컨테이너
→ 공통 기능 (로깅, 보안, 트레이싱) 처리
→ 언어 무관하게 동일한 사이드카 사용
→ 서비스 메시의 기반
```

---

## MSA 전체 구성도

```
클라이언트 (모바일/웹)
    ↓
BFF (클라이언트별 최적화)
    ↓
API Gateway (인증, 라우팅, Rate Limiting)
    ↓
서비스 디스커버리 (Eureka)
    ↓
┌─────────┬─────────┬─────────┐
│주문서비스│결제서비스│배송서비스│
│사이드카  │사이드카  │사이드카  │
└─────────┴─────────┴─────────┘
    ↓           ↓          ↓
  주문DB      결제DB     배송DB
    ↓
Kafka (EDA)
    ↓
분산 트레이싱 (Zipkin)
```

---

## 핵심 요약

```
서비스 구조:
  소규모 → 모놀리식
  대규모 → MSA

통신:
  즉시 응답 → REST/gRPC
  비동기    → Kafka/EDA
  진입점    → API Gateway
  클라이언트별 → BFF

트랜잭션:
  분산 트랜잭션 → Saga
  이벤트 유실 방지 → Outbox

인프라:
  주소 관리 → 서비스 디스커버리
  공통 기능 → 사이드카
  디버깅    → 분산 트레이싱

코드 설계:
  단순 → 레이어드
  복잡 → 헥사고날 / 클린 아키텍처
```