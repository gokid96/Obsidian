# 조인 동작 원리

## 조인이란?

**두 개 이상의 테이블을 연결해서 데이터를 조회하는 것**

```sql
SELECT o.id, u.name
FROM orders o
JOIN users u ON o.user_id = u.id
```

---

## 조인 종류

**INNER JOIN**


```sql
SELECT * FROM orders o
INNER JOIN users u ON o.user_id = u.id
```

```
양쪽 모두 있는 것만

orders: [1,2,3]
users:  [1,2,4]
결과:   [1,2]  ← 둘 다 있는 것만
```

**LEFT JOIN**

sql

```sql
SELECT * FROM orders o
LEFT JOIN users u ON o.user_id = u.id
```

```
왼쪽 테이블 전부 + 오른쪽 매칭되는 것

orders: [1,2,3]
users:  [1,2,4]
결과:   [1,2,3]  ← orders 전부
        (3은 users 없으니 NULL)
```

**RIGHT JOIN**

sql

```sql
SELECT * FROM orders o
RIGHT JOIN users u ON o.user_id = u.id
```

```
오른쪽 테이블 전부 + 왼쪽 매칭되는 것

orders: [1,2,3]
users:  [1,2,4]
결과:   [1,2,4]  ← users 전부
        (4는 orders 없으니 NULL)
```

**FULL OUTER JOIN**

sql

```sql
-- MySQL은 직접 지원 안 해서 UNION으로 구현
SELECT * FROM orders o LEFT JOIN users u ON o.user_id = u.id
UNION
SELECT * FROM orders o RIGHT JOIN users u ON o.user_id = u.id
```

```
양쪽 전부

orders: [1,2,3]
users:  [1,2,4]
결과:   [1,2,3,4]  ← 전부
```

**CROSS JOIN**

sql

```sql
SELECT * FROM orders CROSS JOIN users
```

```
모든 조합 (카테시안 곱)

orders: [A,B]
users:  [1,2,3]
결과:   [A1, A2, A3, B1, B2, B3]

→ 데이터 많으면 폭발적으로 증가
→ 의도적으로 쓰는 경우 드묾
```

---

## 조인 알고리즘

조인 종류가 아니라 **내부적으로 어떻게 처리하는가**

### 중첩 루프 조인 (Nested Loop Join)

```
for each row in 외부 테이블:
    for each row in 내부 테이블:
        if 조인 조건 맞으면:
            결과에 추가
```

```
users (10명) JOIN orders (1000건)

users 10번 반복
  각각 orders 1000번 반복
  → 총 10 × 1000 = 10,000번 비교
```

**인덱스 있을 때:**

```
users 10번 반복
  각각 orders 인덱스로 바로 찾음
  → 10 × O(log N) 비교
  → 훨씬 빠름
```

```
장점: 작은 테이블 or 인덱스 있을 때 빠름
단점: 큰 테이블 풀스캔 시 매우 느림
적합: 외부 테이블 작고 내부 테이블에 인덱스 있을 때
```

### 해시 조인 (Hash Join)

```
1단계: 빌드 (Build)
  작은 테이블로 해시 테이블 생성
  user_id → user 행

2단계: 프로브 (Probe)
  큰 테이블을 스캔하면서
  해시 테이블에서 매칭 찾기
```

```
users (10명) → 해시 테이블 생성
  {1: 김철수, 2: 이영희, ...}

orders (1000건) 스캔
  각 주문의 user_id로 해시 테이블 조회
  → O(1) 탐색
  → 총 1000번 비교
```

```
장점: 인덱스 없어도 빠름, 대용량에 효율적
단점: 해시 테이블을 메모리에 올려야 함
     메모리 부족 시 디스크 사용 → 느려짐
적합: 인덱스 없는 대용량 테이블 조인
```

### 정렬 병합 조인 (Sort Merge Join)

```
1단계: 양쪽 테이블을 조인 키로 정렬
2단계: 정렬된 두 테이블을 순서대로 병합
```

```
users 정렬:   [1,2,3,4,5]
orders 정렬:  [1,1,2,3,3]

포인터로 순서대로 비교:
  user=1, order=1 → 매칭
  user=1, order=1 → 매칭
  user=2, order=2 → 매칭
  ...
```

```
장점: 이미 정렬된 데이터에서 매우 빠름
     인덱스가 정렬을 대신할 수 있음
단점: 정렬 비용 발생
적합: 대용량 테이블, 이미 정렬된 데이터
```

---

## 알고리즘 비교

|항목|중첩 루프|해시 조인|정렬 병합|
|---|---|---|---|
|인덱스 필요|O (성능용)|X|X|
|메모리 사용|적음|많음|중간|
|소규모 데이터|빠름|오버헤드|오버헤드|
|대규모 데이터|느림|빠름|빠름|
|정렬된 데이터|무관|무관|매우 빠름|

```
MySQL 옵티마이저가 자동으로 선택
→ 통계 기반으로 가장 빠른 알고리즘 결정
→ MySQL 8.0부터 해시 조인 지원
```

---

## 조인 성능 최적화

**1. 드라이빙 테이블을 작게**

```
중첩 루프 조인에서
외부 테이블(드라이빙 테이블)이 작을수록 빠름

나쁜 예:
orders(1000건) JOIN users(10명)
→ 1000번 반복하면서 users 탐색

좋은 예:
users(10명) JOIN orders(1000건)
→ 10번 반복하면서 orders 인덱스 탐색
```

**2. 조인 컬럼에 인덱스**

sql

```sql
-- user_id에 인덱스 없으면
SELECT * FROM orders o JOIN users u ON o.user_id = u.id
→ orders 풀스캔 + users 풀스캔
→ 매우 느림

-- user_id에 인덱스 있으면
→ 인덱스로 빠르게 매칭
```

**3. 조인 전에 데이터 줄이기**

sql

```sql
-- 나쁜 예
SELECT * FROM orders o
JOIN users u ON o.user_id = u.id
WHERE o.status = 'PENDING'

-- 좋은 예
SELECT * FROM (
    SELECT * FROM orders WHERE status = 'PENDING'
) o
JOIN users u ON o.user_id = u.id

→ 조인 전에 필터링으로 데이터 줄임
→ 조인 대상 행 수 감소
```

**4. 불필요한 조인 제거**

sql

```sql
-- 나쁜 예
SELECT o.id, o.amount
FROM orders o
JOIN users u ON o.user_id = u.id  ← users 안 씀
WHERE o.status = 'PENDING'

-- 좋은 예
SELECT id, amount
FROM orders
WHERE status = 'PENDING'
```

---

## EXPLAIN으로 조인 확인

sql

```sql
EXPLAIN SELECT o.id, u.name
FROM orders o
JOIN users u ON o.user_id = u.id
WHERE o.status = 'PENDING';
```

```
id | table  | type | key           | rows
1  | orders | ref  | idx_status    | 100   ← 드라이빙
1  | users  | ref  | PRIMARY       | 1     ← 드리븐

읽는 법:
위에 있는 테이블이 드라이빙 테이블
rows가 작을수록 좋음
type이 ALL이면 해당 테이블 풀스캔 → 인덱스 필요
```

---

## 핵심 Q&A

**Q. INNER JOIN vs LEFT JOIN 차이는?**

INNER JOIN은 양쪽 모두 매칭되는 행만 반환 LEFT JOIN은 왼쪽 테이블 전체를 반환하고 오른쪽에 매칭 없으면 NULL로 채움

**Q. 해시 조인이 뭔가?**

작은 테이블로 해시 테이블을 만들고 큰 테이블을 스캔하면서 O(1)로 매칭하는 방식 인덱스 없어도 대용량에서 효율적

**Q. 조인 성능을 어떻게 높이나?**

드라이빙 테이블을 작게 유지하고 조인 컬럼에 인덱스를 추가하고 조인 전에 WHERE로 데이터를 먼저 줄임 EXPLAIN으로 실행 계획 확인 필수

---

## 핵심 요약

|항목|내용|
|---|---|
|INNER JOIN|양쪽 매칭되는 것만|
|LEFT JOIN|왼쪽 전부 + 오른쪽 매칭|
|CROSS JOIN|모든 조합, 카테시안 곱|
|중첩 루프|인덱스 있을 때 빠름, 소규모 적합|
|해시 조인|인덱스 없어도 빠름, 대규모 적합|
|정렬 병합|정렬된 데이터에 빠름|
|최적화|드라이빙 테이블 작게, 조인 컬럼 인덱스|