# 스핀락 vs 뮤텍스 vs 세마포어

## 먼저 개념 정리

셋 다 **동시성 제어 도구**인데 동작 방식이 다름

```
공유 자원에 여러 스레드가 동시 접근하려 할 때
→ 어떻게 제어할 것인가?

스핀락  : 계속 확인하며 대기
뮤텍스  : 잠들었다가 깨어나서 대기
세마포어: 여러 개 동시 접근 허용하며 제어
```

---

## 스핀락 (Spinlock)

**락이 풀릴 때까지 계속 확인 (바쁜 대기)**

```
스레드 B: 락 획득 실패
while (락이 안 풀림) {
    // 계속 확인... 확인... 확인...
}
→ 락 풀림 → 획득
```

CAS가 내부적으로 스핀락 방식

```java
// 스핀락 개념 코드
AtomicBoolean lock = new AtomicBoolean(false);

void lock() {
    while (!lock.compareAndSet(false, true)) {
        // 계속 시도 (스핀)
    }
}

void unlock() {
    lock.set(false);
}
```

**장점**

- 컨텍스트 스위칭 없음
- 락 대기 시간이 아주 짧을 때 빠름

**단점**

- 대기 중에도 CPU 계속 사용
- 락 대기가 길면 CPU 낭비 심각

**적합한 상황**

- 락 보유 시간이 매우 짧은 경우
- 컨텍스트 스위칭 비용이 더 비싼 경우
- 멀티코어 환경

---

## 뮤텍스 (Mutex)

**락을 못 얻으면 잠들고, 해제되면 깨어남**

```
스레드 B: 락 획득 실패 → 잠듦 (블로킹)

스레드 A: 작업 완료 → 락 해제 → 스레드 B 깨움

스레드 B: 깨어남 → 락 획득 → 작업
```

Java의 `synchronized`, `ReentrantLock`이 뮤텍스

```java
// synchronized
synchronized (this) {
    // 임계 구역
}

// ReentrantLock
ReentrantLock lock = new ReentrantLock();

lock.lock();
try {
    // 임계 구역
} finally {
    lock.unlock();
}
```

**장점**

- 대기 중 CPU 사용 없음
- 락 대기가 긴 상황에 적합

**단점**

- 컨텍스트 스위칭 비용 발생
- 잠들고 깨어나는 오버헤드

**적합한 상황**

- 락 보유 시간이 긴 경우
- DB 접근, 파일 I/O 등

---

## 스핀락 vs 뮤텍스

```
락 대기 시간이 짧다 → 스핀락
  이유: 잠들고 깨는 비용 > 그냥 기다리는 비용

락 대기 시간이 길다 → 뮤텍스
  이유: CPU 낭비 > 잠들고 깨는 비용
```

|항목|스핀락|뮤텍스|
|---|---|---|
|대기 방식|루프 돌며 확인 (스핀)|잠들었다 깨어남|
|CPU 사용|대기 중에도 사용|대기 중 사용 안 함|
|컨텍스트 스위칭|없음|발생|
|적합한 상황|대기 시간 짧음|대기 시간 김|
|Java 예시|AtomicInteger|synchronized|

---

## 세마포어 (Semaphore)

**N개의 스레드까지 동시 접근 허용**

뮤텍스는 1개만 허용 (0 or 1) 세마포어는 N개까지 허용 (카운터)

```
세마포어 값: 3  (동시에 3개 스레드 허용)

스레드 A 진입 → 세마포어: 2
스레드 B 진입 → 세마포어: 1
스레드 C 진입 → 세마포어: 0
스레드 D 진입 시도 → 대기 (0이라 못 들어감)

스레드 A 완료 → 세마포어: 1
스레드 D 진입 → 세마포어: 0
```

```java
// 동시에 3개 스레드만 허용
Semaphore semaphore = new Semaphore(3);

semaphore.acquire(); // 진입 (카운터 -1)
try {
    // 작업
} finally {
    semaphore.release(); // 완료 (카운터 +1)
}
```

**실제 사용 예시**

```
DB 커넥션 풀: 최대 10개 커넥션
→ 세마포어(10)
→ 동시에 10개 요청만 DB 접근
→ 11번째는 커넥션 날 때까지 대기

외부 API 호출 제한: 초당 5개
→ 세마포어(5)
→ 동시 요청 5개로 제한
```

---

## 뮤텍스 vs 세마포어

```
뮤텍스: 화장실 1칸 (1명만)
        열쇠를 가진 사람만 들어갈 수 있고
        나올 때 열쇠 반납

세마포어: 주차장 (N대까지)
          빈 자리 있으면 들어가고
          꽉 차면 대기
          나오면 자리 생김
```

|항목|뮤텍스|세마포어|
|---|---|---|
|동시 접근|1개|N개|
|소유권|획득한 스레드만 해제 가능|누구나 release 가능|
|용도|임계 구역 보호|자원 개수 제한|
|Java|synchronized, ReentrantLock|Semaphore|

**소유권 차이가 핵심**

```
뮤텍스: 내가 잠근 것은 내가 열어야 함
세마포어: A가 acquire, B가 release 가능
          → 생산자/소비자 패턴에 활용
```

---

## 전체 비교

|항목|스핀락|뮤텍스|세마포어|
|---|---|---|---|
|동시 접근|1개|1개|N개|
|대기 방식|스핀|블로킹|블로킹|
|CPU 대기 중|사용|미사용|미사용|
|소유권|있음|있음|없음|
|적합한 상황|짧은 임계 구역|긴 임계 구역|자원 개수 제한|
|Java|AtomicInteger|synchronized|Semaphore|

---

## 연결

```
단일 서버
  ├── 스핀락     → 짧은 임계 구역, CAS 기반
  ├── 뮤텍스     → 긴 임계 구역, synchronized
  └── 세마포어   → 동시 접근 수 제한

DB
  ├── 비관적 락  → 뮤텍스와 유사 (1개만, 블로킹)
  ├── 낙관적 락  → 스핀락과 유사 (실패 시 재시도)
  └── 네임드 락  → 뮤텍스와 유사 (이름 기반)

분산 환경
  └── Redis 분산 락 → 뮤텍스의 분산 버전
```

---

## 핵심 Q&A

**Q. 스핀락이 뭔가?**

락이 풀릴 때까지 루프를 돌며 계속 확인하는 방식 대기 중에도 CPU를 사용하지만 컨텍스트 스위칭이 없어 락 대기 시간이 짧은 상황에서 뮤텍스보다 빠름

**Q. 뮤텍스와 세마포어의 차이는?**

뮤텍스는 1개 스레드만 허용하고 획득한 스레드만 해제 가능 세마포어는 N개까지 허용하고 누구나 release 가능 뮤텍스는 임계 구역 보호, 세마포어는 자원 개수 제한에 적합

**Q. Java에서 어떻게 쓰나?**

스핀락 → AtomicInteger, compareAndSet() 뮤텍스 → synchronized, ReentrantLock 세마포어 → Semaphore(N)

---

## 핵심 요약

|항목|내용|
|---|---|
|스핀락|루프 돌며 대기, CPU 사용, 짧은 임계 구역|
|뮤텍스|잠들었다 깨어남, 긴 임계 구역, 소유권 있음|
|세마포어|N개 동시 허용, 자원 개수 제한, 소유권 없음|
|스핀 vs 뮤텍스|대기 시간 짧으면 스핀, 길면 뮤텍스|
|뮤텍스 vs 세마포어|1개 vs N개, 소유권 유무|