# [mini커머스] 0. 프로젝트 로드맵

## 왜 시작했나
 커머스는 주문/결제/재고/쿠폰이 자연스럽게 분리되고, 대용량 트래픽 고민이 도메인 자체에서 나오는 구조라 선택했다.

---

## 시리즈 목차

```
[mini커머스] 0. 로드맵 (현재 글)
[mini커머스] 1. 요구사항 분석과 설계
[mini커머스] 2. TDD로 도메인 구현하기
[mini커머스] 3. 동시성 이슈 - 재고/잔액/쿠폰
[mini커머스] 4. DB 성능 최적화 - 인덱스와 쿼리 튜닝
[mini커머스] 5. Redis 캐시 전략
[mini커머스] 6. 이벤트 기반 아키텍처 - ApplicationEvent에서 Kafka로
[mini커머스] 7. MSA 분리
[mini커머스] 8. 부하테스트와 장애대응
```

---

## 단계별 가이드

### 1단계 — 설계 (1~2주)

**목표: 코드 없이 문서만으로 시스템을 정의한다**

할 것:

- 요구사항 분석 (기능적/비기능적 분리)
- ERD 설계
- 시퀀스 다이어그램
- API 명세 작성
- Mock API 구현 + E2E 테스트 작성

기능 범위 (이것만):

- 잔액 조회/충전
- 상품 목록 조회
- 선착순 쿠폰 발급/조회
- 주문/결제
- 인기상품 Top5 (배치)

**기록 포인트: 왜 이렇게 설계했는지 이유를 남긴다**

---

### 2단계 — 모놀리식 TDD 구현 (3~4주)

**목표: 동작하는 시스템을 먼저 만든다**

도메인 구현 순서:

- 상품/재고 → 잔액 → 쿠폰 → 주문/결제

각 도메인마다:

```
단위 테스트 작성 (실패 확인)
→ 비즈니스 로직 구현 (통과)
→ 통합 테스트 작성
→ 컨트롤러 구현
→ Mock API → 실제 API 교체
```

이 시점엔 성능 고민 없이 기능만 완성.

---

### 3단계 — 동시성 (1주)

**목표: 동시 요청에서 데이터 정합성을 보장한다**

대상:

- 잔액 동시 충전
- 재고 동시 차감
- 선착순 쿠폰 동시 발급

방식:

- 멀티스레드 테스트로 이슈 먼저 재현
- 낙관적락 → 비관적락 → 분산락 순서로 비교
- 왜 그걸 선택했는지 근거 기록

**기록 포인트: 각 방식의 트레이드오프 비교**

---

### 4단계 — DB 성능 + 캐시 (1주)

**목표: 느린 쿼리를 찾고 개선한다**

DB 최적화:

- EXPLAIN으로 실행 계획 분석
- 복합 인덱스 적용
- before/after 수치 측정

캐시:

- 캐시 적용 대상 선정 (인기상품, 상품 목록)
- Cache-Aside 전략 적용
- TTL 설정 근거 기록

**기록 포인트: 수치가 반드시 있어야 함 (응답시간 X ms → Y ms)**

---

### 5단계 — 이벤트 기반 전환 (1~2주)

**목표: 서비스 간 결합도를 낮춘다**

순서:

- Spring ApplicationEvent로 먼저 이벤트 흐름 설계
- 주문 생성 → 잔액/재고/쿠폰 비동기 병렬 처리
- 보상 트랜잭션 설계 (실패 케이스)
- 한계 직접 확인 (단일 인스턴스, 이벤트 유실)
- Kafka로 교체
- Outbox 패턴으로 이벤트 유실 방지

**기록 포인트: ApplicationEvent의 한계를 직접 경험하고 Kafka로 넘어간 이유**

---

### 6단계 — MSA 분리 (1~2주)

**목표: 서비스를 독립적으로 배포 가능하게 분리한다**

서비스 분리:

```
user-service
product-service
order-service
payment-service
notification-service
```

분리 기준:

- 루트 애그리거트 단위
- Bounded Context 기준
- 생명주기 일치 여부

**기록 포인트: 왜 이 경계로 나눴는지 DDD 기반 근거**

---

### 7단계 — 부하테스트 (1주)

**목표: 병목을 찾고 수치로 개선을 증명한다**

환경:

- Docker + k6 + Grafana + Prometheus

시나리오:

- 주문/결제 Load Test (300 VU)
- 쿠폰 발급 Peak Test (1000 VU)

SLA:

- P99 응답시간 1초 이하
- 에러율 1% 미만

순서:

```
테스트 데이터 생성 (10,000건)
→ 시나리오 작성
→ 측정
→ 병목 원인 분석
→ 개선 적용
→ 재측정
→ before/after 비교표 작성
```

**기록 포인트: 개선 전후 수치 비교표가 핵심**

---

## 각 글에서 반드시 지킬 구조

```
1. 왜 이게 필요했나 (문제 정의)
2. 어떤 선택지가 있었나 (트레이드오프)
3. 왜 이걸 선택했나 (결정 근거)
4. 결과가 어떻게 됐나 (수치)
```

---

## 타임라인

|단계|기간|
|---|---|
|설계|1~2주|
|모놀리식 구현|3~4주|
|동시성 + 성능|2주|
|이벤트 + MSA|3~4주|
|부하테스트|1주|
|**총합**|**약 10~12주**|