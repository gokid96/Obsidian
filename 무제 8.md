# DB 풀텍스트 검색

## LIKE의 한계

```sql
-- 이름에 '김'이 포함된 사용자 검색
SELECT * FROM users WHERE name LIKE '%김%';
```

```
문제 1: 인덱스 못 탐
  LIKE 앞에 % → 인덱스 무력화
  → 풀 테이블 스캔
  → 데이터 많을수록 느려짐

문제 2: 단순 문자열 매칭
  '김치찌개' 검색 시
  → '김' 포함된 모든 것 나옴
  → 관련성 점수 없음
  → 정확도 낮음

문제 3: 형태소 분석 없음
  '먹다' 검색 시
  → '먹었다', '먹고' 안 나옴
  → 언어 특성 반영 불가
```

---

## 풀텍스트 검색이란?

**문서 전체를 색인해서 빠르고 정확하게 검색하는 방법**

```
일반 인덱스: 정확한 값 비교
풀텍스트:   문서 내용을 분석해서 색인

'오늘 점심으로 김치찌개를 먹었다'
  ↓ 형태소 분석
['오늘', '점심', '김치찌개', '먹다']
  ↓ 역색인
'김치찌개' → [문서1, 문서5, 문서9]
'먹다'     → [문서1, 문서3, 문서7]
```

---

## 역색인 (Inverted Index)

풀텍스트 검색의 핵심 자료구조

```
일반 인덱스:
  문서 ID → 내용
  문서1   → '오늘 점심으로 김치찌개를 먹었다'
  문서2   → '저녁에 삼겹살을 먹었다'

역색인:
  단어 → 문서 ID 목록
  '김치찌개' → [문서1]
  '삼겹살'   → [문서2]
  '먹다'     → [문서1, 문서2]

'먹다' 검색:
  역색인에서 바로 [문서1, 문서2] 찾음
  → O(1)에 가까운 탐색
```

---

## MySQL 풀텍스트 검색

```sql
-- 풀텍스트 인덱스 생성
CREATE FULLTEXT INDEX idx_content ON posts(content);

-- 풀텍스트 검색
SELECT * FROM posts
WHERE MATCH(content) AGAINST('김치찌개' IN BOOLEAN MODE);
```

**검색 모드:**

```sql
-- 자연어 모드 (기본)
AGAINST('김치찌개')
→ 관련성 점수로 정렬
→ 너무 많은 문서에 있는 단어는 무시

-- 불리언 모드
AGAINST('+김치찌개 -라면' IN BOOLEAN MODE)
→ + : 반드시 포함
→ - : 반드시 제외
→ 더 세밀한 검색

-- 쿼리 확장 모드
AGAINST('김치' WITH QUERY EXPANSION)
→ 1차 검색 결과로 관련 단어 찾아서 재검색
→ 연관 문서까지 포함
```

**MySQL 풀텍스트 한계:**

```
한국어 형태소 분석 미흡
  → '먹었다' 검색 시 '먹다' 못 찾음
  → n-gram 파서로 일부 보완 가능

대용량에서 성능 한계
  → 수천만 건 이상이면 느려짐

관련성 점수 조정 어려움
  → 커스터마이징 한계
```

---

## Elasticsearch

MySQL 풀텍스트의 한계를 넘어서면 Elasticsearch

```
분산 검색 엔진
→ 역색인 기반
→ 수억 건도 빠르게 검색
→ 한국어 형태소 분석 플러그인 지원 (nori)
→ 관련성 점수 커스터마이징 가능
```

**기본 구조:**

```
MySQL          Elasticsearch
────────────────────────────
데이터베이스  → 인덱스
테이블        → 타입 (현재는 인덱스와 동일)
행(row)       → 문서(document)
컬럼          → 필드
```

**데이터 동기화 패턴:**

```
MySQL (원본 데이터)
  ↓ 동기화
Elasticsearch (검색용)

쓰기: MySQL에 저장
검색: Elasticsearch에서 검색
→ 각자 잘하는 것만

동기화 방법:
  1. 애플리케이션에서 직접 양쪽에 씀
  2. CDC (Change Data Capture)
     → MySQL 바이너리 로그 감지
     → 변경사항 자동으로 ES에 반영
     → Debezium, Kafka 활용
```

---

## Elasticsearch 검색 동작

```json
// '김치찌개 맛집' 검색
GET /posts/_search
{
  "query": {
    "match": {
      "content": "김치찌개 맛집"
    }
  }
}
```

```
내부 동작:

1. '김치찌개 맛집' 형태소 분석
   → ['김치찌개', '맛집']

2. 역색인에서 각 단어 탐색
   '김치찌개' → [문서1, 문서5]
   '맛집'     → [문서1, 문서3, 문서8]

3. 관련성 점수 계산 (TF-IDF, BM25)
   문서1: '김치찌개' + '맛집' 둘 다 → 높은 점수
   문서3: '맛집'만 → 낮은 점수

4. 점수 순으로 정렬해서 반환
```

**관련성 점수 (BM25):**

```
TF (Term Frequency): 문서에 단어가 많을수록 높음
IDF (Inverse Document Frequency): 희귀한 단어일수록 높음

'맛집'이 모든 문서에 있으면 → 점수 낮음
'김치찌개 맛집'이 특정 문서에만 있으면 → 점수 높음
```

---

## 언제 뭘 쓰나?

```
데이터 적음 + 단순 검색
  → MySQL LIKE

데이터 중간 + 한국어 검색 필요
  → MySQL FULLTEXT + n-gram

데이터 많음 + 정확한 검색 + 관련성 필요
  → Elasticsearch

실시간성보다 검색 품질이 중요
  → Elasticsearch

검색이 핵심 기능인 서비스
  → Elasticsearch (쇼핑몰, 블로그, 뉴스)
```

---

## 핵심 Q&A

**Q. LIKE가 왜 느린가?**

앞에 %가 붙으면 인덱스를 못 타서 풀 테이블 스캔 발생 데이터가 많을수록 선형으로 느려짐 형태소 분석이 없어서 정확도도 낮음

**Q. 역색인이 뭔가?**

단어에서 문서를 찾는 자료구조 일반 인덱스와 반대로 단어 → 문서 ID 목록 형태 풀텍스트 검색의 핵심으로 O(1)에 가까운 탐색 가능

**Q. MySQL 풀텍스트 vs Elasticsearch?**

MySQL 풀텍스트는 간단한 검색에 적합하지만 한국어 지원과 대용량에서 한계가 있음 Elasticsearch는 대용량, 정확한 검색, 관련성 점수가 필요할 때

---

## 핵심 요약

|항목|내용|
|---|---|
|LIKE 한계|인덱스 무력화, 형태소 없음, 관련성 없음|
|역색인|단어 → 문서 ID, 풀텍스트 핵심 자료구조|
|MySQL FULLTEXT|간단한 검색, 한국어 미흡, 대용량 한계|
|Elasticsearch|분산, 형태소 분석, 관련성 점수, 대용량|
|동기화|MySQL 원본 → ES 검색용, CDC로 자동 동기화|
|BM25|TF-IDF 기반 관련성 점수 계산|
|선택 기준|데이터 양, 검색 정확도, 한국어 필요 여부|