# 동시성 제어 정리

## 전체 개념 

```
동시성 문제
    ↓
어떤 환경인가?
    ├── 단일 서버
    │     ├── 단순 카운터/플래그  → volatile, AtomicInteger (CAS)
    │     ├── 임계 구역 보호      → synchronized, ReentrantLock (뮤텍스)
    │     └── 자원 개수 제한      → Semaphore
    │
    ├── DB
    │     ├── 충돌 잦음           → 비관적 락 (FOR UPDATE)
    │     ├── 충돌 드묾           → 낙관적 락 (version)
    │     └── Redis 없는 분산     → 네임드 락 (GET_LOCK)
    │
    └── 분산 환경
          └── 여러 서버           → Redis 분산 락 (Redisson)
```

---

## 상황별 선택 가이드

**단순 숫자 증감**

```
count++을 여러 스레드가 동시에?
→ AtomicInteger

이유:
  synchronized보다 가볍고
  CAS로 락 없이 원자적 처리
```

**단순 플래그**

```
boolean 값을 한 스레드가 쓰고 다른 스레드가 읽기만?
→ volatile

이유:
  가시성만 필요
  원자성 필요 없음
  가장 가벼운 해결책
```

**임계 구역 (복합 연산)**

```
읽기 → 계산 → 쓰기 를 하나의 단위로?
→ synchronized or ReentrantLock

이유:
  원자성 + 가시성 모두 보장
  happens-before 규칙으로 안전
```

**동시 접근 수 제한**

```
DB 커넥션, 외부 API를 N개까지만 동시 접근?
→ Semaphore

이유:
  뮤텍스는 1개만
  세마포어는 N개 허용
```

**DB row 보호 (충돌 잦음)**

```
재고 차감, 좌석 예약처럼 동시 접근 많고 정확성 중요?
→ 비관적 락 (SELECT FOR UPDATE)

이유:
  미리 잠가서 충돌 원천 차단
  정확성 최우선
```

**DB row 보호 (충돌 드묾)**

```
게시글 수정, 프로필 변경처럼 동시 접근 적음?
→ 낙관적 락 (version 컬럼)

이유:
  락 없이 성능 좋음
  충돌 시 재시도
```

**분산 환경 (Redis 있음)**

```
여러 서버가 같은 자원에 접근?
외부 API 호출 포함한 전체 로직에 락?
→ Redis 분산 락 (Redisson)

이유:
  DB 밖에서도 유효
  Watchdog으로 안전한 갱신
  빠름
```

**분산 환경 (Redis 없음)**

```
Redis 인프라 없이 분산 락 필요?
→ 네임드 락 (GET_LOCK)

이유:
  MySQL만으로 분산 락 구현
  추가 인프라 불필요
```

**중복 요청 방지**

```
네트워크 오류로 같은 요청이 여러 번 올 때?
→ 멱등성 (Idempotency Key)

이유:
  락이 아닌 요청 식별로 해결
  재시도가 안전하게
```

---

## 대기 방식 선택

```
락 대기 시간이 짧다   → Busy Wait (스핀락)
락 대기 시간이 길다   → Notify (뮤텍스)
모르겠다 / 일반적     → Adaptive (synchronized, InnoDB)
```

---

## 전체 개념 한 줄 정리

|개념|한 줄 정리|
|---|---|
|트랜잭션/ACID|DB 작업을 하나의 단위로, 전부 성공 or 전부 실패|
|MVCC|읽기는 스냅샷으로, 락 없이 동시성 확보|
|비관적 락|미리 잠가서 충돌 차단, 충돌 잦을 때|
|낙관적 락|version으로 나중에 감지, 충돌 드물 때|
|네임드 락|MySQL 이름 기반 락, Redis 없는 분산 환경|
|Redis 분산 락|여러 서버 동시성 제어, Redisson으로 안전하게|
|멱등성|같은 요청 여러 번 와도 결과 동일|
|데드락|서로의 락 기다리다 영원히 대기, 순서 통일로 방지|
|CAS|예상값과 같을 때만 교체, 락 없는 낙관적 방식|
|스핀락|Busy Wait, 짧은 대기에 유리|
|뮤텍스|Notify, 긴 대기에 유리, 1개만 허용|
|세마포어|N개 동시 허용, 자원 개수 제한|
|2PL|락 획득/해제 단계 분리, 직렬성 보장|
|Busy Wait|루프 돌며 확인, CPU 점유|
|Notify|잠들었다 깨어남, CPU 반납|
|Adaptive|스핀 후 잠듦, 실무 표준|
|volatile|CPU 캐시 건너뛰고 메인 메모리 직접 접근|
|happens-before|A 결과가 B에게 반드시 보인다는 JMM 보장|
|Thread Pool|스레드 미리 생성해두고 재사용, 수 제한|

---

## QnA

**Q. 동시성 문제를 어떻게 해결하나?**

```
환경에 따라 다름

단일 서버:
  단순 연산 → AtomicInteger (CAS)
  복합 연산 → synchronized

분산 서버:
  Redis 있음 → Redis 분산 락
  Redis 없음 → 네임드 락

DB:
  충돌 잦음 → 비관적 락
  충돌 드묾 → 낙관적 락
```

**Q. 비관적 락 vs 낙관적 락**

```
비관적: 미리 잠금, 충돌 많을 때, 대기 발생
낙관적: version으로 감지, 충돌 적을 때, 재시도 필요
```

**Q. Redis 분산 락 vs DB 락**

```
Redis: 빠름, 외부 API 포함 전체 로직에 적용 가능
DB 락: Redis 없을 때, DB 내 자원만, 느림
```

**Q. synchronized vs volatile**

```
synchronized: 가시성 + 원자성, 임계 구역 보호
volatile: 가시성만, 단순 플래그/상태값
```

**Q. 스핀락 vs 뮤텍스**

```
스핀락: Busy Wait, 짧은 대기, CPU 점유
뮤텍스: Notify, 긴 대기, CPU 반납
실무: Adaptive (둘을 섞은 방식)
```

**Q. 데드락을 어떻게 방지하나?**

```
락 획득 순서 통일 (가장 효과적)
타임아웃 설정
트랜잭션 짧게 유지
```

---

## 핵심 요약

```
동시성 제어의 핵심 질문 3가지

1. 어떤 환경인가?
   단일 서버 / DB / 분산

2. 충돌이 얼마나 잦은가?
   잦음 → 비관적 (미리 차단)
   드묾 → 낙관적 (나중에 감지)

3. 대기 시간이 얼마나 긴가?
   짧음 → Busy Wait
   김   → Notify
   모름 → Adaptive
```