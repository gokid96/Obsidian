# Busy Wait vs Notify

## 락 대기 방식이란?

락을 획득하지 못했을 때 **어떻게 기다릴 것인가**에 대한 전략

```
락 획득 실패 → 어떻게 기다리지?

방법 1: 계속 확인하면서 기다린다 (Busy Wait)
방법 2: 잠들었다가 깨워주면 일어난다 (Notify)
```

---

## Busy Wait (바쁜 대기)

**락이 풀릴 때까지 계속 확인**

```
while (락이 안 풀림) {
    확인... 확인... 확인...
}
→ 락 풀림 → 즉시 획득
```

CPU를 점유한 채로 루프를 돔 = 스핀락 (Spinlock)

**비용 구조**

```
대기 중 CPU 사용 O
컨텍스트 스위칭  X
OS 개입          X
깨어나는 지연    X (이미 깨어있으니까)
```

---

## Notify 방식 (통보 대기)

**락을 못 얻으면 잠들고, OS가 깨워줌**

```
락 획득 실패
→ 잠듦 (sleep)
→ 락 해제됨
→ OS가 notify로 깨움
→ 락 획득
```

= 뮤텍스 (Mutex), synchronized

**비용 구조**

```
대기 중 CPU 사용 X
컨텍스트 스위칭  O (잠들 때 + 깨어날 때)
OS 개입          O (스케줄러)
깨어나는 지연    O (스케줄링 될 때까지)
```

---

## 그럼 Notify가 항상 좋은가?

**아니다. 대기 시간에 따라 다름**

```
대기 시간이 짧을 때:

Busy Wait:
  10ms 스핀 → 락 획득

Notify:
  잠듦 (컨텍스트 스위칭 1ms)
  → 락 해제 대기 10ms
  → OS 깨움 (컨텍스트 스위칭 1ms)
  → 락 획득

→ 대기가 짧으면 컨텍스트 스위칭 비용이 더 비쌈
→ Busy Wait이 유리
```

```
대기 시간이 길 때:

Busy Wait:
  1000ms 동안 루프
  → CPU 코어 하나를 1초간 독점
  → 다른 스레드가 그 코어 못 씀

Notify:
  잠듦 → CPU 반납
  → 다른 스레드가 코어 사용 가능
  → 락 해제되면 깨어남

→ 대기가 길면 코어 낭비가 더 비쌈
→ Notify가 유리
```

---

## 트레이드오프 판단 기준

**"Notify 방식이 자원이 덜 들 것 같다"**

```
틀린 말은 아닌데 조건이 있음

대기 시간이 길면 → Notify가 유리 (CPU 반납)
대기 시간이 짧으면 → Busy Wait이 유리
  (컨텍스트 스위칭 비용 > 스핀 비용)
```

**"그냥 계속 폴링하면 컨텍스트 스위치 비용도 I/O 비용도 안 드는데?"**

```
맞음. Busy Wait의 장점:
  컨텍스트 스위칭 없음
  OS 스케줄러 개입 없음
  락 풀리는 즉시 획득

단, 전제 조건:
  대기 시간이 짧아야 함
  멀티코어 환경이어야 함
  (싱글코어면 스핀하는 동안 락 가진 스레드가 실행 못 함)
```

결론:

```
대기 시간이 짧다 → Busy Wait
대기 시간이 길다 → Notify
상황에 따라 트레이드오프
```

---

## 비교 정리

|항목|Busy Wait|Notify|
|---|---|---|
|대기 중 CPU|점유|반납|
|컨텍스트 스위칭|없음|잠들 때 + 깨어날 때|
|OS 개입|없음|있음|
|반응 속도|즉시|스케줄링 지연|
|적합한 상황|대기 시간 짧음|대기 시간 김|
|예시|스핀락|뮤텍스, synchronized|

---

## 핵심 Q&A

**Q. Busy Wait이 뭔가?**

락이 풀릴 때까지 루프를 돌며 계속 확인하는 방식 CPU를 점유하지만 컨텍스트 스위칭이 없어서 대기 시간이 짧은 상황에서 Notify보다 빠름

**Q. Notify 방식이 뭔가?**

락을 못 얻으면 잠들고 OS가 락 해제 시 깨워주는 방식 대기 중 CPU를 반납하지만 잠들고 깨어날 때 컨텍스트 스위칭 비용 발생

**Q. 뭐가 더 좋은가?**

대기 시간이 짧다 → Busy Wait 대기 시간이 길다 → Notify 정답은 없고 상황에 따른 트레이드오프

---

## 핵심 요약

|항목|내용|
|---|---|
|Busy Wait|루프 돌며 확인, CPU 점유, 즉시 반응|
|Notify|잠들었다 깨어남, CPU 반납, 스케줄링 지연|
|짧은 대기|Busy Wait 유리 (스위칭 비용 > 스핀 비용)|
|긴 대기|Notify 유리 (코어 낭비 > 스위칭 비용)|
|스핀락|Busy Wait 구현체|
|뮤텍스|Notify 구현체|