## STEP01 — 설계 기본과제

### 📝 #1 요구사항 분석 문서 작성

**어떻게 했냐면:**

마크다운으로 작성. 도메인별로 나눠서 기능적/비기능적 요구사항을 분리.

markdown

```markdown
### 잔액 (Balance)

✅ 기능적 요구사항
[잔액 조회]
- 사용자 검증
- 잔액 없으면 0 반환

[잔액 충전]
- 충전 금액은 양수여야 함
- 최대 잔액 1,000만원 초과 불가
- 충전 이력 저장

☑️ 비기능적 요구사항
- 동시 충전 시 동시성 보장 (동시성 제어)
```

**핵심:** 비기능적 요구사항에 나중에 해결할 기술 고민을 미리 명시. 동시성, 캐싱, 트랜잭션이 여기서 이미 예고됨.

---

### 📝 #2 마일스톤 문서화 및 GitHub 연동

**어떻게 했냐면:**

두 가지를 같이 함.

GitHub Projects에서:

- 마일스톤(주차별) 생성
- 이슈 생성 후 마일스톤에 연결
- 칸반 보드 뷰 + 타임라인 뷰 설정

docs/architecture/02.Milestones.md에:

markdown

```markdown
## 📌 마일스톤 플로우
요구사항 분석 → 설계 문서 → 도메인 구현(TDD) → 통합테스트
→ 주문/결제 → 동시성 제어 → 캐싱/인덱스 튜닝 → Kafka 기반 메시징

## 📅 2주차 : 2025.03.29 ~ 2025.04.04
- 요구사항 분석 문서 작성
- 시퀀스 다이어그램 작성
- ERD 설계
...

## 📅 3주차 : 2025.04.05 ~ 2025.04.11
- 잔액/쿠폰/상품 비즈니스 로직 구현
...
```

---

### 📝 #3 시퀀스 다이어그램 작성

**어떻게 했냐면:**

Mermaid 문법으로 작성. GitHub에서 렌더링됨.

도메인별로 나눠서 작성:

- 잔액 조회 / 충전
- 상품 조회
- 쿠폰 목록 조회 / 발급
- 주문 생성 + 결제 완료 + 외부 전송
- 인기상품 배치 + 조회

**핵심:** 정상 흐름만이 아니라 예외 케이스를 반드시 포함.

```
sequenceDiagram
    autonumber
    actor 클라이언트
    participant 잔고
    participant 사용자

    클라이언트 ->>+ 잔고: 잔액 충전 요청
    잔고 ->>+ 사용자: 사용자 조회

    opt 사용자 존재 ❌
        사용자 -->> 클라이언트: 사용자 미존재 예외 ⚡️
    end

    잔고 ->> 잔고: 최대 잔액 검증
    opt 최대 잔액 초과 ❌
        잔고 -->> 클라이언트: 최대 잔액 초과 예외 ⚡️
    end

    잔고 -->>- 클라이언트: 잔액 충전 성공 ✅
```

각 다이어그램 아래에 번호별 설명도 함께 작성.

---

### 📝 #4 ERD 설계 및 작성

**어떻게 했냐면:**

Mermaid erDiagram으로 작성.

```
erDiagram
    USERS ||--o{ BALANCE : has
    USERS ||--o{ USER_COUPON : has
    COUPON ||--o{ USER_COUPON : issued
    USERS ||--o{ ORDER : places
    ORDER ||--o{ ORDER_PRODUCT : contains
    PRODUCT ||--o{ ORDER_PRODUCT : included
    PRODUCT ||--|| STOCK : has
    ORDER ||--|| PAYMENT : paid_by
```

테이블 목록:

```
users            사용자
balance          잔액
balance_history  잔액 이력 (충전/사용 기록)
product          상품
stock            재고 (상품과 분리)
coupon           쿠폰
user_coupon      사용자 쿠폰
order            주문
order_product    주문 상품 (주문-상품 N:M)
payment          결제
product_rank     인기상품 배치 집계
outbox           이벤트 유실 방지용
```

**핵심:** stock을 product와 분리한 이유, outbox 테이블 포함한 이유를 설명할 수 있어야 함.

---

### 📝 #5 API 명세 작성

**어떻게 했냐면:**

마크다운 테이블로 작성.

markdown

````markdown
## 잔액 API

### 잔액 조회
- Method: GET
- URL: /api/v1/users/{userId}/balance
- Response:
```json
  {
    "code": 200,
    "data": { "amount": 50000 }
  }
```
- Error: 404 사용자 미존재

### 잔액 충전
- Method: POST
- URL: /api/v1/users/{userId}/balance/charge
- Request:
```json
  { "amount": 10000 }
```
- Error: 400 충전 금액 오류, 400 최대 잔액 초과
````

각 API마다 Request/Response 스펙 + 에러 케이스 명시.

---

## STEP02 — 설계 심화과제

### ✨ #6 Mock API 구현

**어떻게 했냐면:**

실제 비즈니스 로직 없이 컨트롤러 레이어만 구현. 하드코딩된 응답 반환.

java

```java
@GetMapping("/users/{userId}/balance")
public ApiResponse<BalanceResponse> getBalance(@PathVariable Long userId) {
    // Mock: 실제 로직 없이 고정값 반환
    return ApiResponse.ok(new BalanceResponse(50000L));
}
```

**왜 하냐면:** E2E 테스트를 먼저 작성하기 위해. API 스펙이 확정된 상태에서 테스트를 미리 만들어두고, 나중에 실제 구현으로 교체.

---

### 📝 #7 Spring REST Docs 문서화

**어떻게 했냐면:**

테스트 코드 기반으로 API 문서 자동 생성.

java

```java
@Test
void 잔액_조회() throws Exception {
    mockMvc.perform(get("/api/v1/users/{userId}/balance", 1L))
        .andExpect(status().isOk())
        .andDo(document("balance-get",
            pathParameters(
                parameterWithName("userId").description("사용자 ID")
            ),
            responseFields(
                fieldWithPath("code").description("응답 코드"),
                fieldWithPath("data.amount").description("잔액")
            )
        ));
}
```

생성된 문서를 GitHub Pages로 배포해서 외부에서 볼 수 있게 함.

---

### ✨ #8 E2E 테스트 작성

**어떻게 했냐면:**

Mock API 기반으로 API 공통 응답 스펙 + 예외 케이스 테스트 먼저 작성.

java

```java
@Test
void 잔액_조회_사용자_미존재() throws Exception {
    // given: 존재하지 않는 userId
    // when
    mockMvc.perform(get("/api/v1/users/{userId}/balance", 999L))
    // then
        .andExpect(status().isNotFound())
        .andExpect(jsonPath("$.code").value(404));
}
```

나중에 실제 구현으로 교체해도 이 테스트들이 통과하면 스펙이 맞는 것.

---

### 📝 #9 API Request http 파일 작성

**어떻게 했냐면:**

IntelliJ .http 파일로 실제 API 호출 테스트용 파일 작성.

http

```http
### 잔액 조회
GET {{base_url}}/api/v1/users/1/balance

### 잔액 충전
POST {{base_url}}/api/v1/users/1/balance/charge
Content-Type: application/json

{
  "amount": 10000
}

### 쿠폰 발급
POST {{base_url}}/api/v1/users/1/coupons/publish
Content-Type: application/json

{
  "couponId": 1
}
```

`http/api/` 폴더에 도메인별로 분리해서 저장.

---

### 📝 #10 상태 다이어그램 작성

**어떻게 했냐면:**

Mermaid stateDiagram으로 작성. 상태가 변하는 도메인만.

```
stateDiagram-v2
    [*] --> PENDING : 주문 생성
    PENDING --> COMPLETED : 결제 성공
    PENDING --> CANCELLED : 결제 실패 / 보상 트랜잭션
    COMPLETED --> [*]
    CANCELLED --> [*]
```

```
stateDiagram-v2
    [*] --> PUBLISHABLE : 쿠폰 생성
    PUBLISHABLE --> PUBLISHED : 발급 완료 (수량 소진)
    PUBLISHABLE --> EXPIRED : 만료일 초과
    PUBLISHED --> EXPIRED : 만료일 초과
```

---

## 한눈에 정리

|이슈|방법|도구|
|---|---|---|
|#1 요구사항 분석|기능적/비기능적 분리 작성|Markdown|
|#2 마일스톤|문서 작성 + GitHub 연동|GitHub Projects|
|#3 시퀀스 다이어그램|정상+예외 케이스|Mermaid|
|#4 ERD|테이블 설계|Mermaid erDiagram|
|#5 API 명세|Request/Response/Error|Markdown|
|#6 Mock API|하드코딩 응답 컨트롤러|Spring|
|#7 REST Docs|테스트 기반 문서 자동 생성|Spring REST Docs|
|#8 E2E 테스트|Mock API 기반 스펙 테스트|MockMvc|
|#9 http 파일|API 호출 테스트 파일|IntelliJ .http|
|#10 상태 다이어그램|상태 변화 도메인만|Mermaid stateDiagram|