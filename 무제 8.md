# 테이블에 Row를 Insert할 때 DB가 다운되어 있다면?

DB가 일시적으로 응답하지 않을 때 단순히 예외를 던지고 데이터를 잃어버리면 서비스 신뢰도는 바닥으로 떨어집니다. 이 글에서는 DB 장애 상황에서 데이터를 안전하게 보존하는 4가지 전략을 소개합니다.

---

## 전략 1 — Retry with Exponential Backoff

가장 단순한 접근입니다. 실패하면 대기 시간을 지수적으로 늘리며 재시도합니다.

```java
@Retryable(
    value = { DataAccessException.class },
    maxAttempts = 5,
    backoff = @Backoff(delay = 500, multiplier = 2)
)
public Order insertOrder(Order order) {
    return orderRepository.save(order);
}

@Recover
public Order recover(DataAccessException e, Order order) {
    // 모든 재시도 실패 시 폴백 처리 (알림, 큐 적재 등)
    failureQueue.enqueue(order);
    return null;
}
```

> `spring-retry` 의존성 추가 후 `@EnableRetry`를 설정 클래스에 붙여줘야 합니다.

**장점:** 구현이 단순하고 일시적 장애에 효과적  
**단점:** 장기 장애 시 쓰레드 블로킹, 데이터 영속성 보장 불가

---

## 전략 2 — Transactional Outbox Pattern

비즈니스 데이터와 이벤트를 **같은 트랜잭션**으로 로컬 DB에 저장하고, 별도 프로세스가 목적지에 relay합니다. 데이터 유실 방지에 가장 강력한 패턴입니다.

```
[비즈니스 로직] --트랜잭션--> [orders 테이블 + outbox 테이블] --> [Relay] --> [Kafka / 외부 DB]
```

### 1. Outbox 테이블 엔티티

```java
@Entity
@Table(name = "outbox_events")
public class OutboxEvent {
    @Id @GeneratedValue
    private Long id;
    private String aggregateType;  // ex) "ORDER"
    private String aggregateId;
    private String eventType;      // ex) "ORDER_CREATED"
    @Lob
    private String payload;        // JSON 직렬화
    private Boolean processed = false;
}
```

### 2. 주문 저장 + Outbox 기록 (하나의 트랜잭션)

```java
@Transactional
public Order createOrder(Order order) throws JsonProcessingException {
    Order savedOrder = orderRepository.save(order);

    OutboxEvent event = new OutboxEvent();
    event.setAggregateType("ORDER");
    event.setAggregateId(savedOrder.getId().toString());
    event.setEventType("ORDER_CREATED");
    event.setPayload(objectMapper.writeValueAsString(savedOrder));
    outboxRepository.save(event);

    return savedOrder;
}
```

### 3. Relay Scheduler (outbox → Kafka 발행)

```java
@Scheduled(fixedDelay = 1000)
@Transactional
public void relay() {
    List<OutboxEvent> events = outboxRepository
        .findTop100ByProcessedFalseOrderByCreatedAtAsc();

    for (OutboxEvent event : events) {
        kafkaTemplate.send("orders", event.getAggregateId(), event.getPayload());
        event.setProcessed(true);
    }
}
```

**장점:** 데이터 유실 원천 차단, 원자성 보장 (2PC 없이)  
**단점:** outbox 테이블 관리 필요, Relay 프로세스 별도 운영

---

## 전략 3 — Message Queue 비동기 처리

DB에 직접 쓰는 대신 Kafka 같은 메시지 큐에 먼저 발행합니다. DB가 다운되어도 메시지는 큐에 안전하게 보관됩니다.

### Producer (요청 즉시 Kafka에 발행)

```java
public void publishInsertRequest(Order order) throws JsonProcessingException {
    String payload = objectMapper.writeValueAsString(order);
    kafkaTemplate.send("order-insert-requests", order.getId().toString(), payload);
}
```

### Consumer (DB가 복구되면 자동 처리)

```java
@KafkaListener(topics = "order-insert-requests", groupId = "order-consumer")
public void consume(ConsumerRecord<String, String> record) {
    Order order = objectMapper.readValue(record.value(), Order.class);
    orderRepository.save(order);
    // DB 실패 시 오프셋 커밋 안 됨 → 자동 재처리
}
```

**장점:** 대용량 트래픽 처리에 강함, DB 복구 후 자동 재처리  
**단점:** Kafka 인프라 구축 필요, 아키텍처 복잡도 증가

---

## 전략 4 — Circuit Breaker

DB가 지속적으로 장애일 때 계속 시도하면 오히려 시스템 전체에 과부하를 줍니다. Circuit Breaker는 장애를 감지하면 즉시 차단(Open)하여 빠른 실패(Fail Fast)를 유도합니다.

```java
public Order insertOrder(Order order) {
    return CircuitBreaker
        .decorateSupplier(circuitBreaker, () -> orderRepository.save(order))
        .recover(CallNotPermittedException.class, ex -> {
            // OPEN 상태 → 즉시 fallback 실행
            failureQueue.enqueue(order);
            return null;
        })
        .get();
}
```

```yaml
# application.yml
resilience4j:
  circuitbreaker:
    instances:
      dbInsert:
        failure-rate-threshold: 50        # 50% 실패 시 OPEN
        wait-duration-in-open-state: 10s  # 10초 후 HALF-OPEN 재시도
        sliding-window-size: 10
```

**장점:** 장기 DB 장애 시 시스템 전체 보호, 빠른 실패로 사용자 응답 속도 개선  
**단점:** 상태 관리 복잡, Fallback 전략 별도 설계 필요

---

## 전략 비교

|전략|데이터 안전성|구현 복잡도|적합한 상황|
|---|:-:|:-:|---|
|Retry + Backoff|보통|낮음|일시적(수 초) 장애|
|Outbox Pattern|높음|중간|금융, 주문 등 유실 불가 도메인|
|Message Queue|높음|높음|대용량 트래픽, MSA 환경|
|Circuit Breaker|보통|중간|장기 장애, 시스템 전체 보호 우선|

---

## 결론

대부분의 프로덕션 환경에서는 **Outbox Pattern + Circuit Breaker** 조합이 가장 이상적입니다.  
Outbox로 원자성과 데이터 안전성을 확보하고, Circuit Breaker로 연쇄 장애를 방지하세요.  
단순한 서비스라면 **Spring Retry**만으로도 충분한 경우가 많습니다.