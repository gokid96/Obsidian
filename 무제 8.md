# 실행 계획 심화 (EXPLAIN)

## 기본 복습

슬로우 쿼리 글에서 EXPLAIN을 다뤘는데 더 깊이 들어가면
```sql
EXPLAIN SELECT * FROM orders
WHERE user_id = 1
AND status = 'PENDING';
```

```
id | select_type | table  | type | key         | rows | Extra
1  | SIMPLE      | orders | ref  | idx_user_id | 100  | Using where
```

각 컬럼이 정확히 뭘 의미하는지 파악하면 쿼리 문제를 정확히 진단 가능

---

## id

```
쿼리 실행 순서

id가 같으면: 위에서 아래로 실행
id가 다르면: 큰 번호부터 실행 (서브쿼리)

예시:
id=1 | orders
id=1 | users   → orders와 JOIN, 같이 실행

id=2 | subquery → 먼저 실행
id=1 | orders   → 그 다음 실행
```

---

## select_type

```
SIMPLE
  → 서브쿼리, UNION 없는 단순 SELECT

PRIMARY
  → 가장 바깥쪽 SELECT

SUBQUERY
  → SELECT 절의 서브쿼리
  SELECT (SELECT COUNT(*) FROM orders) ...

DERIVED
  → FROM 절의 서브쿼리 (인라인 뷰)
  SELECT * FROM (SELECT * FROM orders) AS sub

UNION
  → UNION의 두 번째 이후 SELECT

DEPENDENT SUBQUERY
  → 외부 쿼리에 의존하는 서브쿼리 (성능 위험!)
  → 외부 행마다 서브쿼리 반복 실행
```

---

## type (핵심)

**성능 순서: system > const > eq_ref > ref > range > index > ALL**

```
system
  → 테이블에 행이 1개
  → 거의 안 나옴

const
  → PK나 유니크 인덱스로 1건 조회
  → 가장 빠름
  WHERE id = 1

eq_ref
  → JOIN 시 PK나 유니크 인덱스로 1건 매칭
  → JOIN에서 볼 수 있는 최선

ref
  → 일반 인덱스로 여러 건 조회
  WHERE user_id = 1  (user_id가 일반 인덱스)

range
  → 인덱스 범위 스캔
  WHERE created_at BETWEEN '2024-01-01' AND '2024-12-31'

index
  → 인덱스 풀 스캔
  → 테이블 풀 스캔보단 낫지만 느림

ALL
  → 테이블 풀 스캔 (위험!)
  → 인덱스 없거나 못 타는 상황
  → 반드시 개선 필요
```

---

## key 관련 컬럼

```
possible_keys
  → 사용 가능한 인덱스 목록
  → NULL이면 쓸 수 있는 인덱스 없음

key
  → 실제로 선택된 인덱스
  → NULL이면 인덱스 안 탐 (문제!)

key_len
  → 사용된 인덱스 길이 (바이트)
  → 복합 인덱스에서 몇 개 컬럼을 썼는지 확인 가능

  인덱스: (user_id INT, status VARCHAR(20))
  key_len = 4  → user_id만 사용
  key_len = 24 → user_id + status 모두 사용
```

---

## rows

```
예상 스캔 행 수 (통계 기반 추정값)

실제 값과 다를 수 있음
→ 통계가 오래됐으면 부정확
→ ANALYZE TABLE로 통계 갱신

rows가 크면:
  → 인덱스 추가 검토
  → 쿼리 조건 개선
  → 파티셔닝 고려
```

---

## Extra (핵심)

```
Using where
  → WHERE 조건으로 필터링
  → 일반적인 상황

Using index
  → 커버링 인덱스 사용
  → 테이블 접근 없음 → 빠름!

Using filesort
  → 인덱스로 정렬 못하고 별도 정렬 작업
  → ORDER BY가 인덱스와 불일치
  → 개선 필요

  나쁜 예:
  인덱스: (user_id)
  ORDER BY created_at  → filesort 발생

  좋은 예:
  인덱스: (user_id, created_at)
  ORDER BY created_at  → filesort 없음

Using temporary
  → 임시 테이블 사용
  → GROUP BY, DISTINCT, UNION 시 발생
  → 메모리 or 디스크 사용 → 느림
  → 인덱스로 해결 가능한지 검토

Using index condition
  → 인덱스 컨디션 푸시다운 (ICP)
  → 스토리지 엔진 레벨에서 필터링
  → Using where보다 효율적

NULL
  → 특별한 처리 없음
  → 좋은 신호
```

---

## EXPLAIN ANALYZE

MySQL 8.0.18부터 지원 실제 실행 후 결과 분석

```sql
EXPLAIN ANALYZE SELECT * FROM orders
WHERE user_id = 1
ORDER BY created_at DESC
LIMIT 10;
```

```
결과:
→ Filter: (orders.user_id = 1)
   (cost=100.5 rows=50)
   (actual time=0.5..2.3 rows=48 loops=1)

cost: 예상 비용
rows: 예상 행 수
actual time: 실제 실행 시간 (ms)
actual rows: 실제 행 수

예상 rows vs 실제 rows 차이 크면
→ 통계 업데이트 필요 (ANALYZE TABLE)
```

---

## 실전 진단 패턴

**패턴 1: type=ALL 발생**
```sql
EXPLAIN SELECT * FROM orders WHERE status = 'PENDING';

type: ALL  ← 풀 스캔
key: NULL  ← 인덱스 없음

해결:
CREATE INDEX idx_status ON orders(status);
```

**패턴 2: 인덱스 있는데 안 탐**
```sql
EXPLAIN SELECT * FROM orders
WHERE YEAR(created_at) = 2024;

type: ALL
key: NULL  ← 함수 때문에 인덱스 무력화

해결:
WHERE created_at >= '2024-01-01'
AND created_at < '2025-01-01'
```

**패턴 3: Using filesort**
```sql
EXPLAIN SELECT * FROM orders
WHERE user_id = 1
ORDER BY created_at DESC;

Extra: Using filesort  ← 정렬 문제

해결:
인덱스: (user_id, created_at)
→ user_id로 필터 + created_at으로 정렬
→ filesort 제거
```

**패턴 4: Using temporary**
```sql
EXPLAIN SELECT user_id, COUNT(*)
FROM orders
GROUP BY user_id;

Extra: Using temporary  ← 임시 테이블

해결:
CREATE INDEX idx_user_id ON orders(user_id);
→ 인덱스로 그룹핑 → 임시 테이블 불필요
```

**패턴 5: rows 예상과 실제 차이 큼**
```sql
-- 통계 갱신
ANALYZE TABLE orders;

-- 인덱스 힌트로 강제 지정
SELECT * FROM orders
USE INDEX (idx_user_id)
WHERE user_id = 1;
```

---

## 핵심 Q&A

**Q. EXPLAIN에서 가장 먼저 봐야 할 것은?**

type과 key type이 ALL이면 풀 스캔으로 즉시 개선 필요 key가 NULL이면 인덱스를 못 타는 것

**Q. Using filesort가 항상 나쁜가?**

데이터가 적으면 큰 문제 없음 데이터가 많을수록 성능 저하 심각 ORDER BY 컬럼을 인덱스에 포함시켜서 해결

**Q. EXPLAIN과 EXPLAIN ANALYZE의 차이는?**

EXPLAIN은 실행 계획만 보여주고 실제 실행 안 함 EXPLAIN ANALYZE는 실제 실행 후 예상값과 실제값 비교 예상 rows와 실제 rows 차이가 크면 통계 갱신 필요

---

## 핵심 요약

|항목|내용|
|---|---|
|type ALL|풀 스캔, 즉시 개선 필요|
|type const|PK 1건 조회, 가장 빠름|
|key NULL|인덱스 미사용, 문제|
|key_len|복합 인덱스 사용 컬럼 수 확인|
|Using index|커버링 인덱스, 빠름|
|Using filesort|별도 정렬, 인덱스로 개선|
|Using temporary|임시 테이블, 인덱스로 개선|
|EXPLAIN ANALYZE|실제 실행 후 예상 vs 실제 비교|