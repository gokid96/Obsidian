# DB 락 (Database Lock)

## 동시성 시리즈에서 배운 것 복습

```
비관적 락  → SELECT FOR UPDATE (row 잠금)
낙관적 락  → version 컬럼 비교
네임드 락  → GET_LOCK() 이름 기반
```

DB 락을 더 넓게 보면 종류가 더 있음

---

## DB 락 전체 구조

```
DB 락
├── 락 범위 기준
│     ├── 테이블 락 (Table Lock)
│     ├── 행 락 (Row Lock)
│     └── 갭 락 (Gap Lock)
│
└── 락 종류 기준
      ├── 공유 락 (Shared Lock)
      └── 배타 락 (Exclusive Lock)
```

---

## 공유 락 vs 배타 락

**공유 락 (Shared Lock / Read Lock)**

```sql
SELECT * FROM products WHERE id = 1 LOCK IN SHARE MODE;
```

```
여러 트랜잭션이 동시에 읽기 가능
쓰기는 불가

트랜잭션 A: 공유 락 획득 → 읽기 O
트랜잭션 B: 공유 락 획득 → 읽기 O  (동시 가능)
트랜잭션 C: 배타 락 획득 시도 → 대기 (공유 락 풀릴 때까지)
```

**배타 락 (Exclusive Lock / Write Lock)**

```sql
SELECT * FROM products WHERE id = 1 FOR UPDATE;
```

```
하나의 트랜잭션만 접근 가능
다른 트랜잭션은 읽기도 쓰기도 대기

트랜잭션 A: 배타 락 획득
트랜잭션 B: 공유 락 시도 → 대기
트랜잭션 C: 배타 락 시도 → 대기
```

|항목|공유 락|배타 락|
|---|---|---|
|목적|읽기 보호|쓰기 보호|
|동시 획득|여러 트랜잭션 가능|하나만 가능|
|SQL|LOCK IN SHARE MODE|FOR UPDATE|
|다른 읽기|허용|대기|
|다른 쓰기|대기|대기|

---

## 락 범위

**테이블 락 (Table Lock)**

```sql
LOCK TABLES products WRITE;
-- 작업
UNLOCK TABLES;
```

```
테이블 전체를 잠금
→ 다른 트랜잭션은 테이블 자체에 접근 불가
→ 동시성 매우 낮음
→ MyISAM 엔진 기본 방식
→ InnoDB에서는 거의 안 씀
```

**행 락 (Row Lock)**


```sql
SELECT * FROM products WHERE id = 1 FOR UPDATE;
```

```
특정 행만 잠금
→ 다른 행은 접근 가능
→ 동시성 높음
→ InnoDB 기본 방식
```

```
트랜잭션 A: id=1 행 락
트랜잭션 B: id=2 행 락  ← 동시에 가능
트랜잭션 C: id=1 행 락 시도 → 대기
```

**갭 락 (Gap Lock)**

```
행과 행 사이의 빈 공간을 잠금
→ 새로운 행 삽입 방지
→ Phantom Read 방지

id: 1, 3, 5 존재

WHERE id BETWEEN 1 AND 5 FOR UPDATE
→ id 2, 4 (없는 값)에 갭 락
→ 다른 트랜잭션이 id 2, 4 삽입 불가
```

**넥스트 키 락 (Next-Key Lock)**

```
행 락 + 갭 락을 함께 적용
→ InnoDB REPEATABLE READ 기본 방식
→ Phantom Read 완전 방지
```

---

## InnoDB 락 동작 방식

**인덱스 기반 락**

InnoDB는 인덱스에 락을 검

```sql
-- user_id에 인덱스 있음
SELECT * FROM orders WHERE user_id = 1 FOR UPDATE;
→ user_id = 1인 행에만 락

-- user_id에 인덱스 없음
SELECT * FROM orders WHERE user_id = 1 FOR UPDATE;
→ 테이블 전체 풀스캔
→ 모든 행에 락!
→ 동시성 급격히 저하
```

인덱스 없으면 행 락이 테이블 락처럼 동작

**의도 락 (Intention Lock)**

```
테이블 락과 행 락의 충돌 방지용

행 락 걸기 전에 테이블에 의도 락을 먼저 검
→ 다른 트랜잭션이 테이블 락 걸려할 때
→ 의도 락 확인 → 충돌 감지 → 대기

IS (Intention Shared): 공유 락 걸 예정
IX (Intention Exclusive): 배타 락 걸 예정
```

---

## 락 대기 확인

```sql
-- 현재 락 대기 상황 확인
SELECT * FROM information_schema.INNODB_TRX;

-- 락 점유/대기 상세
SELECT * FROM performance_schema.data_locks;

-- 락 대기 타임아웃 설정
SET innodb_lock_wait_timeout = 5;
→ 5초 안에 락 못 얻으면 에러 반환
```

---

## 동시성 시리즈와 연결

```
단일 서버
  공유 락  → 읽기 보호
  배타 락  → 쓰기 보호 (비관적 락)
  갭 락    → Phantom Read 방지
  넥스트키락 → REPEATABLE READ 완성

분산 환경
  DB 락만으로 부족
  → Redis 분산 락으로 확장

락 대기 방식
  DB 락 대기 → Notify 방식
  (락 못 얻으면 대기 큐에서 잠듦)
```

---

## 핵심 Q&A

**Q. 공유 락과 배타 락의 차이는?**

공유 락은 여러 트랜잭션이 동시에 읽기 가능하고 쓰기만 막음 배타 락은 하나의 트랜잭션만 접근 가능하고 읽기/쓰기 모두 막음 비관적 락(FOR UPDATE)이 배타 락

**Q. 인덱스 없으면 행 락이 왜 테이블 락이 되나?**

InnoDB는 인덱스에 락을 걸기 때문에 인덱스 없이 풀스캔하면 모든 행을 거치면서 전부 락이 걸림 인덱스 설계가 락 범위에 직접 영향을 줌

**Q. 갭 락이 왜 필요한가?**

행 락만으로는 새로운 행 삽입을 막을 수 없어서 Phantom Read가 발생할 수 있음 갭 락으로 빈 공간을 잠가서 삽입 자체를 막음

---

## 핵심 요약

|항목|내용|
|---|---|
|공유 락|읽기 보호, 동시 읽기 허용, LOCK IN SHARE MODE|
|배타 락|쓰기 보호, 하나만 접근, FOR UPDATE|
|테이블 락|전체 잠금, 동시성 낮음, MyISAM|
|행 락|특정 행만, 동시성 높음, InnoDB 기본|
|갭 락|빈 공간 잠금, Phantom Read 방지|
|넥스트 키 락|행 락 + 갭 락, InnoDB 기본|
|주의|인덱스 없으면 행 락 → 테이블 락처럼 동작|