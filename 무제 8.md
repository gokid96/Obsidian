## CDN이란?

**전 세계에 분산된 서버로 정적 콘텐츠를 가까운 곳에서 제공하는 것**

```
CDN 없이:

한국 사용자 → 미국 원본 서버
  → 거리 멀어서 느림 (200ms~)
  → 모든 요청이 원본 서버로

CDN 적용:

한국 사용자 → 한국 CDN 서버 (엣지 서버)
  → 가까워서 빠름 (10ms~)
  → 원본 서버 부하 감소
```

---

## 동작 원리

```
1. 사용자가 이미지 요청
   https://cdn.example.com/image.jpg

2. 가장 가까운 엣지 서버 확인
   → 한국 엣지 서버

3. 엣지 서버 캐시 확인
   → Hit: 바로 반환 (빠름)
   → Miss: 원본 서버에서 가져옴
           → 엣지 서버에 저장
           → 사용자에게 반환

4. 다음 요청부터
   → 엣지 서버에서 바로 반환
```

---

## CDN이 캐시하는 것

**정적 자원**

```
이미지 (jpg, png, gif, webp)
CSS, JavaScript 파일
폰트 파일
동영상
HTML (변경 없는 페이지)
```

**동적 컨텐츠도 가능**

```
API 응답 캐시
  → 자주 바뀌지 않는 응답
  → 상품 목록, 공지사항 등
  → TTL 짧게 설정
```

---

## Cache-Control 헤더

CDN과 브라우저가 캐시를 어떻게 할지 결정하는 헤더
```http
Cache-Control: max-age=86400, public
→ 86400초(1일) 동안 캐시
→ public: CDN, 브라우저 모두 캐시 가능

Cache-Control: no-cache
→ 항상 서버에 검증 요청
→ 변경 없으면 304 반환

Cache-Control: no-store
→ 캐시 절대 안 함
→ 민감한 데이터 (개인정보, 결제 정보)

Cache-Control: private
→ 브라우저만 캐시
→ CDN은 캐시 안 함
→ 사용자별 다른 데이터
```

---

## CDN 캐시 무효화

**문제:**

```
이미지 업데이트했는데
CDN에 이전 이미지가 캐시됨
→ 사용자가 이전 이미지 계속 봄
```

**해결 1: URL에 버전 포함 (가장 권장)**

```
image.jpg?v=1  → 구버전
image.jpg?v=2  → 신버전 (새 URL이라 캐시 Miss)

또는 파일명에 해시 포함:
image.abc123.jpg → 구버전
image.def456.jpg → 신버전

→ 파일 바뀌면 URL 자동으로 달라짐
→ CDN 캐시 무효화 불필요
→ 빌드 도구가 자동으로 처리 (Webpack, Vite)
```

**해결 2: CDN 캐시 직접 삭제 (Purge)**

```
CloudFront, Cloudflare 등
→ 특정 URL 캐시 강제 삭제
→ 즉시 반영

단점:
  비용 발생 (건당 과금)
  전파 시간 필요 (수 분)
```

**해결 3: TTL 짧게**

```
Cache-Control: max-age=300  → 5분
→ 최대 5분 불일치
→ 단순하지만 캐시 효율 낮음
```

---

## CDN 활용 패턴

**정적 자원 분리**

```
원본 서버: API만 처리
CDN: 이미지, CSS, JS 처리

→ 원본 서버 부하 대폭 감소
→ 정적 자원은 CDN이 전담
```

**이미지 최적화**

```
CDN에서 실시간 이미지 변환:
  원본: image.jpg (5MB)
  모바일 요청: image.jpg?w=300&f=webp
  → CDN이 리사이징 + webp 변환
  → 최적화된 이미지 반환

Cloudflare Images, CloudFront + Lambda@Edge 활용
```

**글로벌 서비스**

```
서울 사용자  → 서울 엣지
도쿄 사용자  → 도쿄 엣지
뉴욕 사용자  → 뉴욕 엣지

→ 어디서 접속해도 빠름
→ 원본 서버는 한 곳이어도 됨
```

---

## 주요 CDN 서비스

```
AWS CloudFront
  → AWS 인프라와 연동 쉬움
  → S3, EC2와 바로 연결

Cloudflare
  → 무료 플랜 있음
  → DDoS 방어 포함
  → 가장 많이 씀

Fastly
  → 실시간 캐시 무효화
  → 동적 콘텐츠 캐시에 강함

Akamai
  → 가장 오래된 CDN
  → 엔터프라이즈 급
```

---

## 캐시 시리즈와 연결

```
브라우저 캐시
  → 사용자 브라우저에 저장
  → Cache-Control 헤더로 제어

CDN 캐시 (엣지 캐시)
  → 가까운 엣지 서버에 저장
  → 정적 자원 전담

로컬 캐시
  → 서버 메모리에 저장
  → 빠르지만 불일치 위험

분산 캐시 (Redis)
  → 공유 캐시 서버
  → DB 부하 감소

DB 캐시
  → DB 내부 쿼리 캐시
  → 자동으로 동작
```

---

## 핵심 Q&A

**Q. CDN이 뭔가?**

전 세계에 분산된 엣지 서버로 정적 콘텐츠를 가까운 곳에서 제공하는 것 원본 서버까지 가지 않고 가까운 엣지에서 바로 반환 속도 향상과 원본 서버 부하 감소가 목적

**Q. CDN 캐시 무효화를 어떻게 하나?**

URL에 버전이나 해시를 포함하는 것이 가장 좋음 파일이 바뀌면 URL이 달라져서 자동으로 새로 받음 긴급하면 CDN 콘솔에서 직접 Purge 가능하지만 비용 발생

**Q. Cache-Control 헤더가 뭔가?**

브라우저와 CDN에게 캐시 방법을 알려주는 HTTP 헤더 max-age로 캐시 유지 시간 설정 no-store로 캐시 금지, private으로 CDN 캐시 제외 가능

---

## 핵심 요약

|항목|내용|
|---|---|
|CDN|전 세계 엣지 서버로 가까운 곳에서 제공|
|캐시 대상|이미지, CSS, JS 등 정적 자원|
|Cache-Control|캐시 방법 지정 헤더|
|max-age|캐시 유지 시간|
|no-store|캐시 금지 (민감 데이터)|
|무효화|URL 버전 포함이 가장 권장|
|주요 서비스|CloudFront, Cloudflare, Fastly|
|효과|속도 향상, 원본 서버 부하 감소|