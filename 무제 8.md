# RabbitMQ 개념 & 활용

**메시지 브로커 (Message Broker)**

```
생산자(Producer)가 메시지를 보내고
소비자(Consumer)가 메시지를 받는
전통적인 메시지 큐 시스템

AMQP (Advanced Message Queuing Protocol) 기반
→ 표준 프로토콜 사용
→ 다양한 언어/프레임워크 지원
```

---

## 핵심 구성 요소

**Producer (생산자)**

```
메시지를 보내는 쪽
→ Exchange에 메시지 발행
→ 어느 큐로 갈지는 Exchange가 결정
```

**Exchange (교환기)**

```
메시지를 받아서 적절한 큐로 라우팅
→ 우편물 분류소 같은 역할

라우팅 방식에 따라 4가지 타입:
  Direct, Fanout, Topic, Headers
```

**Queue (큐)**

```
메시지가 실제로 저장되는 곳
→ Consumer가 여기서 꺼내감
→ 메시지 순서 보장 (FIFO)
```

**Consumer (소비자)**

```
큐에서 메시지를 꺼내서 처리하는 쪽
→ 처리 완료 후 ACK 전송
→ ACK 없으면 큐에 다시 올라감
```

**Binding (바인딩)**

```
Exchange와 Queue를 연결하는 규칙
→ 어떤 메시지가 어느 큐로 갈지 정의
```

---

## Exchange 타입

**Direct Exchange**

```
라우팅 키가 정확히 일치하는 큐로 전달

Exchange → [routing_key: "order"] → 주문 큐
Exchange → [routing_key: "payment"] → 결제 큐

정확한 1:1 라우팅
```

**Fanout Exchange**

```
연결된 모든 큐에 브로드캐스트

Exchange → 큐 A
         → 큐 B
         → 큐 C

라우팅 키 무시
→ 모든 Consumer에게 동일한 메시지
→ 알림, 로그 수집에 적합
```

**Topic Exchange**

```
라우팅 키 패턴으로 매칭

order.* → 주문 관련 모든 메시지
order.created → 주문 생성만
*.payment → 모든 서비스의 결제

* : 단어 하나
# : 단어 여러 개

order.# → order.created, order.updated, order.deleted 모두
```

**Headers Exchange**

```
메시지 헤더 속성으로 라우팅
→ 라우팅 키 대신 헤더 값으로 판단
→ 복잡한 라우팅 조건에 사용
→ 잘 안 씀
```

---

## 메시지 흐름

```
Producer
  ↓ 메시지 발행 (routing_key: "order.created")
Exchange (Topic)
  ↓ 패턴 매칭
  ├── order.* 패턴 → 주문 큐
  └── *.created 패턴 → 생성 이벤트 큐
  ↓
Consumer A (주문 처리)
Consumer B (이벤트 로그)
```

---

## ACK / NACK

**메시지 처리 보장의 핵심**

```
Consumer: 메시지 수신
  → 처리 성공 → ACK 전송 → 큐에서 삭제
  → 처리 실패 → NACK 전송 → 큐에 다시 올라감
  → 응답 없음 → 타임아웃 → 큐에 다시 올라감

→ 메시지 유실 없음
→ 반드시 한 번은 처리됨 (At Least Once)
```

```java
@RabbitListener(queues = "order.queue")
public void handleOrder(Order order, Channel channel,
                        @Header(AmqpHeaders.DELIVERY_TAG) long tag) {
    try {
        orderService.process(order);
        channel.basicAck(tag, false);   // 성공 → ACK
    } catch (Exception e) {
        channel.basicNack(tag, false, true);  // 실패 → NACK, 재큐
    }
}
```

---

## Dead Letter Queue (DLQ)

**처리 실패한 메시지를 별도 큐로 보내는 것**

```
메시지 처리 실패 반복
  → 재시도 횟수 초과
  → Dead Letter Queue로 이동

DLQ:
  → 실패 메시지 보관
  → 나중에 분석/재처리
  → 메시지 유실 없음
```

```java
// DLQ 설정
@Bean
public Queue orderQueue() {
    return QueueBuilder.durable("order.queue")
        .withArgument("x-dead-letter-exchange", "dlx")
        .withArgument("x-dead-letter-routing-key", "order.dead")
        .withArgument("x-message-ttl", 30000)  // 30초 후 DLQ로
        .build();
}
```

---

## Spring Boot 연동

**의존성:**

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-amqp</artifactId>
</dependency>
```

**설정:**


```yaml
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
```

**Exchange, Queue 선언:**

```java
@Configuration
public class RabbitConfig {

    @Bean
    public TopicExchange orderExchange() {
        return new TopicExchange("order.exchange");
    }

    @Bean
    public Queue orderQueue() {
        return QueueBuilder.durable("order.queue").build();
    }

    @Bean
    public Binding orderBinding() {
        return BindingBuilder
            .bind(orderQueue())
            .to(orderExchange())
            .with("order.*");  // routing key 패턴
    }
}
```

**메시지 발행:**

```java
@Service
public class OrderProducer {

    private final RabbitTemplate rabbitTemplate;

    public void sendOrder(Order order) {
        rabbitTemplate.convertAndSend(
            "order.exchange",    // exchange
            "order.created",     // routing key
            order                // 메시지
        );
    }
}
```

**메시지 수신:**

```java
@Service
public class OrderConsumer {

    @RabbitListener(queues = "order.queue")
    public void handleOrder(Order order) {
        orderService.process(order);
        // 자동 ACK
    }
}
```

---

## 실제 활용 패턴

**1. 작업 큐 (Task Queue)**

```
무거운 작업을 비동기로 처리

이미지 업로드 → 큐에 넣음 → 응답 즉시 반환
                              ↓
                    Consumer: 이미지 리사이징 처리
```

**2. 알림 발송**

```
주문 완료 → Fanout Exchange
  → 이메일 큐 → 이메일 발송
  → SMS 큐   → SMS 발송
  → 푸시 큐  → 푸시 알림
```

**3. 우선순위 큐**

```
x-max-priority 설정으로 우선순위 처리

VIP 주문: priority=10 → 먼저 처리
일반 주문: priority=1  → 나중 처리
```

**4. 재시도 패턴**

```
처리 실패 → NACK → 재큐
일정 횟수 초과 → DLQ → 관리자 알림
```

---

## 주의사항

**1. 메시지 중복 처리**

```
NACK 후 재큐 → 같은 메시지 다시 처리
→ 멱등성 보장 필요
→ 메시지 ID로 중복 체크
```

**2. 큐 모니터링**

```
Consumer보다 Producer가 빠르면
→ 큐에 메시지 쌓임
→ 메모리 부족 → 장애

모니터링:
  RabbitMQ Management UI
  큐 길이, 처리 속도 확인
  → Consumer 수 조절
```

**3. 연결 관리**

```
Connection, Channel 재사용
→ 매번 새로 만들면 비용 큼
→ Spring AMQP가 자동으로 관리
```

---

## 핵심 Q&A

**Q. RabbitMQ가 뭔가?**

AMQP 기반의 메시지 브로커 Producer가 Exchange에 메시지를 보내면 Exchange가 라우팅 규칙에 따라 Queue로 전달 Consumer가 Queue에서 메시지를 꺼내서 처리

**Q. Exchange 타입 4가지는?**

Direct: 라우팅 키 정확히 일치 Fanout: 모든 큐에 브로드캐스트 Topic: 패턴으로 매칭 (* 단어 하나, # 여러 개) Headers: 헤더 속성으로 라우팅

**Q. ACK가 왜 중요한가?**

Consumer가 메시지를 받아도 처리 실패할 수 있음 ACK를 보내야 큐에서 삭제되고 NACK나 응답 없으면 큐에 다시 올라와서 재처리 메시지 유실 없이 반드시 처리됨을 보장

---

## 핵심 요약

|항목|내용|
|---|---|
|정의|AMQP 기반 메시지 브로커|
|Exchange|메시지 라우팅 담당|
|Direct|정확한 키 매칭|
|Fanout|모든 큐 브로드캐스트|
|Topic|패턴 매칭|
|ACK/NACK|처리 성공/실패 응답|
|DLQ|실패 메시지 보관 큐|
|활용|작업 큐, 알림, 우선순위 처리|