# WAL (Write-Ahead Logging)

## WAL이란?

**데이터를 실제로 쓰기 전에 로그를 먼저 쓰는 것**

```
WAL 없이:
데이터 변경 → 디스크에 바로 씀
→ 쓰는 도중 서버 크래시
→ 데이터 일부만 써짐
→ 데이터 손상!

WAL 적용:
데이터 변경 → 로그 먼저 씀 → 디스크에 씀
→ 쓰는 도중 서버 크래시
→ 로그로 복구 가능
```

---

## 왜 로그를 먼저 써야 하나?

디스크 쓰기의 특성 때문

```
메모리 쓰기: 빠름
디스크 쓰기: 느림

DB는 성능을 위해:
  변경사항을 메모리(버퍼)에 모아뒀다가
  나중에 한꺼번에 디스크에 씀

문제:
  메모리에만 있는 상태에서 크래시
  → 메모리는 휘발성
  → 데이터 유실!
```

WAL이 해결하는 방법:

```
1. 변경 내용을 로그 파일에 먼저 기록 (디스크)
2. 메모리(버퍼)에 변경 반영
3. 나중에 실제 데이터 파일에 씀

크래시 발생 시:
→ 로그 파일은 디스크에 있으니 살아있음
→ 로그로 복구 가능
```

---

## WAL 동작 흐름

```
UPDATE products SET stock = 9 WHERE id = 1;

1. 로그 파일에 기록
   "products id=1 stock 10→9 변경 예정"

2. 메모리 버퍼에 반영
   버퍼: stock = 9

3. 커밋
   "커밋 완료" 로그 기록
   → 클라이언트에 응답

4. 나중에 (백그라운드)
   메모리 버퍼 → 실제 디스크 데이터 파일에 씀
```

```
크래시가 3번 이후 4번 전에 발생하면?

재시작 시:
  로그 파일 확인
  "커밋 완료" 로그 있음
  → 실제 파일에 반영 안 됐음 확인
  → 로그로 다시 반영 (Redo)
  → 복구 완료
```

---

## Redo 로그 vs Undo 로그

**Redo 로그 (다시 하기)**

```
커밋된 변경사항을 재실행

용도:
  크래시 후 재시작 시
  커밋됐지만 디스크에 안 쓰인 것들 재반영

"이걸 해야 했는데 못 했어 → 다시 해"
```

**Undo 로그 (되돌리기)**

```
커밋 안 된 변경사항을 롤백

용도:
  크래시 후 재시작 시
  커밋 안 됐는데 일부 반영된 것들 취소
  MVCC 스냅샷 제공 (이전 버전 데이터)

"이걸 하다가 중간에 멈췄어 → 없던 일로"
```

```
크래시 복구 순서:

1. Redo 로그로 커밋된 것 재반영
2. Undo 로그로 커밋 안 된 것 롤백
→ 일관된 상태로 복구
```

---

## MySQL InnoDB WAL

**바이너리 로그 (Binary Log)**

```
모든 변경사항 기록
→ 레플리카 복제에 사용
→ Point-in-Time Recovery (특정 시점으로 복구)

Master → Slave 복제:
  Master의 바이너리 로그
  → Slave가 읽어서 동일하게 실행
  → 데이터 동기화
```

**InnoDB 리두 로그 (Redo Log)**

```
InnoDB 내부 WAL
→ 크래시 복구용
→ 링 버퍼 구조로 순환 사용

ib_logfile0, ib_logfile1
→ 가득 차면 다시 처음부터
```

**InnoDB 언두 로그 (Undo Log)**

```
변경 전 데이터 저장
→ 롤백 시 사용
→ MVCC 스냅샷 제공
   (이전 버전 데이터를 다른 트랜잭션에게 보여줌)
```

---

## Checkpoint

로그가 무한정 쌓이면 안 됨 → 체크포인트로 해결

```
체크포인트:
  주기적으로 메모리 버퍼 → 디스크에 씀
  → 이 시점 이전 로그는 복구에 필요 없음
  → 로그 공간 재사용 가능

크래시 복구 시:
  마지막 체크포인트부터만 redo 하면 됨
  → 복구 시간 단축
```

```
체크포인트 너무 자주:
  → 디스크 I/O 증가
  → 성능 저하

체크포인트 너무 드물게:
  → 로그 파일 커짐
  → 크래시 복구 시간 길어짐
```

---

## Failover와의 연결

```
DB 장애 대응 글에서 배운 것:

Master 장애 → Slave 승격 → 복구

WAL이 여기서 하는 역할:

1. 바이너리 로그로 Slave 복제
   → Slave가 최신 상태 유지

2. 크래시 복구
   → 재시작 시 WAL로 자동 복구
   → Redo: 커밋된 것 재반영
   → Undo: 미완성 트랜잭션 롤백

3. Point-in-Time Recovery
   → 특정 시점으로 복구 가능
   → 실수로 데이터 삭제해도 복구
```

---

## 성능과의 트레이드오프

```
WAL은 쓰기가 2번 발생
  1. 로그 파일 쓰기
  2. 실제 데이터 파일 쓰기

성능 최적화:
  로그는 순차 쓰기 (빠름)
  → 로그 파일에 append만 함
  → 랜덤 I/O 없음

  실제 데이터는 랜덤 쓰기 (느림)
  → 나중에 한꺼번에 씀 (배치)
  → I/O 횟수 최소화

결론:
  WAL 덕분에 안전성 + 성능 둘 다 확보
```

---

## 핵심 Q&A

**Q. WAL이 뭔가?**

데이터를 실제로 쓰기 전에 로그를 먼저 기록하는 방식 크래시 발생 시 로그로 데이터를 복구할 수 있음 MySQL InnoDB의 리두 로그, 언두 로그가 WAL 구현체

**Q. Redo 로그와 Undo 로그의 차이는?**

Redo 로그는 커밋된 변경사항을 재실행 크래시 후 커밋됐지만 디스크에 안 쓰인 것을 재반영 Undo 로그는 커밋 안 된 변경사항을 롤백 MVCC 스냅샷 제공에도 활용

**Q. WAL이 성능에 영향을 주지 않나?**

로그 쓰기는 순차 쓰기라 랜덤 I/O보다 빠름 실제 데이터는 나중에 한꺼번에 배치로 씀 안전성을 확보하면서도 성능을 유지하는 구조

---

## 핵심 요약

|항목|내용|
|---|---|
|정의|데이터 쓰기 전 로그 먼저 기록|
|목적|크래시 시 데이터 복구|
|Redo 로그|커밋된 것 재반영|
|Undo 로그|미완성 트랜잭션 롤백, MVCC 스냅샷|
|바이너리 로그|레플리카 복제, Point-in-Time Recovery|
|체크포인트|주기적 디스크 반영, 복구 시간 단축|
|성능|로그는 순차 쓰기, 실제 데이터는 배치|