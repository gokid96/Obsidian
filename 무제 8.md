# 클린 아키텍처 (Clean Architecture)

**비즈니스 로직을 중심에 두고 의존성이 항상 안쪽을 향하는 아키텍처**

```
로버트 마틴 (Uncle Bob)이 제안
헥사고날 아키텍처와 같은 목표
→ 비즈니스 로직을 외부로부터 보호
→ 의존성 역전
```

**동심원 구조:**

```
        ┌─────────────────────────┐
        │      Frameworks         │  ← 가장 바깥
        │  ┌───────────────────┐  │
        │  │    Interface      │  │
        │  │  ┌─────────────┐  │  │
        │  │  │ Application │  │  │
        │  │  │  ┌───────┐  │  │  │
        │  │  │  │Domain │  │  │  │  ← 가장 안쪽
        │  │  │  └───────┘  │  │  │
        │  │  └─────────────┘  │  │
        │  └───────────────────┘  │
        └─────────────────────────┘

의존성 방향: 항상 바깥 → 안쪽
안쪽은 바깥쪽을 절대 모름
```

---

## 4개의 레이어

**1. Domain (Entity)**

```
가장 안쪽
핵심 비즈니스 규칙

Order, Product, User 같은 도메인 객체
비즈니스 규칙을 메서드로 포함

외부 의존 없음
→ 프레임워크, DB, UI 몰라도 됨
→ 가장 변하지 않는 레이어
```

**2. Application (Use Case)**

```
비즈니스 흐름 정의
도메인 객체를 조합해서 유스케이스 구현

CreateOrderUseCase
PaymentUseCase
CancelOrderUseCase

Domain에만 의존
→ DB, 프레임워크 몰라도 됨
```

**3. Interface (Adapter)**

```
외부 입출력을 내부 형식으로 변환

Controller: HTTP 요청 → UseCase 호출
Presenter: UseCase 결과 → HTTP 응답
Gateway: UseCase 요청 → DB 쿼리

Application, Domain에 의존
```

**4. Frameworks (Infrastructure)**

```
가장 바깥
구체적인 기술 구현

Spring, JPA, MySQL, Redis
외부 API 클라이언트
웹 프레임워크

가장 자주 바뀌는 레이어
→ 안쪽에 영향 없이 교체 가능
```

---

## 의존성 규칙

**핵심 규칙:**

```
소스 코드의 의존성은
항상 안쪽을 향해야 한다

바깥쪽 → 안쪽 O
안쪽 → 바깥쪽 X

Domain은 아무것도 모름
Application은 Domain만 앎
Interface는 Application, Domain만 앎
Frameworks는 모두 앎
```

**의존성 역전 원칙 (DIP):**

```
Application이 DB를 써야 할 때

나쁜 예:
  OrderService → JpaRepository (바깥쪽 의존)

좋은 예:
  OrderService → OrderRepository (인터페이스)
  JpaOrderRepository → implements OrderRepository

→ 인터페이스로 의존성 역전
→ 안쪽이 바깥쪽 몰라도 됨
```

---

## 코드로 보기

**Domain (Entity)**

```java
// 가장 안쪽, 순수 Java
public class Order {
    private OrderId id;
    private List<OrderItem> items;
    private OrderStatus status;
    private Money totalAmount;

    // 비즈니스 규칙
    public void validateStock() {
        items.forEach(item -> {
            if (item.getQuantity() <= 0) {
                throw new InvalidOrderException("수량은 0보다 커야 함");
            }
        });
    }

    public Money calculateTotal() {
        return items.stream()
                .map(OrderItem::getSubtotal)
                .reduce(Money.ZERO, Money::add);
    }
}
```

**Application (Use Case)**

```java
// Domain에만 의존
public class CreateOrderUseCase {

    private final OrderRepository orderRepository;  // 인터페이스
    private final PaymentGateway paymentGateway;    // 인터페이스

    public OrderId execute(CreateOrderCommand command) {
        Order order = Order.create(command.getItems());
        order.validateStock();

        Money total = order.calculateTotal();
        paymentGateway.requestPayment(total);

        orderRepository.save(order);
        return order.getId();
    }
}
```

**Interface (Controller)**

```java
// Application에 의존
@RestController
public class OrderController {

    private final CreateOrderUseCase createOrderUseCase;

    @PostMapping("/orders")
    public ResponseEntity<OrderResponse> createOrder(
            @RequestBody OrderRequest request) {

        // HTTP 요청 → Command 변환
        CreateOrderCommand command = OrderMapper.toCommand(request);

        // UseCase 실행
        OrderId orderId = createOrderUseCase.execute(command);

        // 결과 → HTTP 응답 변환
        return ResponseEntity.ok(new OrderResponse(orderId));
    }
}
```

**Frameworks (JPA 구현체)**

```java
// 가장 바깥, 인터페이스 구현
@Repository
public class JpaOrderRepository implements OrderRepository {

    private final SpringDataOrderRepository jpaRepository;

    @Override
    public void save(Order order) {
        OrderEntity entity = OrderMapper.toEntity(order);
        jpaRepository.save(entity);
    }
}
```

---

## 클린 아키텍처 vs 헥사고날 아키텍처

```
목표: 같음
  → 비즈니스 로직 보호
  → 외부 의존성 분리
  → 테스트 용이

차이: 표현 방식
  헥사고날: 육각형, 포트와 어댑터
  클린: 동심원, 레이어 명확히 구분

실무:
  둘을 엄격히 구분하지 않고 혼용
  핵심 원칙은 같음
  → 의존성은 항상 안쪽으로
```

|항목|헥사고날|클린 아키텍처|
|---|---|---|
|제안자|Alistair Cockburn|Robert Martin|
|표현|육각형|동심원|
|핵심 개념|포트와 어댑터|의존성 규칙|
|레이어 구분|느슨함|명확함|
|목표|동일|동일|

---

## 레이어드 아키텍처와 비교

```
레이어드:
Controller → Service → Repository → DB
  모든 의존성이 아래로
  Service가 JpaRepository에 직접 의존

클린 아키텍처:
Controller → UseCase → (인터페이스) ← JpaRepository
  의존성이 항상 안쪽으로
  UseCase는 인터페이스만 앎
  JpaRepository가 인터페이스를 구현
```

---

## 장단점

**장점:**

```
테스트 용이
  → 도메인, 유스케이스를 DB 없이 테스트

기술 교체 용이
  → JPA → MyBatis 교체 시 안쪽 코드 무변경
  → MySQL → MongoDB 교체 가능

비즈니스 로직 명확
  → 도메인 레이어에 핵심 규칙 집중
  → 어디에 뭐가 있는지 명확
```

**단점:**

```
초기 복잡도 높음
  → 인터페이스, 매퍼 등 파일 수 증가
  → 단순 CRUD에는 과함

러닝 커브
  → 팀원 모두 아키텍처 이해 필요

오버엔지니어링 위험
  → 단순한 서비스에 적용 시
  → 생산성 저하
```

---

## 언제 도입하나?

```
도입 적합:
  복잡한 도메인 로직
  장기 운영 서비스
  테스트 커버리지 중요
  기술 교체 가능성 있음

도입 부적합:
  단순 CRUD
  빠른 프로토타이핑
  소규모 팀
  단기 프로젝트
  → 레이어드 아키텍처로 충분
```

---

## 핵심 Q&A

**Q. 클린 아키텍처가 뭔가?**

비즈니스 로직을 중심에 두고 의존성이 항상 안쪽을 향하는 아키텍처 Domain → Application → Interface → Frameworks 순으로 안쪽이 바깥쪽을 모름

**Q. 헥사고날 아키텍처랑 뭐가 다른가?**

목표와 핵심 원칙은 같음 헥사고날은 포트와 어댑터로 표현 클린 아키텍처는 동심원으로 레이어를 명확히 구분 실무에서는 둘을 엄격히 구분하지 않고 혼용

**Q. 레이어드 아키텍처랑 차이는?**

레이어드는 구현체에 직접 의존 클린은 인터페이스에 의존해서 의존성 역전 도메인이 DB나 프레임워크를 모르기 때문에 테스트와 교체가 쉬움

---

## 핵심 요약

|항목|내용|
|---|---|
|정의|의존성이 항상 안쪽을 향하는 아키텍처|
|Domain|핵심 비즈니스 규칙, 외부 의존 없음|
|Application|유스케이스, Domain에만 의존|
|Interface|입출력 변환, Controller|
|Frameworks|구체적 기술, JPA/Spring|
|핵심 규칙|의존성은 항상 안쪽으로|
|vs 헥사고날|목표 같음, 표현 방식 다름|
|단점|초기 복잡도, 오버엔지니어링 위험|