
**클라이언트와 서비스 사이의 단일 진입점**

```
API Gateway 없이:

클라이언트
  → 주문 서비스 (8081)
  → 결제 서비스 (8082)
  → 배송 서비스 (8083)
  → 회원 서비스 (8084)

→ 클라이언트가 모든 서비스 주소를 알아야 함
→ 인증을 각 서비스마다 처리
→ 서비스 주소 바뀌면 클라이언트도 수정
```

```
API Gateway 적용:

클라이언트
  → API Gateway
      → 주문 서비스
      → 결제 서비스
      → 배송 서비스
      → 회원 서비스

→ 클라이언트는 Gateway 주소만 알면 됨
→ 인증을 Gateway에서 한 번만 처리
→ 서비스 주소 바뀌어도 클라이언트 수정 불필요
```

---

## 주요 기능

**1. 라우팅 (Routing)**

```
요청 URL에 따라 적절한 서비스로 전달

/api/orders/**   → 주문 서비스
/api/payments/** → 결제 서비스
/api/users/**    → 회원 서비스

클라이언트는 Gateway 주소만 알면 됨
```

**2. 인증/인가 (Auth)**

```
Gateway에서 토큰 검증
→ 유효하면 서비스로 전달
→ 유효하지 않으면 즉시 거부

각 서비스마다 인증 로직 중복 작성 불필요
→ Gateway에서 한 번에 처리
```

**3. 로드 밸런싱**

```
같은 서비스가 여러 대일 때
→ 요청을 균등하게 분배

주문 서비스 인스턴스 3대:
  요청 1 → 인스턴스 1
  요청 2 → 인스턴스 2
  요청 3 → 인스턴스 3
  요청 4 → 인스턴스 1
  ...
```

**4. 서킷 브레이커**

```
서비스 장애 시 빠르게 실패 반환
→ 장애 전파 방지

주문 서비스 장애:
  → Gateway가 감지
  → 즉시 에러 반환
  → 주문 서비스로 요청 안 보냄
  → 다른 서비스 영향 없음
```

**5. Rate Limiting**

```
특정 클라이언트의 요청 수 제한

IP당 초당 100요청 제한
→ 100개 초과 시 429 Too Many Requests

→ DDoS 방어
→ API 남용 방지
```

**6. 요청/응답 변환**

```
클라이언트 요청을 서비스에 맞게 변환
헤더 추가/제거
요청 파라미터 변환
응답 형식 통일
```

**7. 로깅/모니터링**

```
모든 요청이 Gateway를 통과
→ 중앙에서 로깅
→ 응답 시간 모니터링
→ 에러율 추적
```

---

## Spring Cloud Gateway 예시

```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: order-service
          uri: lb://ORDER-SERVICE    # 로드 밸런싱
          predicates:
            - Path=/api/orders/**
          filters:
            - AuthFilter              # 인증 필터
            - StripPrefix=1

        - id: payment-service
          uri: lb://PAYMENT-SERVICE
          predicates:
            - Path=/api/payments/**
          filters:
            - AuthFilter
            - RequestRateLimiter     # Rate Limiting
```

**인증 필터 예시:**

```java
@Component
public class AuthFilter implements GatewayFilter {

    @Override
    public Mono<Void> filter(ServerWebExchange exchange,
                             GatewayFilterChain chain) {
        String token = exchange.getRequest()
                               .getHeaders()
                               .getFirst("Authorization");

        if (!jwtValidator.isValid(token)) {
            exchange.getResponse().setStatusCode(HttpStatus.UNAUTHORIZED);
            return exchange.getResponse().setComplete();
        }

        return chain.filter(exchange);  // 다음 필터로
    }
}
```

---

## Gateway vs 로드 밸런서

```
로드 밸런서 (L4):
  IP/Port 기반으로 트래픽 분산
  단순 분배가 목적

API Gateway (L7):
  URL, 헤더, 쿠키 기반으로 라우팅
  인증, Rate Limiting, 변환 등 부가 기능
  애플리케이션 레이어에서 동작
```

|항목|로드 밸런서|API Gateway|
|---|---|---|
|동작 레이어|L4 (TCP)|L7 (HTTP)|
|라우팅 기준|IP/Port|URL, 헤더|
|인증|X|O|
|Rate Limiting|X|O|
|요청 변환|X|O|
|복잡도|낮음|높음|

---

## 주의사항

**1. 단일 장애점 (SPOF)**

```
모든 요청이 Gateway를 통과
→ Gateway 다운 = 전체 서비스 중단

해결:
→ Gateway 다중화 (여러 대 운영)
→ 로드 밸런서 앞에 두기
```

**2. 병목 지점**

```
모든 트래픽이 Gateway 통과
→ Gateway 성능이 전체 성능에 영향

해결:
→ Gateway 수평 확장
→ 불필요한 로직 최소화
→ 캐싱 활용
```

**3. Gateway 로직 비대화**

```
편하다고 Gateway에 모든 로직 넣으면
→ Gateway가 복잡해짐
→ 단일 책임 원칙 위반

Gateway에 넣을 것:
  인증, 라우팅, Rate Limiting, 로깅

Gateway에 넣으면 안 되는 것:
  비즈니스 로직
  복잡한 데이터 변환
```

---

## MSA에서의 위치

```
클라이언트 (앱/웹)
    ↓
API Gateway
  ├── 인증/인가
  ├── 라우팅
  ├── Rate Limiting
  └── 로드 밸런싱
    ↓
┌──────────┬──────────┬──────────┐
│ 주문서비스│ 결제서비스│ 회원서비스│
└──────────┴──────────┴──────────┘
    ↓           ↓           ↓
  주문 DB     결제 DB     회원 DB
```

---

## 핵심 Q&A

**Q. API Gateway가 뭔가?**

클라이언트와 여러 서비스 사이의 단일 진입점 라우팅, 인증, 로드 밸런싱, Rate Limiting 등을 담당 클라이언트는 Gateway 주소만 알면 내부 서비스 구조 몰라도 됨

**Q. 로드 밸런서와 뭐가 다른가?**

로드 밸런서는 L4에서 단순 트래픽 분산 API Gateway는 L7에서 URL 기반 라우팅과 인증, Rate Limiting 등 부가 기능 제공 MSA에서는 보통 둘 다 사용

**Q. Gateway의 단점은?**

모든 요청이 통과해서 단일 장애점이 될 수 있음 트래픽이 집중되어 병목 지점이 될 수 있음 다중화와 수평 확장으로 해결

---

## 핵심 요약

|항목|내용|
|---|---|
|정의|클라이언트와 서비스 사이 단일 진입점|
|라우팅|URL 기반으로 적절한 서비스로 전달|
|인증|Gateway에서 한 번에 처리|
|Rate Limiting|요청 수 제한, DDoS 방어|
|서킷 브레이커|장애 서비스로 요청 차단|
|단점|SPOF, 병목 가능 → 다중화로 해결|
|실무|Spring Cloud Gateway, Kong, Nginx|