# DB 정리

## 전체 개념
```
DB 기초
  └── 정규화/역정규화  → 테이블 설계 원칙
  └── ERD/데이터 모델링 → 관계 설계
  └── 인덱스           → 빠른 탐색
  └── B-Tree/B+Tree    → 인덱스 내부 구조

쿼리
  └── 슬로우 쿼리      → 느린 쿼리 찾기
  └── 실행 계획        → 쿼리 진단
  └── 조인 동작 원리   → 조인 최적화
  └── 풀텍스트 검색    → LIKE 한계 극복

트랜잭션 / 동시성
  └── 트랜잭션/ACID    → 데이터 정합성
  └── 격리 수준        → 동시성 제어
  └── DB 락            → 락 종류와 범위
  └── WAL              → 장애 복구 원리

분산 / 확장
  └── 레플리카         → 읽기 분산
  └── 샤딩             → 쓰기 분산
  └── 파티셔닝         → 테이블 분할
  └── CAP 이론         → 분산 시스템 한계
  └── CQRS             → 읽기/쓰기 분리

운영
  └── 커넥션 풀        → DB 연결 관리
  └── Failover         → 장애 대응
  └── DB 설계 패턴     → 소프트 삭제, 이력 관리

비교
  └── NoSQL vs SQL     → DB 선택 기준
  └── 풀텍스트 검색    → MySQL vs MongoDB vs ES
```

---

## 상황별 선택 가이드

**데이터가 많아서 느릴 때**

```
조회가 느림
  → EXPLAIN으로 실행 계획 확인
  → type ALL이면 인덱스 추가
  → 커버링 인덱스 고려
  → 파티셔닝으로 테이블 분할

쓰기가 느림
  → 인덱스 수 줄이기 (쓰기 시 인덱스도 갱신)
  → 배치 처리로 묶어서 쓰기
  → 샤딩으로 DB 서버 분산
```

**트래픽이 많을 때**

```
읽기 트래픽 많음
  → 레플리카로 읽기 분산
  → 캐시 (Redis) 적용
  → CQRS로 읽기/쓰기 분리

쓰기 트래픽 많음
  → 샤딩으로 DB 서버 분산
  → 배치 처리
  → 커넥션 풀 튜닝
```

**검색 기능 구현**

```
단순 검색 (데이터 적음)
  → LIKE

중간 규모 검색
  → MySQL FULLTEXT
  → MongoDB $text

대용량 / 정확한 검색
  → Elasticsearch
  → MongoDB Atlas Search
```

**DB 선택**

```
정합성 중요 + JOIN 많음
  → RDB (MySQL, PostgreSQL)

대용량 + 유연한 스키마
  → MongoDB

캐시 / 세션
  → Redis

검색
  → Elasticsearch
```

**장애 대응**

```
DB 다운
  → Failover (Slave → Master 승격)
  → WAL로 크래시 복구

데이터 실수 삭제
  → Point-in-Time Recovery
  → 바이너리 로그로 복구
  → 소프트 삭제로 예방

성능 저하
  → 슬로우 쿼리 로그 확인
  → EXPLAIN으로 실행 계획 분석
  → 인덱스 추가
```

---

## 전체 개념 한 줄 정리

|개념|한 줄 정리|
|---|---|
|인덱스|데이터 빠르게 찾기 위한 자료구조|
|B+Tree|인덱스 내부 구조, 리프 노드 연결로 범위 조회 빠름|
|정규화|중복 제거, 데이터 정합성 보장|
|역정규화|조회 성능을 위해 의도적으로 중복 허용|
|슬로우 쿼리|기준 시간 초과 쿼리, 커넥션 풀 고갈 위험|
|실행 계획|EXPLAIN으로 쿼리 진단, type/key/rows 주목|
|조인|테이블 연결, 중첩 루프/해시/정렬 병합 알고리즘|
|풀텍스트 검색|역색인으로 빠른 검색, LIKE 한계 극복|
|트랜잭션/ACID|원자성/일관성/격리성/지속성 보장|
|격리 수준|동시성과 정합성 트레이드오프|
|DB 락|공유/배타, 행/테이블/갭 락|
|WAL|로그 먼저 쓰고 크래시 복구|
|레플리카|Master/Slave 복제, 읽기 분산|
|샤딩|DB 서버 여러 대, 쓰기 분산|
|파티셔닝|테이블 물리적 분할, 1대 서버|
|CAP 이론|분산 시스템에서 C/A/P 동시 불가|
|CQRS|읽기/쓰기 모델 분리|
|커넥션 풀|DB 연결 미리 생성해두고 재사용|
|Failover|장애 시 자동으로 다른 서버로 전환|
|DB 설계 패턴|소프트 삭제, 이력 관리, UUID vs Auto Increment|
|NoSQL vs SQL|정합성 vs 유연성, 상황에 따라 선택|

---

## 면접 단골 질문

**Q. 인덱스를 무조건 많이 만들면 좋은가?**

```
아니다
쓰기 시 인덱스도 함께 갱신
→ 인덱스 많을수록 쓰기 느려짐
→ 조회가 많은 컬럼에만 선택적으로
```

**Q. 샤딩 vs 파티셔닝 차이는?**

```
샤딩: 여러 DB 서버로 분산 (수평 확장)
파티셔닝: 하나의 서버 안에서 테이블 분할
샤딩이 더 복잡하고 강력한 확장
```

**Q. RDB vs NoSQL 언제 선택하나?**

```
RDB: 정합성 중요, JOIN 많음, 스키마 고정
NoSQL: 대용량, 유연한 스키마, 빠른 개발
하나만 고집하지 말고 상황에 따라 혼용
```

**Q. 레플리카 vs 샤딩 차이는?**

```
레플리카: 읽기 분산, 장애 대응
샤딩: 쓰기 분산, 대용량 처리
읽기가 많으면 레플리카
쓰기가 많으면 샤딩
```

**Q. 트랜잭션 격리 수준은 뭘 써야 하나?**

```
대부분: REPEATABLE READ (MySQL 기본값)
최신 데이터 항상 필요: READ COMMITTED
금융 등 정합성 최우선: SERIALIZABLE
```

**Q. 조인 성능을 어떻게 높이나?**

```
드라이빙 테이블 작게
조인 컬럼에 인덱스
조인 전에 WHERE로 데이터 줄이기
EXPLAIN으로 실행 계획 확인
```

---

## 핵심 요약

```
DB 성능 문제 → 인덱스, 실행 계획, 쿼리 최적화
트래픽 증가  → 레플리카, 샤딩, 커넥션 풀
장애 대응    → Failover, WAL, 소프트 삭제
검색 기능    → FULLTEXT, Elasticsearch
DB 선택      → RDB(정합성), NoSQL(유연성), Redis(캐시)
```