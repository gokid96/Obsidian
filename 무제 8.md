## 동시성 문제란?

```
사용자 A: 재고 확인 → 1개 있음 → 차감하려는 순간
사용자 B: 재고 확인 → 1개 있음 → 차감하려는 순간

→ 둘 다 재고 있다고 판단
→ 동시에 차감
→ 재고 -1개 발생! (Lost Update)
```

이걸 해결하는 방법이 두 가지

---

## 비관적 락 (Pessimistic Lock)

**"어차피 충돌날 거야, 미리 잠가버리자"**

sql

```sql
SELECT * FROM products WHERE id = 1 FOR UPDATE;
```

- 조회하는 순간 해당 row에 락을 검
- 다른 트랜잭션은 락이 풀릴 때까지 대기
- 트랜잭션이 끝나면 자동 해제

```
트랜잭션 A: SELECT FOR UPDATE → 락 획득
트랜잭션 B: SELECT FOR UPDATE → 대기 중...

트랜잭션 A: 재고 차감 → COMMIT → 락 해제
트랜잭션 B: 진입 → 재고 0 확인 → 실패
```

**언제 쓰나?**

- 충돌이 자주 발생하는 상황
- 재고 차감, 좌석 예약처럼 정확성이 중요한 경우
- 짧은 트랜잭션

**단점**

- 대기 시간 발생 → 성능 저하
- DB 커넥션을 오래 점유
- 데드락 위험

---

## 낙관적 락 (Optimistic Lock)

**"충돌이 드물겠지, 일단 하고 나중에 확인하자"**

DB에 락을 걸지 않고 version 컬럼으로 충돌을 감지

sql

```sql
-- 테이블에 version 컬럼 추가
CREATE TABLE products (
    id BIGINT,
    stock INT,
    version INT  -- 핵심
);
```

```
조회: SELECT id, stock, version FROM products WHERE id = 1
      → stock: 10, version: 5

차감: UPDATE products
      SET stock = 9, version = 6
      WHERE id = 1 AND version = 5  ← 핵심
```

내가 읽었을 때의 version과 업데이트 시점의 version이 다르면 → 누군가 먼저 수정한 것 → 실패

```
트랜잭션 A: 조회 (version=5) → 차감 시도 → version 5 맞음 → 성공 (version→6)
트랜잭션 B: 조회 (version=5) → 차감 시도 → version이 이미 6 → 실패!
            → 재시도 or 예외 반환
```

**언제 쓰나?**

- 충돌이 드문 상황
- 게시글 수정, 프로필 변경처럼 동시 접근이 적은 경우
- 읽기가 많고 쓰기가 적은 경우

**단점**

- 충돌 시 재시도 로직 필요
- 충돌이 잦으면 오히려 성능 나쁨
- 재시도가 반복되면 사용자 경험 저하

---

## 비교 정리

|항목|비관적 락|낙관적 락|
|---|---|---|
|방식|DB 락 (FOR UPDATE)|version 컬럼 비교|
|충돌 처리|미리 차단|나중에 감지|
|적합한 상황|충돌 잦음|충돌 드묾|
|성능|대기 발생|대기 없음|
|단점|데드락, 커넥션 점유|재시도 로직 필요|
|예시|재고 차감, 좌석 예약|게시글 수정, 프로필 변경|

---

## 그럼 분산 환경에서는?

비관적 락, 낙관적 락 모두 **DB 안에서만** 유효

```
서버 1 ─┐
서버 2 ─┼─→ DB (FOR UPDATE)  ← DB 레벨에서만 동시성 제어
서버 3 ─┘
```

외부 API 호출이 포함된 로직이라면? → DB 커넥션을 외부 API 응답 기다리는 동안 점유 → 커넥션 고갈 위험

이럴 때 **Redis 분산 락**이 필요

---

## 핵심 Q&A

**Q. 비관적 락이 뭔가?**

조회 시점에 `FOR UPDATE`로 row에 락을 걸어 다른 트랜잭션이 접근하지 못하게 미리 차단하는 방식 충돌이 자주 발생하는 상황에 적합

**Q. 낙관적 락이 뭔가?**

DB 락 없이 version 컬럼으로 충돌을 감지하는 방식 업데이트 시 version이 달라져 있으면 누군가 먼저 수정한 것으로 판단해 실패 처리 충돌이 드문 상황에 적합

**Q. 둘 중 뭘 써야 하나?**

충돌이 잦다 → 비관적 락 충돌이 드물다 → 낙관적 락 분산 서버 + 외부 API 포함 → Redis 분산 락

---

## 핵심 요약

|항목|내용|
|---|---|
|비관적 락|FOR UPDATE로 미리 잠금, 충돌 많을 때|
|낙관적 락|version으로 나중에 감지, 충돌 적을 때|
|공통 한계|DB 내에서만 유효|
|분산 환경|Redis 분산 락으로 확장|