# 트랜잭션 / ACID

## 트랜잭션이란?

**여러 DB 작업을 하나의 단위로 묶는 것**

```
전부 성공하거나 전부 실패
중간 상태는 없음
```

**대표 예시 - 계좌이체:**

```
1. A 계좌에서 10,000원 출금
2. B 계좌에 10,000원 입금

트랜잭션 없으면:
  1번 성공 → 서버 크래시
  2번 실패
  → A 계좌에서 돈은 빠졌는데
  → B 계좌엔 안 들어옴
  → 돈 증발!

트랜잭션 적용:
  1번 + 2번을 하나의 단위로
  → 둘 다 성공 or 둘 다 실패
  → 중간 상태 없음
```

---

## ACID

트랜잭션이 보장해야 하는 4가지 특성

**A - 원자성 (Atomicity)**

```
트랜잭션 안의 작업은 전부 성공 or 전부 실패

계좌이체:
  출금 성공 + 입금 실패
  → 전체 롤백
  → 출금도 취소

"원자처럼 더 이상 쪼갤 수 없다"
```

**C - 일관성 (Consistency)**

```
트랜잭션 전후로 DB가 일관된 상태 유지

계좌이체 전: A=10만, B=5만  (총 15만)
계좌이체 후: A=0만, B=15만  (총 15만)

→ 총합이 항상 같아야 함
→ 비즈니스 규칙 위반 불가
```

**I - 격리성 (Isolation)**

```
동시에 실행되는 트랜잭션이 서로 영향 안 줌

트랜잭션 A: 재고 확인 중
트랜잭션 B: 재고 수정 중

→ A는 B의 중간 상태를 볼 수 없음
→ 격리 수준으로 조절 가능
```

**D - 지속성 (Durability)**

```
커밋된 트랜잭션은 영구적으로 저장

커밋 완료 → 서버 크래시
→ 재시작해도 데이터 유지
→ WAL로 보장
```

---

## 트랜잭션 기본 사용

```sql
START TRANSACTION;

UPDATE accounts SET balance = balance - 10000 WHERE id = 1;
UPDATE accounts SET balance = balance + 10000 WHERE id = 2;

-- 성공 시
COMMIT;

-- 실패 시
ROLLBACK;
```

```java
// Spring @Transactional
@Transactional
public void transfer(Long fromId, Long toId, int amount) {
    accountRepository.decrease(fromId, amount);
    accountRepository.increase(toId, amount);
    // 예외 발생 시 자동 롤백
    // 정상 종료 시 자동 커밋
}
```

---

## 동시성 문제

격리성이 완벽하면 좋지만 격리 수준이 높을수록 동시성이 낮아짐

```
격리 수준 높음 → 동시성 낮음 → 성능 저하
격리 수준 낮음 → 동시성 높음 → 데이터 문제
```

발생할 수 있는 문제:

**Dirty Read**

```
A: 재고 10 → 5로 수정 (미커밋)
B: 재고 읽기 → 5 (커밋 안 된 값!)
A: 롤백 → 재고 다시 10

→ B는 존재하지 않는 값을 읽음
```

**Non-Repeatable Read**

```
A: 재고 읽기 → 10
B: 재고 수정 후 커밋 → 5
A: 재고 다시 읽기 → 5

→ 같은 트랜잭션에서 같은 데이터가 달라짐
```

**Phantom Read**

```
A: 재고 > 0 상품 조회 → 3개
B: 새 상품 추가 후 커밋
A: 재고 > 0 상품 다시 조회 → 4개

→ 같은 조건인데 결과 행 수가 달라짐
```

→ 이걸 해결하는 것이 **격리 수준** (다음 글)

---

## 핵심 Q&A

**Q. 트랜잭션이란?**

여러 DB 작업을 하나의 단위로 묶는 것 전부 성공하거나 전부 실패해서 중간 상태가 없음 계좌이체처럼 출금과 입금이 반드시 같이 성공해야 하는 경우

**Q. ACID가 뭔가?**

트랜잭션이 보장해야 하는 4가지 특성 원자성(전부 성공 or 실패), 일관성(규칙 유지), 격리성(서로 영향 없음), 지속성(커밋 후 영구 저장)

**Q. 격리성과 동시성은 왜 트레이드오프인가?**

격리성을 높이면 다른 트랜잭션을 기다리게 해야 해서 동시성이 낮아짐 격리성을 낮추면 동시성은 높아지지만 Dirty Read 등 문제 발생 격리 수준으로 트레이드오프를 조절

---

## 핵심 요약

|항목|내용|
|---|---|
|트랜잭션|여러 작업을 하나의 단위로, 전부 성공 or 전부 실패|
|원자성|더 이상 쪼갤 수 없음, 중간 상태 없음|
|일관성|트랜잭션 전후 DB 규칙 유지|
|격리성|동시 트랜잭션이 서로 영향 없음|
|지속성|커밋 후 영구 저장, WAL로 보장|
|Dirty Read|미커밋 데이터 읽기|
|Non-Repeatable|같은 데이터 두 번 읽었는데 다름|
|Phantom Read|같은 조건인데 행 수가 달라짐|