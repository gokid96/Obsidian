## 핵심 답변 구조

이 질문은 크게 세 가지 레이어로 나눠서 생각할 수 있어요.

### 1. 즉각적인 예외 처리 (애플리케이션 레벨)

DB 커넥션 자체가 실패하면 애플리케이션은 예외를 던져요. 이걸 그냥 에러로 터뜨리면 안 되고, 먼저 **재시도(Retry) 로직**을 고려해야 해요.

```
DB 다운 감지 → 즉시 실패시키지 않고 → N회 재시도 (Exponential Backoff)
```

Exponential Backoff란 재시도 간격을 1초 → 2초 → 4초 → ... 식으로 늘려가는 방식이에요. 순간적인 네트워크 끊김이나 DB 재시작 중이라면 이것만으로 해결되는 경우도 많아요.

---

### 2. 데이터를 잃으면 안 될 때 → 메시지 큐(Message Queue)

재시도도 실패하면, **요청 자체를 버리면 안 되는 상황**에서는 메시지 큐에 데이터를 임시 보관해요.

```
요청 → [Kafka / RabbitMQ / SQS] → DB 복구 후 Consumer가 꺼내서 Insert
```

이 패턴의 핵심은 **쓰기 요청을 비동기로 분리**해서, DB가 살아났을 때 밀린 작업을 처리하는 거예요. 데이터 유실 없이 결과적 일관성(Eventual Consistency)을 보장할 수 있어요.

---

### 3. 아예 DB 이중화가 되어 있다면 → Failover

운영 환경이라면 DB 자체가 단일 장애점(SPOF)이 되면 안 돼요. 면접관이 원했던 또 다른 포인트일 수 있어요.

- **Primary / Replica 구조** - Primary가 죽으면 Replica가 자동으로 Primary로 승격
- **AWS RDS Multi-AZ**, **MySQL Group Replication** 같은 솔루션이 이걸 담당해요

---

##  이렇게 답했다면 좋았을 것

> _"DB 다운 여부에 따라 단계적으로 대응하겠습니다. 먼저 Retry 로직으로 일시적 장애를 커버하고, 그래도 안 된다면 메시지 큐에 요청을 넣어 DB 복구 후 처리되도록 설계하겠습니다. 근본적으로는 DB 이중화로 단일 장애점을 없애는 게 맞다고 생각합니다."_

