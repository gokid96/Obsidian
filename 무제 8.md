# 스레드 경합이 일어나는 이유

## 메모리 구조

JVM 메모리 구조

```
┌─────────────────────────────────┐
│            힙 (Heap)            │  ← 모든 스레드가 공유
│  객체, 인스턴스 변수, 배열      │
├─────────────────────────────────┤
│          메서드 영역             │  ← 모든 스레드가 공유
│  클래스 정보, static 변수       │
├────────────┬────────────────────┤
│ 스택 A     │ 스택 B             │  ← 각 스레드가 독립적으로 보유
│ 지역변수   │ 지역변수           │
│ 메서드 호출│ 메서드 호출        │
└────────────┴────────────────────┘
```

```
스택은 스레드마다 독립
→ 지역변수는 공유 안 됨
→ 경합 없음

힙은 모든 스레드가 공유
→ 객체, 인스턴스 변수는 공유됨
→ 동시 접근 시 경합 발생
```

---

## 경합이 생기는 3가지 근본 원인

### 1. 원자성 문제

**하나의 연산처럼 보여도 사실 여러 단계**

```java
count++;  // 한 줄이지만 사실 3단계

1. 힙에서 count 읽기  (read)
2. +1 계산            (modify)
3. 힙에 count 저장    (write)
```

```
스레드 A: count 읽기 (0)
                        스레드 B: count 읽기 (0)
스레드 A: +1 계산 (1)
                        스레드 B: +1 계산 (1)
스레드 A: count 저장 (1)
                        스레드 B: count 저장 (1)

→ 둘 다 +1 했는데 결과는 2가 아닌 1
→ Lost Update!
```

읽고 → 계산하고 → 쓰는 사이에 다른 스레드가 끼어들 수 있음

**해결:** synchronized, CAS (AtomicInteger)

---

### 2. 가시성 문제

**CPU 캐시로 인해 변경이 안 보임**

```
CPU 코어 1          CPU 코어 2
┌──────────┐        ┌──────────┐
│  캐시    │        │  캐시    │
│ count=1  │        │ count=0  │  ← 갱신 안 됨
└──────────┘        └──────────┘
       ↕                  ↕
         메인 메모리 (count=1)
```

```
스레드 A (코어 1): count = 1로 변경
  → 코어 1 캐시에 반영
  → 메인 메모리 반영은 나중에

스레드 B (코어 2): count 읽기
  → 코어 2 캐시에서 읽음
  → 아직 0
  → 스레드 A의 변경을 못 봄
```

**해결:** volatile, synchronized

---

### 3. 명령어 재정렬 문제

**컴파일러/CPU가 성능을 위해 순서를 바꿈**

```java
// 내가 작성한 코드
x = 1;
y = 2;
flag = true;

// 실제 실행 순서 (재정렬 발생)
flag = true;   ← 순서 바뀜!
x = 1;
y = 2;
```

단일 스레드에서는 결과가 같으니 문제없음 멀티스레드에서는?

```
스레드 A:
  flag = true  ← 재정렬로 먼저 실행
  x = 1

스레드 B:
  flag 확인 → true
  x 읽기   → 0  ← 아직 x = 1 안 됨!
```

**해결:** volatile, happens-before

---

## 3가지 원인 정리

```
힙 공유
  ↓
동시 접근
  ↓
┌─────────────────────────────────────┐
│ 원자성 문제  │ 읽고-계산-쓰는 사이  │
│              │ 끼어들기 가능        │
├─────────────────────────────────────┤
│ 가시성 문제  │ CPU 캐시로 인해      │
│              │ 변경이 안 보임       │
├─────────────────────────────────────┤
│ 재정렬 문제  │ 컴파일러/CPU가       │
│              │ 순서를 바꿈          │
└─────────────────────────────────────┘
```

---

## 각 해결책과 연결

|문제|원인|해결책|
|---|---|---|
|원자성|읽기-계산-쓰기 사이 끼어들기|synchronized, CAS|
|가시성|CPU 캐시|volatile, synchronized|
|재정렬|컴파일러/CPU 최적화|volatile, happens-before|

```
synchronized:
  원자성 + 가시성 모두 해결
  하지만 무거움

volatile:
  가시성 + 재정렬 해결
  원자성은 해결 못함
  가벼움

CAS (AtomicInteger):
  원자성 + 가시성 해결
  락 없이 가볍게
```

---

## DB / 분산 환경에서도 같은 원리

```
단일 서버 스레드 경합
  힙(메모리) 공유 → 동시 접근 → 경합

DB 트랜잭션 경합
  테이블(디스크) 공유 → 동시 접근 → 경합
  → 비관적 락, 낙관적 락으로 해결

분산 서버 경합
  외부 자원 공유 → 동시 접근 → 경합
  → Redis 분산 락으로 해결
```

레벨만 다를 뿐 근본 원인은 같음 **공유 자원에 동시 접근하기 때문**

---

## 핵심 Q&A

**Q. 스레드 경합이 왜 생기나?**

힙 메모리를 모든 스레드가 공유하기 때문 스택은 각 스레드가 독립적으로 가져서 경합 없음 힙의 객체나 변수에 동시 접근할 때 경합 발생

**Q. 가시성 문제가 뭔가?**

각 CPU 코어가 자체 캐시를 가지기 때문에 한 스레드의 변경이 다른 스레드에 즉시 안 보이는 문제 volatile로 메인 메모리에서 직접 읽고 써서 해결

**Q. 원자성 문제가 뭔가?**

count++ 같은 연산이 사실 읽기-계산-쓰기 3단계라서 그 사이에 다른 스레드가 끼어들 수 있는 문제 synchronized나 CAS로 해결

---

## 핵심 요약

|항목|내용|
|---|---|
|근본 원인|힙(공유 메모리)에 동시 접근|
|원자성 문제|연산 중간에 끼어들기 가능|
|가시성 문제|CPU 캐시로 변경이 안 보임|
|재정렬 문제|컴파일러/CPU가 순서 바꿈|
|원자성 해결|synchronized, CAS|
|가시성 해결|volatile, synchronized|
|재정렬 해결|volatile, happens-before|
