## 프로젝트 관리 방식 (시작 전 세팅)

### GitHub Projects 세팅

**칸반 보드** (이슈 관리)

```
Todo → In Progress → Done
```

**타임라인** (주차별 일정)

- 주차별 마일스톤 설정
- 각 이슈를 타임라인에 배치

**이슈 네이밍 규칙**

```
✨ [STEP01] 설계 기본과제        ← 상위 이슈 (주차 과제)
📝 요구사항 분석 문서 작성       ← 세부 작업
🧪 동시성 실패 테스트 작성       ← 테스트
♻️ 병목 탐색 및 개선            ← 리팩토링/개선
```

**산출물 구조 (최종)**

```
mini-commerce/
├── docs/
│   ├── architecture/
│   │   ├── 01.Requirements.md
│   │   ├── 02.Milestones.md
│   │   ├── 03-1.SequenceDiagram.md
│   │   ├── 03-2.StateDiagram.md
│   │   ├── 04.ERD.md
│   │   └── 05.ApiDocument.md
│   ├── report/
│   │   ├── 01.DBPerformanceOptimizationReport.md
│   │   ├── 02.ConcurrencyReport.md
│   │   ├── 03.CacheStrategyReport.md
│   │   ├── 04.RedisDesignArchitectureReport.md
│   │   ├── 05.MsaEventDrivenArchitectureReport.md
│   │   ├── 06.KafkaDesignArchitectureReport.md
│   │   └── 07.LoadTestReport.md
│   ├── study/
│   │   └── 01.Kafka.md
│   └── WIL/
│       ├── week2/README.md
│       ├── week3/README.md
│       └── ...
├── common/
│   ├── cache
│   ├── client
│   ├── lock
│   ├── message
│   ├── outbox
│   ├── querydsl
│   └── serialize
├── service/
│   ├── balance
│   ├── coupon
│   ├── order
│   ├── payment
│   ├── product
│   └── user
├── support/
│   └── rest-docs
├── k6/
├── http/api/
├── sql/
├── infra-compose.yml
├── service-compose.yml
└── prometheus.yml
```

---

## 1단계 — 설계 (1~2주)

### 진행 방식

코드 한 줄 없이 문서만 작성. GitHub Issue로 세부 작업 관리.

### 이슈 구성

```
✨ [STEP01] 설계 기본과제
  ├── 📝 요구사항 분석 문서 작성         #1
  ├── 📝 마일스톤 문서화 및 GitHub 연동  #2
  ├── 📝 시퀀스 다이어그램 작성          #3
  ├── 📝 ERD 설계 및 작성              #4
  └── 📝 API 명세 작성                 #5
✨ [STEP02] 설계 심화과제
  ├── ✨ Mock API 구현                 #6
  ├── 📝 Spring REST Docs 문서화       #7
  ├── ✨ E2E 테스트 작성               #8
  ├── 📝 API Request http 파일 작성    #9
  └── 📝 상태 다이어그램 작성           #10
```

### 01. 요구사항 분석

도메인별로 기능적/비기능적 요구사항 분리해서 작성.

markdown

```markdown
### 잔액 (Balance)

✅ 기능적 요구사항
[잔액 조회]
- 사용자 검증
- 잔액 없으면 0 반환

[잔액 충전]
- 충전 금액은 양수여야 함
- 최대 잔액 1,000만원 초과 불가
- 충전 이력 저장

☑️ 비기능적 요구사항
- 동시 충전 시 동시성 보장 필요
```

비기능적 요구사항에 **동시성, 캐싱, 트랜잭션 보장**을 명시해두면 나중 단계와 자연스럽게 연결돼요.

**기능 범위 (이것만)**

```
잔액 조회/충전
상품 목록 조회
선착순 쿠폰 발급/조회
주문/결제
인기상품 Top5 (배치)
```

### 02. 시퀀스 다이어그램

Mermaid로 작성. 정상 흐름 + 예외 케이스 모두 포함.

markdown

```markdown
### 잔액 충전

sequenceDiagram
    autonumber
    actor 클라이언트
    participant 잔고
    participant 사용자
    클라이언트 ->>+ 잔고: 잔액 충전 요청
    잔고 ->>+ 사용자: 사용자 조회

    opt 사용자 존재 ❌
        사용자 -->> 클라이언트: 사용자 미존재 예외 ⚡️
    end

    잔고 ->> 잔고: 최대 잔액 검증
    opt 최대 잔액 초과 ❌
        잔고 -->> 클라이언트: 최대 잔액 초과 예외 ⚡️
    end

    잔고 -->>- 클라이언트: 잔액 충전 성공 ✅
```

도메인별로 작성:

- 잔액 조회 / 충전
- 상품 조회
- 쿠폰 목록 조회 / 발급
- 주문 생성 + 결제 완료 + 외부 전송
- 인기상품 배치 + 조회

### 03. 상태 다이어그램

상태가 변하는 도메인만 작성.

```
주문: PENDING → COMPLETED / CANCELLED
쿠폰: PUBLISHABLE → PUBLISHED → USED
```

### 04. ERD

```
users            사용자
balance          잔액
balance_history  잔액 이력
product          상품
stock            재고
coupon           쿠폰
user_coupon      사용자 쿠폰
order            주문
order_product    주문 상품
payment          결제
product_rank     인기상품 (배치 집계)
outbox           Outbox 패턴용
```

### 05. API 명세 + Mock API + E2E 테스트

```
GET  /users/{userId}/balance          잔액 조회
POST /users/{userId}/balance/charge   잔액 충전
GET  /products                        상품 목록
GET  /products/ranks                  인기상품 Top5
GET  /users/{userId}/coupons          보유 쿠폰 목록
POST /users/{userId}/coupons/publish  쿠폰 발급
POST /orders                          주문/결제
GET  /orders/{orderId}                주문 상태 확인
```

Mock API 먼저 구현 → E2E 테스트 작성 → 나중에 실제 API로 교체.

---

## 2단계 — 모놀리식 TDD 구현 (3~4주)

### 진행 방식

단일 Spring Boot 모듈. bottom-up TDD.

### 이슈 구성

```
✨ [STEP03] 비즈니스 로직 개발 및 단위 테스트
  ├── ✨ 잔액 비즈니스 로직 구현 및 단위 테스트  #11
  ├── ✨ 쿠폰 비즈니스 로직 구현 및 단위 테스트  #12
  ├── ✨ 상품 비즈니스 로직 구현 및 단위 테스트  #13
  └── ✨ 주문/결제 비즈니스 로직 구현 및 단위 테스트 #14
```

### 각 도메인 구현 순서

```
1. 단위 테스트 작성 (실패 확인)
2. 비즈니스 로직 구현 (통과)
3. 통합 테스트 작성
4. 컨트롤러 구현
5. Mock API → 실제 API 교체
6. @WebMvcTest 컨트롤러 테스트
```

### 패키지 구조

```
src/main/java/com.minicommerce/
├── domain/
│   ├── balance/
│   │   ├── entity/Balance.java
│   │   ├── repository/BalanceRepository.java
│   │   └── service/BalanceService.java
│   ├── coupon/
│   ├── order/
│   ├── payment/
│   ├── product/
│   └── user/
└── global/
    ├── config/
    ├── exception/
    │   ├── BusinessException.java
    │   ├── ErrorCode.java
    │   └── GlobalExceptionHandler.java
    └── aop/LoggingAspect.java
```

### http 파일 작성

http

```http
### 잔액 조회
GET {{base_url}}/users/1/balance

### 잔액 충전
POST {{base_url}}/users/1/balance/charge
Content-Type: application/json

{
  "amount": 10000
}
```

---

## 3단계 — 동시성 이슈 해결 (1주)

### 진행 방식

실패하는 테스트 먼저 → 해결책 적용 → 통과 확인 → 보고서 작성.

### 이슈 구성

```
✨ [STEP04] 동시성 이슈 분석 및 해결
  ├── 🧪 동시성 실패 테스트 작성          #15
  ├── ♻️ 잔액 동시성 해결 (낙관적 락)      #16
  ├── ♻️ 재고 동시성 해결 (비관적 락)      #17
  ├── ♻️ 쿠폰 동시성 해결 (분산 락)       #18
  └── 📝 동시성 이슈 분석 및 해결 보고서   #19
```

### 동시성 실패 테스트

java

```java
@Test
void 재고_동시_차감_100명() throws InterruptedException {
    int threadCount = 100;
    CountDownLatch latch = new CountDownLatch(threadCount);
    ExecutorService executor = Executors.newFixedThreadPool(threadCount);

    for (int i = 0; i < threadCount; i++) {
        executor.submit(() -> {
            try {
                stockService.deductStock(productId, 1);
            } finally {
                latch.countDown();
            }
        });
    }

    latch.await();
    // 재고가 정확히 차감됐는지 검증
}
```

### 방식별 비교 및 선택

```
잔액 충전 → 낙관적 락 (@Version)
  이유: 충돌 빈도 낮음, 성능 우선

재고 차감 → 비관적 락 (SELECT FOR UPDATE)
  이유: 충돌 빈도 높음, 정확성 필수

선착순 쿠폰 → 분산 락 (Redis)
  이유: 멀티 인스턴스 환경, 공정 모드 필요
```

### 보고서 작성 포인트

```
1. 어떤 이슈가 발생했나 (테스트 결과)
2. 각 해결책의 트레이드오프
3. 왜 이 방식을 선택했나
4. 적용 후 테스트 결과
```

---

## 4단계 — DB 성능 + 캐시 (1주)

### 이슈 구성

```
✨ [STEP05] DB 성능 최적화 및 캐시 전략
  ├── 🧪 쿼리 성능 측정                  #20
  ├── ♻️ 복합 인덱스 적용               #21
  ├── 📝 DB 성능 최적화 보고서           #22
  ├── ♻️ Redis 캐시 적용               #23
  └── 📝 캐시 전략 설계 보고서           #24
```

### DB 최적화

sql

```sql
-- 1. EXPLAIN으로 실행 계획 확인
EXPLAIN SELECT * FROM order_product WHERE order_id = 1;

-- 2. 인덱스 적용
CREATE INDEX idx_order_product_order_id ON order_product(order_id);

-- 3. 수치 비교
-- before: 500ms, after: 50ms
```

### 캐시 전략

```
인기상품 Top5 → Redis ZSET
  ZINCRBY rank:sell:{yyyyMMdd} {판매량} {productId}
  TTL: 24시간 (배치 결과라 실시간 불필요)

상품 목록 → Cache-Aside
  캐시 히트 → 캐시 반환
  캐시 미스 → DB 조회 후 캐시 저장
```

### Redis 아키텍처 보고서 포인트

```
어떤 자료구조를 왜 선택했나
TTL은 어떻게 설정했나
캐시 일관성 문제는 어떻게 처리했나
```

---

## 5단계 — 이벤트 기반 아키텍처 (1~2주)

### 이슈 구성

```
✨ [STEP06] 이벤트 기반 아키텍처 설계 및 구현
  ├── 📚 Kafka 스터디                           #25
  ├── ♻️ Spring ApplicationEvent 이벤트 흐름 설계  #26
  ├── ♻️ Kafka로 교체                            #27
  ├── ♻️ Outbox 패턴 적용                        #28
  ├── 🧪 E2E 이벤트 흐름 테스트                   #29
  └── 📝 MSA 이벤트 기반 아키텍처 보고서           #30
```

### 순서

**① Kafka 스터디 먼저**

```
docs/study/01.Kafka.md 작성
- Kafka 개념, 파티션, 컨슈머 그룹
- 왜 RabbitMQ가 아닌 Kafka인가
```

**② Spring ApplicationEvent로 먼저 설계**

java

```java
@Async
@TransactionalEventListener(phase = AFTER_COMMIT)
public void handle(OrderEvent.Created event) {
    balanceService.useBalance(...);
    balanceEventPublisher.used(event);
}
```

**③ 한계 직접 확인 후 보고서에 명시**

```
- 단일 인스턴스 내에서만 동작
- 서버 재시작 시 이벤트 유실
- 재처리 불가
→ Kafka 도입 필요
```

**④ Kafka 토픽 설계**

```
order.created
balance.deducted / balance.deduct.failed
coupon.used / coupon.use.failed
stock.deducted / stock.deduct.failed
order.payment.waited
order.failed
order.completed
coupon.publish.requested
```

**⑤ Redis Hash로 주문 프로세스 상태 관리**

```
key: order:process:{orderId}
  balance → PENDING/SUCCESS/FAILED
  coupon  → PENDING/SUCCESS/FAILED
  stock   → PENDING/SUCCESS/FAILED

3개 다 완료 시 → 결제 대기 이벤트 발행
하나라도 실패 시 → 주문 실패 이벤트 발행
```

**⑥ Outbox 패턴 적용**

```
트랜잭션 안에서 outbox 테이블에 이벤트 저장
별도 폴러가 outbox 읽어서 Kafka 발행
발행 완료 후 outbox 삭제
→ 이벤트 유실 방지
```

**⑦ Consumer 예외 처리**

java

```java
// 재시도해도 의미없는 예외는 바로 커밋
// → Consumer Lag 방지
catch (CoreException e) {
    ack.acknowledge();
}
```

---

## 6단계 — MSA 분리 (1~2주)

### 이슈 구성

```
✨ [STEP07] MSA 분리
  ├── ♻️ 멀티모듈 구조 세팅              #31
  ├── ♻️ common 모듈 분리               #32
  ├── ♻️ 서비스별 모듈 분리              #33
  ├── ♻️ 서비스별 DB 분리               #34
  └── 📝 MSA 보고서 (AS-IS → TO-BE)    #35
```

### 분리 기준 (DDD)

```
루트 애그리거트 단위
Bounded Context 기준
생명주기 일치 여부
```

### 멀티모듈 최종 구조

```
service/
  ├── balance    잔액
  ├── coupon     쿠폰
  ├── order      주문
  ├── payment    결제
  ├── product    상품/재고/랭킹
  └── user       사용자
common/
  ├── cache      Redis 캐시 공통
  ├── client     서비스간 HTTP 통신
  ├── lock       분산락 공통
  ├── message    Kafka 공통
  ├── outbox     Outbox 패턴 공통
  ├── querydsl   QueryDSL 공통
  └── serialize  직렬화 공통
support/
  └── rest-docs  API 문서
```

### 보고서 포인트

```
AS-IS: 모놀리식 (단일 DB, 단일 앱)
TO-BE: MSA (서비스별 독립 DB, 독립 배포)

왜 이 경계로 나눴나 (DDD 기준)
트레이드오프 (복잡도 증가, 분산 트랜잭션)
한계 (테스트 난이도, 흐름 추적 어려움)
```

---

## 7단계 — 부하테스트 + 장애대응 (1주)

### 이슈 구성

```
✨ [STEP08] 부하테스트 및 장애대응
  ├── 📝 부하테스트 대상 선정 및 시나리오 계획  #36
  ├── 🧪 부하테스트 스크립트 작성             #37
  ├── ♻️ 병목 탐색 및 개선                  #38
  └── 📝 성능 테스트 및 장애대응 보고서       #39
```

### 환경 구성

yaml

```yaml
# docker-compose
api:
  deploy:
    resources:
      limits:
        cpus: '2.0'
        memory: 4G

prometheus:  # 메트릭 수집
grafana:     # 대시보드
influxdb:    # k6 결과 저장
```

### 테스트 데이터 생성

sql

```sql
-- 상품/재고 10,000건
-- 사용자/잔액 10,000건
-- 쿠폰 1건 (수량 10,000)
-- 인기상품 Redis 사전 적재
ZINCRBY rank:sell:{date} {score} {productId}
```

### 시나리오

**주문/결제 Load Test**

javascript

```javascript
stages: [
  { duration: '10s', target: 100 },
  { duration: '10s', target: 200 },
  { duration: '10s', target: 300 },
  { duration: '30s', target: 300 },  // 유지
  { duration: '10s', target: 0 }
]
thresholds: {
  http_req_duration: ['p(99)<1000'],
  http_req_failed: ['rate<0.01']
}

// 시나리오
1. 인기상품 조회
2. 잔액 충전 (20% 확률)
3. 주문/결제 (10% 확률)
4. 주문 상태 확인
```

**쿠폰 발급 Peak Test**

javascript

```javascript
stages: [
  { duration: '10s', target: 10 },
  { duration: '10s', target: 700 },  // 급격한 증가
  { duration: '10s', target: 10 },
  { duration: '10s', target: 1000 }, // 최대 피크
  { duration: '10s', target: 0 }
]
```

### before/after 비교표 필수

```
| 지표 | 개선 전 | 개선 후 | 개선율 |
|------|---------|---------|--------|
| 평균 응답시간 | 282ms | 35ms | 87% ↓ |
| P99 응답시간 | 2.19s | 405ms | 81% ↓ |
| TPS | 62 | 115 | 87% ↑ |
```

---

## WIL (주차별 회고)

매주 마무리할 때 작성:

markdown

```markdown
## WIL N주차

### 이번 주 한 것
- ...

### 어려웠던 점
- ...

### 배운 것
- ...

### 다음 주 계획
- ...
```

---

## 각 글에서 반드시 지킬 구조

```
1. 왜 이게 필요했나 (문제 정의)
2. 어떤 선택지가 있었나 (트레이드오프)
3. 왜 이걸 선택했나 (결정 근거)
4. 결과가 어떻게 됐나 (수치)
```

---

## 전체 흐름 요약

```
GitHub 세팅 (칸반 + 타임라인 + 이슈)
      ↓
1단계: 설계 문서 + Mock API + E2E 테스트
      ↓
2단계: 모놀리식 TDD 구현 (동작하는 것)
      ↓
3단계: 동시성 해결 (올바른 것)
      ↓
4단계: DB 성능 + 캐시 (빠른 것)
      ↓
5단계: ApplicationEvent → Kafka (유연한 것)
      ↓
6단계: MSA 분리 (분산된 것)
      ↓
7단계: 부하테스트 + 수치 증명
      ↓
각 단계마다: 이슈 관리 + 보고서 + WIL
```

---

## 타임라인

|단계|기간|
|---|---|
|설계|1~2주|
|모놀리식 구현|3~4주|
|동시성 + 성능|2주|
|이벤트 + MSA|3~4주|
|부하테스트|1주|
|**총합**|**약 10~12주**|