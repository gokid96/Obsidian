# volatile 키워드

## 가시성 문제란?

멀티스레드 환경에서 한 스레드가 변경한 값을 **다른 스레드가 못 보는 문제**

```
메인 메모리: running = true

스레드 A: running = false 로 변경
스레드 B: running 확인 → true ???
```

왜 이런 일이 발생할까?

---

## CPU 캐시 구조

```
CPU 코어 1          CPU 코어 2
┌──────────┐        ┌──────────┐
│  캐시    │        │  캐시    │
│ running  │        │ running  │
│ = false  │        │ = true   │  ← 아직 갱신 안 됨
└──────────┘        └──────────┘
       ↕                  ↕
    메인 메모리 (running = false)
```

각 CPU 코어는 성능을 위해 **자체 캐시**를 가짐 메인 메모리에서 값을 읽어 캐시에 저장하고 캐시에서 연산

```
스레드 A (코어 1): running = false 로 변경
  → 코어 1 캐시에만 반영
  → 메인 메모리에 언제 반영될지 모름

스레드 B (코어 2): running 읽기
  → 코어 2 캐시에서 읽음
  → 아직 true
  → 스레드 A가 바꾼 걸 모름
```

---

## volatile이란?

**항상 메인 메모리에서 읽고 메인 메모리에 씀**

```java
// volatile 없음
boolean running = true;
// 각 코어 캐시에서 읽고 씀 → 가시성 문제 발생

// volatile 있음
volatile boolean running = true;
// 항상 메인 메모리에서 읽고 씀 → 가시성 보장
```

```
volatile 적용 후:

스레드 A (코어 1): running = false
  → 즉시 메인 메모리에 반영

스레드 B (코어 2): running 읽기
  → 메인 메모리에서 읽음
  → false 확인
  → 정상 동작
```

---

## 대표적인 사용 예시

**스레드 종료 플래그**
```java
class Worker implements Runnable {
    private volatile boolean running = true;

    public void run() {
        while (running) {
            // 작업 수행
        }
    }

    public void stop() {
        running = false;  // 다른 스레드에서 호출
    }
}
```

volatile 없으면:

```
메인 스레드: running = false
워커 스레드: 캐시에서 읽음 → 아직 true → 무한 루프!
```

volatile 있으면:

```
메인 스레드: running = false → 즉시 메인 메모리 반영
워커 스레드: 메인 메모리에서 읽음 → false → 종료
```

---

## volatile의 한계

**가시성은 보장하지만 원자성은 보장 안 함**

```java
volatile int count = 0;

// 스레드 A, B 동시에 실행
count++;
```

```
count++ 는 사실 3단계:
1. count 읽기  (0)
2. +1 계산     (1)
3. count 저장  (1)

스레드 A: count 읽기 (0)
스레드 B: count 읽기 (0)  ← A가 저장하기 전에 읽음
스레드 A: 1 저장
스레드 B: 1 저장

→ 둘 다 +1 했는데 결과는 1
→ 원자성 보장 안 됨!
```

원자성까지 필요하면 → AtomicInteger (CAS) 사용

```java
// volatile (가시성만)
volatile int count = 0;
count++;  // 동시성 문제 발생

// AtomicInteger (가시성 + 원자성)
AtomicInteger count = new AtomicInteger(0);
count.incrementAndGet();  // 안전
```

---

## CAS와의 연결

```
CAS 동작:
1. 메인 메모리에서 현재값 읽기  ← 가시성 필요
2. 예상값과 비교
3. 같으면 새 값으로 교체        ← 원자적으로

→ CAS가 제대로 동작하려면 가시성이 보장되어야 함
→ AtomicInteger 내부의 value가 volatile로 선언된 이유
```

```java
// AtomicInteger 내부 구현
public class AtomicInteger {
    private volatile int value;  // volatile!

    public final boolean compareAndSet(int expect, int update) {
        return unsafe.compareAndSwapInt(this, valueOffset, expect, update);
    }
}
```

volatile이 CAS의 가시성 기반을 제공

---

## synchronized와 비교

|항목|volatile|synchronized|
|---|---|---|
|가시성|보장|보장|
|원자성|보장 안 함|보장|
|상호 배제|없음|있음|
|성능|빠름|느림|
|적합한 상황|단순 플래그, 상태값|복합 연산, 임계 구역|

```
단순히 값을 읽고 쓰는 것만 → volatile
읽고 → 계산 → 쓰기 같은 복합 연산 → synchronized or AtomicInteger
```

---

## 핵심 Q&A

**Q. volatile이 뭔가?**

CPU 캐시를 거치지 않고 항상 메인 메모리에서 읽고 쓰는 키워드 멀티스레드 환경에서 한 스레드의 변경이 다른 스레드에 즉시 보이는 가시성을 보장

**Q. volatile만으로 동시성 문제가 해결되나?**

가시성은 해결되지만 원자성은 보장 안 됨 count++ 같은 복합 연산은 volatile만으로 안전하지 않음 원자성까지 필요하면 AtomicInteger나 synchronized 사용

**Q. CAS랑 무슨 관계인가?**

AtomicInteger 내부의 value가 volatile로 선언됨 CAS가 메인 메모리의 최신값을 읽고 비교해야 하기 때문 volatile이 CAS의 가시성 기반을 제공

---

## 핵심 요약

|항목|내용|
|---|---|
|문제|CPU 캐시로 인한 가시성 문제|
|해결|volatile → 항상 메인 메모리에서 읽고 씀|
|보장|가시성 O, 원자성 X|
|한계|count++ 같은 복합 연산은 안전하지 않음|
|대안|AtomicInteger (가시성 + 원자성)|
|CAS 연결|AtomicInteger.value가 volatile로 선언|