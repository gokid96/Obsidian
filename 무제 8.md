# 캐시 전략

## 캐시 읽기 전략

**Cache-Aside (Lazy Loading)**

```
가장 많이 쓰는 방식

흐름:
1. 캐시 먼저 확인
2. Hit → 캐시에서 반환
3. Miss → DB 조회 → 캐시 저장 → 반환

코드:
public Product getProduct(Long id) {
    // 1. 캐시 확인
    Product cached = redis.get("product:" + id);
    if (cached != null) return cached;  // Hit

    // 2. Miss → DB 조회
    Product product = productRepository.findById(id);

    // 3. 캐시 저장
    redis.set("product:" + id, product, TTL);

    return product;
}
```

```
장점:
  필요한 데이터만 캐시
  → 캐시 공간 효율적
  캐시 장애 시 DB로 폴백 가능

단점:
  첫 요청은 항상 Miss (Cold Start)
  → 캐시 워밍으로 해결
  데이터 불일치 가능
  → DB 업데이트 후 캐시 삭제 필요
```

**Read-Through**

```
캐시가 DB 조회를 대신 처리

흐름:
1. 항상 캐시에만 요청
2. Hit → 캐시에서 반환
3. Miss → 캐시가 직접 DB 조회 → 저장 → 반환

애플리케이션은 캐시만 바라봄
→ DB 접근 로직이 캐시 레이어에 숨겨짐
```

```
Cache-Aside vs Read-Through:

Cache-Aside:
  애플리케이션이 캐시 Miss 시 DB 직접 조회
  → 애플리케이션이 캐시 로직 포함

Read-Through:
  캐시가 Miss 시 DB 직접 조회
  → 애플리케이션은 캐시만 바라봄
  → 캐시 로직이 캐시 레이어에 집중
```

---

## 캐시 쓰기 전략

**Write-Through**

```
DB와 캐시에 동시에 씀

흐름:
데이터 변경
  → 캐시 업데이트
  → DB 업데이트
  → 완료

장점:
  캐시와 DB 항상 일치
  → 데이터 불일치 없음

단점:
  쓰기 시 두 번 저장 → 쓰기 느림
  안 읽을 데이터도 캐시에 저장
  → 캐시 공간 낭비
```

**Write-Back (Write-Behind)**

```
캐시에만 먼저 쓰고 나중에 DB에 반영

흐름:
데이터 변경
  → 캐시 업데이트 (즉시)
  → 완료 반환
  → 나중에 배치로 DB 반영

장점:
  쓰기 매우 빠름
  DB 쓰기 부하 감소
  배치로 모아서 DB에 한 번에 씀

단점:
  캐시 장애 시 데이터 유실 위험
  → 캐시에만 있고 DB 반영 전에 장애
  구현 복잡
```

**Write-Around**

```
캐시를 건너뛰고 DB에만 씀

흐름:
데이터 변경
  → DB에만 저장
  → 캐시는 TTL 만료 후 갱신

장점:
  자주 읽지 않는 데이터에 적합
  캐시 오염 방지

단점:
  변경 후 첫 읽기는 Miss
  → 데이터 불일치 기간 발생
```

---

## 전략 비교

|항목|Cache-Aside|Read-Through|Write-Through|Write-Back|Write-Around|
|---|---|---|---|---|---|
|읽기|앱이 직접|캐시가 처리|-|-|-|
|쓰기|-|-|캐시+DB 동시|캐시 먼저|DB만|
|일관성|낮음|낮음|높음|낮음|낮음|
|쓰기 성능|-|-|느림|빠름|빠름|
|데이터 유실|없음|없음|없음|위험|없음|

---

## 상황별 선택

```
읽기 많고 쓰기 적음
  → Cache-Aside + Write-Through
  → 읽기 빠름, 데이터 일치

쓰기 매우 많음
  → Write-Back
  → 쓰기 성능 최우선
  → 유실 감수 or 보완책 필요

자주 안 읽는 데이터
  → Write-Around
  → 캐시 공간 아낌

단순하게 시작
  → Cache-Aside
  → 가장 흔하고 구현 단순
```

---

## 실무 패턴

**Cache-Aside + 캐시 삭제**

```
가장 많이 쓰는 조합

읽기: Cache-Aside
쓰기: DB 업데이트 후 캐시 삭제

흐름:
  상품 수정 → DB 업데이트 → 캐시 삭제
  다음 읽기 → Miss → DB 조회 → 캐시 저장

장점:
  구현 단순
  데이터 불일치 최소화
```

**Spring Cache 예시**

```java
@Service
public class ProductService {

    // 캐시에서 먼저 읽음 (Cache-Aside)
    @Cacheable(value = "products", key = "#id")
    public Product getProduct(Long id) {
        return productRepository.findById(id);
    }

    // DB 업데이트 후 캐시 삭제
    @CacheEvict(value = "products", key = "#product.id")
    public Product updateProduct(Product product) {
        return productRepository.save(product);
    }

    // DB 업데이트 후 캐시도 업데이트 (Write-Through)
    @CachePut(value = "products", key = "#product.id")
    public Product updateProductWithCache(Product product) {
        return productRepository.save(product);
    }
}
```

---

## 핵심 Q&A

**Q. Cache-Aside와 Read-Through 차이는?**

Cache-Aside는 애플리케이션이 Miss 시 DB를 직접 조회하고 캐시에 저장 Read-Through는 캐시가 Miss 시 DB를 직접 조회해서 저장 애플리케이션이 캐시 로직을 포함하느냐 캐시 레이어에 숨기느냐의 차이

**Q. Write-Through vs Write-Back 차이는?**

Write-Through는 캐시와 DB에 동시에 써서 일관성 보장하지만 쓰기 느림 Write-Back은 캐시에만 먼저 쓰고 나중에 DB에 반영해서 쓰기 빠르지만 유실 위험 쓰기 성능과 데이터 안전성의 트레이드오프

**Q. 실무에서 가장 많이 쓰는 패턴은?**

Cache-Aside로 읽고 DB 업데이트 후 캐시 삭제하는 조합 구현이 단순하고 데이터 불일치를 최소화할 수 있음 Spring의 @Cacheable, @CacheEvict로 쉽게 구현 가능

---

## 핵심 요약

|전략|내용|
|---|---|
|Cache-Aside|Miss 시 앱이 DB 조회 후 캐시 저장, 가장 흔함|
|Read-Through|Miss 시 캐시가 DB 조회, 앱은 캐시만 바라봄|
|Write-Through|캐시+DB 동시 쓰기, 일관성 보장, 쓰기 느림|
|Write-Back|캐시 먼저, 나중에 DB 반영, 빠르지만 유실 위험|
|Write-Around|DB만 씀, 자주 안 읽는 데이터에 적합|
|실무|Cache-Aside + 캐시 삭제 조합이 가장 일반적|