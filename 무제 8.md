## 네임드 락이란?

MySQL이 제공하는 사용자 정의 이름 기반의 락

```sql
SELECT GET_LOCK('lock_name', timeout);
```

비관적 락은 **row**에 락을 검 네임드 락은 **이름(문자열)**에 락을 검

```
비관적 락: products 테이블의 id=1 row → 잠금
네임드 락: 'lock:product:1' 이라는 이름    → 잠금
```

---

## 기본 사용법

```sql
-- 락 획득 (최대 3초 대기)
SELECT GET_LOCK('lock:product:1', 3);
→ 1: 획득 성공
→ 0: 타임아웃 (다른 세션이 이미 보유)
→ NULL: 오류

-- 락 해제
SELECT RELEASE_LOCK('lock:product:1');
→ 1: 해제 성공
→ 0: 내가 건 락이 아님
→ NULL: 존재하지 않는 락

-- 락 확인
SELECT IS_FREE_LOCK('lock:product:1');
→ 1: 사용 가능
→ 0: 사용 중
```

---

## 트랜잭션과 독립적으로 동작

**비관적 락 (FOR UPDATE)**

```
트랜잭션 시작
  → SELECT FOR UPDATE   ← 락 획득
  → 작업
COMMIT or ROLLBACK      ← 락 자동 해제
```

**네임드 락**

```
GET_LOCK()              ← 락 획득
  → 트랜잭션 시작
  → 작업
  → COMMIT or ROLLBACK  ← 락 안 풀림!
RELEASE_LOCK()          ← 직접 해제해야 함
```

트랜잭션이 끝나도 락이 자동 해제되지 않음 → 반드시 명시적으로 `RELEASE_LOCK()` 호출해야 함 → 안 하면 세션이 끊길 때까지 락 유지

---

## 분산 환경에서 네임드 락

Redis 없이 MySQL만으로 분산 락 구현 가능

```
서버 1 ─┐
서버 2 ─┼─→ MySQL GET_LOCK('lock:product:1', 3)
서버 3 ─┘

서버 1: 획득 성공 → 작업 수행
서버 2: 대기 중...
서버 3: 대기 중...

서버 1: RELEASE_LOCK() → 해제
서버 2: 획득 성공 → 진입
```

모든 서버가 같은 MySQL에 접근 → MySQL이 락 중재자 역할

---

## 비관적 락과의 차이

|항목|비관적 락|네임드 락|
|---|---|---|
|락 대상|특정 row|문자열 이름|
|해제 시점|트랜잭션 종료 시 자동|직접 RELEASE_LOCK()|
|범위|해당 테이블/row|이름만 같으면 어디든|
|용도|DB row 보호|비즈니스 로직 보호|

```
비관적 락: products id=1 row를 잠그기 때문에
           해당 row 조회/수정 자체가 블로킹됨

네임드 락: 'lock:product:1' 이름만 잠그기 때문에
           락을 획득한 서버만 로직을 실행 못하게 막음
           실제 row는 잠기지 않음
```

---

## 실제 사용 패턴

**선착순 쿠폰 발급**
```sql
-- 락 이름을 쿠폰 ID 기반으로
SELECT GET_LOCK(CONCAT('coupon:', couponId), 3);

-- 재고 확인 + 차감
UPDATE coupons SET stock = stock - 1 WHERE id = couponId AND stock > 0;

SELECT RELEASE_LOCK(CONCAT('coupon:', couponId));
```

**중복 요청 방지**
```sql
-- 같은 유저의 동시 요청 차단
SELECT GET_LOCK(CONCAT('user:', userId), 0);
-- timeout 0 → 이미 락 있으면 즉시 실패 반환
```

---

## Redis 분산 락과 비교

|항목|네임드 락|Redis 분산 락|
|---|---|---|
|저장소|MySQL|Redis|
|속도|상대적으로 느림|매우 빠름|
|추가 인프라|불필요|Redis 서버 필요|
|Watchdog|❌|✅ (Redisson)|
|만료 시간|세션 종료까지|EX로 자동 설정|
|적합한 상황|Redis 없는 환경|고성능, 고가용성|

**Redis가 없는 환경이라면 → 네임드 락** **트래픽이 많고 성능이 중요하다면 → Redis 분산 락**

---

## 주의사항

**1. 반드시 해제**
```java
try {
    // GET_LOCK
    // 작업
} finally {
    // RELEASE_LOCK  ← 예외 발생해도 반드시
}
```

**2. 커넥션 주의** 네임드 락은 **세션(커넥션) 단위**로 관리 → 커넥션 풀 환경에서 커넥션이 반환되면 락도 해제될 수 있음 → 락 획득과 해제를 같은 커넥션에서 처리해야 함

**3. 타임아웃 설정**
```sql
GET_LOCK('lock_name', 0)   -- 즉시 실패
GET_LOCK('lock_name', 3)   -- 3초 대기
GET_LOCK('lock_name', -1)  -- 무한 대기 (위험!)
```

무한 대기는 데드락 위험 → 반드시 타임아웃 설정

---

## 핵심 Q&A

**Q. 네임드 락이 뭔가?**

MySQL에서 문자열 이름에 락을 거는 방식 row가 아닌 이름 기반이라 비즈니스 로직 단위로 동시성 제어 가능 Redis 없이 MySQL만으로 분산 락 구현할 때 사용

**Q. 비관적 락이랑 뭐가 다른가?**

비관적 락은 특정 row를 잠가서 트랜잭션 종료 시 자동 해제 네임드 락은 이름에 잠그고 직접 해제해야 함 트랜잭션과 독립적으로 동작하는 게 핵심 차이

**Q. Redis 분산 락이랑 언제 선택하나?**

Redis 인프라가 없거나 추가하기 어려운 환경 → 네임드 락 성능과 고가용성이 중요한 환경 → Redis 분산 락

---

## 핵심 요약

| 항목         | 내용                          |
| ---------- | --------------------------- |
| 정의         | MySQL 이름 기반 락               |
| 획득         | GET_LOCK('이름', timeout)     |
| 해제         | RELEASE_LOCK('이름') 직접 호출 필수 |
| 특징         | 트랜잭션과 독립 동작                 |
| 장점         | Redis 없이 분산 락 구현 가능         |
| 단점         | 속도 느림, Watchdog 없음          |
| vs Redis 락 | 인프라 없을 때 대안                 |