# Kafka vs RabbitMQ

## 한 줄 비교

```
RabbitMQ: 메시지를 전달하는 것이 목적
          → 전통적인 메시지 브로커

Kafka:    데이터를 저장하고 스트리밍하는 것이 목적
          → 분산 이벤트 스트리밍 플랫폼
```

---

## 핵심 차이: 메시지 보관

```
RabbitMQ:
  Consumer가 메시지 읽으면 삭제
  → 메시지는 일회성

  Producer → Exchange → Queue → Consumer → 삭제

Kafka:
  Consumer가 읽어도 메시지 보관
  → 설정 기간 동안 유지 (기본 7일)
  → 여러 Consumer가 같은 메시지 읽기 가능
  → 재처리 가능

  Producer → Topic → Consumer A 읽음 (메시지 유지)
                   → Consumer B도 읽음 (같은 메시지)
```

---

## 아키텍처 차이

**RabbitMQ**

```
Producer
  ↓
Exchange (라우팅)
  ↓
Queue (임시 저장)
  ↓
Consumer (읽으면 삭제)

→ Push 방식
→ Exchange가 Consumer에게 밀어넣음
→ Consumer가 처리 속도 따라가야 함
```

**Kafka**

```
Producer
  ↓
Topic / Partition (영구 저장)
  ↑
Consumer (직접 가져감, offset 관리)

→ Pull 방식
→ Consumer가 자기 속도에 맞게 가져감
→ 처리 속도 유연하게 조절 가능
```

---

## 상세 비교

|항목|RabbitMQ|Kafka|
|---|---|---|
|메시지 보관|읽으면 삭제|기간 기반 유지|
|전달 방식|Push|Pull|
|순서 보장|큐 내 보장|파티션 내 보장|
|처리량|중간|매우 높음|
|지연 시간|매우 낮음 (ms 이하)|낮음 (ms)|
|재처리|어려움|쉬움 (offset 조정)|
|라우팅|유연함 (Exchange)|단순 (Topic)|
|복잡도|낮음|높음|
|메시지 우선순위|지원|미지원|
|프로토콜|AMQP|자체 프로토콜|

---

## 트레이드오프

**RabbitMQ를 선택하면 포기하는 것:**

```
재처리 어려움
  → 메시지 읽으면 삭제
  → 장애 후 처음부터 다시 불가
  → DLQ로 일부 보완하지만 한계 있음

낮은 처리량
  → 초당 수만 이상이면 한계
  → 스케일 아웃 복잡

메시지 히스토리 없음
  → 과거 메시지 조회 불가
  → 감사 로그 불가
```

**Kafka를 선택하면 포기하는 것:**

```
복잡한 라우팅 불가
  → Direct, Fanout, Topic Exchange 없음
  → 세밀한 메시지 분기 어려움

낮은 지연 시간
  → RabbitMQ보다 느림
  → ms 이하 응답이 필요하면 부적합

운영 복잡도
  → Zookeeper (or KRaft) 필요
  → 파티션 수, 복제 설정 등 튜닝 필요
  → 소규모 팀에 부담

메시지 우선순위 없음
  → 먼저 온 것부터 처리
  → VIP 우선 처리 불가
```

**공통 트레이드오프:**

```
둘 다:
  비동기 처리 → 즉시 응답 불가
  네트워크 비용 → 브로커 거치는 오버헤드
  운영 포인트 추가 → 모니터링, 장애 대응
  메시지 중복 → 멱등성 처리 필요
```

---

## 언제 RabbitMQ?

```
작업 큐 (Task Queue)
  → 이미지 처리, 이메일 발송
  → 처리 후 삭제해도 됨

복잡한 라우팅
  → Direct, Fanout, Topic, Headers
  → 다양한 조건으로 메시지 분기

낮은 지연 시간
  → ms 이하 응답 필요
  → 실시간성이 중요한 경우

메시지 우선순위
  → VIP 요청 먼저 처리
  → 우선순위 큐 필요

소규모 시스템
  → 구현/운영 단순
  → 팀 규모 작을 때
```

---

## 언제 Kafka?

```
대용량 처리
  → 초당 수십만 메시지
  → 로그 수집, 클릭스트림

재처리 필요
  → 메시지 다시 읽어야 할 때
  → 장애 후 특정 시점부터 재처리

여러 Consumer가 같은 메시지 필요
  → 주문 이벤트를
  → 알림 서버도 읽고
  → 분석 서버도 읽고
  → 배송 서버도 읽음

이벤트 소싱
  → 모든 이벤트 영구 보관
  → 상태를 이벤트로 재현

실시간 스트리밍
  → 실시간 데이터 파이프라인
  → Kafka Streams, Flink 연동
```

---

## 실무 선택 기준

```
이 메시지가 처리되면 버려도 되나?
  → 그렇다 → RabbitMQ
  → 아니다 (재처리, 여러 소비자) → Kafka

처리량이 얼마나 되나?
  → 초당 수천 이하 → RabbitMQ
  → 초당 수만 이상 → Kafka

라우팅이 복잡한가?
  → 복잡하다 → RabbitMQ
  → 단순하다 → Kafka

팀 규모/운영 여건이 어떤가?
  → 작다/단순하게 → RabbitMQ
  → 크다/감당 가능 → Kafka
```

---

## 함께 쓰는 경우

```
RabbitMQ + Kafka 혼용:

실시간 작업 처리 → RabbitMQ
  주문 즉시 처리, 알림 발송

데이터 파이프라인 → Kafka
  주문 이벤트 수집, 분석, 로그

→ 각자 잘하는 것만
→ 오버엔지니어링 주의
```

---

## 실무에서 Kafka만 쓰는 경우

```
토스, 당근, 카카오 같은 대규모 서비스
→ 채용공고에 RabbitMQ 없고 Kafka만 있는 이유
```

**이미 Kafka 인프라가 있음**

```
어차피 대용량 처리를 위해 Kafka 운영 중
→ RabbitMQ 추가하면 관리 포인트만 늘어남
→ Kafka 하나로 통일하는 게 운영 편함
→ 모니터링, 장애 대응, 학습 비용 단일화
```

**RabbitMQ의 장점을 Kafka로 대체 가능**

```
복잡한 라우팅
  → Topic을 잘게 쪼개서 해결
  → order.created, order.payment, order.delivery
  → Exchange 없어도 Topic으로 충분히 분기 가능

우선순위
  → VIP 전용 Topic 따로 만들기
  → Consumer가 VIP Topic 먼저 처리

낮은 지연 시간
  → Kafka 튜닝으로 ms 이하도 가능
  → 배치 사이즈, 압축 설정 등
```

**팀이 Kafka에 익숙함**

```
이미 잘 쓰고 있는 기술
→ 러닝 커브 없음
→ RabbitMQ 도입하면 오히려 리스크
→ 기술 스택 단순화가 생산성에 유리
```

**결론:**

```
RabbitMQ가 Kafka보다 나은 상황이 있어도
Kafka를 이미 잘 쓰고 있다면
굳이 RabbitMQ를 도입할 이유가 없음

→ 기술 선택은 성능/기능만의 문제가 아님
→ 팀의 기술 스택, 운영 여건이 더 중요
→ 이미 잘 쓰고 있는 것을 쓰는 게 최선

RabbitMQ가 빛나는 순간:
  → Kafka 없는 소규모 서비스
  → Kafka 도입 비용이 부담될 때
  → 복잡한 라우팅이 핵심 요구사항일 때
```

---

## 핵심 Q&A

**Q. RabbitMQ와 Kafka의 가장 큰 차이는?**

메시지 보관 여부 RabbitMQ는 읽으면 삭제, Kafka는 기간 동안 보관 Kafka는 여러 Consumer가 같은 메시지를 읽고 장애 후 재처리도 가능

**Q. 둘 다 쓸 수 있는 상황에서 뭘 선택하나?**

메시지가 처리 후 버려도 되면 RabbitMQ 재처리, 여러 소비자, 대용량이면 Kafka 단순하고 빠른 구현이 필요하면 RabbitMQ 장기적인 데이터 파이프라인이면 Kafka

**Q. 대규모 서비스에서 Kafka만 쓰는 이유는?**

이미 Kafka 인프라가 있으면 RabbitMQ를 추가할 이유가 없음 RabbitMQ의 장점(라우팅, 우선순위)을 Topic 분리와 튜닝으로 대체 가능 기술 스택 단일화로 운영 복잡도를 낮추는 것이 더 중요

**Q. Kafka가 처리량이 높은 이유는?**

순차 디스크 I/O로 메시지를 append only로 저장 Zero Copy로 네트워크 전송 최적화 파티션으로 병렬 처리 Pull 방식으로 Consumer가 자기 속도에 맞게 처리

---

## 핵심 요약

|항목|RabbitMQ|Kafka|
|---|---|---|
|핵심 목적|메시지 전달|이벤트 스트리밍|
|메시지 보관|읽으면 삭제|기간 유지|
|재처리|어려움|쉬움|
|처리량|중간|매우 높음|
|라우팅|유연|단순|
|트레이드오프|재처리/히스토리 포기|라우팅/지연/복잡도 포기|
|적합한 상황|작업 큐, 복잡한 라우팅|대용량, 스트리밍, 재처리|
|실무 트렌드|소규모, Kafka 없을 때|대규모 서비스 표준|
|복잡도|낮음|높음|