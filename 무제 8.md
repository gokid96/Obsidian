# 데드락 (Deadlock)

## 데드락이란?

두 개 이상의 트랜잭션이 서로가 가진 락을 기다리며 **영원히 진행되지 못하는 상태**

```
트랜잭션 A: 상품 락 보유 → 결제 락 기다리는 중
트랜잭션 B: 결제 락 보유 → 상품 락 기다리는 중

→ A는 B를 기다리고
→ B는 A를 기다리고
→ 둘 다 영원히 대기
```

---

## 발생 원리

**계좌이체 데드락 예시**

```
트랜잭션 A: 계좌 1 → 계좌 2 이체
트랜잭션 B: 계좌 2 → 계좌 1 이체
```

```
시간 순서:

T=1  A: 계좌 1 락 획득
T=2  B: 계좌 2 락 획득
T=3  A: 계좌 2 락 요청 → 대기 (B가 보유 중)
T=4  B: 계좌 1 락 요청 → 대기 (A가 보유 중)

→ 데드락!
```

필요한 4가지 조건이 모두 충족되어야 발생

```
1. 상호 배제   : 하나의 자원은 하나의 트랜잭션만 사용
2. 점유 대기   : 락을 가진 채로 다른 락을 기다림
3. 비선점      : 다른 트랜잭션의 락을 강제로 빼앗지 못함
4. 순환 대기   : A→B→C→A 처럼 순환 구조로 대기
```

4가지 중 하나라도 깨면 데드락 발생 안 함

---

## MySQL의 데드락 처리

MySQL은 데드락을 **자동으로 감지**

```
A ←→ B 대기 순환 감지
→ 둘 중 하나를 희생자(victim)로 선택
→ 희생자 트랜잭션 롤백
→ 나머지 트랜잭션 진행
```


```sql
-- 희생된 트랜잭션에서 발생하는 에러
ERROR 1213: Deadlock found when trying to get lock;
            try restarting transaction
```

→ 애플리케이션에서 이 에러를 잡아서 재시도 처리 필요

---

## 데드락 확인 방법

sql

```sql
-- 가장 최근 데드락 정보 확인
SHOW ENGINE INNODB STATUS;

-- 결과에서 LATEST DETECTED DEADLOCK 섹션 확인
------------------------
LATEST DETECTED DEADLOCK
------------------------
트랜잭션 A가 보유한 락
트랜잭션 A가 기다리는 락
트랜잭션 B가 보유한 락
트랜잭션 B가 기다리는 락
→ 트랜잭션 B를 롤백함
```

---

## 데드락 방지 전략

**1. 락 획득 순서 통일**

가장 효과적인 방법 순환 대기 조건을 깨버림

```
나쁜 예:
트랜잭션 A: 상품 락 → 결제 락
트랜잭션 B: 결제 락 → 상품 락  ← 순서 다름 → 데드락 위험

좋은 예:
트랜잭션 A: 상품 락 → 결제 락
트랜잭션 B: 상품 락 → 결제 락  ← 순서 동일 → 데드락 없음
```

계좌이체라면 항상 **작은 ID 순서**로 락 획득

```java
// 항상 id가 작은 계좌부터 락
if (accountA.getId() < accountB.getId()) {
    lock(accountA);
    lock(accountB);
} else {
    lock(accountB);
    lock(accountA);
}
```

**2. 타임아웃 설정**

```sql
-- 락 대기 타임아웃 설정 (초)
SET innodb_lock_wait_timeout = 5;

→ 5초 안에 락 못 얻으면 자동 롤백
→ 데드락 아니어도 오래 대기 방지
```

**3. 트랜잭션을 짧게**

```
나쁜 예:
트랜잭션 시작
  → 상품 락
  → 외부 API 호출 (3초)   ← 이 동안 락 점유
  → 결제 락
  → 커밋

좋은 예:
외부 API 호출 (락 밖에서)
트랜잭션 시작
  → 상품 락
  → 결제 락
  → 커밋                  ← 빠르게 끝냄
```

락을 짧게 잡을수록 데드락 확률 감소

**4. 적은 수의 락 사용**

```
나쁜 예:
상품 락 → 재고 락 → 쿠폰 락 → 결제 락  ← 락이 많을수록 순환 위험

좋은 예:
주문 락 하나로 통합  ← 락 수 최소화
```

---

## 분산 환경에서의 데드락

Redis 분산 락도 데드락 발생 가능

```
서버 A: lock:product 획득 → lock:coupon 대기
서버 B: lock:coupon 획득  → lock:product 대기

→ 데드락!
```

Redis는 MySQL처럼 자동 감지 없음 → 만료 시간(EX)이 데드락 방지의 핵심

```
서버 A: lock:product 획득 (EX 30)
서버 B: lock:coupon  획득 (EX 30)

→ 둘 다 대기
→ 30초 후 만료 시간 도달
→ 락 자동 해제
→ 재시도
```

만료 시간 없으면 → 영원히 대기 → 진짜 데드락

---

## 핵심 Q&A

**Q. 데드락이 뭔가?**

두 트랜잭션이 서로가 가진 락을 기다리며 영원히 진행 못하는 상태 A는 B를 기다리고, B는 A를 기다리는 순환 구조

**Q. MySQL은 데드락을 어떻게 처리하나?**

자동으로 순환 대기를 감지하고 둘 중 하나를 희생자로 선택해 롤백 애플리케이션에서 에러를 잡아 재시도 처리 필요

**Q. 데드락을 어떻게 방지하나?**

가장 효과적인 방법은 락 획득 순서 통일 순환 대기 조건 자체를 없애버림 추가로 트랜잭션을 짧게, 타임아웃 설정

---

## 핵심 요약

|항목|내용|
|---|---|
|정의|서로의 락을 기다리며 영원히 대기|
|발생 조건|상호 배제, 점유 대기, 비선점, 순환 대기|
|MySQL 처리|자동 감지 → 희생자 롤백|
|방지법 1|락 획득 순서 통일 (가장 효과적)|
|방지법 2|타임아웃 설정|
|방지법 3|트랜잭션 짧게 유지|
|분산 환경|만료 시간(EX)으로 자동 해제|