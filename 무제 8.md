# 고성민 이력서

## Summary

운영 중인 시스템에서 발생한 성능·정합성·유실 문제를 원인 분석부터 구조 개선까지 수행해왔습니다.

- 항공 연계 미들웨어 운영·모니터링 시스템 — 통계 조회 22.0s → 1.3s,
- 사내 문서 기반 RAG 챗봇 — 하이브리드 검색 및 LLM 평가·검증 체계 구축
- Spring Cloud OpenFeign 기여 — PR merged
- AX 인재전쟁 AI 해커톤 — 참가 5,300여 명 중 상위 10%

| 구분 | 스택 |
|---|---|
| Language & Framework | Java, Spring Boot, JPA/MyBatis, Python, FastAPI, Vue 3 |
| Data | MySQL, PostgreSQL, Redis, pgvector, VictoriaLogs, Elasticsearch |
| AI | vLLM, bge-m3, RAG |
| Infra | Docker, Nomad, Vector, Prometheus, Gitea Actions |

---

## Personal Information

**고성민**

- 유한테크노스 (E-border 팀) · 2024.09 ~ 현재

- GITHUB : github.com/gokid96
- BLOG : velog.io/@gokid360
- PORTFOLIO : 고성민_포트폴리오.pdf

---

## Project

### 항공 연계 미들웨어 모니터링 시스템 및 인프라 구축

**2025.08 ~ 2026.01**

`Spring Boot 3.5` `Vue 3` `MyBatis` `MySQL` `PostgreSQL` `Redis` `WebSocket` `VictoriaLogs` `Vector` `Nomad` `Prometheus` `Docker`

항공 탑승객 출입국 메시징 시스템의 웹 모니터링 레거시를 재개발하고 분산 서버 통합 인프라를 구축했습니다.

#### 대용량 통계 조회 성능 개선

- 3일 구간 메시지 통계 조회가 1,137만 행 풀스캔으로 22초 소요. EXPLAIN으로 집계 대상 컬럼이 인덱스에 없어 테이블 접근이 발생함을 확인. 커버링 인덱스 적용 및 동일 조건으로 두 번 훑던 스캔을 1회로 통합하여 **22.0초 → 1.3초 (16.8배), 스캔 대상 1,137만 → 239만 행**
- 통계 캐시 키에 RequestID·MessageID가 누락되어 조건이 다른 검색이 이전 검색의 통계를 재사용하던 정합성 버그 수정

#### 알람 파이프라인 안정화

- 멀티라인 파싱 방식으로 주기 마지막 알람 블록이 유실되던 문제를 종결자 기반 방출로 수정
- 재시도 불가 응답이 수집기 버퍼를 점유해 후속 알람이 전송되지 않던 문제를 상태코드 정책 변경으로 해결
- 병렬 전송으로 알람 해제가 발생보다 먼저 도착해 소실되던 문제를 전송 동시성 제어로 순서 보장
- 재전송 시 비멱등 연산이 다른 알람을 종료시키는 위험을 실행 순서 변경으로 차단
- 해제 유실로 알림이 반복 발송되던 문제를 미종료 알람 탐지 배치로 개선, 발견까지 **수일 → 최대 O분**

#### 로그 수집·검색 통합

- 서버별 SSH 접속 후 grep 하던 로그 조회를 Vector 수집 파이프라인 구축으로 웹 통합 검색 전환 (일 O GB / 채널 O개 / 노드 O개)
- 레거시 코드 수정 없이 로그 파싱만으로 요청ID·송수신방향·메시지유형 구조화
- Elasticsearch에서 VictoriaLogs로 전환하여 O

#### 레거시 현대화

- iBatis/MyBatis 혼용, DataSource 수동 전환, DB 인증정보 평문 하드코딩된 레거시를 Spring Boot 3.5 + Vue 3로 기능 단위 재개발
- 문자열 결합 쿼리로 인한 SQL Injection 취약점 제거
- 평문 인증정보를 GitHub Secrets 관리로 전환

#### 운영 자동화

- 승객 상태 DB 주기 갱신을 Redis Hash 실시간 조회로 전환하여 DB 쓰기 부하 제거
- WebSocket 기반 메시지 적체 현황 및 서비스 연결 상태 10초 주기 실시간 모니터링
- 서버 접속 후 수동 편집하던 프로덕션 config를 웹 편집으로 전환, 콘텐츠 해시 비교로 동시 수정 충돌 차단

<br>

### 사내 문서 기반 RAG 챗봇

**2025.O ~ 2026.O**

`Python` `FastAPI` `PostgreSQL/pgvector` `vLLM` `bge-m3` `Docker`

사내 매뉴얼·FAQ를 검색해 제품 사용 문의에 자동 응답하는 시스템입니다.

#### 검색 구조

- tsvector BM25가 한국어 조사를 분리하지 못하는 문제를 bge-m3 sparse 임베딩 도입으로 형태소 분석기 없이 해결
- dense·sparse 검색 결과를 RRF 결합 후 리랭킹하여 **Recall@5 O% → O%**
- BookStack 증분 동기화 및 계층 경로 임베딩 포함으로 동일 제목 페이지 구분

#### 응답 품질

- 균질한 문서 본문으로 유사도 임계값 변별력이 없던 문제를 LLM 판정·생성 후 검증으로 대체, **오답률 O% → O%**
- temperature 0에도 판정이 뒤집히던 vLLM 연속 배칭 비결정성을 판정 결과 캐싱으로 일관성 확보
- 코퍼스·모델 버전별 지표 이력화로 문서 추가 효과 정량 검증, 오답률 O% 이하를 오픈 기준으로 정의

<br>

### iAPP 시스템 운영

**2024.09 ~ 현재**

항공사·GDS 40여 곳이 국제 표준 프로토콜로 연동된 출입국 사전탑승자정보 시스템의 채널 운영을 담당했습니다. 위 두 프로젝트는 이 시스템을 운영하며 발견한 문제에서 시작했습니다.

- 항공사·GDS 40여 개 채널의 신규 등록·변경·삭제 및 연결 검증
- MATIP 직결, MQ 게이트웨이 경유 두 방식의 채널 설정 및 모니터링·알람 구성
- 운영 절차서 작성으로 작업 표준화

---

## Side Project

### e-commerce MSA

`Java 17` `Spring Boot` `JPA` `MySQL` `Redis` `Kafka` `k6` `Prometheus` `Grafana`

github.com/gokid96/e-commerce

주문·결제·쿠폰·잔액·상품을 독립 서비스로 분리하고, 서비스 간 통신 방식과 장애 시 정합성 복구를 설계했습니다.

- 즉시 응답이 필요한 조회·검증은 동기(Feign), 결합도를 낮춰야 하는 흐름은 비동기(Kafka)로 구분
- 트랜잭션 분리로 발생하는 정합성 문제를 Outbox 패턴으로 해결, 브로커 장애 시 재발행 보장
- 결제 실패 시 재고·쿠폰 복원을 Saga 보상 트랜잭션으로 처리
- 선착순 쿠폰 발급 동시성 제어
- k6 부하 테스트로 병목 분석 후 TPS 기준 필요 리소스 산정

상세 내용은 포트폴리오에 기재했습니다.

---

## Open Source

### Spring Cloud OpenFeign — PR merged

- `FeignClientFactoryBean`에서 Client 빈이 싱글톤으로 관리되지 않으면 초기화마다 커넥션 풀이 재생성되어 고갈될 수 있는 위험이 주석으로만 명시되어 있던 문제
- PR 제출 전 메인테이너에게 WARN 로그 추가와 문서 업데이트 중 어떤 방향이 적합한지 먼저 확인
- 정상 상황에서 로그가 출력되지 않도록 ConcurrentHashMap으로 contextId를 추적해 중복 초기화 시에만 WARN 발생하도록 구현
- 공식 문서에 Client 빈 싱글톤 필수 경고 문구 추가

---

## Awards

### AX 인재전쟁 AI 해커톤 — AI 인재 선정

참가 5,300여 명 중 상위 10%

- 기업 과제 2건 수행
- 여행 플랫폼의 공급사 환불 약관을 규칙(DSL)으로 구조화하고 Spring 구현·JUnit 테스트를 생성하는 도구 설계. 약관과 코드를 직접 매핑하지 않고 중간 규칙 레이어를 두어 한쪽 변경이 전체를 깨지 않도록 구성

---

## AI-assisted Development

- Claude Code·Codex를 활용해 eborder-system 및 RAG 챗봇 개발
- 해커톤 과제로 Codex 플러그인 2건 제작
  - 판정 룰셋을 SKILL.md로 정의하고 LLM은 인터페이스,
    핵심 판정은 결정론적 코드로 분리
- AI가 생성한 구현이 설계 의도와 어긋나는 경우
  거부 사유를 기록해 수정 근거로 활용

---

## Education

**경희사이버대학교** 컴퓨터정보통신공학과 · 2024.03 ~ 재학중