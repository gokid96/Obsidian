Dirty Read
  A: 재고 10 → 5로 수정 (미커밋)
  B: 재고 읽기 → 5 (커밋 안 된 값!)
  A: 롤백 → 재고 다시 10
  → B는 존재하지 않는 값을 읽음
  → B가 재고 5 기준으로 주문 처리하면 재고 데이터 꼬임

Non-Repeatable Read
  A: 재고 읽기 → 10
  B: 재고 수정 후 커밋 → 5
  A: 재고 다시 읽기 → 5
  → 같은 트랜잭션에서 같은 데이터가 달라짐
  → A가 "재고 10 있으니 팔아도 되겠다" 판단했는데
     실제 처리 시점엔 재고 5 → 의사결정 기준이 틀어짐

Phantom Read
  A: 재고 > 0 상품 조회 → 3개
  B: 새 상품 추가 후 커밋
  A: 재고 > 0 상품 다시 조회 → 4개
  → 같은 조건인데 결과 행 수가 달라짐
  → A가 3개 기준으로 집계/통계 처리 중이었다면 결과 오염

※ 셋의 차이
  Dirty Read          → 커밋 안 된 값을 읽음
  Non-Repeatable Read → 커밋된 값인데 같은 행이 달라짐
  Phantom Read        → 커밋된 값인데 행 자체가 생기거나 사라짐