# 순서 보장 엣지케이스

## 1. Kafka 파티션 간 순서 미보장

| 항목 | 내용 |
|------|------|
| **문제** | 서로 다른 파티션의 메시지 순서는 보장되지 않음 |
| **왜 발생** | 파티션은 병렬 처리 단위이므로, 파티션 간 순서 보장 불가 |
| **해결** | 같은 사용자/계좌의 이벤트를 같은 파티션으로 라우팅 (파티션 키 = userId or accountId) |
| **트레이드오프** | 특정 파티션에 트래픽 편중(hot partition) 가능 → 파티션 키 설계가 중요 |
