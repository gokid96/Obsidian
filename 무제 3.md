1. 다음 달 중순쯤 모니터링웹 설명(서버 상황 , 사용한 오픈 소스 설명 ,데이터 모니터링 처리 방식)  
2. 패덱스에서 사용하는 WBS 비주얼 베이직으로 만든거 쓰고 있는데 보안 감사를 받았는데 로그인부터 걸림 (id,pwd 로그 파일에 적나라하게 다보임, DB 경로도 보임)





**엔티티 생성 방식 5가지**

---

**1. 수동 매핑 (Setter)**

```java
User user = new User();
user.setName(request.getName());
user.setEmail(request.getEmail());
```

- 책임: Service
- setter가 열려있어 어디서든 값 변경 가능
- 필드 추가 시 매핑 누락해도 컴파일 에러 없음

---

**2. 생성자 직접 호출**

```java
User user = new User(request.getName(), request.getPassword(), request.getEmail());
```

- 책임: Service
- 인자 순서 실수 위험 (특히 String 타입 여러 개일 때)

---

**3. toEntity() — DTO 인스턴스 메서드**

```java
User user = request.toEntity();
```

- 책임: DTO
- 간결하고 많이 씀
- DTO가 Entity를 알아야 하고, 비즈니스 규칙이 DTO 안에 숨어버림

---

**4. from(request) — 엔티티 정적 팩토리**

```java
User user = User.from(request);
```

- 책임: Entity
- Entity가 DTO를 직접 참조 → 계층 간 의존 방향 역전 문제

---

**5. create() — 엔티티 정적 팩토리 + 내부 빌더 ✦ 권장**

```java
// Entity 안
public static User create(String name, String password, String email) {
    return User.builder()
            .name(name)
            .password(password)
            .email(email)
            .build();
}

// 호출부
User user = User.create(request.getName(), request.getPassword(), request.getEmail());
```

- 책임: Entity
- Entity가 DTO를 모름 (의존 방향 정상)
- 비즈니스 규칙을 한 곳에서 강제 가능
- 필드 추가 시 컴파일 에러로 누락 즉시 감지

---

**정리**

|               | 1       | 2       | 3          | 4          | 5      |
| ------------- | ------- | ------- | ---------- | ---------- | ------ |
| 생성 책임         | Service | Service | DTO        | Entity     | Entity |
| DTO↔Entity 의존 | -       | -       | DTO→Entity | Entity→DTO | 없음     |
| 규칙 강제         | X       | X       | △          | △          | O      |
| 실무 권장         | X       | X       | △          | △          | O      |

단순 CRUD 빠르게 칠 때는 3번, 그 외엔 5번이 가장 무난