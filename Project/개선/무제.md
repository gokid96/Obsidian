1. 읽기 성능 측정 
2. N+1 개선 
3. 인덱스 
4. 동시성 (낙관적 락)
5. 벌크연산
6. 커버링 인덱스
① workspace_member(workspace_id, user_id, role)  권한 확인
② post(workspace_id, folder_id, post_id, title, created_at, updated_at) 루트 게시글 조회
7. 이메일 @Async x
8. 캐시 
9. 스케일아웃 멀티 서버 
10. Redis 캐시
11. 가능하면 네임드락,커버링 인덱스,정규화,비정규화


| 순위  | 내용                                      | 효과                 |
| --- | --------------------------------------- | ------------------ |
| 1   | `PublicViewService` 캐싱                  | 비로그인 조회 쿼리 4개 → 0개 |
| 3   | `post(workspace_id, folder_id)` 커버링 인덱스 | 루트 게시글 목록 조회 최적화   |
| 4   | `folderCount` 비정규화                      | 트래픽 많아지면 그때 고려     |


**1단계**

```
k6 + Grafana + CloudWatch로 단일 서버 베이스라인 측정.
병목이 EC2 CPU임을 확인하고 개선 우선순위 결정.
(vus 100 기준 p95 ___ms, TPS ___)
```

**2단계**

```
getRootFolders() Hibernate 로그로 폴더 N+1 발견.
Fetch Join으로 쿼리 1+N번 → 1번 개선.
EXPLAIN으로 parent_id IS NULL 조건 Using where 발견.
복합 인덱스 (workspace_id, parent_id) 추가로 제거.
----
getMemberOrThrow 권한 확인 쿼리 3번 발견 (Hibernate 로그). 
-> findByWorkspaceWorkspaceIdAndUserUserId로 1번으로 개선.
EXPLAIN으로 UK 복합 인덱스 동작 확인.

p95 ___ms → ___ms 개선
```

**3단계**

```
`data_received`가 408MB → 1.0GB로 늘었음
```

**4단계**

```
k6로 동일 게시글 동시 50건 수정 시나리오 재현.
Lost Update 발생 확인 → @Version 낙관적 락 + 409 처리로 정합성 보장.
DB version 필드 검증으로 충돌 감지 동작 직접 확인.
```

**5단계**

```
이메일 발송 API 응답시간 측정 → SES 동기 처리 블로킹 확인.
@Async 전환으로 응답시간 ___ms → ___ms 개선.
```

**6단계**

```
Caffeine 로컬 캐싱으로 공개 페이지 RDS ReadIOPS ___% 감소.
단계적 부하테스트로 단일 서버 최대 TPS ___ 한계 확인.
```

**7단계**

```
EC2 2대 + ALB 구성 후 세션 불일치(401) curl 반복 요청으로 직접 재현.
Redis 세션 공유 적용 → Redis 강제 중단 시 전체 로그아웃 SPOF 직접 경험.
JWT Stateless 전환으로 어느 서버로 라우팅되어도 200 응답 확인.
```

**8단계**

```
EC2 간 Caffeine 캐시 불일치 직접 재현.
Redis Remote 캐싱 전환 후 불일치 0건 확인.
최종 TPS ___ → ___, p95 ___ms → ___ms 달성.
	```