# 05. 벌크연산 — Cascade DELETE vs JPQL 벌크 DELETE

## 1. 벌크연산이란?

보통 JPA로 엔티티를 삭제하면 Hibernate가 **엔티티 하나씩 DELETE 쿼리를 날린다.**

```sql
DELETE FROM post WHERE post_id = 1
DELETE FROM post WHERE post_id = 2
DELETE FROM post WHERE post_id = 3
...
```

벌크연산은 이걸 **한 번의 쿼리로 묶어서 처리**하는 방식이다.

```sql
DELETE FROM post WHERE folder_id IN (1, 2, 3, ...)
```

건별 처리와 벌크 처리의 차이는 데이터가 적을 땐 미미하지만, 연관 엔티티가 많아질수록 격차가 벌어진다.

---

## 2. 문제 발견

`Workspace`, `Folder` 엔티티를 열어보면 이런 패턴이 눈에 띈다.

```java
// Workspace.java
@OneToMany(mappedBy = "workspace", cascade = CascadeType.ALL, orphanRemoval = true)
private List<Folder> folders;

@OneToMany(mappedBy = "workspace", cascade = CascadeType.ALL, orphanRemoval = true)
private List<Post> posts;

@OneToMany(mappedBy = "workspace", cascade = CascadeType.ALL, orphanRemoval = true)
private List<WorkspaceMember> members;
```

```java
// Folder.java
@OneToMany(mappedBy = "parent", cascade = CascadeType.ALL, orphanRemoval = true)
private List<Folder> children;

@OneToMany(mappedBy = "folder", cascade = CascadeType.ALL, orphanRemoval = true)
private List<Post> posts;
```

`CascadeType.ALL + orphanRemoval = true` 는 **"부모가 자식의 생명주기를 완전히 소유한다"** 는 선언이다.

- `CascadeType.ALL` : 부모에게 한 작업(저장, 삭제 등)을 자식에게 전부 전파
- `orphanRemoval = true` : 부모 컬렉션에서 제거된 자식을 자동으로 DELETE

문제는 Hibernate가 이 CASCADE를 처리할 때 **영속성 컨텍스트에서 엔티티를 하나씩 추적하며 건별로 DELETE를 날린다**는 것이다.

`workspaceRepository.delete(workspace)` 한 줄이 내부에서 이렇게 동작한다.

```
workspace 삭제 요청
    ↓
Hibernate: "folders에 CascadeType.ALL 있네"
    ↓
folder 하나씩 삭제 전파
    ↓
Hibernate: "folder.posts에도 CascadeType.ALL 있네"
    ↓
post 하나씩 삭제 전파
    ↓
...
```

### Hibernate 로그로 확인

폴더 130개(depth 3) + 게시글 650개가 담긴 워크스페이스를 삭제하고 로그를 확인했다.

```bash
1 2026-03-10T02:29:27.735Z DEBUG 1 --- [untitles-api] [io-8070-exec-59] org.hibernate.SQL                        : delete from folder where folder_id=?
1 2026-03-10T02:29:27.735Z DEBUG 1 --- [untitles-api] [io-8070-exec-59] org.hibernate.SQL                        : delete from workspace_member where id=?
2 2026-03-10T02:29:27.736Z DEBUG 1 --- [untitles-api] [io-8070-exec-59] org.hibernate.SQL                        : delete from workspace_member where id=?
1 2026-03-10T02:29:27.737Z DEBUG 1 --- [untitles-api] [io-8070-exec-59] org.hibernate.SQL                        : delete from workspace where workspace_id=?
1 2026-03-10T02:29:27.737Z DEBUG 1 --- [untitles-api] [io-8070-exec-59] org.hibernate.SQL                        : delete from workspace_member where id=?
```

다음과 같이 각각 쿼리가 나가고있었고 k6로 응답시간도 측정했다.

```
삭제 응답시간: 1,123ms
```

쉽게 말하면 SELECT N+1과 같은 패턴이다. SELECT N+1은 조회할 때 연관 엔티티를 건별로 SELECT하는 문제고, 여기서는 삭제할 때 연관 엔티티를 건별로 DELETE하는 문제다. 원인도 같고, 해결 방향도 같다. 쿼리를 묶어서 줄이는 것.

### 1. JPQL 벌크 DELETE (`@Modifying`)

```java
@Modifying(clearAutomatically = true)
@Query("DELETE FROM Post p WHERE p.workspace.workspaceId = :workspaceId")
void deleteAllByWorkspaceId(@Param("workspaceId") Long workspaceId);
```


| **장점** | 테이블당 쿼리 1번, 삭제 순서를 서비스에서 명시적 제어 가능           |
| ------ | -------------------------------------------- |
| **단점** | 영속성 컨텍스트 우회 → `clearAutomatically = true` 필요 |

---

### 2. `deleteAllInBatch`

```java
postRepository.deleteAllInBatch(posts);
```


| **장점** | 영속성 컨텍스트가 인지한 상태에서 IN절 벌크 삭제  |
| ------ | ----------------------------- |
| **단점** | 삭제 대상 엔티티 목록을 서비스에서 직접 수집해야 함 |

---

### 3. DB 레벨 `ON DELETE CASCADE`

```sql
FOREIGN KEY (folder_id) REFERENCES folder(folder_id) ON DELETE CASCADE;
```

| **장점** | DB가 알아서 처리, JPA 코드 변경 없음, 가장 빠름                 |
| ------ | ----------------------------------------------- |
| **단점** | JPA 영속성 컨텍스트가 삭제 사실을 모름, JPA-DB 동작 분리로 유지보수 어려움 |

---

### 왜 `@Modifying`을 선택했나

세 가지 중 **`@Modifying` JPQL**을 선택했다.

`ON DELETE CASCADE`가 성능상 가장 빠르지만, JPA가 삭제 사실을 모르는 채로 트랜잭션이 끝나는 구조가 불편했다. 나중에 삭제 전후 처리 로직(예: 알림, 이벤트 발행)이 추가될 때 JPA 레이어에서 제어하기 어려워진다.

`deleteAllInBatch`는 삭제 대상 엔티티 목록을 서비스 코드에서 직접 수집해야 하는데, 이미 워크스페이스 ID만 알고 있는 상황에서 굳이 엔티티를 조회해서 넘기는 건 불필요한 SELECT가 추가되는 셈이다.

`@Modifying`은 워크스페이스 ID 하나로 연관된 모든 데이터를 한 방에 지울 수 있고, 삭제 순서를 서비스 코드에서 명시적으로 제어할 수 있다. 영속성 컨텍스트를 우회하기 때문에 `clearAutomatically = true`가 필요하지만, 삭제 후 트랜잭션이 바로 끝나는 이 케이스에서는 문제가 없다.

|          | CascadeType.ALL | @Modifying      | deleteAllInBatch | ON DELETE CASCADE |
| -------- | --------------- | --------------- | ---------------- | ----------------- |
| 쿼리 수     | N번 (건별)         | 테이블당 1번         | 테이블당 1번          | DB 처리             |
| 영속성 컨텍스트 | 인지 O            | 인지 X (clear 필요) | 인지 O             | 인지 X              |
| 삭제 순서 제어 | JPA 자동          | 서비스에서 명시        | 서비스에서 명시         | DB 자동             |
| 추가 조회 필요 | X               | X               | O (엔티티 목록)       | X                 |