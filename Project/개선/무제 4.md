1 JVM 힙 튜닝 — 지금 478MB만 쓰고 있어서 t3.small 2GB를 제대로 못 쓰고 있음
2 Tomcat 스레드 튜닝 — VU 1000명에서 끊기는 직접적인 원인
3 DB 커넥션 풀 튜닝 (HikariCP) 스레드 늘렸을 때 DB 커넥션이 병목될 수 있음
4 읽기/쓰기 DB 분리 (Read Replica) RDS 붙이면 한 단계 더 고도화 가능



