## CS 전체 공부 지도

### 1. 자료구조

- 배열, 연결리스트, 스택, 큐
- 트리 (BST, AVL, Red-Black)
- 힙, 해시테이블
- 그래프 (인접행렬, 인접리스트)
- 트라이 (Trie)

### 2. 알고리즘

- 정렬 (버블, 선택, 삽입, 퀵, 머지, 힙)
- 탐색 (BFS, DFS, 이진탐색)
- 동적프로그래밍 (DP)
- 그리디
- 백트래킹
- 시간복잡도 / 공간복잡도 (Big-O)

### 3. 운영체제

- 프로세스 vs 스레드
- 컨텍스트 스위칭
- 스케줄링 (FCFS, SJF, Round Robin)
- 메모리 관리 (페이징, 세그멘테이션)
- 가상메모리 / 페이지폴트
- 교착상태 (Deadlock)
- 뮤텍스 / 세마포어
- 인터럽트

### 4. 네트워크

- OSI 7계층
- TCP vs UDP
- TCP 3-way / 4-way handshake
- HTTP / HTTPS
- HTTP 1.1 vs 2 vs 3
- DNS 동작원리
- 로드밸런서 (L4 vs L7)
- 소켓 통신
- REST vs GraphQL vs gRPC
- CDN

### 5. 데이터베이스

- 관계형 DB 기본 (정규화, 비정규화)
- 인덱스 (B-Tree, Hash)
- 트랜잭션 / ACID
- 격리수준 (Read Uncommitted ~ Serializable)
- 락 (낙관적, 비관적, 네임드락)
- 커버링 인덱스
- 실행계획 (EXPLAIN)
- N+1 문제
- 샤딩 / 파티셔닝 / 레플리케이션
- NoSQL (Redis, MongoDB)

### 6. 자바 / JVM

- JVM 구조 (힙, 스택, 메서드영역)
- GC 종류 (Serial, G1, ZGC)
- GC 동작원리 (Mark & Sweep)
- 클래스로더
- 스레드 / 동기화
- volatile / synchronized / Atomic
- 제네릭 / 컬렉션
- 람다 / 스트림
- 리플렉션

### 7. 스프링

- IoC / DI
- AOP
- 빈 생명주기
- 트랜잭션 동작원리 (프록시)
- 필터 vs 인터셉터
- Spring MVC 동작흐름
- JPA / Hibernate
- 영속성 컨텍스트
- N+1, Fetch Join, BatchSize

### 8. 시스템 설계

- 캐시 전략 (Cache-Aside, Write-Through 등)
- Multi-Level Cache
- 메시지 큐 (Kafka, RabbitMQ)
- 분산락 (Redis, DB 네임드락)
- 세션 vs 토큰 (JWT)
- OAuth2
- 스케일아웃 / 스케일업
- CQRS
- 이벤트 드리븐 아키텍처
- 마이크로서비스 vs 모놀리식

### 9. 보안

- 인증 vs 인가
- SQL Injection
- XSS / CSRF
- HTTPS / TLS 동작원리
- 암호화 (대칭키, 비대칭키)
- JWT 구조 및 취약점

### 10. 성능 최적화

- 읽기 성능 측정 (슬로우쿼리, EXPLAIN)
- N+1 개선
- 인덱스 튜닝
- 동시성 제어
- 벌크연산
- 비동기 처리
- 캐시
- 스케일아웃