

> ⚠️ 이 파일은 레퍼런스 커밋 해시를 포함하므로 **레포 밖에 두고 절대 커밋 금지**. 개인 작업용.

## 0. 한눈 요약

- **지금 하던 재구성(feat/step08-order-kafka)을 그대로 계속한다.** 그게 리뷰 버그의 근본 해결이다.
- 리뷰 지적 대부분은 "재구성하다 해당 단계 올 때 제대로 지으면" 자연히 사라진다.
- 예외: **#13(중복 @KafkaListener)은 재구성과 무관한 현존 버그** → 아무 때나 5분이면 끝.
- 방향 결정: **B안 채택** = 레퍼런스 순서대로 Outbox도 지금 구현, #49는 "Outbox 단순화"로 의미 변경.

---

## 1. 완료됨 (feat/step08-order-kafka)

- [x] Kafka 설정 `7b164f2`
- [x] 데이터 역직렬화 클래스 `b4502e7`
- [x] 기존 Message 도메인 제거
- [x] Message-Kafka 인터페이스

---

## 2. STEP08 남은 재구성 (레퍼런스 실제 순서)

- [ ] **Event 객체 + 토픽/컨슈머그룹 정의** (ref `424c904`) — Event/EventType, Jackson3 변환
- [ ] **Outbox 구현** (ref `c48d9e0`, 16파일) — 도메인/스케줄러/리포지토리/리스너
- [ ] **주문완료 Kafka 발행** (ref `a3d08cf`) — OrderEventPublisherImpl
- [ ] 카프카 기초 문서 (ref `c7e81e2`) — **우리 #46에서 이미 함 → 스킵**
- [ ] **쿠폰 Kafka 전환 #48** (ref `54c8e37`, `ab4cdfd`, `07b5283`, `b74571f`)
- [ ] 주문 생성 간소화 / 결제 로직 추가 (ref `7d6e701`, `32a5048`)
- [ ] **주문/결제 이벤트 리스너** (ref `eaa03bc`) ← ⭐ 여기서 #3·#4 제대로
- [ ] **주문/결제 완료 테스트** (ref `897a9b4`) ← ⭐ 여기서 #6 제대로 (진짜 E2E)
- [ ] 쿠폰 발급 설계 문서 #50 (ref `f8ca4e6`)
- [ ] **Outbox 단순화 = 우리 #49** (ref `bf49124`) — 트랜잭션 없는 로직은 직접 발행

이후: STEP09 부하테스트 → STEP10 MSA 모듈 분리

---

## 3. 리뷰 지적 → 어디서 고치나

### 재구성하면서 자연히 해결 (따로 시간 X)

- [ ] **#3 실패이벤트 증발** → "주문/결제 리스너" 단계에서: 실패 이벤트를 **실패하는 트랜잭션 밖**에서 발행 (BEFORE_COMMIT + throw 조합 금지)
- [ ] **#4 보상 dangling** → 같은 단계에서 실패 이벤트 → refund/cancel 보상 **호출 배선**
- [ ] **#6 가짜 E2E** → "완료 테스트" 단계에서 최종 상태(재고·잔액·주문상태) 검증하는 진짜 E2E
- [ ] **#5 중복결제** → Payment.orderId **유니크 제약** + 컨슈머 **멱등성**

### 재구성과 별개, 짬날 때 (백로그)

- [ ] **#13 중복 @KafkaListener** ⚡5분 — order/payment/rank 리스너에서 겹친 `@KafkaListener` 이전 줄 삭제
- [ ] **#14 N+1** — getUserCoupons: couponId 모아서 in절 한 방 조회
- [ ] **#16 예외핸들러** — 일반 `Exception` 핸들러 추가 + "재고부족"은 409로
- [ ] **#19 DLT** — DLT 토픽 소비 컨슈머 + 에러 로깅/알림
- [ ] **#17 Order.cancel 가드** — 이미 CANCELED면 무시 (멱등)
- [ ] **#8 스케줄러 분산락** — ShedLock (다중 인스턴스 대비)
- [ ] **#9 UK 제약** — Balance.user_id / Stock.product_id 유니크
- [ ] **#10 ZSET TTL** — 일별 랭킹 키 만료 설정
- [ ] **#11/#12 랭킹 캐시** — union 쓰기 최소화 / 캐시 조합·TTL 재검토
- [ ] **#7 dev ddl-auto** — dev 프로필에서 `ddl-auto: none`
- [ ] **#15 @Transactional 정합** — CouponService 트랜잭션 편차 정리

### 신경 끄기 (이미 해결/과장/칭찬)

- **#1, #2** — 로컬(step10-msa)에서 실제 클라이언트로 배선됨. 리뷰가 옛 상태 봄.
- **#18** — 사실상 칭찬(락 없는 선착순 설계). 미사용 `findWithLockById`만 제거하면 끝.

---

## 4. 규칙 리마인더

- 커밋 메시지에 레퍼런스/AI 언급 금지, 본인이 직접 타이핑
- 브랜치 먼저 만들고 파일 수정 (main 직접 커밋 사고 방지)
- `git add`는 커밋 직전에 해당 파일만
- 코드 변경은 내가 직접 쓰지 않고 가이드로 전달 (기계적 재생성만 요청 시)
- 각 단계: 코드 반영 → `./gradlew build` green → 커밋

---

## 5. 다음 액션

**지금**: `424c904` Event/토픽 정의부터. 그 시점 커밋 떠서 우리 구조(Jackson3)로 변환 → 전달. (원하면 그 전에 #13만 먼저 후딱)