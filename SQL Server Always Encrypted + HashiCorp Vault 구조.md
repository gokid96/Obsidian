```
┌─────────────────────────────────────────────────────────────────┐
│                     클라이언트 앱 (.NET WinForms)                 │
│                                                                 │
│  ┌──────────────────┐         ┌──────────────────────────┐     │
│  │  MainForm.cs     │         │  VaultCmkProvider.cs     │     │
│  │  (UI + 쿼리)     │◄───────►│  (커스텀 CMK Provider)   │     │
│  └──────────────────┘         └──────────────────────────┘     │
│         │                              │                        │
│         ▼                              ▼                        │
│  ┌──────────────────────────────────────────────┐              │
│  │  Microsoft.Data.SqlClient (드라이버)          │              │
│  │  - 자동 암호화/복호화                          │              │
│  │  - CEK 메모리 캐시 (기본 2시간)                │              │
│  └──────────────────────────────────────────────┘              │
│         │                              │                        │
└─────────┼──────────────────────────────┼────────────────────────┘
          │ 1.암호문 저장/조회             │ 2.CEK wrap/unwrap 요청
          │                              │   (첫 호출만, 이후 캐시)
          ▼                              ▼
┌────────────────────────┐    ┌──────────────────────────────┐
│   SQL Server           │    │   HashiCorp Vault            │
│                        │    │                              │
│  ┌──────────────────┐  │    │  ┌────────────────────────┐  │
│  │ Users 테이블      │  │    │  │ Transit Engine         │  │
│  │  - Name          │  │    │  │  └─ ufms-cmk (CMK)     │  │
│  │  - Jumin (암호문)│  │    │  │     (평문 보관)         │  │
│  └──────────────────┘  │    │  └────────────────────────┘  │
│                        │    │                              │
│  ┌──────────────────┐  │    │  AppRole 인증                │
│  │ sys.column_      │  │    │  (RoleId + SecretId)         │
│  │ encryption_keys  │  │    │                              │
│  │  └─ uFMS_CEK     │  │    └──────────────────────────────┘
│  │     (암호화됨)   │  │
│  └──────────────────┘  │
└────────────────────────┘
```

| 구성요소                            | 역할                       | 위치                   |
| ------------------------------- | ------------------------ | -------------------- |
| **CMK** (Column Master Key)     | CEK를 암호화하는 마스터 키         | Vault Transit        |
| **CEK** (Column Encryption Key) | 실제 데이터 암호화용 AES-256 키    | SQL Server (암호화된 상태) |
| **VaultCmkProvider**            | SqlClient ↔ Vault 통신 브리지 | 클라이언트 앱              |
| **SqlClient Driver**            | 자동 암복호화 + CEK 캐싱         | 클라이언트 앱              |
| **Transit Engine**              | CMK로 wrap/unwrap 수행      | Vault                |
### 데이터 흐름

#### Insert (암호화)

```
앱 → SqlClient → [CEK 캐시 확인]
                  │
                  ├─ 캐시 있음 → 바로 암호화 → DB 저장
                  │
                  └─ 캐시 없음 → Vault에 CEK unwrap 요청
                                 → CMK로 CEK 복호화
                                 → 평문 CEK 캐시
                                 → 데이터 암호화 → DB 저장
```

#### Select (복호화)

```
앱 → SqlClient → DB에서 암호문 조회
              → 캐시된 CEK로 복호화
              → 평문을 앱에 반환
```

---

### 신뢰 경계 (Trust Boundary)

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   클라이언트     │    │   SQL Server    │    │     Vault       │
│                 │    │                 │    │                 │
│  ✅ 평문 데이터  │    │  ❌ 평문 없음   │    │  ✅ CMK 평문    │
│  ✅ 평문 CEK    │    │  ✅ 암호 CEK    │    │  ❌ CEK 없음    │
│     (캐시)      │    │  ✅ 암호 데이터 │    │  ❌ 데이터 없음 │
│  ❌ CMK 없음    │    │  ❌ CMK 없음    │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
       신뢰 필요          DBA 불신 가능        독립 관리
```

