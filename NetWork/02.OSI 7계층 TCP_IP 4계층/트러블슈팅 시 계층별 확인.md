
```bash
# L1: 물리 연결 확인
ethtool eth0

# L2: MAC 테이블, ARP
arp -a
ip neigh

# L3: IP, 라우팅
ip addr
ip route
ping [IP]

# L4: 포트, 연결 상태
ss -tuln
nc -zv [IP] [Port]

# L7: 애플리케이션 레벨
curl -v http://example.com
```
  
