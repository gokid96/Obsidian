**[ 1. 목록 조회 기능 만들기 ]** ``` 
① Entity 작성 (id, subject, content만) 
② Repository 작성 (findAll만) 
③ DTO 작성 (응답용만)
④ Service 작성 (getList만) ⑤ Controller 작성 (GET /api/questions만) ⑥ 테스트! ← 바로 동작 확인 ``` **[ 2. 상세 조회 기능 추가 ]** ``` ① Service에 getQuestion 추가 ② Controller에 GET /{id} 추가 ③ 테스트! ``` **[ 3. 생성 기능 추가 ]** ``` ① DTO 추가 (생성용) ② Service에 create 추가 ③ Controller에 POST 추가 ④ 테스트!