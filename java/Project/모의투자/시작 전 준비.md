아 맞아요! 제가 너무 앞서 나갔네요 😅 차근차근 처음부터 정리하겠습니다!

---

# 📋 주식 모의투자 교육 플랫폼 - 개발 

---

## 1️⃣ 요구사항 정리 (MVP 기준)

### 👤 일반 사용자 기능

#### 회원 관리


□ 회원가입
  - 이메일, 비밀번호, 닉네임
  - 가입 시 가상 계좌 자동 생성 (초기 잔고: 1,000만원)

□ 로그인
  - 이메일 + 비밀번호

□ 로그아웃
```

#### 주식 조회
```
□ 주식 목록 조회
  - 전체 종목 리스트 (페이징)
  - 종목명, 현재가, 전일 대비 등락률

□ 주식 상세 조회
  - 기본 정보 (종목명, 현재가, 시가총액)
  - 전일 종가, 등락률
  - 간단한 설명
```

#### 매매
```
□ 매수
  - 종목 선택
  - 수량 입력
  - 잔고 확인
  - 체결 (거래 내역 생성 + 보유 종목 추가)

□ 매도
  - 보유 종목에서 선택
  - 수량 입력 (보유 수량 이하)
  - 체결 (거래 내역 생성 + 보유 수량 차감)
```

#### 포트폴리오
```
□ 내 계좌 조회
  - 현재 잔고
  - 총 자산 (잔고 + 보유 주식 평가금액)
  - 총 수익률

□ 보유 종목 조회
  - 종목명, 보유 수량
  - 평균 단가, 현재가
  - 평가금액, 손익, 수익률

□ 거래 내역 조회
  - 매수/매도 기록
  - 거래일시, 종목, 수량, 가격
```

### 🔧 관리자 기능 (일단 없음)
```
- Phase 2 이후 추가 고려
```

---

## 2️⃣ ERD 설계

### 주요 엔티티

┌─────────────┐
│    User     │ 회원
├─────────────┤
│ id (PK)     │
│ email       │
│ password    │
│ nickname    │
│ created_at  │
└─────────────┘
       │ 1
       │
       │ 1
       ↓
┌─────────────┐
│   Account   │ 계좌
├─────────────┤
│ id (PK)     │
│ user_id(FK) │
│ balance     │ 현재 잔고
│ initial_bal │ 초기 금액
│ created_at  │
└─────────────┘

┌─────────────┐
│    Stock    │ 주식 종목
├─────────────┤
│ stock_code  │ PK (005930)
│ stock_name  │ 삼성전자
│ current_prc │ 현재가
│ prev_close  │ 전일 종가
│ market_cap  │ 시가총액
│ updated_at  │
└─────────────┘

       ↑
       │ N
       │
┌─────────────┐
│    Trade    │ 거래 내역
├─────────────┤
│ id (PK)     │
│ user_id(FK) │
│ stock_code  │
│ trade_type  │ BUY/SELL
│ quantity    │ 수량
│ price       │ 체결가
│ total_amt   │ 총 금액
│ created_at  │
└─────────────┘

       User
        │ 1
        │
        │ N
        ↓
┌─────────────┐
│   Holding   │ 보유 종목
├─────────────┤
│ id (PK)     │
│ user_id(FK) │
│ stock_code  │
│ quantity    │ 보유 수량
│ avg_price   │ 평균 단가
│ updated_at  │
└─────────────┘

```
### 테이블 상세 정의

#### 1. users
```sql
CREATE TABLE users (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(100) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    nickname VARCHAR(50) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_email (email)
);
```

#### 2. accounts
```sql
CREATE TABLE accounts (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL UNIQUE,
    balance DECIMAL(15, 2) NOT NULL DEFAULT 10000000.00,
    initial_balance DECIMAL(15, 2) NOT NULL DEFAULT 10000000.00,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (user_id) REFERENCES users(id),
    INDEX idx_user_id (user_id)
);
```

#### 3. stocks
```sql
CREATE TABLE stocks (
    stock_code VARCHAR(10) PRIMARY KEY,
    stock_name VARCHAR(100) NOT NULL,
    current_price DECIMAL(15, 2) NOT NULL,
    previous_close DECIMAL(15, 2) NOT NULL,
    market_cap BIGINT,
    description TEXT,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    INDEX idx_stock_name (stock_name)
);
```

#### 4. trades
```sql
CREATE TABLE trades (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    stock_code VARCHAR(10) NOT NULL,
    trade_type ENUM('BUY', 'SELL') NOT NULL,
    quantity INT NOT NULL,
    price DECIMAL(15, 2) NOT NULL,
    total_amount DECIMAL(15, 2) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (stock_code) REFERENCES stocks(stock_code),
    INDEX idx_user_created (user_id, created_at)
);
```

#### 5. holdings
```
sql
CREATE TABLE holdings (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    stock_code VARCHAR(10) NOT NULL,
    quantity INT NOT NULL,
    average_price DECIMAL(15, 2) NOT NULL,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (stock_code) REFERENCES stocks(stock_code),
    UNIQUE KEY uk_user_stock (user_id, stock_code),
    INDEX idx_user_id (user_id)
);
```

---

## 3️⃣ API 명세서

### 인증 API

#### 회원가입
```
POST /api/auth/signup

Request:
{
    "email": "user@example.com",
    "password": "password123",
    "nickname": "주린이"
}

Response (201):
{
    "id": 1,
    "email": "user@example.com",
    "nickname": "주린이",
    "createdAt": "2025-01-20T10:00:00"
}

Error (400):
{
    "error": "EMAIL_ALREADY_EXISTS",
    "message": "이미 존재하는 이메일입니다"
}
```

#### 로그인
```
POST /api/auth/login

Request:
{
    "email": "user@example.com",
    "password": "password123"
}

Response (200):
{
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "user": {
        "id": 1,
        "email": "user@example.com",
        "nickname": "주린이"
    }
}

Error (401):
{
    "error": "INVALID_CREDENTIALS",
    "message": "이메일 또는 비밀번호가 올바르지 않습니다"
}
```

---

### 주식 API

#### 주식 목록 조회
```
GET /api/stocks?page=0&size=20

Response (200):
{
    "content": [
        {
            "stockCode": "005930",
            "stockName": "삼성전자",
            "currentPrice": 72000,
            "previousClose": 71500,
            "changeRate": 0.7,
            "changeAmount": 500
        },
        {
            "stockCode": "000660",
            "stockName": "SK하이닉스",
            "currentPrice": 125000,
            "previousClose": 123000,
            "changeRate": 1.6,
            "changeAmount": 2000
        }
    ],
    "totalElements": 10,
    "totalPages": 1,
    "size": 20,
    "number": 0
}
```

#### 주식 상세 조회
```
GET /api/stocks/{stockCode}

Response (200):
{
    "stockCode": "005930",
    "stockName": "삼성전자",
    "currentPrice": 72000,
    "previousClose": 71500,
    "changeRate": 0.7,
    "changeAmount": 500,
    "marketCap": 4320000000000,
    "description": "대한민국 대표 전자 기업"
}

Error (404):
{
    "error": "STOCK_NOT_FOUND",
    "message": "종목을 찾을 수 없습니다"
}
```

---

### 거래 API

#### 매수
```
POST /api/trades/buy

Request:
{
    "stockCode": "005930",
    "quantity": 10
}

Response (201):
{
    "tradeId": 123,
    "stockCode": "005930",
    "stockName": "삼성전자",
    "tradeType": "BUY",
    "quantity": 10,
    "price": 72000,
    "totalAmount": 720000,
    "createdAt": "2025-01-20T10:30:00",
    "remainingBalance": 9280000
}

Error (400):
{
    "error": "INSUFFICIENT_BALANCE",
    "message": "잔고가 부족합니다"
}
```

#### 매도
```
POST /api/trades/sell

Request:
{
    "stockCode": "005930",
    "quantity": 5
}

Response (201):
{
    "tradeId": 124,
    "stockCode": "005930",
    "stockName": "삼성전자",
    "tradeType": "SELL",
    "quantity": 5,
    "price": 73000,
    "totalAmount": 365000,
    "createdAt": "2025-01-20T11:00:00",
    "remainingBalance": 9645000
}

Error (400):
{
    "error": "INSUFFICIENT_QUANTITY",
    "message": "보유 수량이 부족합니다"
}
```

---

### 계좌/포트폴리오 API

#### 내 계좌 조회
```
GET /api/account

Response (200):
{
    "userId": 1,
    "balance": 9280000,
    "totalAsset": 9640000,
    "initialBalance": 10000000,
    "totalReturn": -3.6,
    "totalProfit": -360000
}
```

#### 보유 종목 조회
```
GET /api/holdings

Response (200):
[
    {
        "stockCode": "005930",
        "stockName": "삼성전자",
        "quantity": 5,
        "averagePrice": 72000,
        "currentPrice": 73000,
        "evaluationAmount": 365000,
        "profit": 5000,
        "returnRate": 1.4
    }
]
```

#### 거래 내역 조회
```
GET /api/trades/history?page=0&size=20

Response (200):
{
    "content": [
        {
            "tradeId": 124,
            "stockCode": "005930",
            "stockName": "삼성전자",
            "tradeType": "SELL",
            "quantity": 5,
            "price": 73000,
            "totalAmount": 365000,
            "createdAt": "2025-01-20T11:00:00"
        },
        {
            "tradeId": 123,
            "stockCode": "005930",
            "stockName": "삼성전자",
            "tradeType": "BUY",
            "quantity": 10,
            "price": 72000,
            "totalAmount": 720000,
            "createdAt": "2025-01-20T10:30:00"
        }
    ],
    "totalElements": 2,
    "totalPages": 1,
    "size": 20,
    "number": 0
}
```

---

## 4️⃣ 기술 스택 확정

### Backend
```
- Language: Java 17
- Framework: Spring Boot 3.2.x
- ORM: Spring Data JPA
- Database: MySQL 8.0
- Build Tool: Gradle
- Security: Spring Security + JWT (나중에)
```

### Frontend
```
- Framework: Vue.js 3.x
- UI Library: Tailwind CSS (or Bootstrap)
- HTTP Client: Axios
- State Management: Pinia (필요시)
```

### 배포
```
- Backend: Railway / AWS EC2 (선택)
- Frontend: Vercel / Netlify
- Database: Railway MySQL / AWS RDS
```

### 개발 도구
```
- IDE: IntelliJ IDEA / VS Code
- API Test: Postman
- Version Control: Git + GitHub
````

---

## 5️⃣ 초기 데이터 (주식 종목)

### MVP용 종목 10개

````sql
INSERT INTO stocks (stock_code, stock_name, current_price, previous_close, market_cap, description) VALUES
('005930', '삼성전자', 72000, 71500, 4320000000000, '대한민국 대표 전자 기업'),
('000660', 'SK하이닉스', 125000, 123000, 910000000000, '반도체 제조 기업'),
('035420', 'NAVER', 180000, 178000, 590000000000, '인터넷 포털 서비스'),
('035720', '카카오', 45000, 44500, 200000000000, '모바일 메신저 플랫폼'),
('051910', 'LG화학', 380000, 375000, 670000000000, '화학/배터리 제조'),
('006400', '삼성SDI', 420000, 415000, 310000000000, '2차전지 제조'),
('207940', '삼성바이오로직스', 850000, 840000, 670000000000, '바이오의약품 위탁생산'),
('068270', '셀트리온', 185000, 183000, 250000000000, '바이오시밀러 개발'),
('005380', '현대차', 195000, 193000, 420000000000, '자동차 제조'),
('012330', '현대모비스', 240000, 238000, 230000000000, '자동차 부품 제조');
```

---

## 📅 개발 단계별 체크리스트

### Phase 0: 준비 (1일)
```
□ GitHub 레포지토리 생성
□ Spring Boot 프로젝트 생성
□ MySQL 설치 및 DB 생성
□ Vue.js 프로젝트 생성
□ ERD 최종 확정
□ API 명세서 최종 확정
```

### Phase 1: 백엔드 기본 (3-4일)
```
□ Entity 생성 (User, Account, Stock, Trade, Holding)
□ Repository 생성
□ 회원가입 API
□ 로그인 API (간단하게 - JWT는 나중에)
□ 초기 주식 데이터 INSERT
□ 주식 조회 API
```

### Phase 2: 거래 로직 (3-4일)
```
□ 매수 API + 동시성 처리
□ 매도 API
□ 평균 단가 계산
□ 계좌 조회 API
□ 보유 종목 조회 API (N+1 해결)
□ 거래 내역 조회 API
□ Postman 테스트
```

### Phase 3: 프론트엔드 (3-4일)
```
□ 로그인/회원가입 화면
□ 주식 목록 화면
□ 주식 상세 화면
□ 매수/매도 모달
□ 포트폴리오 화면
□ 거래 내역 화면
```

### Phase 4: 통합 및 배포 (2-3일)
```
□ 백엔드-프론트 연동
□ 전체 플로우 테스트
□ 버그 수정
□ 배포
□ README 작성 (트러블슈팅 포함)
````

---

**이제 이 문서를 기준으로 개발 시작하면 될 것 같아요!**

다음 단계:

1. Spring Boot 프로젝트 생성
2. Entity 만들기

시작할까요? 😊