# 핵심 기능 구현

## 1. 인증 시스템

### JWT 기반 인증 흐름
(다이어그램 or 설명)

### 주요 구현 포인트
- Access Token / Refresh Token 분리
- JwtAuthenticationFilter 동작 방식
- 만료 처리

## 2. 워크스페이스 & 권한

### 역할 기반 접근 제어 (RBAC)
- OWNER, ADMIN, MEMBER 권한 분리
- 워크스페이스별 멤버 초대/권한 변경

## 3. 폴더/포스트 계층 구조

### 자기 참조 관계 설계
(Folder - parentFolder 구조)

## 4. 이메일 인증 (AWS SES)
(구현 방식)