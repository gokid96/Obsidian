## 왜 필요한가?

```
EDA와 Saga에서 공통적으로 발생하는 문제
→ DB 저장과 이벤트 발행이 동시에 실패하면?

주문 서비스:
1. DB에 주문 저장 ← 성공
2. Kafka에 이벤트 발행 ← 실패!

결과:
→ 주문은 저장됐는데
→ 재고/결제 서비스는 이벤트를 못 받음
→ 데이터 불일치 발생

반대 경우도 문제:
1. Kafka에 이벤트 발행 ← 성공
2. DB에 주문 저장 ← 실패!

결과:
→ 이벤트는 날아갔는데
→ 주문 데이터는 없음
→ 재고는 차감됐는데 주문이 없는 상태
```

---

## Outbox 패턴이란?

```
이벤트를 Kafka에 바로 발행하지 않고
DB의 Outbox 테이블에 먼저 저장
→ DB 저장과 이벤트 기록을 하나의 트랜잭션으로 묶음
→ 별도 프로세스(Outbox Poller)가 읽어서 Kafka에 발행

핵심:
→ DB 트랜잭션 = 완벽한 원자성 보장
→ Kafka 발행 실패해도 Outbox에 남아있어서 재시도 가능
```

---

## 기존 방식 vs Outbox 패턴

### 기존 방식 (문제 발생)

```
주문 서비스
    ↓
1. DB에 주문 저장
2. Kafka에 이벤트 발행  ← 여기서 실패하면 끝

문제:
→ 1번과 2번이 각각 독립적으로 실행
→ 중간 실패 시 불일치 발생
→ 재시도 로직 넣어도 완벽하지 않음
```

### Outbox 패턴 (해결)

```
주문 서비스
    ↓
1. DB에 주문 저장
   + Outbox 테이블에 이벤트 기록  ← 하나의 트랜잭션!
    ↓
2. Outbox Poller가 주기적으로 Outbox 테이블 조회
    ↓
3. Kafka에 이벤트 발행
    ↓
4. 발행 성공 시 Outbox에서 삭제 (또는 발행완료 상태로 변경)

→ 1번이 실패하면 Outbox에도 기록 안 됨 (일관성 유지)
→ 3번이 실패해도 Outbox에 남아있어서 재시도 가능
```

---

## Outbox 테이블 구조

sql

```sql
CREATE TABLE outbox (
  id          UUID PRIMARY KEY,
  event_type  VARCHAR,   -- "OrderCreated", "PaymentFailed" 등
  payload     JSON,      -- 이벤트 전체 데이터
  status      VARCHAR,   -- PENDING / PUBLISHED
  created_at  TIMESTAMP,
  published_at TIMESTAMP
);
```

```
흐름:
주문 저장 + Outbox에 PENDING 상태로 이벤트 기록
          ↓
Outbox Poller → PENDING 이벤트 조회
          ↓
Kafka 발행 성공 → status를 PUBLISHED로 변경
          ↓
PUBLISHED 이벤트는 주기적으로 정리
```

---

## Outbox Poller 방식 2가지

### 1. 폴링 방식 (Polling)

```
Outbox Poller가 주기적으로 PENDING 이벤트 조회
→ 단순하고 구현 쉬움
→ 주기마다 약간의 지연 발생 (실시간성 낮음)
→ DB 부하 발생

적합한 경우:
→ 이벤트 발행 지연이 수 초 정도 허용될 때
→ 소규모 서비스
```

### 2. CDC 방식 (Change Data Capture)

```
DB의 변경 로그(WAL)를 실시간으로 읽어서 Kafka에 발행
→ Debezium 같은 툴 사용
→ DB 변경 즉시 이벤트 발행 (실시간성 높음)
→ DB 부하 없음
→ 구현 복잡

적합한 경우:
→ 실시간성이 중요할 때
→ 대규모 트래픽 서비스
```

|항목|폴링|CDC|
|---|---|---|
|구현 난이도|쉬움|복잡|
|실시간성|낮음|높음|
|DB 부하|있음|없음|
|대표 도구|직접 구현|Debezium|
|적합한 상황|소규모|대규모|

---

## Saga와의 연결

```
Saga에서도 동일한 문제 발생

결제 실패 감지
→ 재고 복구 보상 이벤트 발행 ← 실패!
→ 재고가 복구 안 된 채로 남음

해결:
→ 보상 이벤트도 Outbox 테이블에 먼저 저장
→ Outbox Poller가 읽어서 Kafka에 발행
→ 발행 실패해도 재시도 가능

EDA + Saga + Outbox = MSA 표준 조합
```

---

## 주의사항

### 중복 이벤트 처리

```
Outbox Poller가 같은 이벤트를 두 번 발행할 수 있음
→ Kafka 발행 성공 후 status 업데이트 전에 장애 발생 시
→ 재시작하면 같은 이벤트를 다시 발행

해결:
→ 구독자 측에서 멱등성 처리 필수
→ 이벤트 ID로 중복 여부 확인
```

### Outbox 테이블 크기 관리

```
PUBLISHED된 이벤트가 쌓이면 테이블이 커짐
→ 주기적으로 오래된 PUBLISHED 이벤트 삭제
→ 또는 별도 아카이브 테이블로 이동
```

---

## 실제 사용 사례

```
쿠팡 / 배민 주문 처리
→ 주문 저장 + Outbox 이벤트 기록을 하나의 트랜잭션
→ Debezium(CDC)으로 실시간 Kafka 발행

MSA 표준 패턴
→ 대부분의 대규모 MSA 서비스에서 사용
→ EDA + Saga + Outbox 조합
```

---

## 핵심 Q&A

**Q. Outbox 패턴이 왜 필요한가?**

> DB 저장과 이벤트 발행은 별개의 작업이라 중간에 하나가 실패하면 불일치 발생 Outbox 패턴은 DB 저장과 이벤트 기록을 하나의 트랜잭션으로 묶어서 원자성 보장 이벤트 발행이 실패해도 Outbox에 남아있어서 재시도 가능

**Q. 폴링과 CDC 중 어떤 걸 선택하나?**

> 소규모 서비스나 약간의 지연이 허용되면 폴링 실시간성이 중요하거나 대규모 트래픽이면 Debezium 같은 CDC 도구 사용

**Q. Outbox 패턴을 쓰면 중복 이벤트가 없나?**

> 중복은 여전히 발생할 수 있음 (At-Least-Once) 구독자 측에서 이벤트 ID로 멱등성 처리가 필수

**Q. Saga랑 어떻게 연결되나?**

> Saga의 보상 이벤트 발행도 동일한 문제를 가짐 보상 이벤트도 Outbox 테이블에 먼저 저장해서 유실 방지 EDA + Saga + Outbox가 MSA의 표준 조합

---

## 핵심 요약

|항목|내용|
|---|---|
|목적|DB 저장과 이벤트 발행의 원자성 보장|
|핵심|이벤트를 Outbox 테이블에 먼저 저장 후 발행|
|Poller 방식 1|폴링 (단순, 소규모, 약간의 지연)|
|Poller 방식 2|CDC / Debezium (실시간, 대규모)|
|주의사항|중복 이벤트 가능 → 구독자 멱등성 처리 필수|
|Saga 연결|보상 이벤트 발행에도 동일하게 적용|
|표준 조합|EDA + Saga + Outbox = MSA 표준|
|카테고리|4. 아키텍처|