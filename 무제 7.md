## DTO 어노테이션 정리

### 1. 각 어노테이션이 필요한 이유

java

```java
@Getter
// Jackson이 객체 → JSON 변환할 때 getter를 통해 필드값을 읽어요.
// getter가 없으면 JSON 응답에 필드가 아예 안 나타나요.

@NoArgsConstructor
// 기본 생성자 자동 생성.
// Jackson이 JSON → 객체 변환할 때
// 1단계로 빈 객체를 먼저 만들고, 2단계로 필드에 값을 주입해요.
// 기본 생성자가 없으면 Jackson이 객체를 못 만들어요.

@AllArgsConstructor(access = AccessLevel.PRIVATE)
// 모든 필드를 받는 생성자 자동 생성.
// AccessLevel.PRIVATE으로 외부에서 직접 호출을 막고,
// of() 팩토리 메서드로만 생성하도록 강제해요.
```

---

### 2. record 사용 이유와 방법

DTO처럼 데이터만 담는 클래스를 더 간결하게 쓰는 문법이에요.

java

```java
// 기존 방식
@Getter
@NoArgsConstructor
@AllArgsConstructor
public class BalanceChargeRequest {
    private long amount;
}

// record 방식
public record BalanceChargeRequest(long amount) {}
```

record를 쓰면 getter, 생성자, equals, hashCode, toString 전부 자동으로 만들어줘요.

**주의:** record는 기본 생성자가 없어요. Request DTO에 record를 쓸 때 Jackson 역직렬화 시 `@JsonCreator` 같은 별도 설정이 필요해요.

---

### 3. 팩토리 패턴 사용 이유

외부에서 Response를 만들 때 정해진 메서드로만 생성할 수 있도록 하는 패턴이에요.

java

```java
public static BalanceResponse of(final long amount) {
    // final을 붙여서 메서드 안에서 amount 값을 실수로 재할당하는 걸 막아요.
    return new BalanceResponse(amount);
}
```

`new BalanceResponse(amount)` 처럼 직접 생성하는 것보다 `BalanceResponse.of(amount)` 가 의도가 명확하고, 나중에 생성 로직이 바뀌어도 한 곳만 수정하면 돼요.

---

### 4. 요청에는 기본 생성자, 응답에는 매개변수 있는 생성자를 쓰는 이유

**Request** — Jackson이 객체를 만들어줌

java

```java
// 1단계: 기본 생성자로 빈 객체 생성
BalanceChargeRequest request = new BalanceChargeRequest();
// 2단계: 필드에 값 주입
// request.amount = 200000
```

Jackson이 외부 라이브러리라 기본 생성자가 `public`이어야 접근할 수 있어요.

**Response** — 개발자가 직접 객체를 만들어서 Jackson한테 넘겨줌

java

```java
// of() 내부에서 AllArgsConstructor로 만든 생성자 호출
private BalanceResponse(long amount) {
    this.amount = amount;
}
```

개발자가 `of()`로 직접 만드니까 기본 생성자가 필요 없고, 매개변수 있는 생성자만 있으면