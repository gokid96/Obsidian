### JVM의 "기계어"

`javac`로 컴파일된 `.class` 파일 안에는 **바이트코드(Bytecode)**가 들어있다. 이는 JVM이 이해하는 일종의 **가상 기계어**다.

실제 CPU는 x86, ARM 등 아키텍처별로 다른 명령어 세트를 쓴다. 하지만 JVM은 **추상화된 단일 명령어 세트**를 정의하고, 이것만 실행할 줄 안다. 그 명령어가 바이트코드다.

---

### 스택 기반 가상머신

JVM의 가장 중요한 설계 결정 중 하나는 **스택 기반(Stack-based)** 아키텍처를 택한 것이다.

#### 스택 기반 vs 레지스터 기반

실제 CPU는 대부분 **레지스터 기반**이다. 연산할 값을 레지스터에 올리고, 레지스터끼리 연산한다.

```
// 레지스터 기반 (x86 예시)
mov eax, 1      ; eax 레지스터에 1 저장
mov ebx, 2      ; ebx 레지스터에 2 저장
add eax, ebx    ; eax = eax + ebx
```

JVM은 다르다. 연산 값을 **피연산자 스택(Operand Stack)**에 쌓고, 스택 top의 값들을 꺼내 연산한다.

```
// 스택 기반 (JVM 바이트코드)
iconst_1        ; 스택에 1 push → [1]
iconst_2        ; 스택에 2 push → [1, 2]
iadd            ; 두 개 pop, 더해서 push → [3]
```

#### 왜 스택 기반인가?

1. **플랫폼 독립성**: 레지스터는 CPU마다 개수와 크기가 다르다. 스택은 어떤 CPU에서도 동일하게 구현 가능.
2. **단순한 명령어**: 피연산자를 명시할 필요가 없어 명령어가 짧다 (1바이트 오피코드).
3. **컴파일러 단순화**: `javac`가 레지스터 할당 같은 복잡한 최적화를 안 해도 됨. 그 일은 JIT이 런타임에 처리.

대가는 **명령어 수가 늘어난다는 것**. `add eax, ebx` 한 줄이 JVM에서는 3줄이다. 하지만 JIT이 네이티브 코드로 변환할 때 이 차이는 사라진다.

---

### 스택 프레임 다시 보기

2편에서 말한 스택 프레임 내부 구조를 바이트코드 관점에서 다시 보자.

```
┌─────────────────────────────┐
│   Local Variable Array      │  ← 지역변수, 파라미터, this
│   [0] [1] [2] ...           │
├─────────────────────────────┤
│   Operand Stack             │  ← 연산 중간값
│   [top]                     │
│   [...]                     │
├─────────────────────────────┤
│   Constant Pool Reference   │  ← 상수 풀 참조
└─────────────────────────────┘
```

- **Local Variable Array**: 배열처럼 인덱스로 접근. `istore_1`은 "스택 top을 지역변수 1번 슬롯에 저장"이라는 뜻.
- **Operand Stack**: LIFO 구조. 연산의 임시 저장소.
- **Constant Pool Reference**: 문자열 리터럴, 클래스 이름 등이 저장된 풀.

모든 바이트코드 명령은 이 세 영역 사이에서 데이터를 움직인다.

---

### 바이트코드 직접 읽어보기

간단한 자바 코드를 컴파일해 바이트코드를 들여다보자.

```java
public class Simple {
    public int add() {
        int a = 1;
        int b = 2;
        return a + b;
    }
}
```

컴파일 후 `javap -c Simple`로 디스어셈블:

```
public int add();
  Code:
     0: iconst_1       // 상수 1을 스택에 push
     1: istore_1       // 스택 top을 지역변수 1번(a)에 저장
     2: iconst_2       // 상수 2를 스택에 push
     3: istore_2       // 스택 top을 지역변수 2번(b)에 저장
     4: iload_1        // 지역변수 1번(a)을 스택에 push
     5: iload_2        // 지역변수 2번(b)을 스택에 push
     6: iadd           // 스택 top 두 개를 꺼내 더하고 push
     7: ireturn        // 스택 top을 int로 반환
```

#### 단계별 스택 상태

```
0: iconst_1     Operand Stack: [1]              Locals: [this, _, _]
1: istore_1     Operand Stack: []               Locals: [this, 1, _]
2: iconst_2     Operand Stack: [2]              Locals: [this, 1, _]
3: istore_2     Operand Stack: []               Locals: [this, 1, 2]
4: iload_1      Operand Stack: [1]              Locals: [this, 1, 2]
5: iload_2      Operand Stack: [1, 2]           Locals: [this, 1, 2]
6: iadd         Operand Stack: [3]              Locals: [this, 1, 2]
7: ireturn      (3을 반환)
```

**지역변수 0번은 `this`** (인스턴스 메서드의 경우). 그래서 `a`는 1번, `b`는 2번 슬롯이다. static 메서드였다면 `a`가 0번부터 시작했을 것이다.

---

### 바이트코드 명령어 카테고리

약 200여 개의 바이트코드가 있지만, 기능별로 분류하면 다음과 같다.

#### 로드/스토어 (Load/Store)

지역변수 ↔ 피연산자 스택 이동.

|명령어|의미|
|---|---|
|`iload`, `iload_0`, `iload_1`|int 지역변수를 스택에 push|
|`istore`, `istore_0`, `istore_1`|스택 top을 int 지역변수에 저장|
|`aload`, `astore`|참조 타입용|
|`lload`, `lstore`|long 타입용|

`iload_1`처럼 뒤에 숫자가 붙은 것은 **특정 슬롯 전용 최적화 명령어**. 일반 `iload 1`보다 짧은 1바이트로 표현된다.

#### 상수 로드

상수값을 스택에 push.

|명령어|의미|
|---|---|
|`iconst_0` ~ `iconst_5`|int 상수 0~5|
|`iconst_m1`|-1|
|`bipush`|-128 ~ 127 범위 int|
|`sipush`|-32768 ~ 32767 범위 int|
|`ldc`|상수 풀에서 값 로드 (큰 수, 문자열 등)|

#### 산술/논리 연산

|명령어|의미|
|---|---|
|`iadd`, `isub`, `imul`, `idiv`|int 사칙연산|
|`ladd`, `ladd` 등|long 연산|
|`iand`, `ior`, `ixor`|비트 연산|
|`ishl`, `ishr`|시프트 연산|

#### 타입 변환

|명령어|의미|
|---|---|
|`i2l`|int → long|
|`i2d`|int → double|
|`d2i`|double → int|
|`checkcast`|참조 타입 캐스팅 검증|

#### 제어 흐름

|명령어|의미|
|---|---|
|`goto`|무조건 점프|
|`if_icmpeq`, `if_icmpne`|두 int 비교 후 점프|
|`ifnull`, `ifnonnull`|null 체크 점프|
|`ireturn`, `areturn`, `return`|메서드 반환|

#### 객체 관련

|명령어|의미|
|---|---|
|`new`|객체 생성 (Heap 할당만, 생성자 호출 안 함)|
|`getfield`, `putfield`|인스턴스 필드 읽기/쓰기|
|`getstatic`, `putstatic`|static 필드 읽기/쓰기|
|`invokevirtual` 등|메서드 호출 (다음 장)|

---

### 5가지 메서드 호출 명령어

메서드 호출은 바이트코드에서 가장 중요한 주제 중 하나다. 호출하는 대상에 따라 **5가지 서로 다른 명령어**가 존재한다.

#### ① invokestatic

**static 메서드 호출.** 가장 단순하고 빠르다.

```java
Math.max(a, b);   // → invokestatic
```

- this가 없어 스택에 파라미터만 push
- 다형성(polymorphism) 고려 불필요 → 컴파일 타임에 호출 대상 확정

#### ② invokespecial

**생성자, private 메서드, super 호출.** 오버라이딩되지 않아야 하는 호출.

```java
new User();              // 내부적으로 invokespecial로 <init> 호출
super.toString();        // → invokespecial
this.privateMethod();    // → invokespecial
```

- 호출 대상이 컴파일 타임에 확정됨
- 동적 디스패치(dynamic dispatch) 안 함

#### ③ invokevirtual

**일반 인스턴스 메서드 호출.** 가장 흔하게 쓰이는 명령어.

```java
user.getName();   // → invokevirtual
```

- **실제 객체의 타입에 따라 호출 대상이 결정됨** (다형성)
- 런타임에 객체의 Klass Pointer를 따라가 메서드 테이블에서 찾음

```java
Animal a = new Dog();
a.sound();   // invokevirtual → Dog.sound() 실행 (a의 선언 타입은 Animal이지만)
```

#### ④ invokeinterface

**인터페이스 메서드 호출.**

```java
List<String> list = new ArrayList<>();
list.add("a");   // → invokeinterface
```

- invokevirtual과 비슷하지만 **인터페이스용 메서드 테이블 조회가 더 복잡**
- 클래스 계층 구조가 다양해 해시 기반 탐색을 사용
- 전통적으로 invokevirtual보다 약간 느리지만, JIT이 최적화해 거의 차이 없음

#### ⑤ invokedynamic

**Java 7에 도입된 동적 호출.** 람다, 메서드 참조, 문자열 concat 등에 사용.

```java
Runnable r = () -> System.out.println("hi");   // → invokedynamic
```

- 호출 대상을 **런타임에 결정**
- 한 번 결정되면 이후 호출은 캐시된 경로로 빠르게 실행
- 뒤에서 자세히 다룸

#### 비교 정리

|명령어|대상|호출 대상 결정 시점|다형성|
|---|---|---|---|
|invokestatic|static 메서드|컴파일 타임|✗|
|invokespecial|생성자, private, super|컴파일 타임|✗|
|invokevirtual|인스턴스 메서드|런타임|✓|
|invokeinterface|인터페이스 메서드|런타임|✓|
|invokedynamic|동적 호출|런타임 (첫 호출 시 확정)|✓|

---

### 람다와 invokedynamic

Java 8에서 추가된 람다는 내부적으로 **invokedynamic**으로 구현된다. 왜 이런 선택을 했을까?

#### 익명 클래스 방식의 문제

Java 7까지 비슷한 일을 하려면 익명 클래스를 썼다.

```java
// 익명 클래스 → 컴파일 시 별도 .class 파일 생성
Runnable r = new Runnable() {
    public void run() { System.out.println("hi"); }
};
```

문제점:

- **컴파일 타임에 .class 파일이 생성됨** (`Outer$1.class` 같은 이름)
- 클래스 로딩 오버헤드
- 사용 안 해도 클래스 파일이 메모리를 차지

#### invokedynamic 방식

```java
Runnable r = () -> System.out.println("hi");
```

컴파일 결과 바이트코드:

```
0: invokedynamic #2,  0  // InvokeDynamic #0:run:()Ljava/lang/Runnable;
```

- **컴파일 타임에는 클래스가 만들어지지 않음**
- 첫 호출 시 `LambdaMetafactory`가 런타임에 클래스를 **동적으로 생성**
- 이후 호출은 캐시된 경로 사용

#### Bootstrap Method

invokedynamic은 **부트스트랩 메서드(Bootstrap Method)**라는 개념을 사용한다.

```
[첫 호출]
  invokedynamic 실행
  → Bootstrap Method 호출 (LambdaMetafactory.metafactory)
  → 람다 클래스를 런타임 생성
  → 호출 대상으로 바인딩 (CallSite 반환)

[이후 호출]
  캐시된 CallSite로 바로 호출
```

이 구조 덕분에 람다는:

- 컴파일 결과물 크기 감소
- 런타임에 구현 최적화 가능 (JVM 버전에 따라 다른 전략 선택)
- 클래스 로딩 지연

#### 어디에 또 쓰이나

invokedynamic은 람다 외에도 여러 곳에서 쓰인다.

- **문자열 concat** (Java 9+): `"a" + b` → invokedynamic (`StringConcatFactory`)
- **메서드 참조**: `User::getName` → invokedynamic
- **switch expression pattern matching** (Java 21+): invokedynamic 기반
- **Record의 equals/hashCode/toString**: invokedynamic으로 자동 구현

---

### 실전 예제 — if문 컴파일 결과

```java
public int abs(int x) {
    if (x < 0) {
        return -x;
    }
    return x;
}
```

바이트코드:

```
0: iload_1         // x를 스택에 push
1: ifge 7          // x >= 0이면 7번 라인으로 점프
4: iload_1         // x를 스택에 push
5: ineg            // 스택 top을 음수화
6: ireturn         // 반환
7: iload_1         // x를 스택에 push
8: ireturn         // 반환
```

`if (x < 0)`을 컴파일러는 **반대 조건 `x >= 0`으로 뒤집어** `ifge`로 구현했다. 왜?

- 조건이 참이면 if 블록을 건너뛰어야 함
- "if 블록을 건너뛰는 점프"를 구현하기 위해 조건을 뒤집음
- 이렇게 하면 한 번의 비교로 분기 가능

이런 작은 최적화도 바이트코드 레벨에서 일어난다.

---

### javap 사용법

바이트코드를 직접 볼 때 쓰는 도구가 `javap`이다.

```bash
# 기본 (public 메서드 시그니처만)
javap Simple

# 바이트코드 포함
javap -c Simple

# 상세 정보 (스택 맵, 로컬 변수 테이블 등)
javap -v Simple

# private 멤버 포함
javap -p -c Simple
```

`-v` 옵션은 특히 유용하다. 상수 풀, 스택 맵, 지역변수 테이블까지 전부 보여준다.

---

### 정리

- JVM은 **스택 기반 가상머신**이다. 피연산자 스택으로 모든 연산을 처리
- 바이트코드는 **Load/Store, 연산, 제어 흐름, 객체 관련** 명령어로 구성
- 메서드 호출은 5가지 명령어로 나뉨: **invokestatic / invokespecial / invokevirtual / invokeinterface / invokedynamic**
- **invokedynamic**은 람다, 문자열 concat, 메서드 참조 등에서 런타임 동적 바인딩에 사용
- `javap -c`로 누구나 바이트코드를 직접 읽을 수 있다