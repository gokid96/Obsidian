## JVM 완벽 정복 [3편] — 객체의 내부 구조

### `new Object()` 하면 메모리에 무엇이 쓰이나

2편에서 "객체는 Heap에 저장된다"고 했다. 그럼 구체적으로 **어떤 모양**으로 저장될까?

java

```java
Object o = new Object();
```

이 한 줄이 Heap에 만드는 바이트 구조를 들여다보면, JVM이 객체를 다루는 방식과 `synchronized`, `hashCode()`, GC의 동작 원리까지 연결된다.

---

### 객체의 3가지 구성 요소

Heap에 놓인 자바 객체는 크게 세 부분으로 구성된다.

```
┌─────────────────────────────────┐
│      Header (객체 헤더)          │  ← 12 ~ 16 bytes
│  ┌───────────────────────────┐  │
│  │   Mark Word               │  │
│  ├───────────────────────────┤  │
│  │   Klass Pointer           │  │
│  └───────────────────────────┘  │
├─────────────────────────────────┤
│   Instance Data (필드 값)        │  ← 필드별 크기
├─────────────────────────────────┤
│   Padding (정렬용 패딩)          │  ← 0 ~ 7 bytes
└─────────────────────────────────┘
```

- **Header**: 객체의 메타정보 (GC, 락, 해시코드 등)
- **Instance Data**: 실제 필드 값
- **Padding**: 객체 크기를 **8바이트 단위로 맞추기 위한** 여분

---

### 객체 헤더 — Mark Word와 Klass Pointer

#### Mark Word

객체의 상태를 나타내는 **가변적 메타데이터**. 64비트 JVM에서는 8바이트를 차지한다.

Mark Word의 내용은 **객체의 현재 상태에 따라 달라진다**. 같은 8바이트가 상황마다 다르게 해석되는 구조다.

|상태|Mark Word 내용|
|---|---|
|**일반 객체**|해시코드, GC age, 락 플래그|
|**Lightweight Lock**|락 레코드 포인터|
|**Heavyweight Lock**|모니터 객체 포인터|
|**GC 마킹 중**|마킹 정보|

이 8바이트 안에 여러 정보가 비트 단위로 압축되어 있다. 예를 들어 일반 객체 상태의 64비트 구성은 대략:

```
[unused:25] [hashCode:31] [unused:1] [age:4] [biased_lock:1] [lock:2]
```

- **hashCode**: `Object.hashCode()` 호출 시 계산되어 저장
- **age**: GC에서 살아남은 횟수 (Young → Old 승격 판단용)
- **lock**: 현재 락 상태 (00: 가벼운 락, 10: 무거운 락, 01: 락 없음/편향)

##### Mark Word와 hashCode의 관계

java

```java
Object a = new Object();
int h1 = a.hashCode();  // 계산되어 Mark Word에 저장
int h2 = a.hashCode();  // 저장된 값 재사용
```

한 번 계산된 해시코드는 Mark Word에 저장되어 이후 호출 시 재사용된다. 그래서 같은 객체에서 `hashCode()`는 항상 같은 값을 반환한다.

단, **객체가 락에 관련되면** 해시코드 저장 공간이 락 정보로 덮어쓰기 된다. 이 경우 해시코드는 **별도 공간**으로 옮겨져 보관된다.

---

#### Klass Pointer

**객체가 어떤 클래스의 인스턴스인지 가리키는 포인터.** Metaspace에 있는 해당 클래스의 메타데이터 주소를 담는다.

java

```java
User u = new User();
// u가 가리키는 객체의 Klass Pointer → Metaspace의 User 클래스 정보
```

`u.getClass()`를 호출하면 이 Klass Pointer를 따라가 `Class` 객체를 반환한다.

64비트 JVM에서 기본 크기는 8바이트. 하지만 뒤에 나올 **Compressed OOPs**로 4바이트까지 줄어든다.

---

### Compressed OOPs — 메모리 절반 줄이기

#### OOP란 무엇인가

**OOP(Ordinary Object Pointer)**는 JVM이 객체를 참조할 때 쓰는 포인터다.

java

```java
User u = new User();
```

`u`는 Heap에 있는 User 객체의 주소를 담은 OOP다.

#### 64비트 환경의 문제

64비트 JVM에서 포인터는 기본 8바이트(64비트). 참조가 많은 자바 프로그램에서 이는 메모리 낭비가 크다.

예를 들어 필드 10개 중 5개가 참조 타입인 객체 1,000만 개가 있다면:

- 64비트 포인터: `5 × 8 × 10,000,000 = 400MB`
- 32비트 포인터: `5 × 4 × 10,000,000 = 200MB`

참조만 줄여도 수백 MB가 절약된다.

#### 압축의 원리

Compressed OOPs는 **8바이트 포인터를 4바이트로 압축**한다. 원리는 간단하다.

자바 객체는 항상 **8바이트 정렬**된다. 즉 객체 주소는 언제나 8의 배수다. 이진수로 보면 **하위 3비트는 항상 0**이다.

```
실제 주소:  0x00000001_00001000  (64비트)
             ↓  8로 나눔 (오른쪽 3비트 제거)
압축 주소:  0x20000200            (32비트로 표현 가능)
```

- 저장 시: 실제 주소 ÷ 8 → 4바이트
- 사용 시: 저장값 × 8 → 원래 주소 복원

#### 제한과 활성화

이 압축으로 표현 가능한 주소 범위는 `2^32 × 8 = 32GB`.

- **힙이 32GB 이하**: Compressed OOPs 자동 활성화 (Java 7+ 기본)
- **힙이 32GB 초과**: 자동 비활성화, 포인터가 8바이트로 돌아감

힙을 31GB로 설정한 서버가 32GB로 설정한 서버보다 **오히려 더 많은 데이터를 담는** 역설적 현상이 이 때문에 발생한다. 실무에서 힙 크기를 정할 때 32GB 경계가 중요한 이유다.

---

### 객체 크기 계산 예시

java

```java
public class User {
    int age;          // 4 bytes
    boolean active;   // 1 byte
    String name;      // 참조 (4 bytes, compressed)
}
```

64비트 JVM + Compressed OOPs 환경에서 `new User()` 객체의 크기:

```
┌─────────────────────────────────┐
│  Mark Word           : 8 bytes   │
│  Klass Pointer       : 4 bytes   │  ← 압축됨
├─────────────────────────────────┤
│  int age             : 4 bytes   │
│  boolean active      : 1 byte    │
│  String name (ref)   : 4 bytes   │  ← 압축됨
├─────────────────────────────────┤
│  Padding             : 3 bytes   │  ← 8의 배수 맞추기
└─────────────────────────────────┘
  총 24 bytes
```

필드 합계는 `4 + 1 + 4 = 9 bytes`이지만, 헤더와 패딩까지 합치면 **24 bytes**다. 객체 하나당 오버헤드가 최소 12바이트(헤더)는 고정적으로 든다.

리스트에 수백만 개 객체를 담는다면, **작은 객체일수록 헤더의 비중이 커진다.** `int` 하나 담은 `Integer` 객체가 `int` 원시값보다 몇 배 무거운 이유다.

---

### 필드 재배치 (Field Reordering)

JVM은 **메모리 낭비를 줄이기 위해 필드를 재정렬한다.**

소스 코드에 선언한 순서:

java

```java
public class Bad {
    byte a;       // 1 byte
    long b;       // 8 bytes
    byte c;       // 1 byte
    long d;       // 8 bytes
}
```

만약 선언된 순서대로 배치하면 정렬 때문에 패딩이 많이 낀다:

```
[a(1)] [padding(7)] [b(8)] [c(1)] [padding(7)] [d(8)] = 32 bytes
```

JVM은 **큰 타입부터 배치**하도록 재정렬한다:

```
[b(8)] [d(8)] [a(1)] [c(1)] [padding(6)] = 24 bytes
```

8바이트 절약. 이 최적화는 개발자가 신경 쓰지 않아도 JVM이 알아서 해준다.

재정렬 규칙(대략):

1. long, double (8 bytes)
2. int, float (4 bytes)
3. short, char (2 bytes)
4. byte, boolean (1 byte)
5. 참조 타입
6. 부모 클래스 필드 → 자식 클래스 필드 순서

---

### synchronized와 객체 헤더

`synchronized` 키워드는 **객체 헤더의 Mark Word를 활용**해 락을 구현한다.

java

```java
synchronized (obj) {
    // ...
}
```

위 코드가 실행될 때 JVM은 `obj`의 Mark Word를 검사하고 갱신한다. 경쟁 상황에 따라 락은 3단계로 승격된다.

```
Biased Lock  ──경쟁 발생──►  Lightweight Lock  ──경쟁 심화──►  Heavyweight Lock
 (편향 락)                    (가벼운 락)                       (무거운 락)
```

#### ① Biased Lock (편향 락)

"이 객체는 한 스레드만 쓸 것 같다"고 가정. Mark Word에 **스레드 ID만 기록**하고 다른 동기화 작업은 하지 않는다. 같은 스레드가 다시 들어오면 거의 공짜다.

> Java 15부터 기본적으로 비활성화되었다. 현대 애플리케이션에서는 실효성이 낮다고 판단됨.

#### ② Lightweight Lock (가벼운 락)

다른 스레드가 경쟁하기 시작하면 승격. **CAS(Compare-And-Swap)** 연산으로 Mark Word를 원자적으로 갱신한다. OS 수준의 락은 아직 걸지 않는다.

#### ③ Heavyweight Lock (무거운 락)

경쟁이 심해지면 **OS의 뮤텍스(Mutex)**를 사용하는 진짜 락으로 승격. Mark Word는 **모니터 객체(ObjectMonitor)의 주소**를 가리킨다. 락을 기다리는 스레드는 블로킹된다.

이 모든 상태 전이가 Mark Word의 락 플래그 비트로 표현된다. **객체 헤더를 이해하면 `synchronized`의 성능 특성이 보인다.**

---

### GC와 객체 헤더

GC도 객체 헤더를 사용한다.

- **Mark 단계**: 살아있다고 판정된 객체의 Mark Word에 "표시" 비트를 세움
- **Age 추적**: Young → Old 승격을 위해 Mark Word의 age 필드 증가 (4비트, 최대 15)
- **Forwarding Pointer**: 객체를 다른 영역으로 복사할 때 Mark Word에 새 주소 저장

객체 헤더는 GC와 락이 공유하는 자원이다. 그래서 상태마다 같은 8바이트를 다르게 해석하는 구조가 필요하다.

---

### JOL로 직접 확인하기

**JOL(Java Object Layout)** 라이브러리로 실제 객체의 메모리 레이아웃을 확인할 수 있다.

xml

```xml
<!-- Maven -->
<dependency>
    <groupId>org.openjdk.jol</groupId>
    <artifactId>jol-core</artifactId>
    <version>0.17</version>
</dependency>
```

java

```java
import org.openjdk.jol.info.ClassLayout;

public class Layout {
    public static void main(String[] args) {
        User user = new User();
        System.out.println(ClassLayout.parseInstance(user).toPrintable());
    }
}
```

실행 결과 예시:

```
User object internals:
OFF  SZ   TYPE DESCRIPTION               VALUE
  0   8        (object header: mark)     0x0000000000000001
  8   4        (object header: class)    0x01001a40
 12   4    int User.age                  0
 16   1    boolean User.active           false
 17   3        (alignment/padding gap)
 20   4    String User.name              null
Instance size: 24 bytes
```

직접 바이트 배치를 눈으로 확인할 수 있다.

---

### 정리

- 자바 객체 = **Header + Instance Data + Padding**, 항상 8바이트 단위로 정렬
- **Mark Word**: GC age, 해시코드, 락 상태 등이 비트 단위로 압축 저장
- **Klass Pointer**: Metaspace의 클래스 정보를 가리킴
- **Compressed OOPs**: 힙 32GB 이하에서 참조를 4바이트로 압축, 메모리 크게 절약
- **필드 재배치**: JVM이 타입 크기 순으로 자동 재정렬해 패딩 최소화
- **`synchronized`**: Mark Word를 활용한 Biased → Lightweight → Heavyweight 3단계 락