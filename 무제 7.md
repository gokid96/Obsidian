### 실무에서 OOM/Full GC를 어떻게 잡나

여기까지 JVM의 내부를 들여다봤다. 이제 **실제 장애를 만났을 때** 이 지식을 어떻게 활용하는지 본다.

실무에서 JVM 관련 장애는 대략 다음 패턴이다.

- 서비스가 주기적으로 멈춘다 (Full GC)
- 시간이 지나면 OOM이 터진다 (메모리 누수)
- CPU는 100%인데 처리량이 안 나온다 (락 경합)
- 시작은 잘 되는데 시간이 지나며 느려진다
- 데드락으로 특정 스레드가 멈춰있다

각 증상의 원인 추적 방법과 해법을 살펴본다.

---

### 필수 도구 모음

#### 1. 정보 수집 도구

**jps** — 실행 중인 JVM 프로세스 목록

```bash
jps -l
# 12345 com.example.MyApp
# 12346 org.gradle.launcher.daemon.bootstrap.GradleDaemon
```

**jstat** — 실시간 GC/메모리 통계

bash

```bash
jstat -gc 12345 1000   # PID 12345, 1초 간격
# S0C    S1C    S0U    S1U      EC       EU        OC         OU       MC     MU    CCSC   CCSU
# 8192.0 8192.0  0.0   8192.0   65536.0  52428.8   131072.0   98304.0  ...
```

**jinfo** — JVM 설정 확인

```bash
jinfo -flags 12345
```

#### 2. 덤프 도구

**jmap** — 힙 덤프 생성
```bash
jmap -dump:live,format=b,file=heap.hprof 12345
```

**jstack** — 스레드 덤프 생성

```bash
jstack 12345 > thread_dump.txt
```

#### 3. 통합 도구

**jcmd** — 다양한 JVM 명령의 통합 인터페이스 (현대적)

```bash
jcmd 12345 GC.heap_dump heap.hprof
jcmd 12345 Thread.print
jcmd 12345 VM.flags
jcmd 12345 GC.class_histogram
```

#### 4. GUI 도구

- **VisualVM**: 무료, 가볍게 쓰기 좋음
- **Eclipse MAT (Memory Analyzer Tool)**: 힙 덤프 분석 최강
- **JProfiler / YourKit**: 상용, 기능 풍부
- **Arthas**: Alibaba 제작, 라이브 디버깅에 강함

---

### OOM의 종류별 대응

`OutOfMemoryError`는 하나가 아니다. **메시지를 정확히 읽는 것**이 첫 단계다.

#### ① `OutOfMemoryError: Java heap space`

**가장 흔한 OOM.** 힙이 꽉 참.

##### 진단 순서

**1단계: 힙 덤프 자동 생성 설정**

운영 환경에는 이 플래그를 반드시 걸어놓는다.
```bash
-XX:+HeapDumpOnOutOfMemoryError
-XX:HeapDumpPath=/var/log/heapdump/
```

OOM 발생 시 자동으로 `.hprof` 파일 생성.

**2단계: MAT로 분석**

Eclipse MAT으로 `.hprof` 열기 → `Leak Suspects` 리포트 실행.

MAT가 보여주는 것:

- **Dominator Tree**: 어떤 객체가 가장 많은 메모리를 독점하고 있는지
- **Histogram**: 클래스별 인스턴스 수와 크기
- **GC Roots 경로**: 의심 객체가 왜 회수 안 되는지

**3단계: 전형적 패턴 확인**

```
흔한 원인들:
- 정적 컬렉션 무한 증가 (static List에 계속 add)
- 캐시 크기 제한 없음
- ThreadLocal 정리 안 함
- ClassLoader 누수 (Tomcat 재배포 등)
- DB 결과셋을 통째로 메모리에 로드
- 이미지/파일 처리 시 스트리밍 안 함
```

##### 해결 사례: Static List 누수
```java
public class RequestLogger {
    private static List<Request> history = new ArrayList<>();
    
    public static void log(Request r) {
        history.add(r);   // 영원히 쌓임 → OOM
    }
}
```

해결:
```java
// 크기 제한 있는 구조
private static final Queue<Request> history = 
    new ArrayBlockingQueue<>(10000);

public static void log(Request r) {
    if (!history.offer(r)) {
        history.poll();      // 오래된 것 제거
        history.offer(r);
    }
}
```

##### 해결 사례: 캐시 누수
```java
// 문제
private static Map<String, User> cache = new HashMap<>();
```

```java
// 해결: 크기 제한 + 만료 정책
private static final Cache<String, User> cache = Caffeine.newBuilder()
    .maximumSize(10000)
    .expireAfterWrite(Duration.ofMinutes(10))
    .build();
```

---

#### ② `OutOfMemoryError: Metaspace`

**클래스 메타데이터 영역 고갈.** 2편에서 본 Metaspace가 차는 경우.

##### 원인

- 동적 클래스 생성 과다 (프록시, 스크립트 엔진)
- ClassLoader 누수 (WAS 재배포 시 자주 발생)
- Groovy, Nashorn 같은 동적 언어 과다 사용

##### 진단

Metaspace 사용량 추이 관찰.
```bash
jstat -gcmetacapacity 12345 1000
```

계속 증가만 하면 클래스 누수.

##### 해결

크기 제한 설정:

```bash
-XX:MaxMetaspaceSize=512m
```

무제한 대신 명시적 제한을 두면 문제가 빨리 드러난다.

클래스 누수는 코드 수정이 필요:

```java
// 동적 프록시를 캐시
private static final Map<Class<?>, Object> proxyCache = new ConcurrentHashMap<>();

// 매번 새로 만들지 말고 재사용
```

---

#### ③ `OutOfMemoryError: unable to create new native thread`

**스레드를 너무 많이 생성.** 메모리가 아닌 스레드 수 한계.

##### 원인

- 스레드 풀 없이 매번 `new Thread()`
- 스레드 풀 크기 제한 없음
- OS의 스레드 수 제한

##### 진단

스레드 개수 확인:
```bash
jstack 12345 | grep "prio=" | wc -l
```

리눅스에서 OS 제한 확인:
```bash
cat /proc/sys/kernel/threads-max
ulimit -u
```

##### 해결

스레드 풀 사용:
```java
// 나쁜 예
new Thread(task).start();   // 매 요청마다

// 좋은 예
ExecutorService pool = Executors.newFixedThreadPool(50);
pool.submit(task);
```

스택 크기 조정:
```bash
-Xss256k   # 기본 1MB를 줄여 더 많은 스레드 가능
```

---

#### ④ `OutOfMemoryError: GC overhead limit exceeded`

**GC가 너무 자주 돌아 실질적 일을 못 하는 상태.** JVM이 "98% 시간을 GC에 쓰는데 2%도 회수 못 한다"고 판단하면 발생.

GC가 필사적으로 돌지만 공간을 못 만들어내는 상황. 힙 부족이지만 힙만 늘리면 임시방편이다. **메모리 누수 조사가 본질적 해결.**

---

#### ⑤ `OutOfMemoryError: Direct buffer memory`

**NIO 다이렉트 버퍼 과다 할당.** 힙이 아닌 네이티브 메모리.

Netty, NIO 기반 애플리케이션에서 주로 발생.
```bash
-XX:MaxDirectMemorySize=1g
```

크기 제한 + 버퍼 재사용 패턴 적용.

---

### Full GC 추적과 해결

#### GC 로그 설정

운영 환경에는 항상 걸어놓는다.
```bash
-Xlog:gc*,safepoint:file=gc.log:time,level,tags:filecount=10,filesize=10M
```

#### 건강한 GC vs 문제 있는 GC

**건강한 패턴**
```
Minor GC: 5-30ms, 수 초마다
Major/Mixed GC: 50-200ms, 수 분마다
Full GC: 거의 안 일어남
GC 후 Old 사용량: 일정 범위 유지
```

**문제 있는 패턴**
```
Full GC: 수 초, 자주 발생
Old 사용량: 계속 증가 (톱니 아닌 우상향)
GC 시간 비율: 10% 이상
```

#### GC 시간 비율 확인
```bash
jstat -gcutil 12345 1000
# S0    S1    E     O     M    CCS   YGC  YGCT  FGC  FGCT   GCT
# 0.00  85.2  45.3  72.1  95.3 92.1  120  2.453  3   0.987  3.440
```

- `GCT`: 전체 GC 시간
- 애플리케이션 실행 시간 대비 5% 이상이면 경고, 10% 넘으면 심각

#### Full GC 원인별 대응

##### 원인 1: Old Gen 누수

가장 흔함. 힙 덤프 분석으로 원인 규명. OOM 섹션의 해결책 적용.

##### 원인 2: 큰 객체 직접 Old 할당

Young에 들어갈 수 없는 큰 객체(수 MB 이상)가 바로 Old로 가면서 Old가 빨리 참.

**해결:**

- 큰 객체 생성 자체를 줄이기
- 배치 크기 조정 (한 번에 10만 건 대신 1000건씩)
- Young 크기 확대

##### 원인 3: Premature Promotion

Survivor가 작아 age 낮은 객체가 Old로 강제 승격.
```bash
# Young 확대
-Xmn4g

# Survivor 비율 조정 (기본 8:1:1)
-XX:SurvivorRatio=6   # Eden 6, Survivor 각각 1
```

##### 원인 4: 부적절한 GC 선택

배치 처리를 Parallel GC 아닌 G1으로 돌리는 경우처럼, 워크로드에 맞지 않는 GC 선택.

7편의 GC 선택 가이드 참고.

---

### CPU 이슈 진단

#### 증상: CPU 100%인데 처리량 안 나옴

세 가지 가능성.

1. GC가 CPU를 다 씀
2. 특정 스레드가 무한 루프
3. 락 경합으로 스레드들이 대기만 함

#### 진단 순서

**1단계: GC 먼저 확인**
```bash
jstat -gc 12345 1000
```

GC 빈도가 비정상이면 GC 이슈. 아니면 다음 단계.

**2단계: CPU 많이 쓰는 스레드 찾기**

리눅스:
```bash
# 프로세스 내 스레드별 CPU 확인
top -H -p 12345

# CPU 많이 쓰는 스레드 TID 확인 (예: 23456)
```

**3단계: 스레드 TID를 16진수로 변환**
```bash
printf "%x\n" 23456
# 5ba0
```

**4단계: 스레드 덤프에서 찾기**
```bash
jstack 12345 > thread.txt
grep -A 30 "nid=0x5ba0" thread.txt
```

해당 스레드가 어느 메서드에서 CPU를 쓰고 있는지 바로 확인 가능.

---

### 데드락 찾기

#### 전형적인 데드락
```java
public void method1() {
    synchronized(lockA) {
        synchronized(lockB) {   // B 대기
            // ...
        }
    }
}

public void method2() {
    synchronized(lockB) {
        synchronized(lockA) {   // A 대기
            // ...
        }
    }
}
```

Thread A가 lockA를 잡고 lockB 기다림, Thread B가 lockB 잡고 lockA 기다림. 영원히 멈춤.

#### 진단
```bash
jstack 12345
```

출력에 `Found X deadlocks` 섹션이 자동으로 나온다.

```
Found one Java-level deadlock:
=============================
"Thread-1":
  waiting to lock monitor 0x... (object 0x..., a java.lang.Object),
  which is held by "Thread-0"
"Thread-0":
  waiting to lock monitor 0x... (object 0x..., a java.lang.Object),
  which is held by "Thread-1"
```

어느 스레드가 어느 락을 잡고 뭘 기다리는지 명확히 나온다.

#### 해결 원칙

**1. 락 획득 순서 통일**

모든 코드가 항상 동일한 순서로 락을 잡으면 데드락 불가능.
```java
// 모든 곳에서 A → B 순서로만 획득
synchronized(lockA) {
    synchronized(lockB) { ... }
}
```

**2. tryLock 사용**
```java
if (lockA.tryLock(1, SECONDS)) {
    try {
        if (lockB.tryLock(1, SECONDS)) {
            try { /* ... */ }
            finally { lockB.unlock(); }
        }
    } finally { lockA.unlock(); }
}
```

일정 시간 후 포기 → 데드락 회피.

**3. 락 최소화**

락이 중첩되는 코드 자체를 줄이는 게 근본적 해결.

---

### 실전 JVM 플래그 레퍼런스

#### 메모리 설정
```bash
-Xms4g                           # 초기 힙
-Xmx4g                           # 최대 힙 (Xms와 같게 권장)
-Xmn1g                           # Young Gen 크기
-Xss256k                         # 스레드 스택 크기
-XX:MaxMetaspaceSize=512m        # Metaspace 최대
-XX:MaxDirectMemorySize=1g       # 다이렉트 메모리 최대
```

#### GC 설정
```bash
# G1 (기본 추천)
-XX:+UseG1GC
-XX:MaxGCPauseMillis=200
-XX:G1HeapRegionSize=16m

# ZGC (응답성 중시)
-XX:+UseZGC
-XX:+ZGenerational     # Java 21+

# Parallel (처리량 중시, 배치 작업)
-XX:+UseParallelGC
```

#### 진단/로깅
```bash
# GC 로그
-Xlog:gc*:file=gc.log:time,tags:filecount=10,filesize=10M

# OOM 자동 힙 덤프
-XX:+HeapDumpOnOutOfMemoryError
-XX:HeapDumpPath=/var/log/dumps/

# OOM 시 스크립트 실행 (알림 등)
-XX:OnOutOfMemoryError="/scripts/notify.sh %p"

# 크래시 로그 위치
-XX:ErrorFile=/var/log/hs_err_%p.log
```

#### 컨테이너 환경

Java 10+ 는 컨테이너의 메모리/CPU 제한을 자동 인식한다. 하지만 명시가 더 안전.
```bash
-XX:MaxRAMPercentage=75.0    # 컨테이너 메모리의 75%를 힙으로
-XX:ActiveProcessorCount=4   # CPU 수 강제 지정
```

---

### 실제 장애 사례 스터디

#### 사례 1: 배포 후 점진적 응답 지연

**증상**: 배포 직후는 정상. 6시간 뒤부터 응답 시간 지연. 24시간 뒤 OOM.

**진단**:

1. GC 로그 확인 → Minor GC는 정상, Full GC가 점점 자주
2. 힙 덤프 → `HashMap$Node` 수천만 개
3. MAT Dominator Tree → `SessionCache` 클래스가 힙 60% 점유

**원인**: 세션 캐시에 만료 정책 없음. 세션은 계속 생성되기만 함.

**해결**: Caffeine Cache로 교체하며 TTL 30분 설정.

---

#### 사례 2: 주기적인 1초 스파이크

**증상**: 평상시 응답 50ms. 5분마다 1초 스파이크.

**진단**:

1. GC 로그 → 5분 간격으로 Full GC 발생
2. Old Gen이 천천히 증가 → 한계 도달 → Full GC
3. 힙 덤프 → 대용량 임시 객체 (보고서 PDF 생성)

**원인**: PDF 생성 배치가 5분마다 실행. 한 번에 전체 데이터를 메모리에 로드.

**해결**:

- 배치를 스트리밍 방식으로 변경
- PDF 생성 전용 JVM 분리 (원래 서비스에 영향 없도록)

---

#### 사례 3: CPU 100%, 처리량 제로

**증상**: 서비스가 응답 안 함. CPU는 100%.

**진단**:

1. `top -H` → 특정 스레드 하나가 CPU 99%
2. `jstack` → 그 스레드가 `HashMap.get` 무한 루프
3. 코드 확인 → 여러 스레드가 동시에 non-thread-safe `HashMap`에 put

**원인**: `HashMap`을 동시 수정해 내부 연결 리스트가 순환 구조로 망가짐. Java 7까지 유명한 이슈.

**해결**: `ConcurrentHashMap`으로 교체.

---

#### 사례 4: 시작 후 5분간 느림

**증상**: 서비스 재시작 후 5분간 응답 지연. 이후 정상.

**진단**: JIT 워밍업 이슈. 5편에서 본 워밍업 기간이 원인.

**해결**:

1. **CDS/AppCDS 활성화** - 클래스 로딩 시간 단축
2. **워밍업 스크립트** 추가 - 배포 후 헬스체크 전에 인위적 트래픽
3. **장기 운영 해법**: GraalVM Native Image 검토 (콜드 스타트가 중요하면)

---

#### 사례 5: 간헐적인 데드락

**증상**: 하루 1-2회 특정 API가 멈춤. 재시작하면 복구.

**진단**: 멈췄을 때 `jstack` → 데드락 명시적 발견. 두 서비스 클래스가 서로의 락을 다른 순서로 획득.

**해결**:

- 락 획득 순서 표준화
- 해당 부분은 애초에 락이 과도함 → `ConcurrentHashMap`과 불변 객체로 재설계

---

### 프로덕션 JVM 필수 설정 템플릿

실무에서 쓸 만한 기본 설정.

```bash
# === 메모리 ===
-Xms4g
-Xmx4g
-XX:MaxMetaspaceSize=512m

# === GC (G1) ===
-XX:+UseG1GC
-XX:MaxGCPauseMillis=200

# === 로깅 ===
-Xlog:gc*,safepoint:file=/var/log/gc.log:time,tags:filecount=10,filesize=10M

# === OOM 대응 ===
-XX:+HeapDumpOnOutOfMemoryError
-XX:HeapDumpPath=/var/log/heapdump/
-XX:+ExitOnOutOfMemoryError

# === 크래시 로그 ===
-XX:ErrorFile=/var/log/hs_err_%p.log

# === 컨테이너 ===
-XX:MaxRAMPercentage=75.0

# === 성능 최적화 ===
-XX:+AlwaysPreTouch          # 시작 시 메모리 미리 할당
-XX:+UseStringDeduplication  # 중복 문자열 제거
```

`AlwaysPreTouch`는 JVM 시작 시 힙 전체를 실제로 touch해서 나중에 페이지 폴트로 인한 지연을 방지. 시작은 약간 느려지지만 런타임이 더 예측 가능해진다.

---

### 성능 모니터링 체크리스트

운영 환경에서 주기적으로 확인할 지표.

#### 메모리

- [ ]  Heap 사용량 추이 (우상향이면 누수 의심)
- [ ]  Metaspace 사용량 추이
- [ ]  다이렉트 버퍼 메모리

#### GC

- [ ]  Minor GC 빈도와 시간
- [ ]  Major/Mixed GC 빈도와 시간
- [ ]  Full GC 발생 여부 (일어나면 안 됨)
- [ ]  GC 시간 비율 (< 5%)

#### 스레드

- [ ]  총 스레드 수
- [ ]  스레드 풀 활용률
- [ ]  블록된 스레드 수

#### 응답 시간

- [ ]  p50, p95, p99 latency
- [ ]  GC로 인한 pause 영향

#### 시스템

- [ ]  CPU 사용률
- [ ]  메모리 사용률 (컨테이너 vs JVM)
- [ ]  디스크 I/O

Prometheus + Grafana에 **Micrometer**로 JVM 메트릭을 노출하는 방식이 표준적이다.