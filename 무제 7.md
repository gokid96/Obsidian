## MVCC (Multi-Version Concurrency Control)

```
DB가 락 없이 격리성을 보장하는 방법

모든 트랜잭션에 락을 걸면?
→ 읽기도 쓰기도 전부 대기
→ 성능이 너무 떨어짐

해결책이 MVCC
→ 락 없이 격리성 보장
→ 읽기와 쓰기가 서로를 방해하지 않음
```

---

### 작동 원리

```
데이터를 수정할 때 기존 값을 덮어쓰지 않음
→ 이전 버전을 Undo 영역에 보관
→ 하나의 데이터에 여러 버전이 공존

예시:
트랜잭션 A: 잔액 0원 → 50만원으로 수정 (커밋 전)

Undo 영역: 0원 (이전 버전)
현재 데이터: 50만원 (수정 중)

트랜잭션 B가 잔액 조회
→ 트랜잭션 A가 시작된 이후의 변경이라서
→ Undo 영역의 0원을 읽음
→ 트랜잭션 A의 커밋을 기다리지 않아도 됨
```

### 가시성 판단

```
읽기 트랜잭션은 자신의 시작 시점을 기준으로
→ 그 이후에 커밋된 값은 무시
→ Undo 영역의 과거 데이터를 찾아 읽음

결과:
→ 읽기가 쓰기를 기다리지 않음
→ 쓰기가 읽기를 막지 않음
→ 동시성 비약적으로 향상
```

---

### MVCC가 없으면

```
트랜잭션 A: 데이터 수정 중 (락 보유)
트랜잭션 B: 읽기 시도 → 락 대기
트랜잭션 C: 읽기 시도 → 락 대기
트랜잭션 D: 읽기 시도 → 락 대기

→ 읽기 요청이 전부 줄 서서 대기
→ 서비스 응답 속도 급격히 저하
```

### MVCC가 있으면

```
트랜잭션 A: 데이터 수정 중
트랜잭션 B: Undo 영역에서 이전 버전 읽음 → 즉시 반환
트랜잭션 C: Undo 영역에서 이전 버전 읽음 → 즉시 반환
트랜잭션 D: Undo 영역에서 이전 버전 읽음 → 즉시 반환

→ 읽기가 쓰기를 전혀 기다리지 않음
→ 동시 처리량 극대화
```

---

### MySQL REPEATABLE READ에서 팬텀 리드 방어

```
ANSI 표준상 REPEATABLE READ는 팬텀 리드 발생
→ 범위 조회 사이에 새로운 행이 삽입되면 문제

MySQL(InnoDB)은 다름
→ Next-Key Lock 기술로 팬텀 리드 대부분 방어
→ 범위에 해당하는 행뿐 아니라 그 사이 공간까지 잠금

예시:
SELECT * FROM orders WHERE amount > 1000 FOR UPDATE
→ amount > 1000 범위의 행 + 그 사이 공간도 잠금
→ 다른 트랜잭션이 그 범위에 새 행 삽입 불가

→ MySQL에서는 REPEATABLE READ만 써도
→ 대부분의 팬텀 리드가 방어됨
```

---

### 격리 수준 + MVCC 정리

```
READ UNCOMMITTED → MVCC 미적용, 락도 없음
READ COMMITTED   → MVCC 적용, 커밋된 최신 버전 읽음
REPEATABLE READ  → MVCC 적용, 트랜잭션 시작 시점 버전 읽음
SERIALIZABLE     → MVCC 대신 공유 락으로 완전 직렬화
```