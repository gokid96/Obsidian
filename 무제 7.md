## 왜 대기열에 Kafka 대신 Redis를 쓰는가?

```
대규모 선착순 시스템을 설계할 때
"줄 세우기" 기능에 Kafka를 쓸까? Redis를 쓸까?

둘 다 FIFO 처리가 가능하지만
실제 운영에서는 Redis Sorted Set이 압도적인 선택을 받음
→ 이유를 하나씩 살펴보자
```

---

## 이유 1. 실시간 순번 조회 (UX)

```
대기열의 핵심은 단순히 순서를 지키는 게 아님
→ "현재 내 앞에 몇 명이 남았는지" 실시간으로 보여줘야 함
→ 이게 안 되면 유저 입장에서 그냥 멈춘 화면
```

### Kafka (터널 구조)

```
Kafka는 본질적으로 파이프/터널 구조
→ 데이터가 앞에서부터 순차적으로 소비됨
→ 큐 안에 있는 특정 유저가 몇 번째인지 즉시 조회 불가

Offset 개념은 있지만
→ "어디까지 읽었는가" 관리용
→ 특정 유저의 현재 위치를 찾는 기능이 아님

억지로 구현하려면:
→ 별도 DB나 저장소에 순번 정보를 따로 관리해야 함
→ 구현 복잡도가 크게 올라감
```

### Redis (랭킹 명단 구조)

```
Sorted Set은 실시간 랭킹 명단에 가까움
→ ZRANK 명령어로 특정 유저 순위 즉시 조회 (O(log n))
→ 새로고침, 재접속해도 서버에 줄이 그대로 남아있음
→ 추가 구현 없이 바로 순번 피드백 가능

ZRANK queue:event 유저B → 1 ("2번째 대기 중")

유저 화면:
"현재 3,247번째 대기 중입니다"
→ 5초마다 폴링 or WebSocket으로 실시간 업데이트
```

---

## 이유 2. 멱등성 보장 (중복 클릭 방지)

```
선착순 이벤트에서 가장 흔한 장애 원인
→ 사용자의 중복 클릭, 새로고침 연타
→ 순간적으로 트래픽이 수십 배로 뻥튀기됨
```

### Kafka

```
클릭하는 대로 메시지가 큐에 쌓임
→ 중복 클릭 시 동일 유저 요청이 여러 개 들어감
→ 컨슘 단계에서 별도 중복 제거 로직 필요
→ 이미 처리된 유저인지 DB 조회 필요
→ 구현 복잡도 + 추가 DB 부하
```

### Redis

```
Sorted Set의 멤버를 유저 ID로 설정
→ 같은 유저가 아무리 연타해도
→ 대기열에 단 하나의 데이터만 존재 (자동 멱등성)

ZADD queue:event 1710000001.123 유저A  → 추가됨
ZADD queue:event 1710000001.999 유저A  → 점수 업데이트만 됨 (중복 추가 안 됨)

→ 별도 중복 제거 로직 없이 자연스럽게 해결
→ 트래픽 안정적으로 관리
```

---

## 이유 3. 중간 이탈 처리

```
대기 중 유저가 지쳐서 취소하거나 브라우저를 닫으면
→ 해당 유저를 대기열에서 즉시 제거해야 함
→ 안 하면 이미 이탈한 유저 자리를 계속 유지
→ 뒤에 있는 유저들이 불필요하게 대기
```

### Kafka

```
순차적 처리에 최적화된 구조
→ 큐 중간의 특정 유저만 삭제하기 매우 어려움

억지로 구현하려면:
→ 별도 취소 목록(블랙리스트) 관리
→ 컨슘 단계에서 취소 목록 확인 후 필터링
→ 추가 저장소 + 추가 로직 필요
```

### Redis

```
ZREM 명령어 하나로 해결

ZREM queue:event 유저B
→ 대기열 어느 위치에 있든 즉시 삭제
→ 나머지 순번 자동으로 당겨짐
→ 별도 로직 없음

취소 버튼 클릭 → ZREM 한 줄 → 끝
```

---

## 이유 4. 재접속 시 상태 유지

```
유저가 대기 중 실수로 브라우저를 닫거나 앱을 껐다가 다시 켜면?

Kafka:
→ 큐 내부 상태를 유저에게 다시 보여줄 방법이 없음
→ 재접속 시 순번 복구가 복잡함

Redis:
→ 서버에 줄(Sorted Set)이 그대로 남아있음
→ 재접속 시 ZRANK로 즉시 현재 순번 조회
→ 유저 입장에서 끊김 없이 이어짐

→ 이탈률을 낮추는 핵심 UX 포인트
```

---

## Kafka vs Redis 비교

|비교 항목|Kafka|Redis Sorted Set|
|---|---|---|
|주 용도|비동기 처리 및 버퍼링|실시간 상태 관리 및 운영|
|순번 조회|불가능에 가까움 (터널 구조)|즉시 가능 (ZRANK, O(log n))|
|중복 방지|별도 로직 필요|내장된 멱등성 보장|
|중간 삭제|매우 복잡|ZREM 한 줄로 즉시 가능|
|재접속 복구|복잡|ZRANK로 즉시 복구|
|데이터 안전성|디스크 저장 (안전)|메모리 (유실 위험)|
|적합한 영역|후처리 뒷단|대기열 앞단|

---

## 실무 아키텍처 - Redis와 Kafka 같이 쓰기

```
둘은 대립 관계가 아니라 보완 관계
→ 각자 잘하는 영역에서 쓰는 게 최적
```

```
[유저 10만 명 동시 요청]
        ↓
[Redis Sorted Set - 대기열 앞단]
→ 줄 세우기 (ZADD)
→ 실시간 순번 표시 (ZRANK)
→ 중복 클릭 방지 (멱등성)
→ 중간 이탈 처리 (ZREM)
→ 재접속 상태 복구
        ↓
ZPOPMIN으로 N명씩 통과시킴
        ↓
[Kafka - 후처리 뒷단]
→ DB 저장 (주문, 결제)
→ 알림 발송 (문자, 앱푸시)
→ 로그 기록
→ 비동기 처리로 백엔드 부하 조절
→ 실패 시 재처리 (Offset 기반)
```

```
앞단 (줄 세우기)  → Redis  → UX + 운영 유연성
뒷단 (실제 처리)  → Kafka  → 안정성 + 재처리
```

---

## 실제 사례

```
기차표 예매 (코레일, SRT)
→ 정각에 수십만 명 동시 접속
→ Redis Sorted Set으로 줄 세우기
→ "n번째 대기 중" 실시간 표시
→ 통과된 요청은 Kafka로 후처리

콘서트 티켓 예매 (티켓링크, 인터파크)
→ 동일한 구조
→ Redis 앞단 + Kafka 뒷단 조합
→ 유저가 취소 시 ZREM으로 즉시 제거
→ 다음 순번 유저에게 기회 부여
```

---

## 핵심 Q&A

**Q. 대기열에 Kafka 대신 Redis를 쓰는 이유는?**

> Kafka는 순번 조회, 중복 방지, 중간 삭제, 재접속 복구가 모두 어렵고 별도 로직이 필요함 Redis Sorted Set은 ZRANK로 순번 즉시 조회, 유저 ID 멱등성으로 중복 방지, ZREM으로 중간 삭제까지 명령어 하나로 해결되어 대기열 앞단에 최적

**Q. 그럼 Kafka는 언제 쓰나?**

> 대기열을 통과한 유저의 실제 후처리에 사용 DB 저장, 알림 발송, 로그 기록 등 안정적인 비동기 처리가 필요한 뒷단에 적합 Redis는 메모리 기반이라 유실 위험이 있어서 중요한 데이터 처리는 Kafka가 담당

**Q. Redis Sorted Set의 멱등성이란?**

> 멤버를 유저 ID로 설정하면 같은 유저가 아무리 연타해도 대기열에 단 하나의 데이터만 존재하게 됨 중복 클릭, 새로고침 연타로 인한 트래픽 폭증을 자연스럽게 방어

**Q. 유저가 재접속하면 순번이 유지되나?**

> Redis Sorted Set에 줄이 그대로 남아있어서 재접속 시 ZRANK로 현재 순번을 즉시 조회 가능 Kafka는 이 복구가 복잡해서 별도 구현이 필요함

---

## 핵심 요약

|항목|내용|
|---|---|
|핵심 결론|대기열 앞단은 Redis, 후처리 뒷단은 Kafka|
|Redis 장점 1|ZRANK로 실시간 순번 조회 (O(log n))|
|Redis 장점 2|유저 ID 멱등성으로 중복 클릭 자동 방지|
|Redis 장점 3|ZREM으로 중간 이탈 즉시 처리|
|Redis 장점 4|재접속 시 ZRANK로 순번 즉시 복구|
|Kafka 역할|통과된 유저의 DB 저장, 알림, 로그 후처리|
|Redis 한계|메모리 기반 → 중요한 데이터는 Kafka로|
|실무 조합|Redis(줄 세우기) + Kafka(실제 처리)|