### `volatile` 하나가 왜 그렇게 중요한가

다음 코드를 보자.
```java
public class Task {
    private boolean running = true;
    
    public void stop() {
        running = false;
    }
    
    public void run() {
        while (running) {
            // 일 하기
        }
        System.out.println("종료");
    }
}
```

Thread A가 `run()`을 실행 중이고, Thread B가 `stop()`을 호출한다. 상식적으로는 `running`이 `false`가 되었으니 Thread A의 루프가 끝나야 한다.

**하지만 실제로는 끝나지 않을 수 있다.** 영원히 돌 수 있다.

왜 이런 일이 생기는지 이해하려면 **자바 메모리 모델(Java Memory Model, JMM)**을 알아야 한다.

---

### 왜 이런 문제가 생기나

#### 현대 CPU의 구조

CPU는 메모리보다 압도적으로 빠르다. 매번 메모리에 접근하면 수백 사이클을 기다려야 한다. 그래서 CPU는 **캐시**를 가진다.

```
┌──────────────────────────────────────────┐
│         CPU Core 0          Core 1        │
│         ┌────┐             ┌────┐         │
│         │ L1 │             │ L1 │         │
│         └─┬──┘             └─┬──┘         │
│           │                   │           │
│         ┌─┴──┐             ┌─┴──┐         │
│         │ L2 │             │ L2 │         │
│         └─┬──┘             └─┬──┘         │
│           └────────┬──────────┘           │
│                  ┌─┴──┐                   │
│                  │ L3 │ (공유)             │
│                  └─┬──┘                   │
└────────────────────┼──────────────────────┘
                     │
              ┌──────┴──────┐
              │  Main Memory │
              └──────────────┘
```

각 코어는 자기만의 L1/L2 캐시를 가진다. **변수를 읽으면 캐시에 올라오고, 쓰면 캐시에 먼저 쓴다.** 메인 메모리 반영은 나중에 일어날 수 있다.

#### 앞의 예제에 적용하면

```
[Thread A - Core 0]           [Thread B - Core 1]
 running 읽음                    
 Core 0 캐시: running=true     
 while(running) 루프 시작        
                                running = false
                                Core 1 캐시: running=false
                                (메모리 반영은 나중)
                                
 캐시된 true를 계속 읽음...
 루프 안 끝남
```

Thread A는 **자기 캐시의 `running=true`를 계속 읽는다.** Thread B가 바꾼 값을 볼 수 없다. 이것이 **가시성(Visibility) 문제**다.

---

### 컴파일러와 CPU의 최적화

문제는 여기서 그치지 않는다. 컴파일러와 CPU는 **코드 순서를 자유롭게 바꾼다.**

#### 명령어 재배치

```java
int a = 1;
int b = 2;
int c = a + b;
```

컴파일러가 이걸 다음처럼 바꿔도 결과는 같다.


```java
int b = 2;
int a = 1;
int c = a + b;
```

싱글 스레드에서는 문제없다. 성능을 위한 자연스러운 최적화다.

#### 멀티스레드에서의 파괴력

```java
// Thread A
data = compute();   // (1)
ready = true;       // (2)

// Thread B
while (!ready) {}   // (3)
use(data);          // (4)
```

직관적으로는 B가 `ready`를 본 시점에 `data`도 완성되어야 한다.

하지만 컴파일러/CPU가 (1)과 (2)의 순서를 바꾸면:


```java
// Thread A (재배치 후)
ready = true;       // 먼저 실행됨
data = compute();   // 나중에 실행

// Thread B
while (!ready) {}   // ready=true 보고 통과
use(data);          // data는 아직 미완성!
```

**재배치 때문에 다른 스레드가 깨진 데이터를 본다.**

---

### JMM이 해결하려는 것

JMM은 이런 혼란 속에서 **"언제 어떤 값을 보장받을 수 있는가"**를 정의하는 규약이다.

JMM이 답하는 질문:

- Thread A가 쓴 값을 Thread B가 언제 볼 수 있나?
- 재배치는 어디까지 허용되나?
- 동기화 없이도 안전한 연산은 무엇인가?

JMM은 JVM 구현체가 반드시 따라야 하는 **계약**이다. 이 계약을 지키기 위해 JVM은 필요한 곳에 **메모리 배리어**를 삽입한다.

---

### happens-before — JMM의 핵심 개념

JMM의 중심에는 **happens-before 관계**가 있다.

> **A happens-before B**: A의 모든 효과를 B가 반드시 볼 수 있다.

이 관계가 성립하면:

- A에서의 변경이 B 시점에는 완료되어 있음이 보장됨
- A와 B 사이의 재배치는 허용되지 않음

#### happens-before가 성립하는 경우

**1. 같은 스레드 내의 순서**


```java
int a = 1;    // (1)
int b = 2;    // (2)
```

같은 스레드에서는 코드 순서대로 happens-before가 성립. (1) → (2)

**2. 락 해제와 후속 락 획득**

```java
// Thread A
synchronized(lock) {
    x = 1;
}  // (1) 락 해제

// Thread B
synchronized(lock) {   // (2) 락 획득
    print(x);          // x=1 보장됨
}
```

(1) happens-before (2)

**3. volatile 쓰기와 후속 읽기**

```java
// Thread A
data = 42;
flag = true;  // volatile 쓰기 (1)

// Thread B
if (flag) {   // volatile 읽기 (2)
    print(data);  // data=42 보장됨
}
```

(1) happens-before (2)

**4. 스레드 시작**


```java
int x = 42;
new Thread(() -> print(x)).start();   // x=42 보장됨
```

**5. 스레드 종료**

```java
Thread t = new Thread(() -> x = 42);
t.start();
t.join();
print(x);   // x=42 보장됨
```

**6. 전이성(Transitivity)**

```
A happens-before B
B happens-before C
  ⟹ A happens-before C
```

---

### volatile — 가장 가벼운 동기화

`volatile` 키워드는 두 가지를 보장한다.

#### ① 가시성 보장

`volatile` 변수는 **읽기/쓰기가 메인 메모리에 직접 적용**된다. 캐시를 거치지 않는다.

```java
private volatile boolean running = true;
```

처음 예제에서 `running`에 `volatile`을 붙이면 루프가 정상 종료된다.

#### ② 순서 보장 (재배치 금지)

`volatile` 변수 접근 주변의 재배치가 제한된다.

```java
int data;
volatile boolean ready;

// Thread A
data = 42;       // (1)
ready = true;    // (2) volatile write

// Thread B
if (ready) {     // (3) volatile read
    print(data); // (1)의 결과가 보임
}
```

(1)과 (2)는 재배치되지 않는다. (2)가 보였다면 (1)은 반드시 완료되어 있다.

#### 실제 구현 — 메모리 배리어

`volatile`은 컴파일 시 **메모리 배리어(Memory Barrier)**로 변환된다.

```
// x86 CPU의 경우
volatile int v = 5;   
  ↓ 변환
mov [v_addr], 5
mfence              ; 메모리 배리어 - 이전 쓰기를 전부 flush
```

메모리 배리어는 CPU에게 "이 지점 이전의 쓰기를 전부 메모리에 반영하라"고 명령한다. 이 지점을 지나면 모든 스레드가 최신 값을 본다.

#### volatile의 한계

**원자성(Atomicity)은 보장하지 않는다.**

```java
private volatile int count = 0;

public void increment() {
    count++;   // 이것은 원자적이지 않음!
}
```

`count++`는 실제로 세 연산이다:

1. count 읽기
2. +1 계산
3. count 쓰기

두 스레드가 동시에 실행하면 중간에 끼어들 수 있다.

```
Thread A: count 읽기 (5)
Thread B: count 읽기 (5)
Thread A: +1 계산 (6)
Thread B: +1 계산 (6)
Thread A: count 쓰기 (6)
Thread B: count 쓰기 (6)   ← 둘 다 6, 원래 7이 되어야 함
```

카운터 증가 같은 복합 연산에는 `AtomicInteger`나 `synchronized`가 필요하다.

---

### synchronized — 전통의 동기화

`synchronized`는 **락 기반 동기화**다. 가시성, 원자성, 순서를 모두 보장한다.

#### 기본 동작

```java
public synchronized void add() {
    count++;
}
// 또는
public void add() {
    synchronized(this) {
        count++;
    }
}
```

락을 획득한 스레드만 블록에 진입할 수 있다. 나머지는 대기.

#### JMM 관점에서 `synchronized`

```java
synchronized(lock) {
    // A의 작업
}  // 락 해제 (1)

synchronized(lock) {
    // B의 작업   (2) 락 획득
}
```

(1) happens-before (2).

**락 해제 시점에 락 내부의 모든 변경이 메모리에 flush**되고, **락 획득 시점에 모든 캐시가 무효화**된다. 그래서 `synchronized` 블록 안의 변경은 다음 블록 진입자에게 모두 보인다.

#### 바이트코드 레벨

`synchronized`는 두 바이트코드로 구현된다.

```
monitorenter    ; 모니터 획득
  (블록 내용)
monitorexit     ; 모니터 해제
```

3편에서 본 객체 헤더의 Mark Word가 여기서 사용된다. Biased → Lightweight → Heavyweight 락으로 단계적으로 승격된다.

#### `synchronized` vs `volatile`

|측면|volatile|synchronized|
|---|---|---|
|가시성|✓|✓|
|원자성|✗|✓|
|재배치 방지|✓|✓|
|블로킹|✗|✓|
|성능|빠름|상대적으로 느림|
|용도|단일 읽기/쓰기|복합 연산, 임계 구역|

**원칙**: 단일 변수 읽기/쓰기만 필요하면 `volatile`, 여러 연산이 원자적으로 실행되어야 하면 `synchronized`.

---

### final의 특별한 보장

`final` 필드는 JMM에서 특별한 보장을 받는다.

#### 안전한 생성 보장

```java
public class Config {
    final int value;
    
    public Config(int v) {
        this.value = v;
    }
}

// Thread A
Config c = new Config(42);   // 참조 공유

// Thread B
print(c.value);   // 42 보장 (volatile 없이도!)
```

생성자가 끝나는 시점에 **모든 final 필드가 메모리에 반영**된다. 다른 스레드가 참조를 받으면 final 필드는 항상 완성된 상태.

단, 생성자 중에 `this`를 외부로 노출하면 깨질 수 있다.

```java
public Config(int v) {
    registry.add(this);   // ← 위험! 아직 미완성 상태 노출
    this.value = v;
}
```

#### 불변 객체의 이점

`final` 필드만 가진 불변 객체는 **동기화 없이도 공유 가능**하다. `String`, `Integer` 같은 불변 객체가 스레드 세이프한 이유.

---

### 메모리 배리어의 종류

JVM과 CPU는 상황에 맞는 배리어를 삽입한다.

#### LoadLoad

```
Load1 ───── LoadLoad ───── Load2
```

Load1이 먼저 완료된 후 Load2 실행 보장.

#### StoreStore

```
Store1 ───── StoreStore ───── Store2
```

Store1이 메모리에 반영된 후 Store2 실행. **`final` 필드 초기화가 이 배리어를 사용.**

#### LoadStore

```
Load1 ───── LoadStore ───── Store2
```

Load1 완료 후 Store2.

#### StoreLoad

```
Store1 ───── StoreLoad ───── Load2
```

가장 무거운 배리어. Store1의 결과가 모든 코어에 전파된 후 Load2. **`volatile` 쓰기 후 읽기에 사용.**

`volatile` 쓰기는 뒤에 StoreLoad 배리어가 붙어서 성능에 영향을 준다. 일반 쓰기보다 느린 이유다.

---

### False Sharing — 성능이 갑자기 8배 느려지는 이유

JMM의 실제 동작을 이해하면 이상한 성능 문제도 설명된다.

#### 캐시 라인(Cache Line)

CPU 캐시는 바이트 단위가 아닌 **64바이트 캐시 라인** 단위로 동작한다. 어떤 변수를 읽으면 **그 주변 64바이트가 함께** 캐시에 올라온다.

#### 문제 상황

```java
class Counter {
    volatile long a;   // 8 bytes
    volatile long b;   // 8 bytes
}

// Thread A: a만 수정
counter.a++;

// Thread B: b만 수정
counter.b++;
```

`a`와 `b`는 **서로 다른 변수**다. 논리적으로는 서로 간섭하지 않아야 한다.

하지만 **같은 캐시 라인에 있다면** 문제가 생긴다.

```
[캐시 라인 - 64 bytes]
 │ a(8) │ b(8) │ ... │
```

- Thread A가 `a`를 수정 → Core 0의 캐시 라인 무효화 신호 → Core 1의 캐시 라인도 무효화
- Thread B가 `b`를 수정 → Core 1의 캐시 라인 무효화 → Core 0의 캐시도 무효화
- 계속 캐시가 튕겨 나감

**두 스레드가 서로 다른 변수를 수정하는데도 서로를 방해한다.** 이게 **False Sharing**이다.

#### 증상

- 싱글 스레드보다 멀티 스레드가 느려짐
- CPU 사용률은 높은데 처리량이 안 오름
- 변수 배치만 바꿔도 성능 수 배 차이

#### 해결책 1 — 패딩

```java
class Counter {
    volatile long a;
    long p1, p2, p3, p4, p5, p6, p7;   // 56 bytes 패딩
    volatile long b;
}
```

`a`와 `b`를 다른 캐시 라인에 강제 배치.

#### 해결책 2 — @Contended 어노테이션

Java 8부터 제공.

```java
import jdk.internal.vm.annotation.Contended;

class Counter {
    @Contended volatile long a;
    @Contended volatile long b;
}
```

JVM이 알아서 캐시 라인 정렬 처리.

```bash
java -XX:-RestrictContended MyApp
```

#### 실무 사례

`java.util.concurrent` 패키지의 여러 클래스가 `@Contended`를 내부적으로 사용한다. `LongAdder`, `ConcurrentHashMap`의 내부 구현 등에서 False Sharing 회피 코드를 볼 수 있다.

---

### Double-Checked Locking — JMM의 역사적 사례

싱글톤 패턴에서 자주 쓰이던 패턴.

```java
public class Singleton {
    private static Singleton instance;   // ← volatile 없음
    
    public static Singleton getInstance() {
        if (instance == null) {          // (1) 락 없이 빠른 체크
            synchronized(Singleton.class) {
                if (instance == null) {  // (2) 락 안에서 다시 체크
                    instance = new Singleton();   // (3)
                }
            }
        }
        return instance;
    }
}
```

**이 코드는 잘못됐다.** Java 5 이전에는 고장났고, 지금도 `volatile` 없으면 문제가 될 수 있다.

#### 왜 고장나나

(3)의 `new Singleton()`은 세 단계로 분해된다.

1. 객체 메모리 할당
2. 생성자 실행
3. `instance` 변수에 참조 할당

JVM이 2와 3의 순서를 **재배치**할 수 있다.

```
1. 메모리 할당
2. instance에 참조 할당 (아직 미초기화 상태)
3. 생성자 실행
```

이 사이에 Thread B가 (1)을 통과하면 **미초기화된 객체 참조를 쓰게 된다.**

#### 올바른 버전

```java
private static volatile Singleton instance;   // volatile 추가
```

`volatile`이 재배치를 막는다.

#### 더 나은 대안 — Static Holder

```java
public class Singleton {
    private static class Holder {
        static final Singleton INSTANCE = new Singleton();
    }
    
    public static Singleton getInstance() {
        return Holder.INSTANCE;
    }
}
```

클래스 로딩 메커니즘이 초기화 순서를 보장한다. `volatile`도 필요 없다.

---

### 원자 연산 — Atomic 클래스

`java.util.concurrent.atomic` 패키지는 락 없이 원자적 연산을 제공한다.

```java
AtomicInteger count = new AtomicInteger(0);

count.incrementAndGet();   // 원자적 ++
count.compareAndSet(5, 10); // 5이면 10으로
```

#### 내부 구현 — CAS

**CAS(Compare-And-Swap)**는 CPU 명령어로 원자적 갱신을 수행한다.

```
CAS(메모리, 기대값, 새값):
    if (메모리 == 기대값):
        메모리 = 새값
        return true
    else:
        return false
```

**이 과정 전체가 하드웨어 수준에서 원자적**이다. 다른 스레드가 끼어들 수 없다.

#### Lock-Free 프로그래밍

CAS를 사용하면 락 없이 동시성 자료구조를 만들 수 있다.

```java
public int increment() {
    int current;
    int next;
    do {
        current = count.get();
        next = current + 1;
    } while (!count.compareAndSet(current, next));
    return next;
}
```

경쟁이 심하면 재시도가 많아지지만, 락보다 훨씬 빠른 경우가 많다. `AtomicInteger.incrementAndGet()`이 이런 방식으로 구현되어 있다.

---

### 실전 가이드

#### 1. 동시성은 3단계로 접근

```
1순위: 불변 객체 사용 (final + 방어적 복사)
2순위: java.util.concurrent 라이브러리 활용
3순위: 직접 volatile/synchronized 사용
```

직접 동기화 코드를 짜는 건 **마지막 선택지**여야 한다.

#### 2. volatile은 단순 플래그에만

```java
// 좋은 예
private volatile boolean shutdown = false;

// 나쁜 예
private volatile int count = 0;
count++;   // 원자성 없음!
```

#### 3. synchronized 블록은 작게

락 범위가 크면 병목이 생긴다. 꼭 필요한 부분만 감싼다.

```java
// 나쁜 예
public synchronized void process() {
    // 네트워크 호출 5초
    // DB 쿼리 2초
    // 실제 임계 구역 10ms
}

// 좋은 예
public void process() {
    var result = networkCall();   // 락 밖
    var data = dbQuery();         // 락 밖
    synchronized(this) {
        updateState(result, data);  // 필요한 부분만
    }
}
```

#### 4. 공유 객체는 불변으로

변경 가능한 상태를 공유할수록 버그가 늘어난다. 데이터를 복사하거나 불변 타입으로.

#### 5. 의심되면 도구 사용

- **ThreadSanitizer, JVM -XX:+PrintCompilation** — 동시성 이슈 탐지
- **JMH (Java Microbenchmark Harness)** — 정확한 성능 측정
- **jstack** — 데드락 확인

---

### 정리

- JMM은 **멀티스레드 환경에서 언제 어떤 값을 보장받는지** 정의하는 규약
- **가시성 문제**: 캐시로 인해 한 스레드의 변경이 다른 스레드에 안 보일 수 있음
- **재배치 문제**: 컴파일러/CPU가 코드 순서를 바꿔 의도치 않은 동작 발생
- **happens-before**: A의 효과를 B가 반드시 본다는 보장. JMM의 중심 개념
- **volatile**: 가시성과 순서는 보장, 원자성은 안 됨
- **synchronized**: 가시성, 원자성, 순서 모두 보장하지만 더 무거움
- **final**: 생성자 완료 시점에 가시성 보장
- **False Sharing**: 같은 캐시 라인의 변수들이 서로 방해해 성능 저하
- **CAS**: 락 없는 원자 연산의 기반, `AtomicXxx` 클래스의 구현 원리