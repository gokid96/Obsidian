### 자바는 어떻게 C++만큼 빨라지나

"자바는 느리다"는 말은 오래된 편견이다. 최신 JVM 위에서 돌아가는 자바 코드는 상당수의 경우 C++에 근접하거나 특정 상황에서는 더 빠르다. 이게 어떻게 가능할까?

답은 **JIT(Just-In-Time) 컴파일러**다. 이번 편에서는 바이트코드가 실제 CPU 명령어로 변환되는 과정을 깊게 본다.

---

### 두 가지 실행 방식

JVM이 바이트코드를 실행하는 방식은 두 가지다.

#### 인터프리터 (Interpreter)

바이트코드를 **한 줄씩 읽어서 해석하고 실행**한다.

```
바이트코드  →  인터프리터가 해석  →  CPU 명령어 실행
  iadd              "iadd는 스택 top 2개를          add rax, rbx
                     꺼내 더하는 명령어다"          (실제 CPU 명령)
```

- **장점**: 시작이 빠르다. 컴파일 시간 없이 바로 실행 가능.
- **단점**: 같은 코드를 반복 실행해도 **매번 해석 과정을 거쳐야 한다.**

반복문이 100만 번 돌면, 같은 바이트코드를 100만 번 해석한다. 명백한 낭비다.

#### JIT 컴파일러 (Just-In-Time Compiler)

**자주 실행되는 코드(Hot Spot)를 감지해 네이티브 머신 코드로 컴파일하고 캐시**한다.

```
[첫 실행]
바이트코드 → 인터프리터 → 실행

[반복 실행 감지 후]
바이트코드 → JIT이 네이티브 코드로 컴파일 → 캐시
이후부터는 → 캐시된 네이티브 코드 직접 실행
```

네이티브 코드는 CPU가 바로 이해하는 언어라 해석 과정이 없다. **해석 비용 = 0.**

#### 왜 전부 JIT으로 안 하나?

"처음부터 전부 컴파일하면 안 되나?"라고 생각할 수 있다. 안 되는 이유:

1. **컴파일 시간이 든다**: 짧게 돌다 끝날 코드까지 컴파일하면 시작이 느려짐
2. **컴파일도 최적화하려면 정보가 필요**: 실제로 어떻게 쓰이는지 관찰해야 제대로 최적화 가능
3. **메모리를 씀**: 모든 코드를 컴파일해 캐시하면 메모리 부담

그래서 JVM은 **인터프리터로 시작해서, Hot한 코드만 JIT으로 승격**시킨다. 이것이 HotSpot JVM이라는 이름의 유래다.

---

### C1과 C2 컴파일러

HotSpot JVM은 **두 종류의 JIT 컴파일러**를 가지고 있다.

#### C1 — Client Compiler

- **빨리 컴파일, 최적화는 가볍게**
- 빠른 시작이 중요한 클라이언트/데스크톱 앱에 적합
- 프로파일링 정보 수집 가능

#### C2 — Server Compiler

- **느리게 컴파일, 최적화는 공격적으로**
- 장시간 돌아가는 서버 애플리케이션에 적합
- Escape Analysis, Method Inlining 등 고급 최적화 적용

과거에는 `-client`/`-server` 플래그로 둘 중 하나만 골라 썼다. 지금은 **둘 다 같이 쓰는 방식**이 기본이다.

---

### Tiered Compilation — 계층적 컴파일

Java 7에 도입되고 Java 8부터 기본 활성화된 전략. **인터프리터 → C1 → C2** 순으로 점진적 승격한다.

```
[Level 0]  인터프리터
             │
             │  (메서드가 자주 호출됨)
             ↓
[Level 1-3] C1 컴파일 (가벼운 최적화)
             │
             │  (여전히 hot하고 프로파일 충분함)
             ↓
[Level 4]  C2 컴파일 (공격적 최적화)
```

#### 각 레벨의 역할

|레벨|주체|특징|
|---|---|---|
|0|인터프리터|초기 실행, 프로파일 수집|
|1|C1 (최적화 없음)|간단한 컴파일, 프로파일 수집 안 함|
|2|C1 (제한적 프로파일)|호출/루프 카운터만 수집|
|3|C1 (완전 프로파일)|모든 프로파일 정보 수집|
|4|C2|프로파일 기반 공격적 최적화|

#### 전형적인 흐름

```
메서드 실행 시작
  ↓
Level 0 (인터프리터)
  ↓  호출 횟수 누적
Level 3 (C1 + 프로파일링)
  ↓  여전히 hot
Level 4 (C2 최적화)
```

C1은 빠른 컴파일로 **단기간에 성능 향상**을 제공하고, 그동안 C2는 충분한 프로파일 데이터를 받아 **최고의 최적화**를 수행한다. 두 세계의 장점을 모두 취한다.

---

### Hot Spot 감지 — 무엇을 기준으로?

JIT은 어떤 코드를 컴파일할지 어떻게 결정할까?

#### 두 가지 카운터

모든 메서드에는 두 개의 카운터가 붙어있다.

- **Invocation Counter**: 메서드가 호출된 횟수
- **Back-edge Counter**: 루프의 뒤쪽 점프(역방향 점프) 횟수

java

```java
public void process() {          // Invocation Counter ↑
    for (int i = 0; i < 1000; i++) {  // Back-edge Counter ↑ (1000번)
        doSomething();
    }
}
```

#### 임계값 도달 시 컴파일 큐에 등록

```
Invocation Counter + Back-edge Counter >= Threshold
  → 컴파일 큐에 추가
  → JIT 컴파일러 스레드가 백그라운드에서 컴파일
  → 완료되면 다음 호출부터 네이티브 코드 실행
```

기본 임계값(대략):

- C1: 2,000회
- C2: 15,000회

`-XX:CompileThreshold`로 조정 가능하지만 현대 JVM에서는 Tiered Compilation이 알아서 잘 관리하므로 거의 건드리지 않는다.

#### On-Stack Replacement (OSR)

긴 루프가 실행 중인데 컴파일이 끝났다면? 메서드가 끝날 때까지 기다려야 할까?

**OSR(On-Stack Replacement)**은 **실행 중인 메서드를 중간에 컴파일된 버전으로 교체**하는 기술이다.

```
[인터프리터로 루프 실행 중]
for (int i = 0; i < 10_000_000; i++) { ... }
  ↓  중간에 JIT 컴파일 완료
[남은 반복은 네이티브 코드로 실행]
```

루프의 현재 상태(지역변수, 카운터)를 그대로 옮겨서 컴파일된 코드로 점프한다. 덕분에 긴 루프도 JIT 혜택을 누릴 수 있다.

---

### JIT의 주요 최적화 기법

JIT이 실제로 어떤 최적화를 하는지 살펴보자.

#### ① Method Inlining — 메서드 호출 자체를 없애기

작고 자주 호출되는 메서드는 호출 자체를 없애고 내용을 펼친다.

java

```java
// 원본
int a = add(1, 2);

int add(int x, int y) {
    return x + y;
}

// Inlining 후 (논리적 결과)
int a = 1 + 2;
```

효과:

- **메서드 호출 오버헤드 제거** (스택 프레임 생성/해제, 파라미터 전달 등)
- Inlining 덕분에 **다른 최적화가 가능해짐** (상수 폴딩, 데드 코드 제거 등)

Inlining 대상 기준:

- 메서드 크기가 작음 (기본 325 바이트코드 이하)
- 자주 호출됨
- 가상 호출이면 실제 타겟이 단일하게 관측됨

#### ② Escape Analysis — 힙 할당을 스택으로

**객체가 메서드 밖으로 새어나가지 않는다면**, 힙 대신 스택에 할당할 수 있다.

java

```java
public int calculate() {
    Point p = new Point(1, 2);   // 이 객체는 메서드 밖으로 안 나감
    return p.x + p.y;
}
```

`p`는 `calculate` 바깥으로 전달되거나 저장되지 않는다. JIT은 이를 감지해:

1. **Stack Allocation**: 객체를 힙이 아닌 스택에 할당 (GC 부담 감소)
2. **Scalar Replacement**: 객체를 아예 없애고 필드를 지역변수로 분해

java

```java
// Scalar Replacement 결과 (논리적)
public int calculate() {
    int p_x = 1;
    int p_y = 2;
    return p_x + p_y;
}
```

객체 자체가 사라진다. **GC 부담도, 메모리 할당 비용도 0.**

#### ③ Loop Unrolling — 루프 펼치기

java

```java
// 원본
for (int i = 0; i < 4; i++) {
    arr[i] = i * 2;
}

// Unrolling 후
arr[0] = 0;
arr[1] = 2;
arr[2] = 4;
arr[3] = 6;
```

분기 예측 실패와 루프 오버헤드를 줄인다.

#### ④ Dead Code Elimination — 죽은 코드 제거

실행되지 않거나 결과가 쓰이지 않는 코드 제거.

java

```java
int x = 10;
if (false) {
    doSomething();   // 제거됨
}

int y = calculate();  // y가 어디에도 안 쓰이면
                      // calculate()가 side effect 없으면 통째로 제거
```

#### ⑤ Branch Prediction & Profile-Guided Optimization

프로파일링 정보를 바탕으로 **어느 분기가 자주 실행되는지** 파악해 코드를 재배치한다.

java

```java
if (rare_condition) {   // 거의 실행 안 됨
    // ...
} else {
    // 거의 여기로
}
```

JIT은 "else 분기가 fall-through 경로가 되도록" 코드를 배치해 CPU 분기 예측 적중률을 높인다.

#### ⑥ 가상 호출 최적화 — Devirtualization

java

```java
Animal a = new Dog();
a.sound();   // invokevirtual → 원래 런타임에 대상 결정
```

JIT이 프로파일을 관측해 "`a.sound()` 호출은 **항상 Dog이었다**"는 걸 알면:

- **Inline Caching**: "Dog일 때는 Dog.sound() 바로 호출" 캐시를 만듦
- **Monomorphic Inlining**: 아예 Dog.sound()를 inline해버림

단일 구현만 관측되는 한, **가상 호출이 직접 호출만큼 빨라진다.**

---

### Deoptimization — 낙관적 최적화의 되돌리기

JIT은 종종 **관측된 사실을 가정하고 공격적으로 최적화**한다. 하지만 가정이 깨질 수 있다.

#### 예시

java

```java
Animal a = getAnimal();   // 1만 번 실행되는 동안 항상 Dog였음
a.sound();
```

JIT이 "항상 Dog일 것"으로 가정하고 Dog.sound()를 inline했다. 그런데 어느 날 `getAnimal()`이 `Cat`을 반환한다면?

→ **Deoptimization** 발동.

1. 컴파일된 네이티브 코드를 버리고
2. 인터프리터로 돌아가서
3. 바이트코드를 다시 해석

이후 다시 충분한 프로파일이 쌓이면, **더 보수적인 가정으로 재컴파일**된다.

#### 왜 이렇게까지 하는가?

대부분의 코드는 실제로 **특정 패턴에 치우쳐 있다**. 99.9%가 단일 타입이라면, 99.9%를 위한 최적화가 전체 성능에 유리하다. 가끔 잘못 맞아도 Deoptimization 비용만 감수하면 된다.

**JIT의 철학은 "자주 맞는 가정에 베팅한다"**는 것이다.

---

### JIT 워밍업 (JIT Warm-up)

JVM을 시작하면 처음엔 인터프리터로 돌고, 점차 JIT이 코드를 컴파일하면서 속도가 올라간다. 이 기간을 **워밍업**이라 부른다.

```
시간 →
│
성능│                                    ━━━━━━━━━  ← C2 최적화 후 최고 성능
│                        ━━━━━━━━━                    (안정 상태)
│              ━━━━━━━━━                              C1 컴파일
│    ━━━━━━━━━                                        인터프리터
│
└──────────────────────────────────────────────→ 시간
     시작       수초 후    수십초~수분 후
```

#### 실무 영향

- **서비스 배포 직후 응답 시간이 느리다**: JIT이 아직 워밍업 중
- **로드 테스트 시 초반 데이터는 제외**: 정상 상태가 아님
- **단기 프로세스는 JIT 혜택이 적음**: CLI 도구나 서버리스 함수 같은 경우

#### 해결책들

- **AOT 컴파일 (Ahead-Of-Time)**: 미리 네이티브로 컴파일. GraalVM Native Image가 대표적
- **JIT 캐시 공유 (Class Data Sharing, AppCDS)**: 이전 실행의 컴파일 결과 재사용
- **Project Leyden** (진행 중): 시작 시간 개선을 위한 공식 프로젝트

---

### JIT 컴파일 확인하기

JIT이 어떤 메서드를 컴파일했는지 볼 수 있다.

bash

```bash
java -XX:+PrintCompilation MyApp
```

출력 예시:

```
     58    1       3       java.lang.String::hashCode (55 bytes)
     62    2       3       java.lang.String::charAt (29 bytes)
     95    3       4       java.util.HashMap::hash (20 bytes)
```

형식: `타임스탬프(ms) | 컴파일ID | 레벨 | 메서드`

레벨 1~3은 C1, 4는 C2다. 숫자로 어느 단계까지 최적화됐는지 확인 가능.

#### JIT 로그 상세 보기

bash

```bash
java -XX:+UnlockDiagnosticVMOptions -XX:+PrintInlining MyApp
```

어떤 메서드가 inline됐는지, 안 됐다면 왜인지까지 출력된다.

---

### 성능 팁 — JIT을 돕는 코드 작성

JIT이 최적화하기 좋은 코드를 쓰는 몇 가지 원칙:

1. **메서드를 작게 유지**: Inlining 대상이 되기 쉬움
2. **final을 적절히 사용**: JIT이 가상 호출을 단순 호출로 확신할 수 있게
3. **Hot Path를 단순하게**: 복잡한 분기보다 예측 가능한 흐름
4. **쓸데없는 객체 생성 피하기**: Escape Analysis가 해결해주지만, 애초에 안 만드는 게 최선
5. **Polymorphism 남발 금지**: 구현체가 너무 다양하면 devirtualization 실패

JIT이 모든 것을 알아서 해주는 건 아니다. 기본적으로 **깔끔한 코드**가 JIT 친화적이다.

---

### 정리

- JVM은 **인터프리터로 시작해 JIT으로 hot 코드를 승격**시키는 하이브리드 방식
- **Tiered Compilation**: 인터프리터 → C1 → C2 순 점진적 최적화
- Hot Spot은 **Invocation Counter + Back-edge Counter**로 감지
- 주요 최적화: **Inlining, Escape Analysis, Loop Unrolling, Devirtualization**
- **Deoptimization**은 낙관적 가정이 깨졌을 때 인터프리터로 돌아가는 메커니즘
- **JIT 워밍업** 동안은 성능이 낮으며, 서비스 초기/단기 프로세스에서 체감됨