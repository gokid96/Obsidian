## 프로젝트 컨텍스트

### 프로젝트 개요
- 프로젝트명: mini-commerce (대용량 트래픽 이커머스 시스템)
- 참고 레포: https://github.com/discphy/e-commerce (마켓컬리 합격자 포트폴리오)
- 참고 레포 로컬 클론: C:\Users\eborder\sungmin\git\e-commerce-reference
- 내 레포: https://github.com/gokid96/e-commerce
- 로컬 경로: C:\Users\eborder\sungmin\git\e-commerce
- Git GUI: Fork 사용 중

### 기술 스택
- Java 21 / Spring Boot 4.0.5 (Jackson 3 기반)
- JPA / QueryDSL (STEP03부터 도입 예정)
- MySQL / Redis / Kafka (STEP03~ 도입 예정)
- Docker / k6 / Prometheus / Grafana (STEP09)
- Spring REST Docs

### 환경 주의사항
- Spring Boot 4.x 사용 중 → 인터넷 자료는 대부분 3.x 기준이므로 참고 시 변환 필요
- Jackson 3 사용 (`tools.jackson.databind.*`) — `databind`는 신버전, `annotation`은 구버전(`com.fasterxml.jackson.annotation.*`) 그대로 유지
- Spring Boot 4.x starter 이름 주의: `spring-boot-starter-webmvc` (기존 `web` 아님), `spring-boot-starter-webmvc-test`

### GitHub 프로젝트 관리
- 마일스톤: STEP01~09 생성 완료
- 칸반 보드: Todo / In Progress / Done (자동화 설정됨)
- 테이블 뷰: 상위 이슈 + sub-issue 계층 구조

### 마일스톤 및 이슈 구조
- STEP01 - 설계 기본 ✅ 완료
- STEP02 - 설계 심화 ✅ 완료
- STEP03 - 도메인 구현 잔액/쿠폰/상품 (#3, sub-issue 3개) ← **다음 작업**
- STEP04 - 도메인 구현 주문/결제 (#4, sub-issue 5개)
- STEP05 - 동시성 (#5, sub-issue 5개)
- STEP06 - DB 성능 + 캐시 (#6, sub-issue 7개)
- STEP07 - EDA (#7, sub-issue 3개)
- STEP08 - MSA 분리/Kafka (#8, sub-issue 5개)
- STEP09 - 부하테스트 (#9, sub-issue 4개)

### 브랜치 전략
- main → PR 머지로만 반영
- 브랜치명 규칙:
  - `docs/step0X-xxx` (문서)
  - `feat/step0X-xxx` (기능)
  - `test/step0X-xxx` (테스트)
  - `refactor/step0X-xxx` (리팩토링)
  - `chore/xxx` (설정/잡일)
- 작업 흐름: 브랜치 생성 → 작업 → 커밋 → Push → PR → 머지 → 이슈 Close → 브랜치 삭제

### 커밋 메시지 규칙
- `[DOCS]` 문서 작성/수정
- `[FEAT]` 기능 구현
- `[TEST]` 테스트 작성
- `[REFACTOR]` 리팩토링
- `[FIX]` 버그 수정
### 현재 파일 구조
### 현재 파일 구조
```
e-commerce/
├── .github/
├── docs/
│   ├── architecture/
│   │   ├── 01.Requirements.md
│   │   ├── 02.Milestones.md
│   │   ├── 03.SequenceDiagram.md
│   │   ├── 03-2.StateDiagram.md
│   │   ├── 04.ERD.md
│   │   └── 05.ApiDocument.md
│   ├── devlog/
│   └── study/
├── http/api/
│   ├── balance-api.http
│   ├── coupon-api.http
│   ├── order-api.http
│   └── product-api.http
├── src/
│   ├── main/
│   │   ├── java/com/github/gokid96/e_commerce/
│   │   │   ├── balance/     (controller, dto)
│   │   │   ├── coupon/      (controller, dto)
│   │   │   ├── order/       (controller, dto)
│   │   │   ├── product/     (controller, dto)
│   │   │   └── common/      (ApiResponse, ApiControllerAdvice)
│   │   └── resources/
│   │       └── application.yaml (DataSource exclude 상태 — STEP03에서 해제 필요)
│   ├── test/java/com/github/gokid96/e_commerce/
│   │   ├── balance/controller/BalanceControllerTest.java
│   │   ├── coupon/controller/CouponControllerTest.java
│   │   ├── order/controller/OrderControllerTest.java
│   │   ├── product/controller/ProductControllerTest.java
│   │   ├── docs/ (RestDocsSupport + 4개 DocsTest)
│   │   └── support/ControllerTestSupport.java
│   └── docs/asciidoc/index.adoc
├── build.gradle (Spring Boot 4.0.5, asciidoctor 설정 완료)
└── README.md

```

### STEP01 ~ STEP02 완료 내역
- #11 요구사항 분석 문서 작성
- #12 마일스톤 문서화 + GitHub 연동
- #13 시퀀스 다이어그램 작성
- #14 ERD 설계
- (STEP01 API 명세는 PR #60 이후 반영됨)
- #15 Mock API 구현
- #16 Spring REST Docs 문서화
- #17 Controller 슬라이스 테스트 작성
- #18 API Request http 파일 작성
- #19 상태 다이어그램 작성

### 아키텍처 방침 (현재 기준)
- **현재 구조:** 패키지 바이 피처 (Package by Feature) + 레이어드
```
balance/
├── controller/
├── service/       (STEP03에서 채움)
├── domain/        (STEP03에서 채움)
└── infrastructure/(STEP03에서 채움)
```
- **STEP03 시작 시점에 고민할 것:** 클린 아키텍처(`interfaces/application/domain/infrastructure`)로 전환 여부 — discphy 구조 참고
- **STEP08에서 멀티모듈 분리 예정**

### 전체 진행 방식
1. 설계 단계 (STEP01~02): 문서 작성만 ✅
2. 모놀리식 TDD 구현 (STEP03~04): 단위테스트 → 구현 → 통합테스트 ← **다음**
3. 동시성 (STEP05): 실패 테스트 먼저 → 해결
4. DB 성능 + 캐시 (STEP06): 측정 → 개선 → 보고서
5. 이벤트 기반 (STEP07): ApplicationEvent → Kafka
6. MSA (STEP08): 멀티모듈 분리
7. 부하테스트 (STEP09): k6 시나리오 → 개선

### 테스트 스타일 컨벤션 (STEP02에서 확립)
- BDD 스타일 주석: `// given`, `// when & then`
- `mockMvc.perform()` 다중 라인 들여쓰기
- `@PathVariable("userId") Long userId` — 이름 명시 + Long 래퍼 사용
- Jackson 3 기반 (`tools.jackson.databind.ObjectMapper`)

### 현재 작업 (STEP03 시작 대기)
STEP03 - 도메인 구현: 잔액/쿠폰/상품
- sub-issue 3개 구체 내용 확인 후 시작
- 예상 작업: 도메인 엔티티 설계 → 단위 테스트 → 구현 → 통합 테스트

### 선행 정리 필요 (STEP03 진입 전)
- [ ] `application.yaml` DataSource 자동 구성 제외 해제 (Docker Compose MySQL 또는 H2 프로파일 분리)
- [ ] 의존성 추가: `spring-boot-starter-data-jpa`, Testcontainers, H2, QueryDSL
- [ ] `ControllerTestSupport`에 `@MockitoBean` 추가 (Service 생기면서)
- [ ] README.md 현재 빈 상태 → 프로젝트 개요 작성
- [ ] 참고 레포 `e-commerce-reference`와 구조 비교해 클린 아키텍처 전환 여부 결정
