1. Config 편집 페이지
   ↓
   [호스트 추가/수정/삭제]
   ↓
2. 변경사항 검증
   - YAML 문법 체크
   - 포트 중복 체크
   ↓
3. 적용 방법 선택
   ┌─────────────────────────┐
   │ ○ 자동 반영 (재시작 X)  │
   │ ● 프로세스 재시작       │
   │   ├ 변경된 것만        │
   │   └ 전체 재시작        │
   └─────────────────────────┘
   ↓
4. 실행
   a) Config 파일 저장
   b) Redis 버전 업데이트
   c) 프로세스 재시작 (선택 시)
   

웹 UI
  ↓
[Layer5Address 추가: NH]
  ↓
[저장 버튼 클릭]
  ↓
POST /api/config/handlers/XA2
  ↓
┌─────────────────────────────┐
│ Backend                     │
│ 1. Config 파일 저장         │
│ 2. Redis 버전 업데이트 ⭐   │
│ 3. 로그 기록                │
│ 4. 히스토리 저장            │
└─────────────────────────────┘
  ↓
Redis: ConfigurationVersion[airlineMessageHandler] = 1730000000
  ↓
10초 이내...
  ↓
┌─────────────────────────────┐
│ airlineMessageHandler_XA2   │
│ (실행 중인 프로세스)         │
│                             │
│ ConfigurationReload:        │
│ "어? 버전 바뀌었네?"        │
│ → Configuration.load 실행   │
│ → Layer5Address 재로딩      │
│ → NH 항공사 추가 완료! ✅   │
└─────────────────────────────┘
  ↓
NH 항공사 메시지 처리 가능!




## 요약표

|설정 항목|재시작 필요|반영 시간|변경 빈도|
|---|---|---|---|
|**Layer5Address**|❌|10초|높음 ⭐|
|**Airline**|❌|10초|높음 ⭐|
|**USM**|❌|10초|낮음|
|**RequestTimeout**|❌|10초|중간|
|**UpdateInterval**|❌|10초|낮음|
|**AirlineCodeCheck**|❌|10초|낮음|
|**SSQEnabled**|❌|10초|낮음|
|**PFMS**|❌|10초|낮음|
|---|---|---|---|
|**ServiceName**|✅|즉시|거의 없음|
|**ServerPort**|✅|즉시|거의 없음|
|**AllowedClientAddress**|✅|즉시|낮음|
|**RedisURL**|✅|즉시|거의 없음|
**결론:**

- 항공사 추가/삭제(Layer5Address)는 **자동 반영** → 웹에서 쉽게 관리 ✅
- 포트/IP 변경은 **재시작 필요** → 경고 후 재시작 확인 ⚠️
1. Config 파일 수정 
2. ConfigurationUpdater 실행 (또는 Redis 버전 업데이트) 
3. 10초 이내 자동 반영



### **백엔드 API

```ruby
# POST /api/config/handlers/:service
def update_config
  service = params[:service]
  new_config = params[:config]
  
  # 1. 변경사항 분석
  changes = analyze_changes(service, new_config)
  
  # 2. Config 파일 저장
  save_config_file(new_config)
  
  # 3. ⭐ ConfigurationUpdater 역할 (필수!)
  update_redis_version('airlineMessageHandler', current_user)
  
  # 4. 재시작 필요 여부 판단
  if changes[:restart_required]
    # 재시작 실행
    restart_handler(service)
    
    message = "설정 저장 완료 + #{service} 재시작 완료"
  else
    # 재시작 불필요
    message = "설정 저장 완료. 10초 후 자동 반영됩니다."
  end
  
  render json: { 
    success: true, 
    message: message,
    changes: changes
  }
end

private

def update_redis_version(process, operator)
  # ⭐ ConfigurationUpdater와 동일한 역할
  redis = Redis.new(
    host: '127.0.0.1',
    port: 2099,
    password: 'eborder@',
    db: 1
  )
  
  timestamp = Time.now.to_i
  redis.hset('ConfigurationVersion', process, timestamp)
  
  logger.info "Configuration version updated: #{process} by #{operator} at #{timestamp}"
  
  timestamp
end

def analyze_changes(service, new_config)
  old_config = load_current_config(service)
  
  restart_required = false
  auto_apply = []
  
  # 재시작 필요 여부 체크
  if old_config['ServerPort'] != new_config['ServerPort']
    restart_required = true
  end
  
  if old_config['AllowedClientAddress'] != new_config['AllowedClientAddress']
    restart_required = true
  end
  
  # 자동 반영 항목
  if old_config['Layer5Address'] != new_config['Layer5Address']
    auto_apply << 'Layer5Address'
  end
  
  if old_config['RequestTimeout'] != new_config['RequestTimeout']
    auto_apply << 'RequestTimeout'
  end
  
  {
    restart_required: restart_required,
    auto_apply: auto_apply
  }
end

def restart_handler(service)
  result = system("cd #{BASE_PATH}/bin && ./twn-iapp airlineMessageHandler #{service} restart")
  
  unless result
    raise "Handler restart failed: #{service}"
  end
  
  logger.info "Handler restarted: #{service}"
end
```

## 웹 UI 메시지
```javascript
// 재시작 X
if (!changes.restart_required) {
  showMessage({
    type: 'success',
    title: '설정 저장 완료',
    message: '약 10초 후 자동으로 반영됩니다. 프로세스 재시작은 필요 없습니다.',
    countdown: true
  });
}

// 재시작 O
if (changes.restart_required) {
  showConfirm({
    type: 'warning',
    title: '프로세스 재시작 필요',
    message: `
      다음 항목이 변경되어 재시작이 필요합니다:
      - ${changes.items.join(', ')}
      
      재시작 중 약 3-5초간 서비스가 중단됩니다.
      계속하시겠습니까?
    `,
    onConfirm: () => {
      // 재시작 실행
      saveAndRestart();
    }
  });
}
```


## iAPP 웹 모니터링 시스템 전체 설계

---

## 1. 시스템 구조

````
┌─────────────────────────────────────────────────┐
│             로컬Frontend (Vue 3)                │
│  ┌──────────┬──────────┬──────────┬──────────┐ │
│  │Dashboard │ Handlers │  Logs    │  Config  │ │
│  └──────────┴──────────┴──────────┴──────────┘ │
└────────────────────┬────────────────────────────┘
                     │ REST API / WebSocket
┌────────────────────┴─────────────────────── ───┐
│          로컬 Backend (Spring  )                   │
│  ┌──────────────────────────────────────────┐  │
│  │ API Routes                               │  │
│  │ - GET  /api/handlers                     │  │
│  │ - POST /api/handlers/:id/start           │  │
│  │ - GET  /api/logs/:service/tail           │  │
│  │ - POST /api/config/update                │  │
│  └──────────────────────────────────────────┘  │
└────────────-─┬─────────────────--──────────────┘
               │             
               ↓             
┌────────────────────┴─────────────────────── ───┐
│         리눅스 서버
  ┌─────────┐ ┌─────────┐ ┌──────────────┐
  │  Redis  │ │   PS    │ │ File System  │
  │  :2099  │ │  aux    │ │ (logs/config)│
  └─────────┘ └─────────┘ └──────────────┘
└────┬──────────┬─────────────┬──────────────────┘




```

---

## 2. 페이지 구조

### **Phase 1: MVP (2-3주)**
```
┌─────────────────────────────────────────┐
│  iAPP Monitoring System                 │
├─────────────────────────────────────────┤
│  📊 Dashboard                           │
│  ├─ 전체 상태 Overview                  │
│  ├─ 핸들러 테이블 (28개)                │
│  └─ Quick Actions                       │
│                                         │
│  🔧 Handler Management                  │
│  ├─ 상세 상태 보기                      │
│  ├─ Start/Stop/Restart                 │
│  └─ 실시간 로그 Tail                    │
│                                         │
│  📝 Logs                                │
│  ├─ 핸들러별 로그 조회                  │
│  ├─ 실시간 스트림                       │
│  └─ 검색 & 필터                         │
└─────────────────────────────────────────┘
```

### **Phase 2: 고급 기능 (추가 2주)**
```
│  ⚙️ Configuration                       │
│  ├─ Config 파일 편집                    │
│  ├─ Layer5Address 관리                 │
│  ├─ 변경 이력                           │
│  └─ 적용 (자동 반영 / 재시작)          │
│                                         │
│  📊 Statistics                          │
│  ├─ 시간별 메시지 통계                  │
│  ├─ 항공사별 분포                       │
│  └─ 에러율 추이                         │
│                                         │
│  🔔 Alerts                              │
│  ├─ 실시간 알림                         │
│  ├─ 알림 이력                           │
│  └─ 알림 설정                           │
````

---

## 3. 화면별 상세 설계

### **3.1 Dashboard (메인 화면)**
```vue
<template>
  <div class="dashboard">
    <!-- 상단: 전체 통계 -->
    <div class="stats-cards">
      <StatCard title="전체 핸들러" :value="28" />
      <StatCard title="정상" :value="stats.online" color="green" />
      <StatCard title="경고" :value="stats.warning" color="yellow" />
      <StatCard title="중지" :value="stats.offline" color="red" />
      <StatCard title="금일 메시지" :value="stats.totalMessages.toLocaleString()" />
    </div>
    
    <!-- 필터 & 검색 -->
    <div class="toolbar">
      <input v-model="search" placeholder="핸들러 검색..." />
      <select v-model="statusFilter">
        <option value="all">전체 상태</option>
        <option value="online">정상</option>
        <option value="warning">경고</option>
        <option value="offline">중지</option>
      </select>
      <button @click="refreshData">🔄 새로고침</button>
    </div>
    
    <!-- 핸들러 테이블 -->
    <HandlerTable 
      :handlers="filteredHandlers" 
      @action="handleAction"
      @detail="showDetail"
    />
  </div>
</template>
```

**테이블 컬럼:**

| Status | Service | Connected | MATIP | PAXLST | CUSRES | Reject | Last Message | Actions        |
| ------ | ------- | --------- | ----- | ------ | ------ | ------ | ------------ | -------------- |
| 🟢     | 1A1     | ●         | ●     | 1,234  | 1,200  | 20     | 2분 전         | [▶][⏹][🔄][📝] |
|        |         |           |       |        |        |        |              |                |

---

### **3.2 Handler Detail (상세 페이지)**
````vue
<template>
  <div class="handler-detail">
    <div class="header">
      <h1>{{ handler.serviceName }} Handler</h1>
      <div class="status-badge" :class="statusClass">
        {{ statusText }}
      </div>
    </div>
    
    <!-- 탭 메뉴 -->
    <Tabs v-model="activeTab">
      <Tab name="overview" label="📊 Overview">
        <HandlerOverview :handler="handler" />
      </Tab>
      
      <Tab name="stats" label="📈 Statistics">
        <HandlerStats :handler="handler" />
      </Tab>
      
      <Tab name="logs" label="📝 Logs">
        <LogViewer :service="handler.serviceName" />
      </Tab>
      
      <Tab name="config" label="⚙️ Config">
        <HandlerConfig :service="handler.serviceName" />
      </Tab>
    </Tabs>
  </div>
</template>
```

**Overview 탭:**
```
┌─────────────────────────────────────┐
│ 기본 정보                            │
│ Server: APP-TEST1                   │
│ Port: 2022                          │
│ Connected: 2025-10-22 10:20:15     │
│ Uptime: 15일 6시간 32분             │
├─────────────────────────────────────┤
│ 메시지 통계 (금일)                   │
│ PAXLST:  1,234건                    │
│ CUSRES:  1,200건                    │
│ Reject1:    20건                    │
│ Reject2:    14건                    │
│ USM:       150건                    │
├─────────────────────────────────────┤
│ 연결 정보                            │
│ Allowed IPs:                        │
│ - 192.168.150.1                     │
│ - 192.168.150.2                     │
├─────────────────────────────────────┤
│ 항공사 (Layer5Address)              │
│ - UA (United Airlines)              │
│ - JQ (Jetstar)                      │
│ - 3K, VF, GK, BL                    │
└─────────────────────────────────────┘
````

---

### **3.3 Log Viewer**
````vue
<template>
  <div class="log-viewer">
    <div class="toolbar">
      <select v-model="logLevel">
        <option value="all">전체</option>
        <option value="INFO">INFO</option>
        <option value="WARN">WARN</option>
        <option value="ERROR">ERROR</option>
      </select>
      
      <input v-model="searchKeyword" placeholder="검색..." />
      
      <button @click="toggleAutoScroll">
        {{ autoScroll ? '🔄 자동스크롤 ON' : '⏸️ 자동스크롤 OFF' }}
      </button>
      
      <button @click="downloadLogs">📥 다운로드</button>
    </div>
    
    <div class="log-container" ref="logContainer">
      <div 
        v-for="(log, index) in filteredLogs" 
        :key="index"
        :class="['log-line', `log-${log.level}`]"
      >
        <span class="timestamp">{{ log.timestamp }}</span>
        <span class="level">{{ log.level }}</span>
        <span class="message">{{ log.message }}</span>
      </div>
    </div>
  </div>
</template>
```

**로그 표시:**
```
[2025-10-22 16:15:30] INFO  Message received (Airline --> iAPP, RequestID: 2152876)
[2025-10-22 16:15:31] INFO  Message sent (Airline <-- iAPP, MessageType: CUSRES)
[2025-10-22 16:15:35] ERROR Connection lost to 192.168.150.1
````

---

### **3.4 Configuration Editor**
```vue
<template>
  <div class="config-editor">
    <div class="warning-banner" v-if="hasUnsavedChanges">
      ⚠️ 저장되지 않은 변경사항이 있습니다.
    </div>
    
    <!-- 설정 섹션들 -->
    <ConfigSection title="기본 설정" type="danger">
      <ConfigField 
        label="Service Name" 
        v-model="config.serviceName" 
        :readonly="true"
      />
      <ConfigField 
        label="Server Port" 
        v-model="config.serverPort" 
        :restart-required="true"
      />
      <ConfigField 
        label="Allowed IPs" 
        v-model="config.allowedIPs" 
        type="list"
        :restart-required="true"
      />
    </ConfigSection>
    
    <ConfigSection title="항공사 설정" type="success">
      <Layer5AddressManager 
        v-model="config.layer5Addresses"
        @add="addLayer5Address"
        @remove="removeLayer5Address"
      />
    </ConfigSection>
    
    <ConfigSection title="타임아웃 설정" type="success">
      <ConfigField 
        label="Request Timeout (초)" 
        v-model="config.requestTimeout" 
        type="number"
      />
      <ConfigField 
        label="Update Interval (초)" 
        v-model="config.updateInterval" 
        type="number"
      />
    </ConfigSection>
    
    <!-- 저장 버튼 -->
    <div class="action-bar">
      <div v-if="changes.restartRequired" class="restart-warning">
        ⚠️ 재시작이 필요한 항목이 변경되었습니다.
        <ul>
          <li v-for="item in changes.items" :key="item">{{ item }}</li>
        </ul>
      </div>
      
      <button @click="saveConfig" class="btn-primary">
        {{ changes.restartRequired ? '저장 및 재시작' : '저장 (자동 반영)' }}
      </button>
      
      <button @click="resetChanges" class="btn-secondary">
        취소
      </button>
    </div>
  </div>
</template>
```

---

## 4. 백엔드 API 설계

### **Ruby on Rails (추천)**
```ruby
# config/routes.rb
Rails.application.routes.draw do
  namespace :api do
    # 핸들러 관리
    resources :handlers, only: [:index, :show] do
      member do
        post :start
        post :stop
        post :restart
        get :status
      end
    end
    
    # 로그
    get 'logs/:service/tail', to: 'logs#tail'
    get 'logs/:service/search', to: 'logs#search'
    get 'logs/:service/download', to: 'logs#download'
    
    # 설정
    get 'config/:service', to: 'config#show'
    post 'config/:service', to: 'config#update'
    get 'config/:service/history', to: 'config#history'
    
    # 통계
    get 'stats/overview', to: 'stats#overview'
    get 'stats/:service', to: 'stats#show'
    
    # 시스템
    get 'system/status', to: 'system#status'
  end
end
```

```
┌─────────────────────────────────────────────────┐
│  iAPI-Gateway Management Console                │
├─────────────────────────────────────────────────┤
│   -Dash board                                    │
│  
   ⚙️ Configuration                               │  
│  └─ ✈️ MATIP-Broker (28개 핸들러)                |
│                                                 │
│  🔧 Core Services  ← 🆕 추가                    │
│  ├─ 📨 Immigration Client                      │
│  ├─ 🖥️ Immigration Server                      │
│  ├─ 📦 Archive Manager                         │
│  ├─ 📊 Service Monitor                         │
│  └─ 🗄️ Redis                                   │
│                                                 │
│  📝 Logs          ← 🆕 추가                     │
│  ├─ ✈️ Handlers                                │
│  └─ 🔧 Services                                │
└─────────────────────────────────────────────────┘
그리고 config 탭 MATIP-borker에서 테이블 형태로 전부 나열하고 각 행 클릭시 수정 추가는 별도 상단에 ADD버튼으로 가능하게


```
or
````
📊 Dashboard (메인 - 전체 Overview)
├─ 전체 상태 카드
│  ├─ MATIP-Broker: 28개 핸들러 (정상 25 / 경고 2 / 중지 1)
│  ├─ Immigration Client: 정상 ✅
│  ├─ Immigration Server: 정상 ✅  
│  ├─ Archive Manager: 정상 ✅
│  ├─ Service Monitor: 정상 ✅
│  └─ Redis: 정상 ✅

⚙️ Configuration
├─ ✈️ MATIP-Broker (28개 핸들러 테이블)

- Logs
    - [Handlers]
    - [configurationUpdater]
    - [immigrationClient]
````






---

 1. 프로젝트 구조

  twn-iapi-backend/
  ├── src/main/java/com/twn/iapi/
  │   ├── TwnIapiApplication.java
  │   ├── config/
  │   │   ├── SecurityConfig.java
  │   │   ├── RedisConfig.java
  │   │   ├── WebSocketConfig.java
  │   │   └── CorsConfig.java
  │   ├── domain/
  │   │   ├── matip/
  │   │   │   ├── MatipBroker.java
  │   │   │   ├── MatipHandler.java
  │   │   │   └── MatipHandlerStatus.java (enum)
  │   │   ├── immigration/
  │   │   │   ├── ImmigrationClient.java
  │   │   │   ├── ImmigrationServer.java
  │   │   │   └── ConnectionStatus.java
  │   │   ├── archive/
  │   │   │   ├── ArchiveManager.java
  │   │   │   └── BackupRecord.java
  │   │   ├── monitoring/
  │   │   │   ├── ServiceMonitor.java
  │   │   │   ├── SystemEvent.java
  │   │   │   └── AlertConfig.java
  │   │   └── user/
  │   │       ├── User.java
  │   │       └── Role.java (enum)
  │   ├── dto/
  │   │   ├── request/
  │   │   │   ├── MatipBrokerRequest.java
  │   │   │   ├── LoginRequest.java
  │   │   │   └── ConfigUpdateRequest.java
  │   │   └── response/
  │   │       ├── DashboardResponse.java
  │   │       ├── MatipBrokerResponse.java
  │   │       ├── ServiceStatusResponse.java
  │   │       └── ApiResponse.java
  │   ├── repository/
  │   │   ├── MatipBrokerRepository.java
  │   │   ├── MatipHandlerRepository.java
  │   │   ├── ImmigrationClientRepository.java
  │   │   ├── ImmigrationServerRepository.java
  │   │   ├── ArchiveManagerRepository.java
  │   │   ├── BackupRecordRepository.java
  │   │   ├── SystemEventRepository.java
  │   │   └── UserRepository.java
  │   ├── service/
  │   │   ├── MatipBrokerService.java
  │   │   ├── ImmigrationService.java
  │   │   ├── ArchiveService.java
  │   │   ├── MonitoringService.java
  │   │   ├── DashboardService.java
  │   │   ├── RedisService.java
  │   │   └── AuthService.java
  │   ├── controller/
  │   │   ├── DashboardController.java
  │   │   ├── MatipBrokerController.java
  │   │   ├── ImmigrationController.java
  │   │   ├── ArchiveController.java
  │   │   ├── MonitoringController.java
  │   │   └── AuthController.java
  │   ├── websocket/
  │   │   ├── WebSocketController.java
  │   │   └── EventNotificationHandler.java
  │   ├── security/
  │   │   ├── JwtTokenProvider.java
  │   │   ├── JwtAuthenticationFilter.java
  │   │   └── CustomUserDetailsService.java
  │   ├── exception/
  │   │   ├── GlobalExceptionHandler.java
  │   │   ├── ResourceNotFoundException.java
  │   │   └── BusinessException.java
  │   └── util/
  │       ├── DateTimeUtil.java
  │       └── ValidationUtil.java
  ├── src/main/resources/
  │   ├── application.yml
  │   ├── application-dev.yml
  │   ├── application-prod.yml
  │   └── db/
  │       └── migration/
  │           ├── V1__init_schema.sql
  │           ├── V2__insert_sample_data.sql
  │           └── V3__add_monitoring_tables.sql
  └── pom.xml (또는 build.gradle)

 ---
  📊 전체 아키텍처


● 🎯 SSE 기반 전체 아키텍처 (최신)

  전체 구조도
```
  ┌──────────────────────────────────────────────────────┐
  │              Frontend (Vue) - 개발 PC                │
  │                                                      │
  │  1️⃣ SSE 연결 (실시간 스트리밍)                      │
  │     EventSource('http://aprc1:8080/api/v1/processes/stream')
  │     ↓                                                │
  │     30초마다 자동 업데이트 (서버가 푸시)              │
  │                                                      │
  │  2️⃣ REST API (필요 시 수동 조회)                    │
  │     GET http://aprc1:8080/api/v1/processes           │
  │     GET http://aprc1:8080/api/v1/dashboard/stats     │
  └────────────┬─────────────────────┬───────────────────┘
               │ SSE (HTTP)          │ HTTP GET
               ↓                     ↓
  ┌────────────┴─────────────────────┴───────────────────┐
  │        Backend (Spring Boot) - 리눅스 서버 (aprc1)   │
  │                                                      │
  │  ┌─────────────────────────────────────────────────┐│
  │  │ SSE Controller (/api/v1/processes/stream)       ││
  │  │                                                 ││
  │  │ Flux.interval(30초)                             ││
  │  │   ↓                                             ││
  │  │ ① ps aux 실행 (매번)                            ││
  │  │   ↓                                             ││
  │  │ ② 파싱 (ProcessParser)                         ││
  │  │   ↓                                             ││
  │  │ ③ 필터링 (모니터링 대상만)                      ││
  │  │   ↓                                             ││
  │  │ ④ SSE로 클라이언트에 푸시 ──────────────────────┘│
  │  └─────────────────────────────────────────────────┘│
  │                                                      │
  │  ┌─────────────────────────────────────────────────┐│
  │  │ REST API Controllers                            ││
  │  │                                                 ││
  │  │ GET /api/v1/processes                           ││
  │  │   → ProcessMonitorService.getMonitoredProcesses()││
  │  │   → ps aux 실행 → 파싱 → 응답                   ││
  │  │                                                 ││
  │  │ GET /api/v1/dashboard/stats                     ││
  │  │   → DashboardService.getDashboardData()         ││
  │  │   → 통계 계산 → 응답                            ││
  │  └─────────────────────────────────────────────────┘│
  │                                                      │
  │  ┌─────────────────────────────────────────────────┐│
  │  │ Redis (선택적 - 캐싱용)                         ││
  │  │ localhost:2099                                  ││
  │  │                                                 ││
  │  │ 사용처: REST API 성능 향상                       ││
  │  │ - processes:current (프로세스 목록 캐시)         ││
  │  │ - TTL: 2분                                      ││
  │  └─────────────────────────────────────────────────┘│
  │                                                      │
  │  ┌─────────────────────────────────────────────────┐│
  │  │ Linux 시스템                                    ││
  │  │                                                 ││
  │  │ Runtime.exec("ps aux")                          ││
  │  │   ↓                                             ││
  │  │ airlineMessageHandler_1A1, 1A2, 1T1...          ││
  │  │ immigrationServer                               ││
  │  │ immigrationClient                               ││
  │  │ archiveManager_TwnAppProd                       ││
  │  └─────────────────────────────────────────────────┘│
  └──────────────────────────────────────────────────────┘
```


