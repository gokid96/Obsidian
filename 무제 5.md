## 현재 코드의 문제 — Kafka가 없으면 생기는 일

### 문제 1: 이메일 발송 실패가 멤버 초대를 롤백시킨다

현재 `inviteMember()`는 `@Transactional` 안에서 실행됩니다. 만약 멤버 저장 후 이메일 발송 로직을 동기로 추가하면:
```java
// 이렇게 짜면 안 되는 이유
workspaceMemberRepository.save(newMember);   // DB 저장 성공
emailSender.sendEmail(invitee.getEmail());   // ← 여기서 AWS SES 장애나면?
// → 예외 발생 → @Transactional 롤백 → DB도 없던 일이 됨
```

AWS SES가 잠깐 죽었다고 멤버 초대 자체가 실패하는 건 비즈니스적으로 말이 안 됩니다. **이메일 발송**과 **멤버 저장**은 서로 독립적인 관심사인데, 지금은 하나의 트랜잭션으로 묶여 있는 구조입니다.

현재 `EmailSender`가 `@Async`로 되어 있어 이 문제를 일부 회피하고 있지만, 발송 실패 시 재시도 메커니즘이 없고 유실됩니다.

---

### 문제 2: 워크스페이스 삭제가 중간에 실패하면 데이터가 반쪽이 된다
```java
// deleteWorkspace() 현재 코드
postRepository.deleteByWorkspaceWorkspaceId(workspaceId);      // 성공
folderRepository.clearParentByWorkspaceId(workspaceId);        // 성공
folderRepository.deleteAllByWorkspaceId(workspaceId);          // 성공
workspaceMemberRepository.deleteAllByWorkspaceId(workspaceId); // ← 여기서 실패?
workspaceRepository.delete(member.getWorkspace());
```

단일 `@Transactional`이라 이론상 전부 롤백되지만, 대용량 데이터에서 삭제가 오래 걸리면 트랜잭션이 타임아웃되고, 이때 일부는 삭제되고 일부는 남는 **부분 삭제 상태**가 생길 수 있습니다. 나중에 마이크로서비스로 분리할 때는 이 문제가 더 심각해집니다.