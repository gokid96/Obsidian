# Kafka Saga 도입 검토

## 1. 현재 두 가지 방식의 문제 도출

---

### 멤버 초대 — 이메일 알림 없음 + 실패 유실

**현재 코드 흐름** (`WorkspaceService.inviteMember`)

```
inviteMember() @Transactional
  └─ workspaceMemberRepository.save(newMember)   // DB 저장
  └─ return WorkspaceMemberResponse.from(...)     // 끝. 이메일 없음
```

**`EmailSender.sendAsync()`는 현재 inviteMember에서 호출되지 않는다.**  
인증번호 발송(`sendVerificationCode`)에만 사용 중.

#### 문제 1. 멤버 초대 시 이메일 알림 자체가 없다

초대받은 사용자 입장에서는 워크스페이스에 추가됐다는 사실을 알 방법이 없다.  
비즈니스 요구사항이 누락된 상태.

#### 문제 2. 이메일 발송을 동기로 추가하면 트랜잭션 결합이 발생한다

만약 `inviteMember()` 안에서 이메일을 동기 호출로 추가한다면:

```java
// 이렇게 짜면 안 되는 구조
workspaceMemberRepository.save(newMember);  // DB 저장 성공
emailSender.sendEmail(invitee.getEmail());  // AWS SES 장애 → 예외
// → @Transactional 롤백 → 멤버 저장도 취소됨
```

AWS SES 장애가 멤버 초대 비즈니스 로직 자체를 실패시킨다.  
**외부 시스템 장애가 핵심 DB 트랜잭션을 오염시키는 구조.**

#### 문제 3. @Async로 추가해도 실패 시 메시지 유실

현재 `EmailSender.sendAsync()`의 예외 처리:

```java
} catch (Exception e) {
    log.error("이메일 발송 실패: {} - {}", toEmail, e.getMessage());
    // 재시도 없음. 그냥 소멸.
}
```

`@Async`로 호출하면 트랜잭션 결합은 피할 수 있지만,  
SES 장애로 발송 실패 시 **재시도 메커니즘이 없어 그냥 유실**된다.  
`AsyncConfig`의 `getAsyncUncaughtExceptionHandler()` 역시 로그만 찍을 뿐, 복구 로직이 없다.

---

### 1-2. 워크스페이스 삭제 — 단일 트랜잭션의 위험

**현재 코드 흐름** (`WorkspaceService.deleteWorkspace`)

```java
@Transactional
public void deleteWorkspace(Long userId, Long workspaceId) {
    // 순서 중요: FK 제약 조건 때문
    postRepository.deleteByWorkspaceWorkspaceId(workspaceId);      // ①
    folderRepository.clearParentByWorkspaceId(workspaceId);        // ②
    folderRepository.deleteAllByWorkspaceId(workspaceId);          // ③
    workspaceMemberRepository.deleteAllByWorkspaceId(workspaceId); // ④
    workspaceRepository.delete(member.getWorkspace());             // ⑤
}
```

#### 문제 1. 트랜잭션 타임아웃 시 부분 삭제 상태 발생 가능

데이터가 많을 경우 (posts 수천 건 등) 삭제에 오랜 시간이 걸린다.  
DB 트랜잭션 타임아웃이나 네트워크 단절 시, **일부는 삭제되고 일부는 남는 상태**가 생길 수 있다.

`@Transactional`이 이론상 전체 롤백을 보장하지만,  
타임아웃 발생 시점에 따라 DB 레벨에서 커밋된 부분이 남아있을 수 있다.

#### 문제 2. 5단계 삭제가 하나의 긴 트랜잭션에 묶임

post → folder parent null 처리 → folder 삭제 → member 삭제 → workspace 삭제.  
5단계가 하나의 트랜잭션 안에서 순차 실행되므로, **DB 커넥션을 오래 점유**한다.  
동시에 같은 워크스페이스에 대한 요청이 들어오면 락 경합이 발생한다.

#### 문제 3. 실패 원인 추적이 어렵다

어느 단계에서 실패했는지 알 수 없다.  
롤백이 일어나도 "왜 삭제가 안 됐는지" 로그 외에 상태를 확인할 방법이 없다.

#### 문제 4. 마이크로서비스 분리 시 구조적으로 불가능

posts, folders, members가 각각 별도 서비스로 분리되면  
단일 `@Transactional`로 묶는 것 자체가 불가능해진다.  
현재 구조는 **모놀리식을 전제로만 동작**한다.

---

## 2. 수정 가능 방안 비교

---

### 2-1. 멤버 초대 이메일 알림

|방안|설명|구현 비용|신뢰성|재시도|추가 인프라|
|---|---|---|---|---|---|
|**A. @Async + 단순 호출**|`inviteMember()` 끝에 `emailSender.sendAsync()` 추가|★ 매우 낮음|낮음|없음 (실패 시 유실)|없음|
|**B. @Async + Spring Retry**|`@Retryable`로 3회 재시도 추가|★ 낮음|보통|있음 (서버 재시작 시 유실)|spring-retry 의존성|
|**C. Transactional Outbox**|`outbox` 테이블에 이벤트 저장 → `@Scheduled` 폴링 발송|보통|★ 높음|있음 (DB 기반, 서버 재시작 후에도 유지)|테이블 1개 + 스케줄러|
|**D. Kafka 이벤트 발행**|`workspace.member.invited` 토픽 발행 → `EmailConsumer` 구독|높음|★ 매우 높음|있음 (DLQ, 자동 재처리)|Kafka 클러스터|

**방안 A 코드 예시:**

```java
// WorkspaceService.inviteMember() 마지막에 추가
WorkspaceMember newMember = WorkspaceMember.createMember(inviter.getWorkspace(), invitee, request.role());
workspaceMemberRepository.save(newMember);

// 트랜잭션 커밋 후 비동기 발송 (단순)
emailSender.sendInviteAsync(invitee.getEmail(), inviter.getWorkspace().getName());

return WorkspaceMemberResponse.from(newMember);
```

**방안 B 코드 예시:**

```java
// EmailSender에 추가
@Retryable(
    retryFor = Exception.class,
    maxAttempts = 3,
    backoff = @Backoff(delay = 2000, multiplier = 2)
)
@Async("emailTaskExecutor")
public void sendInviteAsync(String toEmail, String workspaceName) { ... }

@Recover
public void recoverSendInvite(Exception e, String toEmail, String workspaceName) {
    log.error("이메일 최종 실패 - 수동 처리 필요: {}", toEmail);
}
```

**방안 C 구조 예시:**

```
inviteMember() @Transactional
  ├─ workspaceMember 저장
  └─ email_outbox 테이블에 INSERT  ← 같은 트랜잭션으로 원자적 저장
  └─ 커밋

@Scheduled(fixedDelay = 10000)
OutboxPoller.poll()
  ├─ email_outbox에서 미발송 레코드 조회
  ├─ SES 발송 시도
  └─ 성공 시 레코드 삭제 or sent_at 업데이트
```

---

### 2-2. 워크스페이스 삭제

|방안|설명|구현 비용|데이터 정합성|실패 복구|추가 인프라|
|---|---|---|---|---|---|
|**A. 현행 유지 + 타임아웃 튜닝**|`@Transactional(timeout=30)` 명시, 인덱스 최적화|★ 매우 낮음|보통|없음|없음|
|**B. Soft Delete + 배치**|`deleted_at` 컬럼 추가, `@Scheduled`로 후처리|★ 낮음|★ 높음|있음 (배치 재시도)|컬럼 추가 + 스케줄러|
|**C. Saga + Kafka**|단계별 이벤트 발행, Consumer 체인으로 순차 삭제|높음|★ 매우 높음|있음 (보상 트랜잭션)|Kafka 클러스터|

**방안 A — 현행 유지 + 개선:**

```java
@Transactional(timeout = 30)  // 타임아웃 명시
public void deleteWorkspace(Long userId, Long workspaceId) {
    // 기존 코드 동일, 단 인덱스 확인 필수
    // workspace_id 컬럼에 인덱스 없으면 풀스캔 발생
}
```

한계: 데이터가 많아지면 근본적인 해결이 아님.

**방안 B — Soft Delete 구조:**

```java
// Workspace 엔티티에 추가
@Column
private LocalDateTime deletedAt;

public void markAsDeleted() {
    this.deletedAt = LocalDateTime.now();
}

// deleteWorkspace()는 이것만 남김
@Transactional
public void deleteWorkspace(Long userId, Long workspaceId) {
    // ... 권한 체크 ...
    workspace.markAsDeleted();  // 즉시 응답
}

// 별도 스케줄러
@Scheduled(cron = "0 0 3 * * *")  // 매일 새벽 3시
public void purgeDeletedWorkspaces() {
    // deletedAt이 설정된 workspace 찾아서
    // post → folder → member → workspace 순 삭제
    // 각 단계별 try-catch + 로그
}
```

**방안 C — Kafka Saga (Choreography):**

```
deleteWorkspace() → publish "workspace.delete.initiated"
  → PostDeleteConsumer: posts 삭제 → publish "posts.deleted"
  → FolderDeleteConsumer: folders 삭제 → publish "folders.deleted"
  → MemberDeleteConsumer: members 삭제 → publish "members.deleted"
  → WorkspaceDeleteConsumer: workspace 삭제 완료

실패 시: "*.failed" 토픽 → 보상 트랜잭션 or 관리자 알림
```

---

## 3. 현재 프로젝트 기준 권장 방향

```
이메일 알림: 방안 B (@Async + Spring Retry)
             → 구현 비용 최소, 재시도 보장, 인프라 추가 없음
             → 실패 유실 허용 불가하면 방안 C (Outbox)로 업그레이드

워크스페이스 삭제: 방안 B (Soft Delete + 배치)
                  → 단일 서버에서 가장 현실적인 안전장치
                  → Kafka는 마이크로서비스 분리 시점에 도입

Kafka + Saga:     마이크로서비스 분리 또는 트래픽이 일 수만 건 이상일 때
                  기술 학습 목적이라면 지금도 적합한 실습 대상
```

---
