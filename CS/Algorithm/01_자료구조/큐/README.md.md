
# 큐 (Queue)

## 큐란?
**FIFO** (First In, First Out) - 선입선출  
**먼저 넣은 것이 먼저 나온다.**
```
offer(1) → [1]
offer(2) → [1, 2]
offer(3) → [1, 2, 3]
poll()   → 1 반환, [2, 3]  ← 먼저 넣은 1이 먼저 나옴
poll()   → 2 반환, [3]
````

### 실생활 예시

- 줄 서기 (먼저 온 사람이 먼저 처리)
- 프린터 대기열
- 은행 번호표

---

## Java 구현

### ArrayDeque 사용 (권장)

java

```java
import java.util.ArrayDeque;
import java.util.Deque;

Deque<Integer> queue = new ArrayDeque<>();

queue.offer(1);     // 뒤에 삽입
queue.offer(2);
queue.offer(3);     // 현재: [1, 2, 3] (1이 front)

queue.peek();       // 1 반환 (조회만, 제거 안 함)
queue.poll();       // 1 반환 + 제거, 현재: [2, 3]

queue.isEmpty();    // false
queue.size();       // 2
```

### LinkedList 사용

```java
import java.util.LinkedList;
import java.util.Queue;

Queue<Integer> queue = new LinkedList<>();
// 사용법은 동일
```

> 💡 **LinkedList보다 ArrayDeque가 더 빠름**

---

## 주요 메서드

|메서드|설명|시간복잡도|
|---|---|---|
|offer(x)|뒤에 삽입|O(1)|
|poll()|앞에서 제거 + 반환|O(1)|
|peek()|앞 조회 (제거 안 함)|O(1)|
|isEmpty()|비어있는지 확인|O(1)|
|size()|크기 반환|O(1)|

### add/remove vs offer/poll
|                  | 실패 시            |
| ---------------- | --------------- |
| add() / remove() | 예외 발생           |
| offer() / poll() | false / null 반환 |

> 💡 **offer/poll 사용 권장** (예외 처리 없이 안전하게 처리)

---

## 언제 큐를 쓰는가?

1. **순서대로 처리**: 먼저 온 순서대로 처리할 때
2. **BFS (너비 우선 탐색)**: 가까운 노드부터 탐색
3. **대기열 시뮬레이션**: 줄 서기, 프로세스 스케줄링

---

## 활용 예제: 카드 게임

문제: 1~n 카드가 있다. 맨 위 카드를 버리고, 그 다음 카드를 맨 아래로 넣는다. 마지막 카드는?

```java
public int lastCard(int n) {
    Deque<Integer> queue = new ArrayDeque<>();
    
    // 1~n 카드 넣기
    for (int i = 1; i <= n; i++) {
        queue.offer(i);
    }
    
    // 한 장 남을 때까지 반복
    while (queue.size() > 1) {
        queue.poll();              // 맨 위 버리기
        queue.offer(queue.poll()); // 다음 카드 맨 아래로
    }
    
    return queue.poll();
}

// n=4 진행 과정:
// [1,2,3,4] → 1 버림, 2를 맨 뒤로 → [3,4,2]
// [3,4,2]   → 3 버림, 4를 맨 뒤로 → [2,4]
// [2,4]     → 2 버림, 4를 맨 뒤로 → [4]
// 결과: 4
```

---

## 스택 vs 큐 비교

|       | 스택          | 큐           |
| ----- | ----------- | ----------- |
| 순서    | LIFO (후입선출) | FIFO (선입선출) |
| 삽입 위치 | 맨 위         | 맨 뒤         |
| 제거 위치 | 맨 위         | 맨 앞         |
| 예시    | 접시 쌓기       | 줄 서기        |
| 알고리즘  | DFS         | BFS         |

---

## 정리

> 💡 큐 = **선입선출 (FIFO)** - 먼저 넣은 게 먼저 나옴  
> 💡 순서대로 처리, **BFS**에 사용  
> 💡 구현은 **ArrayDeque 권장**  
> 💡 poll() 전에 **isEmpty() 체크** 필수