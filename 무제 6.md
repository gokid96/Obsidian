## 왜 필요한가?

```
모놀리식에서는 ACID 트랜잭션으로 쉽게 처리
BEGIN TRANSACTION
  주문 생성
  재고 차감
  결제 처리
  포인트 적립
COMMIT  → 전부 성공
ROLLBACK → 전부 취소

MSA에서는?
→ 각 서비스가 독립 DB를 가짐
→ 여러 서비스에 걸친 트랜잭션 불가
→ 주문은 성공했는데 결제 실패하면?
→ 데이터 불일치 발생
```

---

## Saga 패턴이란?

```
분산 환경에서 트랜잭션을 여러 단계로 나눠서 처리
→ 각 단계가 성공하면 다음 단계 진행
→ 실패하면 이전 단계를 보상 트랜잭션으로 되돌림

핵심:
→ 전통적 ROLLBACK 대신 보상 트랜잭션(Compensating Transaction)
```

---

## 보상 트랜잭션이란?

```
이미 완료된 작업을 되돌리는 작업

작업               보상 트랜잭션
주문 생성    →    주문 취소
재고 차감    →    재고 복구
결제 처리    →    결제 환불
포인트 적립  →    포인트 차감

→ DB ROLLBACK이 아니라
→ 비즈니스 로직으로 되돌림
```

---

## Saga 구현 방식 2가지

### 1. 코레오그래피 (Choreography) - 이벤트 기반

```
중앙 관리자 없이 각 서비스가 이벤트를 보고 알아서 처리

흐름 (성공):
주문 서비스   → "OrderCreated" 이벤트 발행
재고 서비스   → 구독 → 재고 차감 → "StockReserved" 이벤트 발행
결제 서비스   → 구독 → 결제 처리 → "PaymentCompleted" 이벤트 발행
포인트 서비스 → 구독 → 포인트 적립 → "PointEarned" 이벤트 발행
→ 주문 완료

흐름 (결제 실패):
주문 서비스  → "OrderCreated" 이벤트 발행
재고 서비스  → 재고 차감 → "StockReserved" 이벤트 발행
결제 서비스  → 결제 실패 → "PaymentFailed" 이벤트 발행
재고 서비스  → "PaymentFailed" 구독 → 재고 복구 (보상)
주문 서비스  → "PaymentFailed" 구독 → 주문 취소 (보상)
```

**장점:**

```
구현 단순
서비스 간 결합도 낮음
새 서비스 추가 쉬움
```

**단점:**

```
전체 흐름 파악 어려움
→ 이벤트가 여러 서비스에 퍼져있어서
순환 이벤트 발생 위험
장애 추적 어려움
```

---

### 2. 오케스트레이션 (Orchestration) - 중앙 관리자 방식

```
Saga Orchestrator가 전체 흐름을 중앙에서 제어

흐름 (성공):
[Orchestrator] → 재고 서비스에 "재고 차감" 명령
재고 서비스    → 처리 완료 응답
[Orchestrator] → 결제 서비스에 "결제 처리" 명령
결제 서비스    → 처리 완료 응답
[Orchestrator] → 포인트 서비스에 "포인트 적립" 명령
포인트 서비스  → 처리 완료 응답
[Orchestrator] → 주문 완료 처리

흐름 (결제 실패):
[Orchestrator] → 재고 서비스에 "재고 차감" 명령 → 성공
[Orchestrator] → 결제 서비스에 "결제 처리" 명령 → 실패!
[Orchestrator] → 재고 서비스에 "재고 복구" 명령 (보상)
[Orchestrator] → 주문 취소 처리
```

**장점:**

```
전체 흐름이 한 곳에 있어서 파악 쉬움
장애 추적 쉬움
복잡한 비즈니스 로직 처리 가능
```

**단점:**

```
Orchestrator가 단일 장애점
Orchestrator가 복잡해질 수 있음
서비스 간 결합도 약간 높아짐
```

---

## 두 방식 비교

|항목|코레오그래피|오케스트레이션|
|---|---|---|
|제어 방식|각 서비스가 알아서|중앙 Orchestrator|
|결합도|낮음|약간 높음|
|흐름 파악|어려움|쉬움|
|장애 추적|어려움|쉬움|
|복잡한 로직|어려움|쉬움|
|단일 장애점|없음|Orchestrator|
|적합한 상황|단순한 흐름|복잡한 비즈니스 로직|

---

## Saga의 핵심 과제

### 1. 멱등성 보장

```
같은 이벤트가 두 번 처리돼도 결과가 같아야 함

예시:
"재고 차감" 이벤트가 네트워크 오류로 두 번 전달
→ 재고가 두 번 차감되면 안 됨

해결:
→ 이벤트 ID 기록
→ 이미 처리한 이벤트 ID면 무시
```

### 2. 보상 트랜잭션의 실패

```
보상 트랜잭션도 실패할 수 있음

예시:
결제 실패 → 재고 복구 명령 → 재고 서비스도 장애!
→ 재고가 복구 안 됨

해결:
→ 보상 트랜잭션도 재시도 (지수 백오프)
→ 최종적으로 실패 시 → 수동 처리 알림 (Dead Letter Queue)
```

### 3. 최종 일관성

```
Saga는 즉각적인 일관성이 아닌 최종 일관성
→ 처리 중간에는 데이터 불일치 상태 존재
→ 이를 허용하는 설계 필요
```

---

## Outbox 패턴과의 연결

```
Saga에서도 동일한 문제 발생
→ 보상 이벤트를 발행하다가 실패하면?

예시:
결제 실패 감지
→ 재고 복구 이벤트 발행 ← 실패!
→ 재고가 복구 안 된 채로 남음

해결 - Outbox 패턴 적용:
→ 보상 이벤트도 DB Outbox 테이블에 먼저 저장
→ Outbox Poller가 읽어서 Kafka에 발행
→ 발행 실패해도 Outbox에 남아있어서 재시도 가능

EDA + Saga + Outbox 패턴이 MSA 표준 조합
```

---

## 실제 사용 사례

```
쿠팡 주문 처리
→ 오케스트레이션 Saga
→ 주문 → 재고 차감 → 결제 → 배송 준비
→ 결제 실패 시 재고 자동 복구

여행 예약 시스템
→ 항공권 예약 → 호텔 예약 → 렌터카 예약
→ 렌터카 예약 실패 시
→ 호텔 취소 (보상) → 항공권 취소 (보상)

은행 송금
→ 출금 → 송금 → 입금
→ 입금 실패 시 → 출금 취소 (보상)
```

---

## 핵심 Q&A

**Q. MSA에서 트랜잭션을 어떻게 처리하나?**

> 각 서비스가 독립 DB를 가져서 전통적인 ACID 트랜잭션을 쓸 수 없음 대신 Saga 패턴으로 여러 단계로 나눠서 처리하고 실패 시 보상 트랜잭션으로 이전 단계를 되돌림

**Q. 코레오그래피와 오케스트레이션 차이는?**

> 코레오그래피는 각 서비스가 이벤트를 보고 알아서 처리하는 방식 오케스트레이션은 중앙 Orchestrator가 전체 흐름을 지시하는 방식 단순한 흐름엔 코레오그래피, 복잡한 비즈니스 로직엔 오케스트레이션이 적합

**Q. Saga의 단점은?**

> ACID와 달리 즉각적인 일관성이 아닌 최종 일관성이라서 처리 중간에 데이터 불일치 상태가 존재할 수 있음 보상 트랜잭션 설계가 복잡하고 멱등성도 보장해야 함

**Q. 보상 트랜잭션 발행이 실패하면?**

> Outbox 패턴으로 해결 보상 이벤트도 DB Outbox 테이블에 먼저 저장하고 Outbox Poller가 읽어서 Kafka에 발행하기 때문에 유실 없이 재시도 가능

---

## 핵심 요약

|항목|내용|
|---|---|
|목적|MSA에서 여러 서비스에 걸친 트랜잭션 처리|
|핵심|보상 트랜잭션으로 실패 시 되돌림|
|방식 1|코레오그래피 (이벤트 기반, 단순한 흐름)|
|방식 2|오케스트레이션 (중앙 제어, 복잡한 흐름)|
|단점|최종 일관성, 보상 트랜잭션 복잡, 멱등성 필요|
|vs ACID|ACID는 즉각 일관성, Saga는 최종 일관성|
|Outbox 연결|보상 이벤트 발행 실패 방지 → Outbox 패턴 적용|
|표준 조합|EDA(Kafka) + Saga + Outbox 패턴|