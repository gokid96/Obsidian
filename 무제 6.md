### 레벨 1: 기본 락 (`synchronized`)

**자바가 처음부터 가지고 있던 도구.**

#### 문제 상황

java

```java
public class Counter {
    private int count = 0;
    
    public void increment() {
        count++;  // 여러 스레드가 동시에 호출하면 꼬임
    }
}
```

#### 해결

java

```java
public class Counter {
    private int count = 0;
    
    public synchronized void increment() {  // 한 번에 한 명만!
        count++;
    }
}
```

#### 특징

- ✅ **간단**: 키워드 하나
- ✅ **자동 해제**: 메서드 끝나면 락 풀림
- ❌ **유연성 없음**: 못 잡으면 무조건 무한 대기
- ❌ **타임아웃 불가**: "1초만 시도하고 포기" 같은 거 안 됨

#### 한계 예시

java

```java
synchronized(lock) {
    // 여기서 락 못 잡으면 영원히 기다림
    // 데드락 걸려도 빠져나올 방법 없음
}
```

---

### 레벨 2: 더 똑똑한 락과 락 없는 방법

**"`synchronized`가 너무 단순해서 부족해. 더 좋은 거 만들자!"**

#### 2-A. `ReentrantLock` — 똑똑한 락

java

```java
import java.util.concurrent.locks.ReentrantLock;

private final ReentrantLock lock = new ReentrantLock();

public void increment() {
    if (lock.tryLock(1, TimeUnit.SECONDS)) {  // 1초만 시도!
        try {
            count++;
        } finally {
            lock.unlock();  // ⚠️ 직접 풀어야 함
        }
    } else {
        // 1초 내에 못 잡았으면 다른 처리
        // → 데드락 자동 회피!
    }
}
```

**`synchronized`보다 좋은 점:**

- ✅ 타임아웃 가능 → 데드락 방지
- ✅ "못 잡으면 그냥 포기" 가능
- ❌ 직접 `unlock()` 해야 함 (까먹으면 재앙)

#### 2-B. `AtomicInteger` — 락 없는 마법

java

```java
import java.util.concurrent.atomic.AtomicInteger;

public class Counter {
    private final AtomicInteger count = new AtomicInteger(0);
    
    public void increment() {
        count.incrementAndGet();  // 락 없이도 안전!
    }
}
```

**어떻게 락 없이 안전?**

CPU 명령어 중에 **CAS (Compare-And-Swap)**가 있어요:

```
"현재 값이 10이면 → 11로 바꿔줘"
이걸 CPU가 한 번에 처리 (중간에 끼어들 수 없음)
```

이걸 활용해서 락 없이도 안전하게 동작합니다.

**언제 뭘 써?**

- 단순한 숫자/플래그 → **AtomicInteger** (빠름)
- 여러 변수 함께 변경 → **락**

---

### 레벨 3: 통째로 안전한 도구들

**"락 신경 쓰기 귀찮아. 그냥 안전한 도구를 쓰자!"**

#### 3-A. 동시성 컬렉션

java

```java
// ❌ 위험: 멀티스레드에서 깨질 수 있음
Map<String, Integer> map = new HashMap<>();

// ✅ 안전: 내부적으로 알아서 처리
Map<String, Integer> map = new ConcurrentHashMap<>();

// 그냥 쓰면 됨, 동시성 신경 쓸 필요 없음
map.put("key", 1);
map.get("key");
```

**대표 멤버:**

- `ConcurrentHashMap` — 멀티스레드용 Map
- `CopyOnWriteArrayList` — 읽기 위주 List
- `BlockingQueue` — 생산자-소비자 패턴

#### 3-B. `BlockingQueue` — 생산자/소비자 자동 처리

옛날엔 이런 코드를 직접 짜야 했어요:

java

```java
// 옛날 방식: wait/notify로 직접 구현 (복잡)
synchronized(queue) {
    while (queue.isEmpty()) {
        wait();  // 큐 빌 때까지 대기
    }
    Item item = queue.remove(0);
    notifyAll();
}
```

이제는 한 줄:

java

```java
BlockingQueue<Item> queue = new LinkedBlockingQueue<>(100);

// 생산자
queue.put(item);  // 가득 차면 알아서 대기

// 소비자
Item item = queue.take();  // 비어있으면 알아서 대기
```

내부 동작은 똑같지만 **복잡한 부분을 다 숨김**. 이게 추상화의 힘이에요.

---

### 한눈에 비교

|레벨|도구|한 줄 요약|
|---|---|---|
|1|`synchronized`|"한 명씩 들어가" — 자바 기본|
|1|`volatile`|"변경된 값 바로 보이게" — 가시성만|
|2|`ReentrantLock`|"synchronized + 타임아웃"|
|2|`AtomicInteger`|"락 없이 숫자 안전하게"|
|3|`ConcurrentHashMap`|"안전한 Map"|
|3|`BlockingQueue`|"생산자-소비자 자동"|

---

### 실무 선택 가이드

```
어떤 상황?              →  어떤 도구?
──────────────────────────────────────────
단순한 카운터              AtomicInteger
플래그 (true/false)        AtomicBoolean
멀티스레드 Map             ConcurrentHashMap
생산자-소비자              BlockingQueue
복잡한 동시 변경           ReentrantLock
간단한 메서드 보호         synchronized
```

**황금률:**

1. **가능하면 공유 데이터를 안 쓰기** (가장 안전)
2. 써야 하면 → **레벨 3 (동시성 컬렉션)** 우선
3. 단순 숫자면 → **Atomic**
4. 정말 필요할 때만 → **락**

---

### 흐름 한 문장

> **`synchronized`로 시작했더니 너무 단순해서 → `ReentrantLock`이 나왔고, 락 자체가 비싸서 → `Atomic`이 등장했고, 매번 락 거는 게 귀찮아서 → 동시성 컬렉션이 나왔다.**

각 도구는 이전 도구의 **불편함을 해결하면서 등장**한 거예요. 그래서 위로 갈수록 편해지는 겁니다.

---

### 처음 시작이라면

이 정도만 알아도 90%는 커버돼요:

java

```java
// 1. 단순 카운터
AtomicInteger counter = new AtomicInteger();

// 2. 멀티스레드 Map
ConcurrentHashMap<String, User> users = new ConcurrentHashMap<>();

// 3. 작업 큐
BlockingQueue<Task> queue = new LinkedBlockingQueue<>();

// 4. 어쩔 수 없이 락 필요할 때
synchronized(this) {
    // 짧고 간단하게!
}
```

나머지(`ReentrantLock`, `Semaphore`, `CountDownLatch` 등)는 **필요할 때 찾아서 쓰면 됨**. 처음부터 다 외울 필요 없어요 👍

이 정도면 동시성 문제 해결의 큰 그림이 잡히실 거예요. 더 깊이 보고 싶은 부분 있으면 말씀해주세요!