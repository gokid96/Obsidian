## CAP 이론이란?

```
분산 시스템이 동시에 보장할 수 있는 특성의 한계를 정의한 이론
→ Consistency, Availability, Partition Tolerance 중 최대 2가지만 동시에 보장 가능
```

**대표 예시 - 분산 DB 장애:**

```
서울 서버와 뉴욕 서버가 데이터를 함께 관리 중

네트워크 단절 발생 (Partition)
→ 서울에서 데이터 수정
→ 뉴욕 서버는 반영 못 받음

이때 선택해야 함:
→ 일관성 선택: 뉴욕 서버 응답 거부 (데이터 불일치 방지)
→ 가용성 선택: 뉴욕 서버 오래된 데이터라도 응답 (서비스 유지)

둘 다 완벽하게 지킬 수는 없음
```

---

## CAP이란?

분산 시스템이 보장할 수 있는 3가지 특성

---

### C - 일관성 (Consistency)

```
모든 노드가 항상 동일한 데이터를 봄
어느 서버에 읽어도 최신 데이터가 보장됨

예시:
서울 서버에서 재고를 10 → 5로 수정
→ 뉴욕 서버에서 조회해도 반드시 5가 나와야 함
→ 5가 반영되기 전까지 뉴욕 서버는 응답하지 않음
```

---

### A - 가용성 (Availability)

```
모든 요청에 반드시 응답함
장애가 나도 서비스는 계속 동작

예시:
서울-뉴욕 네트워크 단절 상황에서
→ 뉴욕 서버는 오래된 데이터라도 응답을 돌려줌
→ "서버 오류"가 아니라 항상 무언가를 반환
```

---

### P - 분단 내성 (Partition Tolerance)

```
네트워크가 단절되어도 시스템이 멈추지 않음
서버 간 통신이 끊겨도 각자 계속 동작

현실에서 네트워크 장애는 반드시 발생함
→ 분산 시스템이라면 P는 사실상 필수
→ 결국 C와 A 중 하나를 선택해야 하는 문제
```

---

## CAP 트레이드오프 한눈에 보기

|선택|포기|특징|대표 DB|
|---|---|---|---|
|CP (일관성 + 분단 내성)|가용성|데이터 정확성 우선, 장애 시 응답 거부|HBase, Zookeeper, MongoDB|
|AP (가용성 + 분단 내성)|일관성|서비스 유지 우선, 일시적 데이터 불일치 허용|Cassandra, DynamoDB, CouchDB|
|CA (일관성 + 가용성)|분단 내성|단일 서버에서만 가능, 분산 환경에선 사실상 불가|단일 RDBMS|

```
현실에서 분산 시스템은 P를 포기할 수 없음
→ 진짜 선택지는 CP냐 AP냐
```

---

## CP vs AP 언제 선택하나?

### CP 선택 (일관성 우선)

```
"틀린 데이터를 보여주느니 차라리 오류를 보여줌"

적합한 경우:
→ 금융 서비스 (잔액, 결제)
→ 재고 관리 (중복 차감 방지)
→ 예약 시스템 (좌석 중복 예약 방지)

장애 시 동작:
→ 일관성이 깨질 것 같으면 해당 노드 응답 중단
```

### AP 선택 (가용성 우선)

```
"오래된 데이터라도 일단 보여줌"

적합한 경우:
→ SNS 피드 (좋아요 수 1~2개 틀려도 무방)
→ 상품 추천 (실시간 정확성보다 서비스 유지가 중요)
→ 로그 수집 (일부 유실 허용)

장애 시 동작:
→ 네트워크 단절돼도 각 노드가 계속 응답
→ 단절 복구 후 데이터 동기화 (최종 일관성)
```

---

## 최종 일관성 (Eventual Consistency)

```
AP 시스템에서 사용하는 개념
→ 지금 당장은 불일치해도 결국에는 동일해짐

예시:
서울 서버에서 팔로워 수 1000 → 1001로 업데이트
뉴욕 서버는 잠시 동안 1000을 보여줌
→ 네트워크 복구 후 뉴욕도 1001로 동기화

DNS, SNS 좋아요 수, 장바구니 등에서 사용
→ 잠깐의 불일치는 허용, 서비스 중단은 불허
```

---

## ACID vs BASE

```
CAP 이론과 연결되는 두 가지 철학

ACID (CP 계열 - 주로 RDBMS)
→ 강한 일관성, 트랜잭션 보장
→ 금융, 결제 등 정합성 중요한 서비스

BASE (AP 계열 - 주로 NoSQL)
→ Basically Available (기본적으로 가용)
→ Soft state (일시적 불일치 허용)
→ Eventual consistency (결국엔 일관성 달성)
→ 속도/확장성 중요한 서비스
```

---

## 핵심 Q&A

**Q. CAP 이론이란?**

> 분산 시스템에서 일관성(C), 가용성(A), 분단 내성(P) 세 가지를 동시에 모두 만족할 수 없다는 이론 네트워크 장애는 현실에서 반드시 발생하기 때문에 P는 필수 결국 C(일관성)와 A(가용성) 중 하나를 선택해야 함

**Q. CP와 AP 중 어떻게 선택하나요?**

> 데이터 정확성이 중요한 서비스(금융, 재고)는 CP 서비스 가용성이 중요한 서비스(SNS, 추천)는 AP 틀린 데이터가 허용되는지가 핵심 판단 기준

**Q. 최종 일관성(Eventual Consistency)이란?**

> AP 시스템에서 지금 당장은 불일치할 수 있지만 결국에는 모든 노드가 같은 데이터를 갖게 되는 것 SNS 좋아요 수처럼 잠깐의 불일치는 허용되는 서비스에 적합

---

## 핵심 요약

|항목|내용|
|---|---|
|CAP 이론|분산 시스템에서 C/A/P 셋 다 동시 보장 불가|
|Consistency|모든 노드에서 항상 동일한 데이터|
|Availability|모든 요청에 반드시 응답|
|Partition Tolerance|네트워크 단절 시에도 시스템 동작|
|현실 선택지|P는 필수 → CP 또는 AP 선택|
|CP 사례|금융, 결제, 재고 → HBase, Zookeeper|
|AP 사례|SNS, 추천, 로그 → Cassandra, DynamoDB|
|최종 일관성|AP 시스템에서 결국에는 동기화됨|
|ACID vs BASE|CP 계열 vs AP 계열의 설계 철학|