### JVM 메모리 구조

클래스 로더가 `.class` 파일을 메모리에 올린다고 했다. 그 "메모리"가 정확히 어디를 말하는가? JVM은 용도별로 메모리를 여러 영역으로 나눠 관리한다. 이것이 **런타임 데이터 영역(Runtime Data Areas)**이다.

실무에서 마주치는 대부분의 성능/장애 이슈는 이 영역 중 하나에서 발생한다. 구조를 정확히 알아두면 트러블슈팅의 출발점이 보인다.

---

### 전체 구조

JVM 메모리는 **모든 스레드가 공유하는 영역**과 **스레드마다 독립적으로 가지는 영역**으로 나뉜다.

```
┌──────────────────────────────────────────────────────────┐
│                        JVM Memory                         │
│                                                           │
│  ┌────────────── 스레드 공유 영역 ────────────────────┐  │
│  │                                                     │  │
│  │   ┌─────────────┐       ┌────────────────────┐    │  │
│  │   │    Heap     │       │    Metaspace       │    │  │
│  │   │             │       │  (클래스 메타데이터)│    │  │
│  │   │ (객체/배열) │       │                    │    │  │
│  │   └─────────────┘       └────────────────────┘    │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
│  ┌──────────── 스레드별 독립 영역 ─────────────────────┐ │
│  │                                                      │ │
│  │  ┌─────────┐  ┌──────────┐  ┌──────────────────┐   │ │
│  │  │  Stack  │  │    PC    │  │  Native Method   │   │ │
│  │  │         │  │ Register │  │     Stack        │   │ │
│  │  └─────────┘  └──────────┘  └──────────────────┘   │ │
│  │                                                      │ │
│  │           (스레드가 생길 때마다 하나씩)              │ │
│  └──────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

---

### 스레드 공유 영역

#### ① Heap — 객체가 사는 곳

`new` 키워드로 만든 **모든 객체와 배열**이 저장되는 영역. JVM에서 가장 큰 메모리 영역이자 **GC의 주 무대**다.

java

```java
String s = new String("hello");   // "hello" 객체 → Heap
int[] arr = new int[100];          // 배열 → Heap
User user = new User();            // User 객체 → Heap
```

위 코드의 변수 `s`, `arr`, `user` 자체는 스택에 있지만, 이들이 가리키는 **실제 객체는 모두 Heap**에 있다.

##### Heap의 내부 구조

Heap은 다시 **Young Generation**과 **Old Generation**으로 나뉜다.

```
┌─────────────────── Heap ───────────────────┐
│                                             │
│  ┌─── Young Generation ────┐  ┌─────────┐  │
│  │                          │  │         │  │
│  │  ┌──────┐  ┌────┬────┐  │  │   Old   │  │
│  │  │ Eden │  │ S0 │ S1 │  │  │   Gen   │  │
│  │  │      │  │    │    │  │  │         │  │
│  │  └──────┘  └────┴────┘  │  │         │  │
│  │     8        1    1      │  │   ~2/3  │  │
│  └──────────────────────────┘  └─────────┘  │
└─────────────────────────────────────────────┘
```

- **Young Generation**: 새로 생성된 객체가 살다가 대부분 여기서 사라짐
    - **Eden**: 객체가 처음 할당되는 곳
    - **Survivor 0, 1**: Eden에서 살아남은 객체들이 머무는 곳
- **Old Generation**: Young에서 오래 살아남은 객체들이 이사 오는 곳

이 구조와 동작은 **6편(GC 기초)**에서 깊게 다룬다.

##### 힙 크기 설정

bash

```bash
-Xms512m     # 초기 힙 크기
-Xmx2g       # 최대 힙 크기
-Xmn256m     # Young Gen 크기
```

힙이 꽉 차고 GC로도 공간을 확보할 수 없으면 **`OutOfMemoryError: Java heap space`** 발생.

---

#### ② Metaspace — 클래스 정보 저장소

**클래스 메타데이터, 메서드 바이트코드, 런타임 상수 풀, static 변수**가 저장되는 영역.

이름 그대로 "메타(meta) + 공간(space)" — 클래스 자체에 대한 정보가 들어간다.

##### PermGen에서 Metaspace로

Java 7까지는 이 영역이 **PermGen(Permanent Generation)**이었고, **힙의 일부**였다.

```
[Java 7 이하]
Heap 안에 PermGen이 있음
→ 고정 크기, 자주 OOM 발생

[Java 8 이상]
Metaspace는 네이티브 메모리(OS 메모리)로 이사
→ 기본적으로 무제한, OOM 거의 안 남
```

변경 이유:

- PermGen은 크기 고정이라 클래스가 많은 애플리케이션에서 `OutOfMemoryError: PermGen space`가 자주 발생
- Metaspace는 OS 메모리를 사용해 동적으로 확장 가능
- 기본값은 무제한이지만 `-XX:MaxMetaspaceSize`로 제한 가능

##### 무엇이 저장되나

java

```java
public class User {
    static int count = 0;           // static 변수 → Metaspace
    private String name;            // 필드 정보 → Metaspace
    
    public void setName(String n) { // 메서드 바이트코드 → Metaspace
        this.name = n;
    }
}

User u = new User();  // u 객체 자체는 Heap
```

- `User`라는 **클래스의 구조 정보**: Metaspace
- `User` 클래스의 **메서드 바이트코드**: Metaspace
- `User` 클래스의 **static 변수**: Metaspace
- `new User()`로 만든 **인스턴스**: Heap

---

### 스레드별 독립 영역

JVM의 각 스레드는 자기만의 Stack, PC Register, Native Method Stack을 가진다. 다른 스레드가 접근할 수 없다.

#### ③ JVM Stack — 메서드 호출의 발자국

**메서드가 호출될 때마다 스택 프레임(Stack Frame)이 쌓이는 영역.**

java

```java
public class Example {
    public static void main(String[] args) {
        int a = 10;
        int result = calculate(a);
    }
    
    static int calculate(int x) {
        int y = x * 2;
        return y;
    }
}
```

실행 흐름에 따른 스택 변화:

```
main() 시작:                 calculate() 호출 중:      calculate() 종료 후:

┌─────────────┐              ┌─────────────┐            ┌─────────────┐
│             │              │ calculate() │            │             │
│             │              │  x = 10     │            │             │
│             │              │  y = 20     │            │             │
│             │              ├─────────────┤            │             │
│   main()    │              │   main()    │            │   main()    │
│   args      │              │   args      │            │   args      │
│   a = 10    │              │   a = 10    │            │   a = 10    │
│             │              │             │            │  result=20  │
└─────────────┘              └─────────────┘            └─────────────┘
```

메서드가 끝나면 해당 프레임은 **즉시 제거**된다. GC가 필요 없다.

##### 스택 프레임의 내부

각 스택 프레임은 세 부분으로 구성된다.

```
┌─────────────────────────┐
│   Local Variable Array  │  ← 지역 변수, 파라미터, this
│   (지역 변수 배열)        │
├─────────────────────────┤
│    Operand Stack        │  ← 연산 중간 결과 저장
│    (피연산자 스택)        │
├─────────────────────────┤
│  Constant Pool Ref      │  ← 현재 클래스의 상수 풀 참조
│  (상수 풀 참조)          │
└─────────────────────────┘
```

##### StackOverflowError

재귀 호출이 너무 깊어 스택이 한계를 넘으면 발생한다.

java

```java
public static void infinite() {
    infinite();  // 무한 재귀 → StackOverflowError
}
```

스택 크기 설정:

bash

```bash
-Xss1m     # 스레드당 스택 크기 (기본 512KB ~ 1MB)
```

---

#### ④ PC Register — 현재 위치 표시

**현재 실행 중인 바이트코드 명령어의 주소**를 담는 작은 공간.

CPU의 PC(Program Counter) 레지스터와 같은 개념. 스레드가 중단됐다가 재개될 때 "어디까지 실행했는지" 알기 위해 필요하다.

컨텍스트 스위칭이 일어나면 JVM은 현재 스레드의 PC Register 값을 저장하고, 복귀할 때 그 값으로 실행을 재개한다.

---

#### ⑤ Native Method Stack — JNI용 스택

**JNI(Java Native Interface)로 호출되는 C/C++ 네이티브 메서드**를 위한 별도 스택.

java

```java
public class FileSystem {
    public native void open(String path);  // native 키워드
}
```

자바 메서드는 JVM Stack을, 네이티브 메서드는 Native Method Stack을 사용한다. OS가 제공하는 시스템 콜이나 C 라이브러리 호출이 여기서 일어난다.

---

### 각 영역별 에러 정리

어떤 영역에서 문제가 생겼는지에 따라 발생하는 에러가 다르다.

|영역|발생 에러|원인|
|---|---|---|
|**Heap**|`OutOfMemoryError: Java heap space`|객체가 너무 많아 힙이 꽉 참|
|**Metaspace**|`OutOfMemoryError: Metaspace`|로드된 클래스가 너무 많음|
|**Stack**|`StackOverflowError`|재귀가 너무 깊음|
|**Stack**|`OutOfMemoryError: unable to create new native thread`|스레드를 너무 많이 생성|
|**Direct Memory**|`OutOfMemoryError: Direct buffer memory`|NIO 다이렉트 버퍼 과다|

실무에서 OOM 에러를 만나면 **메시지를 정확히 읽는 것이 첫 단계**다. "Java heap space"와 "Metaspace"는 원인도 해법도 완전히 다르다.

---

### 예제로 보는 메모리 배치

java

```java
public class MemoryExample {
    static int totalCount = 0;           // ① Metaspace
    
    private String name;                 // ② 객체 필드 (Heap의 해당 인스턴스 안)
    
    public static void main(String[] args) {
        int localVar = 100;              // ③ Stack
        
        MemoryExample obj = new MemoryExample();  // ④ 참조는 Stack, 객체는 Heap
        obj.name = "test";               // ⑤ "test" 문자열은 Heap (String Pool)
        
        process(obj);
    }
    
    static void process(MemoryExample e) {  // ⑥ 새 스택 프레임 생성
        int temp = 50;                       // temp는 새 프레임의 Stack
        totalCount++;
    }
}
```

각 데이터의 위치:

|번호|데이터|저장 위치|
|---|---|---|
|①|`totalCount` (static)|Metaspace|
|②|`name` 필드 선언 정보|Metaspace|
|②|`name` 필드의 실제 값|Heap (해당 인스턴스 내부)|
|③|`localVar`|main의 Stack 프레임|
|④|`obj` 참조 변수|main의 Stack 프레임|
|④|`new MemoryExample()` 객체|Heap|
|⑤|`"test"` 문자열|Heap (String Pool)|
|⑥|`process`의 `temp`|process의 Stack 프레임|

---

### 정리

- JVM 메모리는 **공유 영역(Heap, Metaspace)**과 **스레드별 영역(Stack, PC Register, Native Method Stack)**으로 나뉨
- **Heap**: 모든 객체가 사는 곳, GC 대상, Young/Old로 세분화
- **Metaspace**: 클래스 메타데이터, Java 8부터 네이티브 메모리 사용
- **Stack**: 메서드 호출마다 프레임이 쌓임, GC 대상 아님, 자동 해제
- 각 영역에서 발생하는 에러가 다르므로 **메시지를 정확히 읽는 것**이 트러블슈팅의 시작