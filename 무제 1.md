#### Envelope Encryption (이중 자물쇠 구조)

- **CEK (Column Encryption Key)**: AES-256 대칭키, 실제 데이터 암호화 담당, DB에 암호화된 상태로 저장
- **CMK (Column Master Key)**: RSA 비대칭키, CEK를 감싸는(wrap) 역할, Vault에 평문으로 보관
- **핵심 원리**: DB 털려도 CEK는 암호화 상태, Vault 털려도 CEK는 DB에 있음 → **두 시스템 모두 뚫려야 데이터 유출**
#### Insert 흐름

1. 앱이 `INSERT` 시도
2. SqlClient 드라이버가 "암호화 컬럼"임을 감지
3. DB에서 암호화된 CEK 조회
4. Vault에 CMK로 CEK 복호화 요청 → 평문 CEK 획득 + **메모리 캐시**
5. 평문 CEK로 데이터 암호화
6. 암호문을 DB에 저장

#### CEK 캐싱

- 첫 쿼리에서만 Vault 호출, 이후 기본 2시간 캐시
- **운영 중 Vault 부하는 거의 없음**

**인덱스 테스트**

- Deterministic이라 `CREATE INDEX` 성공 (43ms)
- `=` 검색에서 Index Seek 작동

#### 성능 비교 (1000건 기준 실측)

| 항목           | 평문       | 암호       | 비율             |
| ------------ | -------- | -------- | -------------- |
| Insert 개별커밋  | 7810ms   | 7585ms   | 0.97배          |
| Insert 트랜잭션  | 1057ms   | 916ms    | **0.87배** (오차) |
| Select 1000건 | 6ms      | 19ms     | **3.17배**      |
| 검색 100회      | 105ms    | 118ms    | 1.12배          |
| **트랜잭션 가속**  | **7.4배** | **8.3배** | —              |


## Always Encrypted 지원/미지원 연산 정리

###  지원되는 연산 (7개)

모두 **Equality 기반 연산** — "같다/다르다"만 판단하면 되는 것들

|연산|예시|원리|
|---|---|---|
|파라미터 = 비교|`WHERE Jumin = @j`|드라이버가 `@j`를 암호화해서 전송|
|IN (파라미터)|`WHERE Jumin IN (@a, @b)`|각 파라미터 개별 암호화|
|DISTINCT|`SELECT DISTINCT Jumin`|같은 평문 → 같은 암호문|
|GROUP BY|`GROUP BY Jumin`|같은 암호문끼리 묶기|
|COUNT(DISTINCT)|`COUNT(DISTINCT Jumin)`|중복 제거 + 카운트|
|중복검출(HAVING)|`GROUP BY ... HAVING COUNT(*) > 1`|GROUP BY 확장|
|Self JOIN (=)|`ON a.Jumin = b.Jumin`|양쪽 암호문 비교|

---

###  지원되지 않는 연산 (10개)

####  Operand type clash 계열 (5개)

**평문 리터럴/함수 결과 ↔ 암호문 컬럼의 데이터 타입 불일치**

|연산|예시|왜 안 되는가|
|---|---|---|
|리터럴 = 비교|`WHERE Jumin = '9001011234567'`|서버가 리터럴을 암호화할 CMK 없음|
|**LIKE 검색**|`WHERE Jumin LIKE '9%'`|암호문엔 패턴 매칭 불가|
|LEN|`SELECT LEN(Jumin)`|암호문 길이 ≠ 평문 길이|
|UPPER|`SELECT UPPER(Jumin)`|내용 변환 필요|
|CONCAT|`SELECT CONCAT(Jumin, '_')`|평문 리터럴과 결합 불가|

#### Encryption scheme mismatch 계열 (5개)

**암호화 방식이 순서·크기 비교를 지원하지 않음**

|연산|예시|왜 안 되는가|
|---|---|---|
|범위 검색 (>)|`WHERE Jumin > '8'`|Deterministic은 순서 보존 안 함|
|ORDER BY|`ORDER BY Jumin`|암호문 순서 ≠ 평문 순서|
|MAX|`SELECT MAX(Jumin)`|대소 비교 필요|
|MIN|`SELECT MIN(Jumin)`|대소 비교 필요|
|SUBSTRING|`SELECT SUBSTRING(Jumin, 1, 6)`|암호문을 평문처럼 잘라낼 수 없음|

---

### 판단 기준

```
서버가 "평문의 내용·순서"를 몰라도 되는 연산 →  가능
서버가 "평문의 내용·순서"를 알아야 하는 연산 →  불가
```

| 필요한 정보   | 연산 예시                               | 결과  |
| -------- | ----------------------------------- | --- |
| "같음/다름"만 | =, IN, DISTINCT, GROUP BY, JOIN     | ✅   |
| "크다/작다"  | >, <, BETWEEN, ORDER BY, MAX, MIN   | ❌   |
| "패턴/내용"  | LIKE, SUBSTRING, LEN, UPPER, CONCAT | ❌   |
