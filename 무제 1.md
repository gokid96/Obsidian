### JVM이란 무엇인가

JVM(Java Virtual Machine)은 이름 그대로 **가상 머신**이다. 즉, **소프트웨어로 구현된 컴퓨터**다. 물리 CPU 위에서 돌아가지만, 자바 입장에서는 JVM이 곧 하드웨어처럼 보인다.

```
┌─────────────────────────────┐
│       Java Application      │  ← 우리가 짜는 코드
├─────────────────────────────┤
│            JVM              │  ← 오늘의 주인공
├─────────────────────────────┤
│     OS (Windows/Linux)      │
├─────────────────────────────┤
│      Hardware (CPU/RAM)     │
└─────────────────────────────┘
```

이 추상화 계층 덕분에 CPU 아키텍처나 OS를 신경 쓰지 않고 자바 코드를 짤 수 있다. `.class` 파일 하나만 있으면 Windows든 Linux든 macOS든 똑같이 돌아간다.

#### 컴파일 과정의 두 단계

```
.java  ──[javac]──►  .class  ──[JVM]──►  네이티브 코드 실행
 소스           바이트코드           실제 CPU 명령어
```

자바는 **컴파일 언어이면서 동시에 인터프리터 언어**다. `javac`는 소스를 바이트코드로 번역하고, 그 바이트코드를 JVM이 실행 시점에 해석하거나 네이티브 코드로 다시 컴파일한다. 이 이중 구조가 JVM의 모든 특징의 출발점이다.

---

### JVM의 3대 구성 요소

JVM은 크게 세 덩어리로 나뉜다.

```
┌──────────────────────────────────────────────────┐
│                      JVM                          │
│                                                   │
│  ┌────────────────┐                               │
│  │  ① Class       │   .class 파일을 읽어          │
│  │     Loader     │   메모리에 올리는 담당        │
│  └────────┬───────┘                               │
│           ↓                                       │
│  ┌────────────────┐                               │
│  │  ② Runtime     │   Heap / Stack / Metaspace    │
│  │  Data Areas    │   실제 데이터가 사는 곳       │
│  └────────┬───────┘                               │
│           ↓                                       │
│  ┌────────────────┐                               │
│  │  ③ Execution   │   Interpreter + JIT + GC      │
│  │     Engine     │   실제로 일하는 엔진          │
│  └────────────────┘                               │
└──────────────────────────────────────────────────┘
```

#### ① 클래스 로더 (Class Loader)

`.class` 파일을 **찾아서, 검증하고, 메모리에 올리는** 역할.

단순히 파일을 읽는 게 아니라 보안 검증까지 수행한다. 누군가 악의적으로 `java.lang.String`을 조작한 클래스를 끼워 넣어도 막아야 하기 때문이다. 이를 위해 **위임 모델(Delegation Model)**이라는 계층 구조를 사용한다.

#### ② 런타임 데이터 영역 (Runtime Data Areas)

JVM의 메모리 구조. 실무에서 성능/장애 이슈가 가장 많이 발생하는 영역이다.

- **Heap**: `new`로 만든 객체들이 사는 곳. GC의 주 무대.
- **Stack**: 메서드 호출할 때마다 프레임이 쌓이는 곳. 스레드마다 하나씩 존재.
- **Metaspace**: 클래스 메타데이터 저장소 (Java 8부터 PermGen 대체).
- **PC Register**: 현재 실행 중인 명령어 주소.
- **Native Method Stack**: JNI 호출용.

#### ③ 실행 엔진 (Execution Engine)

실제로 바이트코드를 돌리는 부분. JVM의 성능 비밀이 전부 여기 있다.

- **Interpreter**: 바이트코드를 한 줄씩 해석해서 실행. 시작은 빠르지만 반복 실행에 약함.
- **JIT Compiler**: 자주 쓰는 코드를 네이티브 머신 코드로 컴파일해 캐시. 자바가 "느리다"는 편견을 깨는 주인공.
- **Garbage Collector**: 더 이상 참조되지 않는 객체를 자동으로 회수.

---

### 자바 코드 한 줄의 여정

개념만으론 감이 안 오니 실제 코드 한 줄이 어떻게 실행되는지 따라가 보자.

java

```java
public class Hello {
    public static void main(String[] args) {
        String msg = "Hello, JVM";
        System.out.println(msg);
    }
}
```

이 코드가 `java Hello`로 실행되는 순간 내부에서는 다음 과정이 순차적으로 일어난다.

**1단계 — 로딩** 클래스 로더가 `Hello.class`를 찾아 바이트코드를 읽는다. 이어서 참조하는 `String`, `System` 같은 클래스도 필요할 때 로드된다.

**2단계 — 검증과 초기화** 바이트코드가 안전한지 검증하고, static 필드들에 기본값을 할당한다.

**3단계 — 메모리 배치**

- 클래스 정보 → **Metaspace**
- `"Hello, JVM"` 문자열 객체 → **Heap** (정확히는 String Pool)
- `main` 메서드 호출 → **Stack**에 프레임 생성
- 지역변수 `msg` → 스택 프레임 안의 지역변수 슬롯

**4단계 — 실행** 인터프리터가 바이트코드를 한 줄씩 실행. 이 메서드가 반복 호출된다면 JIT이 감지해서 네이티브 코드로 컴파일한 뒤 그 코드를 실행한다.

**5단계 — 정리** `msg`가 더 이상 참조되지 않으면 GC가 Heap에서 회수한다.