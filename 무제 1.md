### `.class` 파일이 JVM에 올라오기까지

자바 프로그램이 실행되려면 `.class` 파일이 JVM 메모리로 올라와야 한다. 이 과정을 담당하는 것이 **클래스 로더 서브시스템(Class Loader Subsystem)**이다.

단순히 파일을 읽어 메모리에 복사하는 작업이 아니다. 바이트코드가 안전한지 검증하고, 필요한 구조를 메모리에 배치하고, 심볼릭 참조를 실제 주소로 변환하는 복잡한 절차가 포함된다.

---

### 클래스 로더의 3계층 구조

JVM은 하나의 클래스 로더가 아니라 **세 개의 로더가 계층적으로 협력**하는 구조를 사용한다.

```
         ┌────────────────────────┐
         │  Bootstrap ClassLoader │  ← 최상위 (부모 없음)
         │   (C++로 구현됨)        │
         │   java.lang.*, java.util.* 등
         │   JDK 핵심 클래스 로드  │
         └───────────┬────────────┘
                     │ 부모
         ┌───────────┴────────────┐
         │  Platform ClassLoader  │  ← Java 9+ (이전: Extension)
         │  JDK 확장 모듈 로드     │
         └───────────┬────────────┘
                     │ 부모
         ┌───────────┴────────────┐
         │ Application ClassLoader│  ← 우리 코드를 로드
         │   classpath 내 클래스   │
         └────────────────────────┘
```

#### Bootstrap ClassLoader

- JVM 시작 시 가장 먼저 생성되는 로더
- **C++로 구현**되어 있어 자바 객체로 표현되지 않음 (`getClassLoader()` 호출 시 `null` 반환)
- `java.lang.Object`, `java.lang.String` 같은 핵심 클래스 로드

#### Platform ClassLoader (Java 9+)

- Java 8까지는 "Extension ClassLoader"였음
- JDK 플랫폼 모듈 로드
- Bootstrap이 부모

#### Application ClassLoader

- 개발자가 작성한 코드와 `-classpath`에 지정된 클래스 로드
- 일반적으로 **"시스템 클래스 로더"**라고도 불림
- Platform이 부모

---

### 위임 모델 (Delegation Model)

클래스 로딩 요청이 들어왔을 때, 로더는 **먼저 부모에게 물어본다**. 이것이 위임 모델의 핵심이다.

```
[요청] "java.lang.String 로드해줘"
      │
      ↓
Application ClassLoader
  "부모에게 먼저 물어볼게"
      │
      ↓
Platform ClassLoader
  "부모에게 먼저 물어볼게"
      │
      ↓
Bootstrap ClassLoader
  "내가 가진 클래스야! 로드함"
      ↑
      └── 로드 완료, 결과 반환
```

부모가 로드하지 못하는 경우에만 자식이 시도한다.

#### 왜 이런 구조일까?

만약 위임 모델이 없다면 누군가 악의적으로 `java.lang.String` 클래스를 조작해 classpath에 넣을 수 있다. 위임 모델은 **핵심 클래스는 항상 Bootstrap이 먼저 로드**하도록 보장해 이런 위조를 막는다.

---

### 클래스 로딩의 3단계

클래스 로더가 `.class` 파일을 처리하는 과정은 다음 3단계로 나뉜다.

```
┌──────────────────────────────────────────────┐
│   1. Loading     (로딩)                       │
│      .class → 메모리로 가져오기               │
├──────────────────────────────────────────────┤
│   2. Linking     (링킹)                       │
│      ├─ Verification  (검증)                  │
│      ├─ Preparation   (준비)                  │
│      └─ Resolution    (해석)                  │
├──────────────────────────────────────────────┤
│   3. Initialization  (초기화)                 │
│      static 블록 실행, static 변수 실제 값    │
└──────────────────────────────────────────────┘
```

---

#### 1. Loading (로딩)

`.class` 바이트코드를 찾아 메모리로 읽어들이는 단계.

수행 작업:

- 바이너리 형태의 `.class` 파일을 읽음
- 클래스의 구조 정보(필드, 메서드, 부모 클래스 등)를 파싱
- 해당 클래스를 대표하는 `Class` 객체를 Heap에 생성
- 메타데이터는 **Metaspace**에 저장

이 시점에는 아직 바이트코드 검증이나 메모리 할당이 일어나지 않는다.

---

#### 2. Linking (링킹)

로드된 클래스를 실제로 사용할 수 있도록 준비하는 단계. 세 하위 단계로 나뉜다.

##### ① Verification (검증)

바이트코드가 JVM 사양에 맞고 안전한지 검사한다.

검증 항목:

- 파일 포맷이 올바른가 (매직 넘버 `0xCAFEBABE` 확인)
- 스택 오버플로우나 언더플로우를 일으키는 구조는 아닌가
- 타입 캐스팅이 올바르게 되어 있는가
- private 메서드에 외부에서 접근하는 코드는 없는가

검증 실패 시 `VerifyError` 발생. JVM은 검증을 통과한 바이트코드만 실행한다.

##### ② Preparation (준비)

클래스의 **static 변수에 기본값을 할당**하는 단계.

java

```java
public class Example {
    static int count = 10;
    static String name = "hello";
    static boolean flag = true;
}
```

Preparation 단계에서는 다음과 같이 기본값만 할당된다:

|변수|실제 코드|Preparation 시점 값|
|---|---|---|
|`count`|`= 10`|`0`|
|`name`|`= "hello"`|`null`|
|`flag`|`= true`|`false`|

진짜 값은 **Initialization 단계**에서 할당된다.

##### ③ Resolution (해석)

클래스 파일 안의 **심볼릭 참조(Symbolic Reference)**를 **직접 참조(Direct Reference)**로 변환한다.

심볼릭 참조란 `"java/lang/String"` 같은 문자열 형태의 참조다. JVM은 이 문자열을 실제 메모리 주소로 바꿔야 실행할 수 있다.

```
"java/lang/String"  ──[Resolution]──►  0x7f8b3c...
  (심볼릭 참조)                        (메모리 주소)
```

Resolution은 **지연 실행(lazy)** 전략을 쓸 수 있다. 즉, 실제로 해당 참조가 사용되는 시점에 변환해도 된다.

---

#### 3. Initialization (초기화)

**static 변수에 실제 값을 할당하고, static 블록을 실행**하는 단계.

java

```java
public class Config {
    static int MAX_SIZE = 100;     // 실제 값 할당
    
    static {                        // static 블록 실행
        System.out.println("Config 초기화");
        MAX_SIZE = loadFromFile();
    }
}
```

Initialization이 **트리거되는 시점**은 정확히 정해져 있다:

1. 클래스의 인스턴스 생성 (`new`)
2. 클래스의 static 메서드 호출
3. 클래스의 static 변수에 값 할당/읽기 (단, `final static` 상수는 제외)
4. `Class.forName("...")` 호출
5. JVM 시작 시 main 클래스

반대로 **단순 참조만으로는 초기화되지 않는다**. 예를 들어 `Config.class` 객체에 접근하거나, 배열 타입 `Config[]`를 만드는 것만으로는 `static` 블록이 실행되지 않는다.

---

### 로딩 시점은 언제인가

JVM은 **게으른 로딩(Lazy Loading)**을 원칙으로 한다. 프로그램 시작 시 모든 클래스를 로드하지 않고, **실제로 필요한 순간에 로드**한다.

java

```java
public class Main {
    public static void main(String[] args) {
        System.out.println("시작");
        
        if (args.length > 0) {
            Heavy h = new Heavy();   // 이 시점에 Heavy 클래스 로드
        }
        
        System.out.println("끝");
    }
}
```

`args.length == 0`이면 `Heavy` 클래스는 아예 로드되지 않는다. 이 덕분에 JVM 시작 속도가 빨라지고 메모리도 절약된다.

---

### 실제 코드로 확인하기

클래스 로더 계층을 코드로 직접 확인할 수 있다.

java

```java
public class ClassLoaderTest {
    public static void main(String[] args) {
        // 1. 우리가 만든 클래스의 로더
        ClassLoader appLoader = ClassLoaderTest.class.getClassLoader();
        System.out.println("App: " + appLoader);
        
        // 2. 부모 로더 (Platform)
        ClassLoader platformLoader = appLoader.getParent();
        System.out.println("Platform: " + platformLoader);
        
        // 3. 최상위 (Bootstrap은 null로 반환됨)
        ClassLoader bootstrapLoader = platformLoader.getParent();
        System.out.println("Bootstrap: " + bootstrapLoader);
        
        // 4. 핵심 클래스의 로더
        ClassLoader stringLoader = String.class.getClassLoader();
        System.out.println("String: " + stringLoader);
    }
}
```

실행 결과 예시:

```
App: jdk.internal.loader.ClassLoaders$AppClassLoader@...
Platform: jdk.internal.loader.ClassLoaders$PlatformClassLoader@...
Bootstrap: null
String: null
```

`String`의 로더가 `null`인 이유는 **Bootstrap ClassLoader가 C++로 구현되어 있어 자바 객체로 표현되지 않기** 때문이다.

---

### 정리

- 클래스 로더는 **Bootstrap → Platform → Application**의 3계층 구조
- **위임 모델**을 통해 핵심 클래스의 위조를 방지
- 로딩 과정은 **Loading → Linking(Verification/Preparation/Resolution) → Initialization** 순서
- **Preparation**에서는 기본값, **Initialization**에서는 실제 값이 할당됨
- JVM은 **Lazy Loading** 원칙을 따라 필요한 시점에만 클래스를 로드